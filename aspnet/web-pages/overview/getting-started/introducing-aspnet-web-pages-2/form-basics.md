---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Introduzione a Pagine Web ASP.NET-nozioni di base sui moduli HTML | Microsoft Docs
author: Rick-Anderson
description: Questa esercitazione illustra le nozioni di base su come creare un modulo di input e su come gestire l'input dell'utente quando si usa Pagine Web ASP.NET (Razor). E ora...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574284"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="42ebc-104">Introduzione alle nozioni di base del modulo Pagine Web ASP.NET-HTML</span><span class="sxs-lookup"><span data-stu-id="42ebc-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="42ebc-105">di [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="42ebc-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="42ebc-106">Questa esercitazione illustra le nozioni di base su come creare un modulo di input e su come gestire l'input dell'utente quando si usa Pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="42ebc-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="42ebc-107">Ora che si dispone di un database, si utilizzeranno le competenze del modulo per consentire agli utenti di trovare filmati specifici nel database.</span><span class="sxs-lookup"><span data-stu-id="42ebc-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="42ebc-108">Si presuppone che la serie sia stata completata tramite [Introduzione alla visualizzazione dei dati tramite pagine Web ASP.NET](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="42ebc-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="42ebc-109">Contenuto dell'esercitazione:</span><span class="sxs-lookup"><span data-stu-id="42ebc-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="42ebc-110">Come creare un form usando gli elementi HTML standard.</span><span class="sxs-lookup"><span data-stu-id="42ebc-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="42ebc-111">Come leggere l'input dell'utente in un modulo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="42ebc-112">Creazione di una query SQL che consente di ottenere i dati in modo selettivo utilizzando un termine di ricerca fornito dall'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="42ebc-113">Come fare in modo che i campi nella pagina "ricordino" cosa è stato immesso dall'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="42ebc-114">Funzionalità/tecnologie discusse:</span><span class="sxs-lookup"><span data-stu-id="42ebc-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="42ebc-115">Oggetto `Request`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-115">The `Request` object.</span></span>
> - <span data-ttu-id="42ebc-116">Clausola SQL `Where`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="42ebc-117">Scopo dell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="42ebc-117">What You'll Build</span></span>

<span data-ttu-id="42ebc-118">Nell'esercitazione precedente è stato creato un database, sono stati aggiunti dati ad esso, quindi è stato usato l'helper `WebGrid` per visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="42ebc-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="42ebc-119">In questa esercitazione si aggiungerà una casella di ricerca che consente di trovare i film di un determinato genere o il cui titolo contiene qualsiasi parola immessa.</span><span class="sxs-lookup"><span data-stu-id="42ebc-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="42ebc-120">(Ad esempio, sarà possibile trovare tutti i film il cui genere è "Action" o il cui titolo contiene "Harry" o "Adventure").</span><span class="sxs-lookup"><span data-stu-id="42ebc-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="42ebc-121">Al termine di questa esercitazione, sarà presente una pagina simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Pagina dei film con il genere e la ricerca del titolo](form-basics/_static/image1.png)

<span data-ttu-id="42ebc-123">La parte relativa all'elenco della pagina è identica a quella dell'ultima esercitazione &mdash; una griglia.</span><span class="sxs-lookup"><span data-stu-id="42ebc-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="42ebc-124">La griglia mostrerà solo i film cercati.</span><span class="sxs-lookup"><span data-stu-id="42ebc-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="42ebc-125">Informazioni sui form HTML</span><span class="sxs-lookup"><span data-stu-id="42ebc-125">About HTML Forms</span></span>

<span data-ttu-id="42ebc-126">Se si ha esperienza nella creazione di form HTML e con la differenza tra `GET` e `POST`, è possibile ignorare questa sezione.</span><span class="sxs-lookup"><span data-stu-id="42ebc-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="42ebc-127">Un modulo dispone di elementi di input utente &mdash; caselle di testo, pulsanti, pulsanti di opzione, caselle di controllo, elenchi a discesa e così via.</span><span class="sxs-lookup"><span data-stu-id="42ebc-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="42ebc-128">Gli utenti compilano questi controlli o effettuano selezioni e quindi inviano il modulo facendo clic su un pulsante.</span><span class="sxs-lookup"><span data-stu-id="42ebc-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="42ebc-129">In questo esempio viene illustrata la sintassi HTML di base di un form:</span><span class="sxs-lookup"><span data-stu-id="42ebc-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="42ebc-130">Quando il markup viene eseguito in una pagina, viene creato un formato semplice simile a quello illustrato nella figura seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Form HTML di base di cui viene eseguito il rendering nel browser](form-basics/_static/image2.png)

<span data-ttu-id="42ebc-132">L'elemento `<form>` racchiude gli elementi HTML da inviare.</span><span class="sxs-lookup"><span data-stu-id="42ebc-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="42ebc-133">Un semplice errore consiste nell'aggiungere elementi alla pagina, ma dimenticare di inserirli all'interno di un elemento `<form>`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="42ebc-134">In tal caso, non viene inviato alcun elemento. L'attributo `method` indica al browser come inviare l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="42ebc-135">Questo valore viene impostato su `post` se si esegue un aggiornamento sul server o si `get` se si sta semplicemente recuperando dati dal server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="42ebc-136">**Sicurezza di verbi GET, POST e HTTP**</span><span class="sxs-lookup"><span data-stu-id="42ebc-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="42ebc-137">HTTP, il protocollo usato dai browser e dai server per scambiare informazioni, è molto semplice nelle operazioni di base.</span><span class="sxs-lookup"><span data-stu-id="42ebc-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="42ebc-138">I browser utilizzano solo pochi verbi per eseguire richieste ai server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="42ebc-139">Quando si scrive codice per il Web, è utile comprendere questi verbi e il modo in cui vengono usati dal browser e dal server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="42ebc-140">I verbi usati più di frequente sono i seguenti:</span><span class="sxs-lookup"><span data-stu-id="42ebc-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="42ebc-141">`GET`</span><span class="sxs-lookup"><span data-stu-id="42ebc-141">`GET`.</span></span> <span data-ttu-id="42ebc-142">Il browser utilizza questo verbo per recuperare qualcosa dal server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="42ebc-143">Ad esempio, quando si digita un URL nel browser, il browser esegue un'operazione di `GET` per richiedere la pagina desiderata.</span><span class="sxs-lookup"><span data-stu-id="42ebc-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="42ebc-144">Se la pagina include elementi grafici, il browser esegue operazioni di `GET` aggiuntive per ottenere le immagini.</span><span class="sxs-lookup"><span data-stu-id="42ebc-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="42ebc-145">Se l'operazione di `GET` deve passare informazioni al server, le informazioni vengono passate come parte dell'URL nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="42ebc-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="42ebc-146">`POST`</span><span class="sxs-lookup"><span data-stu-id="42ebc-146">`POST`.</span></span> <span data-ttu-id="42ebc-147">Il browser invia una richiesta `POST` per inviare i dati da aggiungere o modificare nel server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="42ebc-148">Ad esempio, il verbo `POST` viene usato per creare record in un database o modificare quelli esistenti.</span><span class="sxs-lookup"><span data-stu-id="42ebc-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="42ebc-149">Nella maggior parte dei casi, quando si compila un modulo e si fa clic sul pulsante Submit (Invia), il browser esegue un'operazione di `POST`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="42ebc-150">In un'operazione di `POST`, i dati passati al server si trova nel corpo della pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="42ebc-151">Una differenza importante tra questi verbi è che un'operazione di `GET` non deve modificare alcun elemento nel server o per inserirla in un modo leggermente più astratto, un'operazione di `GET` non comporta una modifica dello stato sul server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="42ebc-152">È possibile eseguire un'operazione di `GET` sulle stesse risorse tutte le volte che si preferisce e tali risorse non cambiano.</span><span class="sxs-lookup"><span data-stu-id="42ebc-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="42ebc-153">Un'operazione di `GET` è spesso detta "sicura", o per usare un termine tecnico, è *idempotente*. Al contrario, ovviamente, una richiesta `POST` modifica un elemento sul server ogni volta che si esegue l'operazione.</span><span class="sxs-lookup"><span data-stu-id="42ebc-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="42ebc-154">Due esempi consentiranno di illustrare questa distinzione.</span><span class="sxs-lookup"><span data-stu-id="42ebc-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="42ebc-155">Quando si esegue una ricerca usando un motore come Bing o Google, si compila un modulo costituito da una casella di testo e quindi si fa clic sul pulsante Search (Cerca).</span><span class="sxs-lookup"><span data-stu-id="42ebc-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="42ebc-156">Il browser esegue un'operazione di `GET`, con il valore immesso nella casella passata come parte dell'URL.</span><span class="sxs-lookup"><span data-stu-id="42ebc-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="42ebc-157">L'uso di un'operazione di `GET` per questo tipo di form è corretto, perché un'operazione di ricerca non modifica le risorse sul server, recupera solo le informazioni.</span><span class="sxs-lookup"><span data-stu-id="42ebc-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="42ebc-158">Si consideri ora il processo di ordinamento di un elemento online.</span><span class="sxs-lookup"><span data-stu-id="42ebc-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="42ebc-159">Immettere i dettagli dell'ordine e quindi fare clic sul pulsante Submit (Invia).</span><span class="sxs-lookup"><span data-stu-id="42ebc-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="42ebc-160">Questa operazione sarà una richiesta di `POST`, perché l'operazione comporterà modifiche nel server, ad esempio un nuovo record di ordine, una modifica nelle informazioni sull'account e probabilmente molte altre modifiche.</span><span class="sxs-lookup"><span data-stu-id="42ebc-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="42ebc-161">A differenza dell'operazione di `GET`, non è possibile ripetere la richiesta di `POST`: se si è fatto, ogni volta che si invia nuovamente la richiesta, viene generato un nuovo ordine nel server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="42ebc-162">In casi come questo, i siti Web spesso avvisano di non fare clic su un pulsante di invio più di una volta o di disabilitare il pulsante di invio in modo che non venga inviato di nuovo il modulo accidentalmente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="42ebc-163">Nel corso di questa esercitazione si userà sia un'operazione di `GET` che un'operazione `POST` per lavorare con i moduli HTML.</span><span class="sxs-lookup"><span data-stu-id="42ebc-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="42ebc-164">In ogni caso verrà illustrato il motivo per cui il verbo usato è quello appropriato.</span><span class="sxs-lookup"><span data-stu-id="42ebc-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="42ebc-165">Per ulteriori informazioni sui verbi HTTP, vedere l'articolo relativo alle [definizioni dei metodi](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) nel sito W3C.</span><span class="sxs-lookup"><span data-stu-id="42ebc-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="42ebc-166">La maggior parte degli elementi di input utente sono elementi `<input>` HTML.</span><span class="sxs-lookup"><span data-stu-id="42ebc-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="42ebc-167">Hanno un aspetto simile a `<input type="type" name="name">,` dove *tipo* indica il tipo di controllo di input utente desiderato.</span><span class="sxs-lookup"><span data-stu-id="42ebc-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="42ebc-168">Questi elementi sono quelli comuni:</span><span class="sxs-lookup"><span data-stu-id="42ebc-168">These elements are the common ones:</span></span>

- <span data-ttu-id="42ebc-169">Casella di testo: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="42ebc-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="42ebc-170">Casella di controllo: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="42ebc-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="42ebc-171">Pulsante di opzione: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="42ebc-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="42ebc-172">Pulsante: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="42ebc-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="42ebc-173">Pulsante Invia: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="42ebc-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="42ebc-174">È anche possibile usare l'elemento `<textarea>` per creare una casella di testo a più righe e l'elemento `<select>` per creare un elenco a discesa o un elenco scorrevole.</span><span class="sxs-lookup"><span data-stu-id="42ebc-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="42ebc-175">Per ulteriori informazioni sugli elementi del form HTML, vedere [input e moduli HTML](http://www.w3schools.com/html/html_forms.asp) nel sito di w3schools.</span><span class="sxs-lookup"><span data-stu-id="42ebc-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="42ebc-176">L'attributo `name` è molto importante, perché il nome è il modo in cui si otterrà il valore dell'elemento in un secondo momento, come si vedrà a breve.</span><span class="sxs-lookup"><span data-stu-id="42ebc-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="42ebc-177">La parte interessante è quella che lo sviluppatore della pagina esegue con l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="42ebc-178">A questi elementi non è associato alcun comportamento incorporato.</span><span class="sxs-lookup"><span data-stu-id="42ebc-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="42ebc-179">Al contrario, è necessario ottenere i valori immessi o selezionati dall'utente ed eseguire un'operazione con loro.</span><span class="sxs-lookup"><span data-stu-id="42ebc-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="42ebc-180">Questo è ciò che verrà illustrato in questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="42ebc-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="42ebc-181">**Moduli HTML5 e input**</span><span class="sxs-lookup"><span data-stu-id="42ebc-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="42ebc-182">Come si può intuire, il codice HTML è in transizione e la versione più recente (HTML5) include il supporto per gli utenti che possono accedere alle informazioni in modo più intuitivo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="42ebc-183">Ad esempio, in HTML5, l'utente (lo sviluppatore della pagina) può indicare alla pagina che si desidera che l'utente immetta una data.</span><span class="sxs-lookup"><span data-stu-id="42ebc-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="42ebc-184">Il browser può quindi visualizzare automaticamente un calendario anziché richiedere all'utente di immettere manualmente una data.</span><span class="sxs-lookup"><span data-stu-id="42ebc-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="42ebc-185">Tuttavia, HTML5 è nuovo e non è ancora supportato in tutti i browser.</span><span class="sxs-lookup"><span data-stu-id="42ebc-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="42ebc-186">Pagine Web ASP.NET supporta l'input HTML5 nell'ambito del browser dell'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="42ebc-187">Per un'idea dei nuovi attributi per l'elemento `<input>` in HTML5, vedere [attributo di tipo&gt; input HTML &lt;](http://www.w3schools.com/html/html_form_input_types.asp) nel sito di w3schools.</span><span class="sxs-lookup"><span data-stu-id="42ebc-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="42ebc-188">Creazione del form</span><span class="sxs-lookup"><span data-stu-id="42ebc-188">Creating the Form</span></span>

<span data-ttu-id="42ebc-189">Nell'area di lavoro **file** di WebMatrix aprire la pagina *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42ebc-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="42ebc-190">Dopo il tag di chiusura `</h1>` e prima del tag `<div>` di apertura della chiamata `grid.GetHtml`, aggiungere il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="42ebc-191">Questo markup crea un form con una casella di testo denominata `searchGenre` e un pulsante Submit.</span><span class="sxs-lookup"><span data-stu-id="42ebc-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="42ebc-192">La casella di testo e il pulsante Invia sono racchiusi in un elemento `<form>` il cui attributo `method` è impostato su `get`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="42ebc-193">Tenere presente che se non si inseriscono la casella di testo e il pulsante Invia all'interno di un elemento `<form>`, non verrà inviato nulla quando si fa clic sul pulsante. Si usa il verbo `GET` qui perché si sta creando un modulo che non esegue alcuna modifica nel server. si ottiene solo una ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="42ebc-194">Nell'esercitazione precedente è stato usato un metodo di `post`, ovvero la modalità di invio delle modifiche al server.</span><span class="sxs-lookup"><span data-stu-id="42ebc-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="42ebc-195">Si noterà che nell'esercitazione successiva.</span><span class="sxs-lookup"><span data-stu-id="42ebc-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="42ebc-196">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-196">Run the page.</span></span> <span data-ttu-id="42ebc-197">Anche se non è stato definito alcun comportamento per il modulo, è possibile vedere come appare:</span><span class="sxs-lookup"><span data-stu-id="42ebc-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Pagina dei filmati con la casella di ricerca per il genere](form-basics/_static/image3.png)

<span data-ttu-id="42ebc-199">Immettere un valore nella casella di testo, ad esempio "Comedy".</span><span class="sxs-lookup"><span data-stu-id="42ebc-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="42ebc-200">Quindi fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="42ebc-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="42ebc-201">Prendere nota dell'URL della pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-201">Take note of the URL of the page.</span></span> <span data-ttu-id="42ebc-202">Poiché l'attributo `method` dell'elemento `<form>` viene impostato su `get`, il valore immesso fa ora parte della stringa di query nell'URL, come segue:</span><span class="sxs-lookup"><span data-stu-id="42ebc-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="42ebc-203">Lettura dei valori del modulo</span><span class="sxs-lookup"><span data-stu-id="42ebc-203">Reading Form Values</span></span>

<span data-ttu-id="42ebc-204">La pagina contiene già il codice che ottiene i dati del database e Visualizza i risultati in una griglia.</span><span class="sxs-lookup"><span data-stu-id="42ebc-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="42ebc-205">A questo punto è necessario aggiungere codice per la lettura del valore della casella di testo in modo da poter eseguire una query SQL che includa il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="42ebc-206">Poiché il metodo del form viene impostato su `get`, è possibile leggere il valore immesso nella casella di testo usando un codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="42ebc-207">L'oggetto `Request.QueryString` (la proprietà `QueryString` dell'oggetto `Request`) include i valori degli elementi inviati come parte dell'operazione di `GET`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="42ebc-208">La proprietà `Request.QueryString` contiene una *raccolta* (un elenco) dei valori inviati nel form.</span><span class="sxs-lookup"><span data-stu-id="42ebc-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="42ebc-209">Per ottenere un valore singolo, è necessario specificare il nome dell'elemento desiderato.</span><span class="sxs-lookup"><span data-stu-id="42ebc-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="42ebc-210">Per questo motivo è necessario disporre di un attributo `name` sull'elemento `<input>` (`searchTerm`) per la creazione della casella di testo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="42ebc-211">Per ulteriori informazioni sull'oggetto `Request`, vedere l' [intestazione laterale](#BKMK_TheRequestObject) in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="42ebc-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="42ebc-212">È abbastanza semplice leggere il valore della casella di testo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="42ebc-213">Tuttavia, se l'utente non ha immesso alcun elemento nella casella di testo, ma ha fatto clic su **Cerca** , è possibile ignorare tale clic, perché non è presente alcun elemento da cercare.</span><span class="sxs-lookup"><span data-stu-id="42ebc-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="42ebc-214">Il codice seguente è un esempio che illustra come implementare queste condizioni.</span><span class="sxs-lookup"><span data-stu-id="42ebc-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="42ebc-215">Non è ancora necessario aggiungere questo codice. questa operazione verrà eseguita in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="42ebc-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="42ebc-216">Il test si interrompe in questo modo:</span><span class="sxs-lookup"><span data-stu-id="42ebc-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="42ebc-217">Ottenere il valore di `Request.QueryString["searchGenre"]`, vale a dire il valore immesso nell'elemento `<input>` denominato `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="42ebc-218">Verificare se è vuoto usando il metodo `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="42ebc-219">Questo metodo è il modo standard per determinare se un elemento, ad esempio un elemento del form, contiene un valore.</span><span class="sxs-lookup"><span data-stu-id="42ebc-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="42ebc-220">In realtà, è importante solo se *non* è vuota, quindi...</span><span class="sxs-lookup"><span data-stu-id="42ebc-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="42ebc-221">Aggiungere l'operatore `!` davanti al `IsEmpty` test.</span><span class="sxs-lookup"><span data-stu-id="42ebc-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="42ebc-222">(L'operatore `!` significa NOT logico).</span><span class="sxs-lookup"><span data-stu-id="42ebc-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="42ebc-223">In inglese semplice, l'intera condizione `if` si traduce nel seguente modo: *se l'elemento searchGenre del form non è vuoto, allora...*</span><span class="sxs-lookup"><span data-stu-id="42ebc-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="42ebc-224">Questo blocco imposta la fase per la creazione di una query che usa il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="42ebc-225">Questa operazione viene illustrata nella sezione seguente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="42ebc-226">**Oggetto Request**</span><span class="sxs-lookup"><span data-stu-id="42ebc-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="42ebc-227">L'oggetto `Request` contiene tutte le informazioni che il browser invia all'applicazione quando viene richiesta o inviata una pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="42ebc-228">Questo oggetto include le informazioni fornite dall'utente, ad esempio i valori delle caselle di testo o un file da caricare.</span><span class="sxs-lookup"><span data-stu-id="42ebc-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="42ebc-229">Include anche tutti i tipi di informazioni aggiuntive, ad esempio i cookie, i valori nella stringa di query dell'URL (se presente), il percorso del file della pagina che esegue, il tipo di browser utilizzato dall'utente, l'elenco delle lingue impostate nel browser e molto altro ancora.</span><span class="sxs-lookup"><span data-stu-id="42ebc-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="42ebc-230">L'oggetto `Request` è una *raccolta* (elenco) di valori.</span><span class="sxs-lookup"><span data-stu-id="42ebc-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="42ebc-231">È possibile ottenere un singolo valore dalla raccolta specificandone il nome:</span><span class="sxs-lookup"><span data-stu-id="42ebc-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="42ebc-232">L'oggetto `Request` espone effettivamente diversi subset.</span><span class="sxs-lookup"><span data-stu-id="42ebc-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="42ebc-233">Esempio:</span><span class="sxs-lookup"><span data-stu-id="42ebc-233">For example:</span></span>
> 
> - <span data-ttu-id="42ebc-234">`Request.Form` fornisce valori dagli elementi all'interno dell'elemento `<form>` inviato se la richiesta è una richiesta di `POST`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="42ebc-235">`Request.QueryString` fornisce solo i valori nella stringa di query dell'URL.</span><span class="sxs-lookup"><span data-stu-id="42ebc-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="42ebc-236">(In un URL come `http://mysite/myapp/page?searchGenre=action&page=2`, la sezione `?searchGenre=action&page=2` dell'URL è la stringa di query).</span><span class="sxs-lookup"><span data-stu-id="42ebc-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="42ebc-237">`Request.Cookies` raccolta consente di accedere ai cookie inviati dal browser.</span><span class="sxs-lookup"><span data-stu-id="42ebc-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="42ebc-238">Per ottenere un valore noto si trova nel form inviato, è possibile usare `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="42ebc-239">In alternativa, è possibile usare le versioni più specifiche `Request.Form["name"]` (per richieste `POST`) o `Request.QueryString["name"]` (per le richieste di `GET`).</span><span class="sxs-lookup"><span data-stu-id="42ebc-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="42ebc-240">Ovviamente, *Name* è il nome dell'elemento da ottenere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="42ebc-241">Il nome dell'elemento che si vuole ottenere deve essere univoco all'interno della raccolta che si sta usando.</span><span class="sxs-lookup"><span data-stu-id="42ebc-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="42ebc-242">Questo è il motivo per cui l'oggetto `Request` fornisce i subset come `Request.Form` e `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="42ebc-243">Si supponga che la pagina contenga un elemento form denominato `userName` e che contenga *anche* un cookie denominato `userName`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="42ebc-244">Se si ottiene `Request["userName"]`, è ambiguo se si desidera il valore del form o il cookie.</span><span class="sxs-lookup"><span data-stu-id="42ebc-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="42ebc-245">Tuttavia, se si ottiene `Request.Form["userName"]` o `Request.Cookie["userName"]`, si sta eseguendo esplicitamente il valore da ottenere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="42ebc-246">È consigliabile essere specifici e usare il subset di `Request` a cui si è interessati, ad esempio `Request.Form` o `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="42ebc-247">Per le pagine semplici che si stanno creando in questa esercitazione, è probabile che non si verifichino differenze.</span><span class="sxs-lookup"><span data-stu-id="42ebc-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="42ebc-248">Tuttavia, quando si creano pagine più complesse, l'uso della versione esplicita `Request.Form` o `Request.QueryString` può aiutare a evitare problemi che possono verificarsi quando la pagina contiene un modulo (o più moduli), i cookie, i valori della stringa di query e così via.</span><span class="sxs-lookup"><span data-stu-id="42ebc-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="42ebc-249">Creazione di una query utilizzando un termine di ricerca</span><span class="sxs-lookup"><span data-stu-id="42ebc-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="42ebc-250">Ora che si è appreso come ottenere il termine di ricerca immesso dall'utente, è possibile creare una query che la utilizza.</span><span class="sxs-lookup"><span data-stu-id="42ebc-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="42ebc-251">Tenere presente che per ottenere tutti gli elementi del film dal database, si sta usando una query SQL simile a questa istruzione:</span><span class="sxs-lookup"><span data-stu-id="42ebc-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="42ebc-252">Per ottenere solo determinati filmati, è necessario utilizzare una query che includa una clausola `Where`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="42ebc-253">Questa clausola consente di impostare una condizione per le righe restituite dalla query.</span><span class="sxs-lookup"><span data-stu-id="42ebc-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="42ebc-254">Di seguito è riportato un esempio:</span><span class="sxs-lookup"><span data-stu-id="42ebc-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="42ebc-255">Il formato di base è `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="42ebc-256">È possibile usare operatori diversi oltre a `=`, ad esempio `>` (maggiore di), `<` (minore di), `<>` (non uguale a), `<=` (minore o uguale a) e così via, a seconda di ciò che si sta cercando.</span><span class="sxs-lookup"><span data-stu-id="42ebc-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="42ebc-257">Se ci si chiede, le istruzioni SQL non fanno distinzione tra maiuscole e minuscole &mdash; `SELECT` sono le stesse `Select` (o addirittura `select`).</span><span class="sxs-lookup"><span data-stu-id="42ebc-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="42ebc-258">Tuttavia, le parole chiave vengono spesso capitalizzate in un'istruzione SQL, ad esempio `SELECT` e `WHERE`, per semplificare la lettura.</span><span class="sxs-lookup"><span data-stu-id="42ebc-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="42ebc-259">Passaggio del termine di ricerca come parametro</span><span class="sxs-lookup"><span data-stu-id="42ebc-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="42ebc-260">La ricerca di un genere specifico è abbastanza semplice (`WHERE Genre = 'Action'`), ma si vuole essere in grado di cercare qualsiasi genere che l'utente immette.</span><span class="sxs-lookup"><span data-stu-id="42ebc-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="42ebc-261">A tale scopo, si crea come query SQL che include un segnaposto per il valore da cercare.</span><span class="sxs-lookup"><span data-stu-id="42ebc-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="42ebc-262">Il comando sarà simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="42ebc-263">Il segnaposto è il carattere `@` seguito da zero.</span><span class="sxs-lookup"><span data-stu-id="42ebc-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="42ebc-264">Come si può intuire, una query può contenere più segnaposto e denominarli `@0`, `@1`, `@2`e così via.</span><span class="sxs-lookup"><span data-stu-id="42ebc-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="42ebc-265">Per configurare la query e passarla effettivamente al valore, usare il codice come il seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="42ebc-266">Questo codice è simile a quello già fatto per visualizzare i dati nella griglia.</span><span class="sxs-lookup"><span data-stu-id="42ebc-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="42ebc-267">Le uniche differenze sono:</span><span class="sxs-lookup"><span data-stu-id="42ebc-267">The only differences are:</span></span>

- <span data-ttu-id="42ebc-268">La query contiene un segnaposto (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="42ebc-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="42ebc-269">La query viene inserita in una variabile (`selectCommand`); prima di, la query veniva passata direttamente al metodo `db.Query`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="42ebc-270">Quando si chiama il metodo `db.Query`, si passano sia la query che il valore da usare per il segnaposto.</span><span class="sxs-lookup"><span data-stu-id="42ebc-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="42ebc-271">Se la query include più segnaposto, è possibile passarli tutti come valori separati al metodo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="42ebc-272">Se si inseriscono tutti questi elementi, si ottiene il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="42ebc-273">**Importante**:</span><span class="sxs-lookup"><span data-stu-id="42ebc-273">**Important!**</span></span> <span data-ttu-id="42ebc-274">L'uso di segnaposto (come `@0`) per passare valori a un comando SQL è *estremamente importante* per la sicurezza.</span><span class="sxs-lookup"><span data-stu-id="42ebc-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="42ebc-275">Il modo in cui viene visualizzato qui, con i segnaposto per i dati variabili, è l'unico modo per creare comandi SQL.</span><span class="sxs-lookup"><span data-stu-id="42ebc-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="42ebc-276">Non costruire mai un'istruzione SQL inserendo (concatenando) il testo letterale e i valori che vengono ricavati dall'utente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="42ebc-277">La concatenazione dell'input utente in un'istruzione SQL apre il sito a un *attacco SQL injection* , in cui un utente malintenzionato invia valori alla pagina che consentono di modificare il database.</span><span class="sxs-lookup"><span data-stu-id="42ebc-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="42ebc-278">Per altre informazioni, vedere l'articolo relativo a [SQL injection](https://msdn.microsoft.com/library/ms161953.aspx) nel sito Web MSDN.</span><span class="sxs-lookup"><span data-stu-id="42ebc-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="42ebc-279">Aggiornamento della pagina dei filmati con il codice di ricerca</span><span class="sxs-lookup"><span data-stu-id="42ebc-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="42ebc-280">A questo punto è possibile aggiornare il codice nel file *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42ebc-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="42ebc-281">Per iniziare, sostituire il codice nel blocco di codice nella parte superiore della pagina con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="42ebc-282">La differenza è che la query è stata inserita nella variabile `selectCommand`, che verrà passata a `db.Query` in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="42ebc-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="42ebc-283">L'inserimento dell'istruzione SQL in una variabile consente di modificare l'istruzione, operazione che verrà eseguita per eseguire la ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="42ebc-284">Sono state anche rimosse queste due righe, che verranno riportate in un secondo momento:</span><span class="sxs-lookup"><span data-stu-id="42ebc-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="42ebc-285">Non si vuole ancora eseguire la query (vale a dire, chiamare `db.Query`) e non si vuole ancora inizializzare l'helper `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="42ebc-286">Queste operazioni verranno eseguite dopo aver individuato l'istruzione SQL da eseguire.</span><span class="sxs-lookup"><span data-stu-id="42ebc-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="42ebc-287">Dopo questo blocco riscritto, è possibile aggiungere la nuova logica per gestire la ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="42ebc-288">Il codice completato sarà simile al seguente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-288">The completed code will look like the following.</span></span> <span data-ttu-id="42ebc-289">Aggiornare il codice nella pagina in modo che corrisponda all'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="42ebc-290">La pagina ora funziona come questa.</span><span class="sxs-lookup"><span data-stu-id="42ebc-290">The page now works like this.</span></span> <span data-ttu-id="42ebc-291">Ogni volta che viene eseguita la pagina, il codice apre il database e la variabile `selectCommand` viene impostata sull'istruzione SQL che ottiene tutti i record dalla tabella `Movies`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="42ebc-292">Il codice inizializza anche la variabile `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="42ebc-293">Tuttavia, se la richiesta corrente include un valore per l'elemento `searchGenre`, il codice imposta `selectCommand` a una query diversa, ovvero, in una query che include la clausola `Where` per cercare un genere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="42ebc-294">Imposta anche `searchTerm` a qualsiasi elemento passato per la casella di ricerca (che potrebbe essere Nothing).</span><span class="sxs-lookup"><span data-stu-id="42ebc-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="42ebc-295">Indipendentemente dall'istruzione SQL in `selectCommand`, il codice chiama quindi `db.Query` per eseguire la query, passandogli tutti i `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="42ebc-296">Se non è presente alcun elemento `searchTerm`, non è importante, perché in tal caso non esiste alcun parametro per passare il valore a `selectCommand` comunque.</span><span class="sxs-lookup"><span data-stu-id="42ebc-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="42ebc-297">Infine, il codice inizializza l'helper `WebGrid` usando i risultati della query, esattamente come in precedenza.</span><span class="sxs-lookup"><span data-stu-id="42ebc-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="42ebc-298">Come si può notare, inserendo l'istruzione SQL e il termine di ricerca in variabili, è stata aggiunta la flessibilità al codice.</span><span class="sxs-lookup"><span data-stu-id="42ebc-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="42ebc-299">Come si vedrà più avanti in questa esercitazione, è possibile usare questo framework di base e aggiungere la logica per diversi tipi di ricerche.</span><span class="sxs-lookup"><span data-stu-id="42ebc-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="42ebc-300">Test della funzionalità di ricerca per genere</span><span class="sxs-lookup"><span data-stu-id="42ebc-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="42ebc-301">In WebMatrix eseguire la pagina *Movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="42ebc-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="42ebc-302">Viene visualizzata la pagina con la casella di testo per genere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="42ebc-303">Immettere un genere immesso per uno dei record di test, quindi fare clic su **Cerca**.</span><span class="sxs-lookup"><span data-stu-id="42ebc-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="42ebc-304">Questa volta viene visualizzato un elenco dei film che corrispondono a tale genere:</span><span class="sxs-lookup"><span data-stu-id="42ebc-304">This time you see a listing of just the movies that match that genre:</span></span>

![Presentazione della pagina film dopo la ricerca del genere ' Comedie '](form-basics/_static/image4.png)

<span data-ttu-id="42ebc-306">Immettere un genere diverso e cercare nuovamente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-306">Enter a different genre and search again.</span></span> <span data-ttu-id="42ebc-307">Provare a immettere il genere usando tutte le lettere minuscole o maiuscole, in modo da poter vedere che la ricerca non fa distinzione tra maiuscole e minuscole.</span><span class="sxs-lookup"><span data-stu-id="42ebc-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="42ebc-308">"Ricordo" elemento immesso dall'utente</span><span class="sxs-lookup"><span data-stu-id="42ebc-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="42ebc-309">Si potrebbe notare che dopo aver immesso un genere e fatto clic sul **genere di ricerca**, è stato visualizzato un elenco per quel genere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="42ebc-310">Tuttavia, la casella di testo di ricerca è vuota &mdash; in altre parole, non ha ricordato ciò che è stato immesso.</span><span class="sxs-lookup"><span data-stu-id="42ebc-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="42ebc-311">È importante comprendere il motivo per cui si verifica questo comportamento.</span><span class="sxs-lookup"><span data-stu-id="42ebc-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="42ebc-312">Quando si invia una pagina, il browser invia una richiesta al server Web.</span><span class="sxs-lookup"><span data-stu-id="42ebc-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="42ebc-313">Quando ASP.NET riceve la richiesta, crea una nuova istanza della pagina, ne esegue il codice e quindi esegue nuovamente il rendering della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="42ebc-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="42ebc-314">In pratica, tuttavia, la pagina non è a conoscenza del fatto che si sta lavorando solo con una versione precedente di se stessa.</span><span class="sxs-lookup"><span data-stu-id="42ebc-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="42ebc-315">Tutto ciò che sa è che è stata ottenuta una richiesta che include alcuni dati del modulo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="42ebc-316">Ogni volta che si richiede una pagina &mdash; se per la prima volta o si inviano &mdash; si sta ricevendo una nuova pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="42ebc-317">Il server Web non ha memoria dell'ultima richiesta.</span><span class="sxs-lookup"><span data-stu-id="42ebc-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="42ebc-318">Né ASP.NET né il browser.</span><span class="sxs-lookup"><span data-stu-id="42ebc-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="42ebc-319">L'unica connessione tra le istanze separate della pagina è costituita da tutti i dati trasmessi.</span><span class="sxs-lookup"><span data-stu-id="42ebc-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="42ebc-320">Se si invia una pagina, ad esempio, la nuova istanza della pagina può ottenere i dati del modulo inviati dall'istanza precedente.</span><span class="sxs-lookup"><span data-stu-id="42ebc-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="42ebc-321">Un altro modo per passare i dati tra le pagine consiste nell'usare i cookie.</span><span class="sxs-lookup"><span data-stu-id="42ebc-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="42ebc-322">Un metodo formale per descrivere questa situazione consiste nel dire che le pagine Web sono senza *stato.*</span><span class="sxs-lookup"><span data-stu-id="42ebc-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="42ebc-323">I server Web e le pagine stesse e gli elementi della pagina non mantengono alcuna informazione sullo stato precedente di una pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="42ebc-324">Il Web è stato progettato in questo modo poiché la gestione dello stato per le singole richieste avrebbe esaurito rapidamente le risorse dei server Web, che spesso gestivano migliaia, forse addirittura centinaia di migliaia, di richieste al secondo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="42ebc-325">Questo è il motivo per cui la casella di testo è vuota.</span><span class="sxs-lookup"><span data-stu-id="42ebc-325">So that's why the text box was empty.</span></span> <span data-ttu-id="42ebc-326">Dopo aver inviato la pagina, ASP.NET ha creato una nuova istanza della pagina ed eseguito il codice e il markup.</span><span class="sxs-lookup"><span data-stu-id="42ebc-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="42ebc-327">Non c'era niente nel codice che ha indicato a ASP.NET di inserire un valore nella casella di testo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="42ebc-328">Quindi, ASP.NET non ha fatto nulla e la casella di testo è stata sottoposta a rendering senza un valore.</span><span class="sxs-lookup"><span data-stu-id="42ebc-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="42ebc-329">Esiste infatti un modo semplice per aggirare questo problema.</span><span class="sxs-lookup"><span data-stu-id="42ebc-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="42ebc-330">Il genere immesso nella casella di testo *è* disponibile nel codice &mdash; `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="42ebc-331">Aggiornare il markup per la casella di testo in modo che l'attributo `value` ne ottenga il valore da `searchTerm`, come nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="42ebc-332">In questa pagina è anche possibile impostare l'attributo `value` sulla variabile `searchTerm`, poiché tale variabile contiene anche il genere immesso.</span><span class="sxs-lookup"><span data-stu-id="42ebc-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="42ebc-333">Tuttavia, l'uso dell'oggetto `Request` per impostare l'attributo `value` come illustrato di seguito è il modo standard per eseguire questa attività.</span><span class="sxs-lookup"><span data-stu-id="42ebc-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="42ebc-334">(Presupponendo che si desideri eseguire questa operazione &mdash; in alcune situazioni, potrebbe essere necessario eseguire il rendering della pagina *senza* valori nei campi.</span><span class="sxs-lookup"><span data-stu-id="42ebc-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="42ebc-335">Tutto dipende da ciò che accade con l'app.</span><span class="sxs-lookup"><span data-stu-id="42ebc-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="42ebc-336">Non è possibile "ricordare" il valore di una casella di testo usata per le password.</span><span class="sxs-lookup"><span data-stu-id="42ebc-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="42ebc-337">Si tratta di un problema di sicurezza che consente agli utenti di compilare un campo di password usando il codice.</span><span class="sxs-lookup"><span data-stu-id="42ebc-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="42ebc-338">Eseguire di nuovo la pagina, immettere un genere e fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="42ebc-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="42ebc-339">Questa volta non solo vengono visualizzati i risultati della ricerca, ma la casella di testo ricorda quanto è stato immesso l'ultima volta:</span><span class="sxs-lookup"><span data-stu-id="42ebc-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Pagina che mostra che la voce precedente della casella di testo è' memorizzata '](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="42ebc-341">Ricerca di una parola nel titolo</span><span class="sxs-lookup"><span data-stu-id="42ebc-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="42ebc-342">È ora possibile cercare qualsiasi genere, ma è anche possibile cercare un titolo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="42ebc-343">È difficile ottenere un titolo esattamente nel momento in cui si esegue la ricerca, quindi è possibile cercare una parola che viene visualizzata in qualsiasi punto all'interno di un titolo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="42ebc-344">Per eseguire questa operazione in SQL, usare l'operatore `LIKE` e la sintassi come la seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="42ebc-345">Questo comando ottiene tutti i filmati i cui titoli contengono "Adventure".</span><span class="sxs-lookup"><span data-stu-id="42ebc-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="42ebc-346">Quando si usa l'operatore `LIKE`, includere il carattere jolly `%` come parte del termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="42ebc-347">Il `LIKE 'adventure%'` di ricerca significa "iniziare con" Adventure ".</span><span class="sxs-lookup"><span data-stu-id="42ebc-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="42ebc-348">Tecnicamente, significa "The String" Adventure "seguito da anything". Analogamente, il termine di ricerca `LIKE '%adventure'` significa "qualsiasi elemento seguito dalla stringa" Adventure ", che è un altro modo per indicare" termina con "Adventure".</span><span class="sxs-lookup"><span data-stu-id="42ebc-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="42ebc-349">Il termine di ricerca `LIKE '%adventure%'` significa quindi "con" Adventure "in qualsiasi punto del titolo".</span><span class="sxs-lookup"><span data-stu-id="42ebc-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="42ebc-350">(Tecnicamente, "qualsiasi elemento nel titolo, seguito da" Adventure ", seguito da anything)."</span><span class="sxs-lookup"><span data-stu-id="42ebc-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="42ebc-351">All'interno dell'elemento `<form>` aggiungere il markup seguente sotto il tag di chiusura `</div>` per la ricerca del genere (immediatamente prima dell'elemento `</form>` di chiusura):</span><span class="sxs-lookup"><span data-stu-id="42ebc-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="42ebc-352">Il codice per gestire questa ricerca è simile al codice per la ricerca del genere, ad eccezione del fatto che è necessario assemblare la ricerca `LIKE`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="42ebc-353">All'interno del blocco di codice nella parte superiore della pagina aggiungere questo blocco `if` subito dopo il blocco `if` per la ricerca del genere:</span><span class="sxs-lookup"><span data-stu-id="42ebc-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="42ebc-354">Questo codice utilizza la stessa logica illustrata in precedenza, ad eccezione del fatto che la ricerca utilizza un operatore `LIKE` e il codice inserisce "`%`" prima e dopo il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="42ebc-355">Si noti che è facile aggiungere un'altra ricerca alla pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="42ebc-356">È sufficiente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-356">All you had to do was:</span></span>

- <span data-ttu-id="42ebc-357">Creare un blocco `if` che ha testato per verificare se la casella di ricerca pertinente aveva un valore.</span><span class="sxs-lookup"><span data-stu-id="42ebc-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="42ebc-358">Impostare la variabile `selectCommand` su una nuova istruzione SQL.</span><span class="sxs-lookup"><span data-stu-id="42ebc-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="42ebc-359">Impostare la variabile di `searchTerm` sul valore da passare alla query.</span><span class="sxs-lookup"><span data-stu-id="42ebc-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="42ebc-360">Ecco il blocco di codice completo, che contiene la nuova logica per una ricerca nel titolo:</span><span class="sxs-lookup"><span data-stu-id="42ebc-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="42ebc-361">Ecco un riepilogo delle operazioni di questo codice:</span><span class="sxs-lookup"><span data-stu-id="42ebc-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="42ebc-362">Le variabili `searchTerm` e `selectCommand` vengono inizializzate nella parte superiore.</span><span class="sxs-lookup"><span data-stu-id="42ebc-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="42ebc-363">Queste variabili verranno impostate sul termine di ricerca appropriato (se presente) e sul comando SQL appropriato in base a ciò che l'utente esegue nella pagina.</span><span class="sxs-lookup"><span data-stu-id="42ebc-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="42ebc-364">La ricerca predefinita è il caso più semplice di ottenere tutti i filmati dal database.</span><span class="sxs-lookup"><span data-stu-id="42ebc-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="42ebc-365">Nei test per `searchGenre` e `searchTitle`, il codice imposta `searchTerm` sul valore che si desidera cercare.</span><span class="sxs-lookup"><span data-stu-id="42ebc-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="42ebc-366">Anche questi blocchi di codice impostano `selectCommand` su un comando SQL appropriato per la ricerca.</span><span class="sxs-lookup"><span data-stu-id="42ebc-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="42ebc-367">Il metodo `db.Query` viene richiamato una sola volta, usando qualsiasi comando SQL `selectedCommand` e qualsiasi valore in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="42ebc-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="42ebc-368">Se non è presente alcun termine di ricerca (nessun genere e parola titolo), il valore di `searchTerm` è una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="42ebc-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="42ebc-369">Tuttavia, ciò non è importante, perché in questo caso la query non richiede un parametro.</span><span class="sxs-lookup"><span data-stu-id="42ebc-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="42ebc-370">Test della funzionalità di ricerca del titolo</span><span class="sxs-lookup"><span data-stu-id="42ebc-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="42ebc-371">A questo punto è possibile testare la pagina di ricerca completata.</span><span class="sxs-lookup"><span data-stu-id="42ebc-371">Now you can test your completed search page.</span></span> <span data-ttu-id="42ebc-372">Eseguire *Movies. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="42ebc-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="42ebc-373">Immettere un genere e fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="42ebc-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="42ebc-374">Nella griglia vengono visualizzati i film del genere, ad esempio before.</span><span class="sxs-lookup"><span data-stu-id="42ebc-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="42ebc-375">Immettere una parola del titolo e fare clic su **Cerca titolo**.</span><span class="sxs-lookup"><span data-stu-id="42ebc-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="42ebc-376">Nella griglia vengono visualizzati i film che contengono tale parola nel titolo.</span><span class="sxs-lookup"><span data-stu-id="42ebc-376">The grid displays movies that have that word in the title.</span></span>

![Elenco di pagine di filmati dopo la ricerca di ' il ' nel titolo](form-basics/_static/image6.png)

<span data-ttu-id="42ebc-378">Lasciare vuote entrambe le caselle di testo e fare clic su uno di questi pulsanti.</span><span class="sxs-lookup"><span data-stu-id="42ebc-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="42ebc-379">Nella griglia vengono visualizzati tutti i film.</span><span class="sxs-lookup"><span data-stu-id="42ebc-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="42ebc-380">Combinazione delle query</span><span class="sxs-lookup"><span data-stu-id="42ebc-380">Combining the Queries</span></span>

<span data-ttu-id="42ebc-381">Si potrebbe notare che le ricerche che è possibile eseguire sono esclusive.</span><span class="sxs-lookup"><span data-stu-id="42ebc-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="42ebc-382">Non è possibile eseguire la ricerca del titolo e del genere nello stesso momento, anche se entrambe le caselle di ricerca contengono valori.</span><span class="sxs-lookup"><span data-stu-id="42ebc-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="42ebc-383">Non è ad esempio possibile cercare tutti i filmati delle azioni il cui titolo contiene "Adventure".</span><span class="sxs-lookup"><span data-stu-id="42ebc-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="42ebc-384">(Poiché la pagina è ora codificata, se si immettono valori per genere e titolo, la ricerca del titolo avrà la precedenza). Per creare una ricerca che combini le condizioni, è necessario creare una query SQL con una sintassi simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="42ebc-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="42ebc-385">Ed è necessario eseguire la query usando un'istruzione simile alla seguente (approssimativamente):</span><span class="sxs-lookup"><span data-stu-id="42ebc-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="42ebc-386">La creazione della logica per consentire l'uso di molte permutazioni dei criteri di ricerca può essere un po' complessa, come si può vedere.</span><span class="sxs-lookup"><span data-stu-id="42ebc-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="42ebc-387">Quindi, si arresterà qui.</span><span class="sxs-lookup"><span data-stu-id="42ebc-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="42ebc-388">Prossimi</span><span class="sxs-lookup"><span data-stu-id="42ebc-388">Coming Up Next</span></span>

<span data-ttu-id="42ebc-389">Nell'esercitazione successiva verrà creata una pagina che usa un modulo per consentire agli utenti di aggiungere filmati al database.</span><span class="sxs-lookup"><span data-stu-id="42ebc-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="42ebc-390">Elenco completo per la pagina di film (aggiornato con la ricerca)</span><span class="sxs-lookup"><span data-stu-id="42ebc-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="42ebc-391">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="42ebc-391">Additional Resources</span></span>

- [<span data-ttu-id="42ebc-392">Introduzione alla programmazione Web ASP.NET con la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="42ebc-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="42ebc-393">[Clausola WHERE SQL](http://www.w3schools.com/sql/sql_where.asp) nel sito di W3Schools</span><span class="sxs-lookup"><span data-stu-id="42ebc-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="42ebc-394">Articolo sulle [definizioni dei metodi](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) nel sito W3C</span><span class="sxs-lookup"><span data-stu-id="42ebc-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="42ebc-395">[Precedente](displaying-data.md)
> [Successivo](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="42ebc-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
