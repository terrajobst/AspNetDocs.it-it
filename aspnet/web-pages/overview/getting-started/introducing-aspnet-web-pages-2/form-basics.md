---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Introduzione a pagine Web ASP.NET - nozioni di base di Form HTML | Microsoft Docs
author: Rick-Anderson
description: Questa esercitazione illustra le nozioni di base di come creare un formato di input e come gestire l'input dell'utente quando si usa ASP.NET Web Pages (Razor). E dopo aver...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f88f7a31551abda029bee0ec16aa35ce2ef5d2f0
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/09/2019
ms.locfileid: "59385956"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="20f9b-104">Introduzione a pagine Web ASP.NET - nozioni fondamentali sui moduli HTML</span><span class="sxs-lookup"><span data-stu-id="20f9b-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="20f9b-105">da [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="20f9b-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="20f9b-106">Questa esercitazione illustra le nozioni di base di come creare un formato di input e come gestire l'input dell'utente quando si usa ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="20f9b-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="20f9b-107">E ora che disponibile un database, si userà le tue competenze di form per consentire agli utenti di trovare il film specifici nel database.</span><span class="sxs-lookup"><span data-stu-id="20f9b-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="20f9b-108">Si presuppone di aver completato la serie attraverso [Introduzione alla visualizzazione dati con ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="20f9b-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="20f9b-109">Che cosa si apprenderà come:</span><span class="sxs-lookup"><span data-stu-id="20f9b-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="20f9b-110">Come creare un form utilizzando gli elementi HTML standard.</span><span class="sxs-lookup"><span data-stu-id="20f9b-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="20f9b-111">Come leggere l'utente di input in un form.</span><span class="sxs-lookup"><span data-stu-id="20f9b-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="20f9b-112">Come creare una query SQL che in modo selettivo i dati tramite una ricerca termini che l'utente fornisce.</span><span class="sxs-lookup"><span data-stu-id="20f9b-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="20f9b-113">Come impostare i campi nella pagina "ricorda" ciò che l'utente ha immesso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="20f9b-114">Le funzionalità/tecnologie illustrate:</span><span class="sxs-lookup"><span data-stu-id="20f9b-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="20f9b-115">Oggetto `Request`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-115">The `Request` object.</span></span>
> - <span data-ttu-id="20f9b-116">Il codice SQL `Where` clausola.</span><span class="sxs-lookup"><span data-stu-id="20f9b-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="20f9b-117">Scopo dell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="20f9b-117">What You'll Build</span></span>

<span data-ttu-id="20f9b-118">Nell'esercitazione precedente, è stato creato un database, aggiunto dati a esso e quindi utilizzato il `WebGrid` helper per visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="20f9b-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="20f9b-119">In questa esercitazione si aggiungerà una casella di ricerca che consente di trovare film di genere specifico o il cui titolo contiene qualsiasi parola immesso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="20f9b-120">(Ad esempio, sarà in grado di trovare tutti i film appartenenti al genere è "Action" o il cui titolo contiene "Harry" o "Adventure.")</span><span class="sxs-lookup"><span data-stu-id="20f9b-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="20f9b-121">Al termine di questa esercitazione, si avrà una pagina simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Pagina di film con ricerca Genre e titolo](form-basics/_static/image1.png)

<span data-ttu-id="20f9b-123">La parte di elenco della pagina è uguale a quello nell'ultima esercitazione &mdash; una griglia.</span><span class="sxs-lookup"><span data-stu-id="20f9b-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="20f9b-124">La differenza sarà che la griglia visualizzerà solo i film che cercato.</span><span class="sxs-lookup"><span data-stu-id="20f9b-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="20f9b-125">Informazioni sui moduli HTML</span><span class="sxs-lookup"><span data-stu-id="20f9b-125">About HTML Forms</span></span>

<span data-ttu-id="20f9b-126">(Se hai esperienza con la creazione di form HTML e con la differenza tra `GET` e `POST`, è possibile ignorare questa sezione.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="20f9b-127">Gli elementi di input utente è dotato di un modulo &mdash; caselle di testo, pulsanti, pulsanti di opzione, caselle di controllo, elenchi a discesa e così via.</span><span class="sxs-lookup"><span data-stu-id="20f9b-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="20f9b-128">Gli utenti di compilare in questi controlli o effettuare selezioni e quindi inviare il modulo, fare clic su un pulsante.</span><span class="sxs-lookup"><span data-stu-id="20f9b-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="20f9b-129">In questo esempio è illustrata la sintassi HTML di base di un form:</span><span class="sxs-lookup"><span data-stu-id="20f9b-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="20f9b-130">Quando questo markup viene eseguito in una pagina, viene creato un semplice form che è simile a questa illustrazione:</span><span class="sxs-lookup"><span data-stu-id="20f9b-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Basic modulo HTML come visualizzabile nel browser](form-basics/_static/image2.png)

<span data-ttu-id="20f9b-132">Il `<form>` elemento racchiude gli elementi HTML da inviare.</span><span class="sxs-lookup"><span data-stu-id="20f9b-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="20f9b-133">(Un semplice errore apportare consiste nell'aggiungere elementi alla pagina quindi dimenticare di inserirli all'interno di un `<form>` elemento.</span><span class="sxs-lookup"><span data-stu-id="20f9b-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="20f9b-134">In tal caso, non viene inviato.) Il `method` attributo indica al browser come inviare l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="20f9b-135">È impostata su `post` se si sta eseguendo un aggiornamento nel server o a `get` se si sta semplicemente recupero dei dati dal server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> **<span data-ttu-id="20f9b-136">GET, POST e la sicurezza verbo HTTP</span><span class="sxs-lookup"><span data-stu-id="20f9b-136">GET, POST, and HTTP Verb Safety</span></span>**
> 
> <span data-ttu-id="20f9b-137">HTTP, il protocollo utilizzato per lo scambio di informazioni, browser e server è straordinariamente semplice durante le operazioni di base.</span><span class="sxs-lookup"><span data-stu-id="20f9b-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="20f9b-138">I browser usano soltanto pochi verbi per effettuare richieste al server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="20f9b-139">Quando si scrive codice per il web, è utile comprendere questi verbi e usarli come il browser e il server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="20f9b-140">Indubbiamente i verbi usati più comunemente sono:</span><span class="sxs-lookup"><span data-stu-id="20f9b-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - `GET`<span data-ttu-id="20f9b-141">.</span><span class="sxs-lookup"><span data-stu-id="20f9b-141">.</span></span> <span data-ttu-id="20f9b-142">Il browser Usa questo verbo per recuperare un elemento dal server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="20f9b-143">Ad esempio, quando si digita un URL nel browser, nel browser viene eseguito un `GET` operazione per richiedere la pagina desiderata.</span><span class="sxs-lookup"><span data-stu-id="20f9b-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="20f9b-144">Se la pagina include elementi grafici, il browser esegue ulteriori `GET` operazioni per ottenere le immagini.</span><span class="sxs-lookup"><span data-stu-id="20f9b-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="20f9b-145">Se il `GET` operazione deve passare le informazioni al server, le informazioni vengono passate come parte dell'URL nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="20f9b-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - `POST`<span data-ttu-id="20f9b-146">.</span><span class="sxs-lookup"><span data-stu-id="20f9b-146">.</span></span> <span data-ttu-id="20f9b-147">Il browser invia un `POST` richiesta per poter inviare i dati per essere aggiunti o modificati nel server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="20f9b-148">Ad esempio, il `POST` verbo viene utilizzato per creare i record in un database o modificare quelli esistenti.</span><span class="sxs-lookup"><span data-stu-id="20f9b-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="20f9b-149">La maggior parte dei casi, quando si compila un modulo e fare clic sul pulsante Invia, nel browser viene eseguito un `POST` operazione.</span><span class="sxs-lookup"><span data-stu-id="20f9b-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="20f9b-150">In un `POST` operazione, i dati passati al server sono nel corpo della pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="20f9b-151">Una differenza importante tra questi verbi è che un `GET` operazione non deve per modificare qualsiasi elemento presente nel server, o inserirlo in modo leggermente più astratto, un `GET` operazione non comporta una modifica nello stato nel server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="20f9b-152">È possibile eseguire un `GET` operazione nelle stesse risorse di quante volte si desidera, e non modificare tali risorse.</span><span class="sxs-lookup"><span data-stu-id="20f9b-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="20f9b-153">(Un `GET` operazione viene spesso definita come "sicure", o per usare un termine tecnico, viene *idempotente*.) Al contrario, naturalmente, un `POST` richiesta viene modificato un elemento nel server ogni volta che si esegue l'operazione.</span><span class="sxs-lookup"><span data-stu-id="20f9b-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="20f9b-154">Due esempi aiuteranno a illustrare questa distinzione.</span><span class="sxs-lookup"><span data-stu-id="20f9b-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="20f9b-155">Quando si esegue una ricerca usando un motore, ad esempio, Google o Bing riempire in un formato che è costituito da una casella di testo e quindi si sceglie il pulsante di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="20f9b-156">Nel browser viene eseguito un `GET` operazione con il valore immesso nella casella di passata come parte dell'URL.</span><span class="sxs-lookup"><span data-stu-id="20f9b-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="20f9b-157">Usando un `GET` operazione per questo tipo di modulo non è corretto, perché un'operazione di ricerca non modifica le risorse nel server, recupera solo le informazioni.</span><span class="sxs-lookup"><span data-stu-id="20f9b-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="20f9b-158">Si consideri ora il processo di ordinamento di un elemento in linea.</span><span class="sxs-lookup"><span data-stu-id="20f9b-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="20f9b-159">Compilare nei dettagli dell'ordine, quindi fare clic sul pulsante Invia.</span><span class="sxs-lookup"><span data-stu-id="20f9b-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="20f9b-160">Questa operazione verrà ritentata un `POST` richiedere, perché l'operazione comporterà modifiche nel server, ad esempio un nuovo record di ordine, una modifica nelle informazioni dell'account e probabilmente molte altre modifiche.</span><span class="sxs-lookup"><span data-stu-id="20f9b-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="20f9b-161">A differenza del `GET` operazione, non è possibile ripetere il `POST` richiesta, ovvero se è stato eseguito, ogni volta che inviato di nuovo la richiesta, si genera un nuovo ordine nel server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="20f9b-162">(In casi come questo, i siti Web spesso avviserà l'utente a non fare clic su un pulsante di invio più volte o disabiliterà il pulsante di invio in modo che non inviare nuovamente il modulo accidentalmente.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="20f9b-163">Nel corso di questa esercitazione si userà entrambi una `GET` operazione e un `POST` operazione utilizzare i moduli HTML.</span><span class="sxs-lookup"><span data-stu-id="20f9b-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="20f9b-164">Spiegheremo in ogni caso perché al verbo utilizzato è quello appropriato.</span><span class="sxs-lookup"><span data-stu-id="20f9b-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="20f9b-165">(Per altre informazioni sui verbi HTTP, vedere la [le definizioni di metodo](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) articolo sul sito di W3C.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="20f9b-166">La maggior parte degli elementi di input utente sono in formato HTML `<input>` elementi.</span><span class="sxs-lookup"><span data-stu-id="20f9b-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="20f9b-167">Sono simili `<input type="type" name="name">,` in cui *tipo* indica il tipo di controllo di input utente desiderato.</span><span class="sxs-lookup"><span data-stu-id="20f9b-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="20f9b-168">Questi elementi sono gli eventi più comuni:</span><span class="sxs-lookup"><span data-stu-id="20f9b-168">These elements are the common ones:</span></span>

- <span data-ttu-id="20f9b-169">Casella di testo:</span><span class="sxs-lookup"><span data-stu-id="20f9b-169">Text box:</span></span> `<input type="text">`
- <span data-ttu-id="20f9b-170">Casella di controllo:</span><span class="sxs-lookup"><span data-stu-id="20f9b-170">Check box:</span></span> `<input type="check">`
- <span data-ttu-id="20f9b-171">Pulsante di opzione:</span><span class="sxs-lookup"><span data-stu-id="20f9b-171">Radio button:</span></span> `<input type="radio">`
- <span data-ttu-id="20f9b-172">Button:</span><span class="sxs-lookup"><span data-stu-id="20f9b-172">Button:</span></span> `<input type="button">`
- <span data-ttu-id="20f9b-173">Pulsante di invio:</span><span class="sxs-lookup"><span data-stu-id="20f9b-173">Submit button:</span></span> `<input type="submit">`

<span data-ttu-id="20f9b-174">È anche possibile usare la `<textarea>` elemento per creare una casella di testo su più righe e di `<select>` elemento per creare un elenco a discesa o un elenco scorrevole.</span><span class="sxs-lookup"><span data-stu-id="20f9b-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="20f9b-175">(Per ulteriori informazioni su HTML costituiscono gli elementi, vedere [form HTML e Input](http://www.w3schools.com/html/html_forms.asp) sul sito W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="20f9b-176">Il `name` attributo è molto importante, perché il nome è come si otterrà il valore dell'elemento in un secondo momento, come si vedrà a breve.</span><span class="sxs-lookup"><span data-stu-id="20f9b-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="20f9b-177">La parte interessante è, lo sviluppatore della pagina, operazioni con l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="20f9b-178">Non è disponibile alcun comportamento predefinito associato a questi elementi.</span><span class="sxs-lookup"><span data-stu-id="20f9b-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="20f9b-179">In alternativa, è necessario ottenere i valori che l'utente ha immesso o selezionato ed eseguire un'operazione con essi.</span><span class="sxs-lookup"><span data-stu-id="20f9b-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="20f9b-180">Questo è ciò che verrà illustrato in questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="20f9b-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> **<span data-ttu-id="20f9b-181">HTML5 e moduli di Input</span><span class="sxs-lookup"><span data-stu-id="20f9b-181">HTML5 and Input Forms</span></span>**
> 
> <span data-ttu-id="20f9b-182">Come saprete, HTML è in fase di transizione e la versione più recente (HTML5) include il supporto per la modalità più intuitive agli utenti di immettere le informazioni.</span><span class="sxs-lookup"><span data-stu-id="20f9b-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="20f9b-183">Ad esempio, in formato HTML5 è (sviluppatore della pagina) può indicare la pagina che si desidera che l'utente di immettere una data.</span><span class="sxs-lookup"><span data-stu-id="20f9b-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="20f9b-184">Il browser può quindi visualizzare automaticamente un calendario, anziché richiedere all'utente di immettere manualmente una data.</span><span class="sxs-lookup"><span data-stu-id="20f9b-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="20f9b-185">Tuttavia, HTML5 è nuovo e non è supportato in tutti i browser ancora.</span><span class="sxs-lookup"><span data-stu-id="20f9b-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="20f9b-186">ASP.NET Web Pages supporta HTML5 nella misura in cui il browser dell'utente non di input.</span><span class="sxs-lookup"><span data-stu-id="20f9b-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="20f9b-187">Per avere un'idea dei nuovi attributi per il `<input>` elemento di HTML5, vedere [HTML &lt;input&gt; attributo type](http://www.w3schools.com/html/html_form_input_types.asp) sul sito W3Schools.</span><span class="sxs-lookup"><span data-stu-id="20f9b-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="20f9b-188">Creazione del form</span><span class="sxs-lookup"><span data-stu-id="20f9b-188">Creating the Form</span></span>

<span data-ttu-id="20f9b-189">In WebMatrix nel **file** dell'area di lavoro, aprire il *Movies.cshtml* pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="20f9b-190">Dopo il tag di chiusura `</h1>` tag e prima dell'apertura `<div>` tag del `grid.GetHtml` chiamare, aggiungere il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="20f9b-191">Questo codice crea un form contenente una casella di testo denominata `searchGenre` e un pulsante di invio.</span><span class="sxs-lookup"><span data-stu-id="20f9b-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="20f9b-192">Il testo casella e inviare pulsante sono racchiusi in un `<form>` elemento la cui `method` attributo è impostato su `get`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="20f9b-193">(Tenere presente che se non inserire la casella di testo e inviare pulsante all'interno di un `<form>` elemento, nulla verrà inviata quando si fa clic sul pulsante.) Si utilizza il `GET` verbo qui perché si sta creando un modulo che non apportare modifiche nel server, semplicemente eseguita una ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="20f9b-194">(Nell'esercitazione precedente, utilizzato un `post` metodo, ovvero come si invia le modifiche apportate al server.</span><span class="sxs-lookup"><span data-stu-id="20f9b-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="20f9b-195">Si noterà che nella prossima esercitazione nuovamente.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="20f9b-196">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-196">Run the page.</span></span> <span data-ttu-id="20f9b-197">Anche se è ancora stato definito alcun comportamento per il form, è possibile visualizzare l'aspetto:</span><span class="sxs-lookup"><span data-stu-id="20f9b-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Pagina di film con casella di ricerca per genere](form-basics/_static/image3.png)

<span data-ttu-id="20f9b-199">Immettere un valore nella casella di testo, ad esempio "Commedie".</span><span class="sxs-lookup"><span data-stu-id="20f9b-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="20f9b-200">Quindi fare clic su **Genre ricerca**.</span><span class="sxs-lookup"><span data-stu-id="20f9b-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="20f9b-201">Prendere nota dell'URL della pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-201">Take note of the URL of the page.</span></span> <span data-ttu-id="20f9b-202">Quanto è stato impostato il `<form>` dell'elemento `method` dell'attributo `get`, il valore immesso fa ora parte della stringa di query nell'URL, simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="20f9b-203">Lettura di valori di Form</span><span class="sxs-lookup"><span data-stu-id="20f9b-203">Reading Form Values</span></span>

<span data-ttu-id="20f9b-204">La pagina contiene già un codice che recupera i dati del database e visualizza i risultati in una griglia.</span><span class="sxs-lookup"><span data-stu-id="20f9b-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="20f9b-205">A questo punto è necessario aggiungere il codice che legge il valore della casella di testo in modo che è possibile eseguire una query SQL che include il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="20f9b-206">Poiché il metodo del form è impostata su `get`, è possibile leggere il valore immesso nella casella di testo usando codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="20f9b-207">Il `Request.QueryString` oggetto (il `QueryString` proprietà del `Request` oggetto) include i valori degli elementi che sono stati inviati come parte di `GET` operazione.</span><span class="sxs-lookup"><span data-stu-id="20f9b-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="20f9b-208">Il `Request.QueryString` proprietà contiene un *raccolta* (elenco) dei valori che vengono inviati nei form.</span><span class="sxs-lookup"><span data-stu-id="20f9b-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="20f9b-209">Per ottenere ogni singolo valore, specificare il nome dell'elemento desiderato.</span><span class="sxs-lookup"><span data-stu-id="20f9b-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="20f9b-210">Ecco perché è necessario avere una `name` attributo la `<input>` elemento (`searchTerm`) che consente di creare la casella di testo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="20f9b-211">(Per altre informazioni sul `Request` oggetti, vedere la [sidebar](#BKMK_TheRequestObject) in un secondo momento.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="20f9b-212">È abbastanza semplice leggere il valore della casella di testo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="20f9b-213">Tuttavia, se l'utente non immette alcuna stringa affatto nella casella di testo, ma ha fatto clic **ricerca** comunque, è possibile ignorare tale clic, poiché non c'è niente da cercare.</span><span class="sxs-lookup"><span data-stu-id="20f9b-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="20f9b-214">Il codice seguente è un esempio che illustra come implementare queste condizioni.</span><span class="sxs-lookup"><span data-stu-id="20f9b-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="20f9b-215">(Non è necessario aggiungere questo codice ancora; è necessario usare più avanti).</span><span class="sxs-lookup"><span data-stu-id="20f9b-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="20f9b-216">Il test si interrompe in questo modo:</span><span class="sxs-lookup"><span data-stu-id="20f9b-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="20f9b-217">Ottenere il valore della `Request.QueryString["searchGenre"]`, vale a dire il valore che è stato inserito il `<input>` elemento denominato `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="20f9b-218">Scoprire se è vuoto usando la `IsEmpty` (metodo).</span><span class="sxs-lookup"><span data-stu-id="20f9b-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="20f9b-219">Questo metodo è il modo standard per determinare se un elemento (ad esempio, un elemento di modulo) contiene un valore.</span><span class="sxs-lookup"><span data-stu-id="20f9b-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="20f9b-220">Tuttavia, importante sapere solo in caso affermativo *non* vuota, quindi...</span><span class="sxs-lookup"><span data-stu-id="20f9b-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="20f9b-221">Aggiungere il `!` operatore davanti il `IsEmpty` di test.</span><span class="sxs-lookup"><span data-stu-id="20f9b-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="20f9b-222">(Il `!` operatore significa NOT logico).</span><span class="sxs-lookup"><span data-stu-id="20f9b-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="20f9b-223">In parole semplici, l'intera `if` condizione si traduce in quanto segue: *Se l'elemento searchGenre del form non vuota, quindi...*</span><span class="sxs-lookup"><span data-stu-id="20f9b-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="20f9b-224">Questo blocco Prepara il terreno per la creazione di una query che usa il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="20f9b-225">È necessario usare nella sezione successiva.</span><span class="sxs-lookup"><span data-stu-id="20f9b-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> **<span data-ttu-id="20f9b-226">L'oggetto richiesta</span><span class="sxs-lookup"><span data-stu-id="20f9b-226">The Request Object</span></span>**
> 
> <span data-ttu-id="20f9b-227">Il `Request` oggetto contiene tutte le informazioni inviate dal browser all'applicazione quando viene richiesto o inviata una pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="20f9b-228">Questo oggetto include tutte le informazioni fornite dall'utente, ad esempio i valori di casella di testo o un file da caricare.</span><span class="sxs-lookup"><span data-stu-id="20f9b-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="20f9b-229">Contiene inoltre moltissime informazioni aggiuntive, quali i cookie, i valori nella stringa query dell'URL (se presente), il percorso del file della pagina a cui è in esecuzione, il tipo di browser in cui l'utente sta usando, l'elenco delle lingue che vengono impostate nel browser e altro ancora.</span><span class="sxs-lookup"><span data-stu-id="20f9b-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="20f9b-230">Il `Request` oggetto è una *raccolta* (elenco) di valori.</span><span class="sxs-lookup"><span data-stu-id="20f9b-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="20f9b-231">Ottenere un singolo valore della raccolta specificandone il nome:</span><span class="sxs-lookup"><span data-stu-id="20f9b-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="20f9b-232">Il `Request` oggetto espone diversi subset.</span><span class="sxs-lookup"><span data-stu-id="20f9b-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="20f9b-233">Ad esempio:</span><span class="sxs-lookup"><span data-stu-id="20f9b-233">For example:</span></span>
> 
> - `Request.Form` <span data-ttu-id="20f9b-234">offre valori dagli elementi all'interno di inviata `<form>` elemento se la richiesta non è un `POST` richiesta.</span><span class="sxs-lookup"><span data-stu-id="20f9b-234">gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - `Request.QueryString` <span data-ttu-id="20f9b-235">consente solo i valori presenti nella stringa di query dell'URL.</span><span class="sxs-lookup"><span data-stu-id="20f9b-235">gives you just the values in the URL's query string.</span></span> <span data-ttu-id="20f9b-236">(In un URL come `http://mysite/myapp/page?searchGenre=action&page=2`, il `?searchGenre=action&page=2` sezione dell'URL è la stringa di query.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - `Request.Cookies` <span data-ttu-id="20f9b-237">raccolta consente di accedere ai cookie che è stato inviato dal browser.</span><span class="sxs-lookup"><span data-stu-id="20f9b-237">collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="20f9b-238">Per ottenere un valore che già conosci è nel formato di invio, è possibile usare `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="20f9b-239">In alternativa, è possibile usare le versioni più specifiche `Request.Form["name"]` (per `POST` richieste) o `Request.QueryString["name"]` (per `GET` richieste).</span><span class="sxs-lookup"><span data-stu-id="20f9b-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="20f9b-240">Ovviamente *nome* è il nome dell'elemento da ottenere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="20f9b-241">Il nome dell'elemento di cui che si desidera ottenere deve essere univoco all'interno della raccolta in uso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="20f9b-242">Ecco perché il `Request` oggetto include come subset `Request.Form` e `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="20f9b-243">Si supponga che la pagina contiene un elemento del form denominato `userName` e *inoltre* contiene un cookie denominato `userName`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="20f9b-244">Se si verificano `Request["userName"]`, è ambiguo se si desidera il valore di form o il cookie.</span><span class="sxs-lookup"><span data-stu-id="20f9b-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="20f9b-245">Tuttavia, se si verificano `Request.Form["userName"]` o `Request.Cookie["userName"]`, sta siano espliciti su quale valore da ottenere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="20f9b-246">È consigliabile essere specifici e usare il subset di `Request` che si è interessati, ad esempio `Request.Form` o `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="20f9b-247">Per le pagine semplici che si sta creando questa esercitazione, probabilmente non molto effettua alcuna differenza.</span><span class="sxs-lookup"><span data-stu-id="20f9b-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="20f9b-248">Tuttavia, quando si creano pagine più complesse, utilizzando la versione esplicita `Request.Form` o `Request.QueryString` può aiutare a evitare i problemi che possono verificarsi quando la pagina contiene un modulo (o più moduli), i cookie, i valori di stringa di query e così via.</span><span class="sxs-lookup"><span data-stu-id="20f9b-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="20f9b-249">Creazione di una Query con un termine di ricerca</span><span class="sxs-lookup"><span data-stu-id="20f9b-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="20f9b-250">Ora che sai come ottenere il termine di ricerca immesso dall'utente, è possibile creare una query che lo usa.</span><span class="sxs-lookup"><span data-stu-id="20f9b-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="20f9b-251">Tenere presente che per ottenere tutti gli elementi di film all'esterno del database, si usa una query SQL che è simile a questa istruzione:</span><span class="sxs-lookup"><span data-stu-id="20f9b-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="20f9b-252">Per ottenere solo determinati film, è necessario usare una query che include un `Where` clausola.</span><span class="sxs-lookup"><span data-stu-id="20f9b-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="20f9b-253">Questa clausola consente di impostare una condizione in cui vengono restituite righe dalla query.</span><span class="sxs-lookup"><span data-stu-id="20f9b-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="20f9b-254">Di seguito è riportato un esempio:</span><span class="sxs-lookup"><span data-stu-id="20f9b-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="20f9b-255">Il formato di base è `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="20f9b-256">È possibile usare diversi operatori oltre just `=`, ad esempio `>` (maggiore di), `<` (minore di), `<>` (non uguale a), `<=` (minore o uguale a), e così via, a seconda di ciò che sta cercando.</span><span class="sxs-lookup"><span data-stu-id="20f9b-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="20f9b-257">Nel caso ve lo chiediate, le istruzioni SQL non sono tra maiuscole e minuscole &mdash; `SELECT` equivale a `Select` (o addirittura `select`).</span><span class="sxs-lookup"><span data-stu-id="20f9b-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="20f9b-258">Tuttavia, le persone spesso converte in maiuscolo parole chiave in un'istruzione SQL, ad esempio `SELECT` e `WHERE`per renderlo più facile da leggere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="20f9b-259">Passando il termine di ricerca come parametro</span><span class="sxs-lookup"><span data-stu-id="20f9b-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="20f9b-260">La ricerca per genere specifico è abbastanza semplice (`WHERE Genre = 'Action'`), ma si desidera essere in grado di eseguire la ricerca di qualsiasi genere immessi dall'utente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="20f9b-261">A tale scopo, si crea come query SQL che include un segnaposto per il valore da cercare.</span><span class="sxs-lookup"><span data-stu-id="20f9b-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="20f9b-262">Sarà simile a questo comando:</span><span class="sxs-lookup"><span data-stu-id="20f9b-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="20f9b-263">Il segnaposto è il `@` caratteri seguiti da zero.</span><span class="sxs-lookup"><span data-stu-id="20f9b-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="20f9b-264">Come può immaginare, una query può contenere più segnaposti e verranno denominati `@0`, `@1`, `@2`e così via.</span><span class="sxs-lookup"><span data-stu-id="20f9b-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="20f9b-265">Per impostare l'esecuzione della query e passarlo effettivamente il valore, si usa codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="20f9b-266">Questo codice è simile a ciò che è già stato per visualizzare i dati nella griglia.</span><span class="sxs-lookup"><span data-stu-id="20f9b-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="20f9b-267">Le uniche differenze sono:</span><span class="sxs-lookup"><span data-stu-id="20f9b-267">The only differences are:</span></span>

- <span data-ttu-id="20f9b-268">La query contiene un segnaposto (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="20f9b-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="20f9b-269">La query viene inserita in una variabile (`selectCommand`); prima, la query è stato passato direttamente al `db.Query` (metodo).</span><span class="sxs-lookup"><span data-stu-id="20f9b-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="20f9b-270">Quando si chiama il `db.Query` metodo, si passa la query sia il valore da usare per il segnaposto.</span><span class="sxs-lookup"><span data-stu-id="20f9b-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="20f9b-271">(Se la query conteneva più segnaposto, è necessario passarli come separare i valori al metodo.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="20f9b-272">Se è mettere in pratica tutti questi elementi, viene visualizzato il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> **<span data-ttu-id="20f9b-273">Importante!</span><span class="sxs-lookup"><span data-stu-id="20f9b-273">Important!</span></span>** <span data-ttu-id="20f9b-274">Utilizzo di segnaposti (come `@0`) per passare valori a un comando SQL viene *estremamente importante* per la sicurezza.</span><span class="sxs-lookup"><span data-stu-id="20f9b-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="20f9b-275">Il modo in cui è visualizzato qui, con segnaposto per i dati delle variabili, è l'unico modo, è necessario costruire i comandi SQL.</span><span class="sxs-lookup"><span data-stu-id="20f9b-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="20f9b-276">Mai comporre un'istruzione SQL tramite la realizzazione di testo letterale (concatenando) e i valori che ottenere dall'utente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="20f9b-277">Concatenazione di input dell'utente in un'istruzione SQL viene aperto il sito a un *attacco SQL injection* in cui un utente malintenzionato invia i valori a una pagina che hack del database.</span><span class="sxs-lookup"><span data-stu-id="20f9b-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="20f9b-278">(Altre informazioni nell'articolo [attacchi SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) il sito Web MSDN.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="20f9b-279">Aggiornare la pagina di film con codice di ricerca</span><span class="sxs-lookup"><span data-stu-id="20f9b-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="20f9b-280">A questo punto è possibile aggiornare il codice nel *Movies.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="20f9b-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="20f9b-281">Per iniziare, sostituire il codice nel blocco di codice nella parte superiore della pagina con il seguente codice:</span><span class="sxs-lookup"><span data-stu-id="20f9b-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="20f9b-282">La differenza è che è stata inserita la query nel `selectCommand` variabile, che verranno passate alla `db.Query` in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="20f9b-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="20f9b-283">Inserire l'istruzione SQL in una variabile consente di modificare l'istruzione, che è ciò che si esegue per eseguire la ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="20f9b-284">È stato anche rimosso queste due righe, verranno riportato indietro in un secondo momento:</span><span class="sxs-lookup"><span data-stu-id="20f9b-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="20f9b-285">Non si desidera eseguire la query ancora (vale a dire, chiamare `db.Query`) e non si vuole inizializzare il `WebGrid` helper ancora uno.</span><span class="sxs-lookup"><span data-stu-id="20f9b-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="20f9b-286">Queste operazioni verranno eseguite dopo che è acquisita familiarità con l'istruzione SQL deve essere eseguito.</span><span class="sxs-lookup"><span data-stu-id="20f9b-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="20f9b-287">Dopo questo blocco riscritto, è possibile aggiungere la nuova logica per la gestione di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="20f9b-288">Il codice completo avrà un aspetto simile al seguente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-288">The completed code will look like the following.</span></span> <span data-ttu-id="20f9b-289">Aggiornare il codice della pagina in modo che corrisponda a questo esempio:</span><span class="sxs-lookup"><span data-stu-id="20f9b-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="20f9b-290">La pagina ora funziona come segue.</span><span class="sxs-lookup"><span data-stu-id="20f9b-290">The page now works like this.</span></span> <span data-ttu-id="20f9b-291">Ogni volta che viene eseguita la pagina, il codice verrà aperto il database e il `selectCommand` variabile è impostata per l'istruzione SQL che ottiene tutti i record di `Movies` tabella.</span><span class="sxs-lookup"><span data-stu-id="20f9b-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="20f9b-292">Il codice inizializza inoltre la `searchTerm` variabile.</span><span class="sxs-lookup"><span data-stu-id="20f9b-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="20f9b-293">Tuttavia, se la richiesta corrente include un valore per il `searchGenre` elemento, il codice imposta `selectCommand` a un'altra query, vale a dire, con uno che include il `Where` clausola per cercare un genere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="20f9b-294">Imposta inoltre `searchTerm` per qualunque altro elemento passato per la casella di ricerca (che può essere nothing).</span><span class="sxs-lookup"><span data-stu-id="20f9b-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="20f9b-295">Indipendentemente dal fatto che SQL istruzione si trova in `selectCommand`, il codice quindi chiama `db.Query` per eseguire la query, passandogli qualsiasi è in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="20f9b-296">Se non c'è nulla `searchTerm`, non è importante, perché in tal caso non è presente nessun parametro da passare il valore da `selectCommand` comunque.</span><span class="sxs-lookup"><span data-stu-id="20f9b-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="20f9b-297">Infine, il codice inizializza il `WebGrid` helper con i risultati della query, come in precedenza.</span><span class="sxs-lookup"><span data-stu-id="20f9b-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="20f9b-298">È possibile notare che, inserendo l'istruzione SQL e al termine di ricerca in variabili, aver aggiunto flessibilità al codice.</span><span class="sxs-lookup"><span data-stu-id="20f9b-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="20f9b-299">Come si vedrà più avanti in questa esercitazione, è possibile usare questo framework di base e continuare ad aggiungere logica per diversi tipi di ricerche.</span><span class="sxs-lookup"><span data-stu-id="20f9b-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="20f9b-300">La funzionalità di ricerca-da-Genre di testing</span><span class="sxs-lookup"><span data-stu-id="20f9b-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="20f9b-301">In WebMatrix, eseguire la *Movies.cshtml* pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="20f9b-302">Viene visualizzata la pagina con la casella di testo per il genere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="20f9b-303">Immettere un genere che è immesso un valore o i record di test, quindi fare clic su **ricerca**.</span><span class="sxs-lookup"><span data-stu-id="20f9b-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="20f9b-304">Questa volta è visualizzato un elenco di semplicemente i film che corrispondono a tale genere:</span><span class="sxs-lookup"><span data-stu-id="20f9b-304">This time you see a listing of just the movies that match that genre:</span></span>

![Pagina Movies listato dopo la ricerca di Comedies' genre'](form-basics/_static/image4.png)

<span data-ttu-id="20f9b-306">Immettere un genere diverso e ripetere la ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-306">Enter a different genre and search again.</span></span> <span data-ttu-id="20f9b-307">Provare a immettere il genere con tutte le lettere minuscole o tutte maiuscole, in modo che è possibile vedere che la ricerca non fa distinzione tra maiuscole e minuscole.</span><span class="sxs-lookup"><span data-stu-id="20f9b-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="20f9b-308">"Memorizzazione" ciò che l'utente ha immesso</span><span class="sxs-lookup"><span data-stu-id="20f9b-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="20f9b-309">Si è notato che dopo aver immesso un genere e fatto clic **Genre ricerca**, si è visto una voce per tale genere.</span><span class="sxs-lookup"><span data-stu-id="20f9b-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="20f9b-310">Tuttavia, la casella di testo di ricerca è vuota &mdash; in altre parole, non è stata ricordare cosa sia stato immesso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="20f9b-311">È importante comprendere il motivo per cui questo comportamento si verifica.</span><span class="sxs-lookup"><span data-stu-id="20f9b-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="20f9b-312">Quando si invia una pagina, il browser invia una richiesta al server web.</span><span class="sxs-lookup"><span data-stu-id="20f9b-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="20f9b-313">Quando ASP.NET riceve la richiesta, crea un'istanza nuova della pagina, viene eseguito il codice in esso e quindi esegue il rendering di pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="20f9b-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="20f9b-314">In effetti, tuttavia, la pagina non sa che si stava semplicemente con una versione precedente di se stesso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="20f9b-315">Tutto che risulti evidente è che non ha ricevuto una richiesta contenente alcuni dati del modulo in esso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="20f9b-316">Ogni volta che si richiede una pagina &mdash; se per la prima volta oppure inviandolo &mdash; si ricevono una nuova pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="20f9b-317">Il server web non ha memoria dell'ultima richiesta.</span><span class="sxs-lookup"><span data-stu-id="20f9b-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="20f9b-318">Neanche ASP.NET e neanche il browser.</span><span class="sxs-lookup"><span data-stu-id="20f9b-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="20f9b-319">La connessione sola tra queste istanze separate della pagina è tutti i dati che trasmettono tra di essi.</span><span class="sxs-lookup"><span data-stu-id="20f9b-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="20f9b-320">Se si invia una pagina, ad esempio, la nuova istanza di pagina possa ottenere i dati del modulo che è stati inviati dall'istanza precedente.</span><span class="sxs-lookup"><span data-stu-id="20f9b-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="20f9b-321">(Un altro modo per passare dati tra le pagine prevede l'uso dei cookie).</span><span class="sxs-lookup"><span data-stu-id="20f9b-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="20f9b-322">Un modo formale per descrivere questa situazione è dire che le pagine web siano *senza stato*.</span><span class="sxs-lookup"><span data-stu-id="20f9b-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="20f9b-323">Server Web e le pagine stesse e gli elementi nella pagina non mantengono le informazioni sullo stato precedente di una pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="20f9b-324">Il web è stato progettato in questo modo perché Gestione dello stato per le singole richieste sarebbe rapidamente esaurire le risorse del server web, che spesso gestire forse migliaia, persino centinaia di migliaia, di richieste al secondo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="20f9b-325">Quindi la casella di testo è vuota.</span><span class="sxs-lookup"><span data-stu-id="20f9b-325">So that's why the text box was empty.</span></span> <span data-ttu-id="20f9b-326">Dopo che è stata inviata la pagina, ASP.NET creata una nuova istanza della pagina ed eseguito tramite il codice e markup.</span><span class="sxs-lookup"><span data-stu-id="20f9b-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="20f9b-327">Si è verificato nulla che il codice che ASP.NET per inserire un valore nella casella di testo indicato.</span><span class="sxs-lookup"><span data-stu-id="20f9b-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="20f9b-328">Pertanto, ASP.NET non funziona e la casella di testo è stato eseguito il rendering senza un valore in esso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="20f9b-329">È effettivamente un modo semplice per risolvere questo problema.</span><span class="sxs-lookup"><span data-stu-id="20f9b-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="20f9b-330">Il genere immesso nella casella di testo *viene* disponibili in code &mdash; si trova in `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="20f9b-331">Aggiornare il markup per la casella di testo in modo che il `value` attributo Ottiene il valore dalla `searchTerm`, come in questo esempio:</span><span class="sxs-lookup"><span data-stu-id="20f9b-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="20f9b-332">In questa pagina, è possibile avere impostare anche il `value` dell'attributo di `searchTerm` variabile, poiché tale variabile contiene inoltre il genere immesso.</span><span class="sxs-lookup"><span data-stu-id="20f9b-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="20f9b-333">Ma tramite il `Request` su cui impostare il `value` attributo come illustrato di seguito è il modo standard per questa attività.</span><span class="sxs-lookup"><span data-stu-id="20f9b-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="20f9b-334">(Supponendo che si intende addirittura a tale scopo &mdash; in alcuni casi, si potrebbe voler eseguire il rendering della pagina *senza* valori nei campi.</span><span class="sxs-lookup"><span data-stu-id="20f9b-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="20f9b-335">Tutto dipende cosa sta succedendo con l'app.)</span><span class="sxs-lookup"><span data-stu-id="20f9b-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="20f9b-336">Non si "ricorda" il valore di una casella di testo che viene usato per le password.</span><span class="sxs-lookup"><span data-stu-id="20f9b-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="20f9b-337">Sarebbe un problema di sicurezza per consentire agli utenti di compilare un campo della password tramite il codice.</span><span class="sxs-lookup"><span data-stu-id="20f9b-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="20f9b-338">Eseguire nuovamente la pagina, immettere un genere e fare clic su **Genre ricerca**.</span><span class="sxs-lookup"><span data-stu-id="20f9b-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="20f9b-339">Questa volta non solo vengono visualizzati i risultati della ricerca, ma la casella di testo memorizza l'ora dell'ultimo immesso:</span><span class="sxs-lookup"><span data-stu-id="20f9b-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Pagina che mostra che la casella di testo ha 'memorizzate' la voce precedente](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="20f9b-341">Cercare qualsiasi parola nel titolo</span><span class="sxs-lookup"><span data-stu-id="20f9b-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="20f9b-342">A questo punto è possibile cercare qualsiasi genere, ma è anche possibile cercare un titolo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="20f9b-343">È difficile ottenere esattamente a destra un titolo durante la ricerca, ma che è possibile cercare una parola che compare in un punto qualsiasi all'interno di un titolo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="20f9b-344">A tale scopo, in SQL, si utilizza il `LIKE` operatore e la sintassi analogo al seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="20f9b-345">Questo comando Ottiene tutti i film cui titoli contengono "adventure".</span><span class="sxs-lookup"><span data-stu-id="20f9b-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="20f9b-346">Quando si usa la `LIKE` operatore, si includa il carattere jolly `%` come parte del termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="20f9b-347">La ricerca `LIKE 'adventure%'` significa "inizia con 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="20f9b-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="20f9b-348">(Tecnicamente, significa che "La stringa 'adventure' seguito dal nulla.") Analogamente, il termine di ricerca `LIKE '%adventure'` significa "tutto quello seguito dalla stringa 'adventure'", ovvero un altro modo per dire "finendo con 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="20f9b-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="20f9b-349">Al termine di ricerca `LIKE '%adventure%'` pertanto significa "con"adventure' in un punto qualsiasi nel titolo."</span><span class="sxs-lookup"><span data-stu-id="20f9b-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="20f9b-350">(Tecnicamente, "qualsiasi cosa nel titolo, seguito da 'adventure', seguita da qualsiasi elemento".)</span><span class="sxs-lookup"><span data-stu-id="20f9b-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="20f9b-351">All'interno di `<form>` elemento, aggiungere il markup seguente sotto la chiusura `</div>` tag per la ricerca di genere (subito prima del tag `</form>` elemento):</span><span class="sxs-lookup"><span data-stu-id="20f9b-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="20f9b-352">Il codice per gestire questa ricerca è simile al codice per la ricerca genre, ad eccezione del fatto che sia necessario assemblare la `LIKE` ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="20f9b-353">All'interno del blocco di codice nella parte superiore della pagina, aggiungere questo `if` blocca subito dopo il `if` blocco per la ricerca di genere:</span><span class="sxs-lookup"><span data-stu-id="20f9b-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="20f9b-354">Questo codice Usa la stessa logica illustrato in precedenza, ad eccezione del fatto che la ricerca Usa un' `LIKE` operatore e inserisce il codice "`%`" prima e dopo il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="20f9b-355">Si noti come era facile aggiungere un'altra ricerca per pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="20f9b-356">È stato sufficiente era:</span><span class="sxs-lookup"><span data-stu-id="20f9b-356">All you had to do was:</span></span>

- <span data-ttu-id="20f9b-357">Creare un `if` blocco in cui è stato testato per verificare se la casella di ricerca rilevanti conteneva un valore.</span><span class="sxs-lookup"><span data-stu-id="20f9b-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="20f9b-358">Impostare il `selectCommand` variabile a una nuova istruzione SQL.</span><span class="sxs-lookup"><span data-stu-id="20f9b-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="20f9b-359">Impostare il `searchTerm` variabili al valore da passare alla query.</span><span class="sxs-lookup"><span data-stu-id="20f9b-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="20f9b-360">Ecco il blocco di codice completo, che contiene la nuova logica per una ricerca di titoli:</span><span class="sxs-lookup"><span data-stu-id="20f9b-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="20f9b-361">Ecco un riepilogo delle operazioni eseguite da questo codice:</span><span class="sxs-lookup"><span data-stu-id="20f9b-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="20f9b-362">Le variabili `searchTerm` e `selectCommand` vengono inizializzate nella parte superiore.</span><span class="sxs-lookup"><span data-stu-id="20f9b-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="20f9b-363">Si intende impostare queste variabili per il termine di ricerca appropriato (se presente) e comando SQL appropriate basate su operazioni eseguite dall'utente nella pagina.</span><span class="sxs-lookup"><span data-stu-id="20f9b-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="20f9b-364">La ricerca predefinita è il caso semplice di ottenere tutti i film dal database.</span><span class="sxs-lookup"><span data-stu-id="20f9b-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="20f9b-365">I test per `searchGenre` e `searchTitle`, il codice imposta `searchTerm` sul valore desiderato per la ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="20f9b-366">Questi blocchi di codice anche impostare `selectCommand` a un comando SQL appropriato per la ricerca.</span><span class="sxs-lookup"><span data-stu-id="20f9b-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="20f9b-367">Il `db.Query` metodo viene richiamato una sola volta, usando qualsiasi comando SQL è nel `selectedCommand` e qualsiasi valore è espresso in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="20f9b-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="20f9b-368">Se non è presente alcun termine di ricerca (nessun genre e nessuna parola titolo), il valore di `searchTerm` è una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="20f9b-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="20f9b-369">Tuttavia, che non è rilevante, perché in tal caso la query non richiede un parametro.</span><span class="sxs-lookup"><span data-stu-id="20f9b-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="20f9b-370">La funzionalità di ricerca del titolo di test</span><span class="sxs-lookup"><span data-stu-id="20f9b-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="20f9b-371">È ora possibile testare la pagina di ricerca completata.</span><span class="sxs-lookup"><span data-stu-id="20f9b-371">Now you can test your completed search page.</span></span> <span data-ttu-id="20f9b-372">Eseguire *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="20f9b-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="20f9b-373">Immettere un genere e fare clic su **Genre ricerca**.</span><span class="sxs-lookup"><span data-stu-id="20f9b-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="20f9b-374">Nella griglia vengono visualizzate film di tale genere, come prima.</span><span class="sxs-lookup"><span data-stu-id="20f9b-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="20f9b-375">Immettere una parola di titolo e fare clic su **ricerca titolo**.</span><span class="sxs-lookup"><span data-stu-id="20f9b-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="20f9b-376">Nella griglia vengono visualizzate film che contengono tale parola nel titolo.</span><span class="sxs-lookup"><span data-stu-id="20f9b-376">The grid displays movies that have that word in the title.</span></span>

![Pagina dopo la ricerca di 'La' nel titolo del film](form-basics/_static/image6.png)

<span data-ttu-id="20f9b-378">Lasciare vuoto entrambe le caselle di testo e fare clic su dei pulsanti.</span><span class="sxs-lookup"><span data-stu-id="20f9b-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="20f9b-379">La griglia visualizza tutti i film.</span><span class="sxs-lookup"><span data-stu-id="20f9b-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="20f9b-380">Combinare le query</span><span class="sxs-lookup"><span data-stu-id="20f9b-380">Combining the Queries</span></span>

<span data-ttu-id="20f9b-381">È possibile notare che le ricerche è possibile eseguire sono esclusive.</span><span class="sxs-lookup"><span data-stu-id="20f9b-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="20f9b-382">È possibile cercare il titolo e il genere nello stesso momento, anche se entrambe le caselle di ricerca dispone di valori in essi contenuti.</span><span class="sxs-lookup"><span data-stu-id="20f9b-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="20f9b-383">Ad esempio, è possibile cercare tutti i film di azione il cui titolo contiene "Adventure".</span><span class="sxs-lookup"><span data-stu-id="20f9b-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="20f9b-384">(Come la pagina viene codificata a questo punto, se si immettono valori per il genere e titolo, Microsoft la ricerca di titoli Ottiene la precedenza.) Per creare una ricerca che combina le condizioni, è necessario creare una query SQL che ha una sintassi simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="20f9b-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="20f9b-385">E sarebbe necessario eseguire la query utilizzando un'istruzione simile al seguente (all'incirca a proposito):</span><span class="sxs-lookup"><span data-stu-id="20f9b-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="20f9b-386">Creazione per la logica per consentire diverse permutazioni di criteri di ricerca può ottenere un po' complesso, come si può notare.</span><span class="sxs-lookup"><span data-stu-id="20f9b-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="20f9b-387">Pertanto, occorrerà fermarsi qui.</span><span class="sxs-lookup"><span data-stu-id="20f9b-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="20f9b-388">In arrivo</span><span class="sxs-lookup"><span data-stu-id="20f9b-388">Coming Up Next</span></span>

<span data-ttu-id="20f9b-389">Nella prossima esercitazione, si creerà una pagina che utilizza una forma per consentire agli utenti di aggiungere film nel database.</span><span class="sxs-lookup"><span data-stu-id="20f9b-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="20f9b-390">Elenco completo per la pagina di film (aggiornata con la ricerca)</span><span class="sxs-lookup"><span data-stu-id="20f9b-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="20f9b-391">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="20f9b-391">Additional Resources</span></span>

- [<span data-ttu-id="20f9b-392">Introduzione alla programmazione Web ASP.NET utilizzando la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="20f9b-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="20f9b-393">[Clausola WHERE SQL](http://www.w3schools.com/sql/sql_where.asp) sul sito W3Schools</span><span class="sxs-lookup"><span data-stu-id="20f9b-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="20f9b-394">[Le definizioni di metodo](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) articolo sul sito W3C</span><span class="sxs-lookup"><span data-stu-id="20f9b-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="20f9b-395">[Precedente](displaying-data.md)
> [Successivo](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="20f9b-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
