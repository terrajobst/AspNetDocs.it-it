---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
title: Introduzione all'Pagine Web ASP.NET l'immissione di dati di database tramite moduli | Microsoft Docs
author: Rick-Anderson
description: Questa esercitazione illustra come creare un modulo di immissione e quindi immettere i dati che si ottengono dal form in una tabella di database quando si usa Pagine Web ASP.NET (...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: d37c93fc-25fd-4e94-8671-0d437beef206
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
msc.type: authoredcontent
ms.openlocfilehash: b9354a7b97a7df9020a681f709e16a92650cfcf0
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78624537"
---
# <a name="introducing-aspnet-web-pages---entering-database-data-by-using-forms"></a><span data-ttu-id="b5d9c-103">Introduzione all'Pagine Web ASP.NET l'immissione di dati di database tramite moduli</span><span class="sxs-lookup"><span data-stu-id="b5d9c-103">Introducing ASP.NET Web Pages - Entering Database Data by Using Forms</span></span>

<span data-ttu-id="b5d9c-104">di [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="b5d9c-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="b5d9c-105">Questa esercitazione illustra come creare un modulo di immissione e quindi immettere i dati che si ottengono dal form in una tabella di database quando si usa Pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-105">This tutorial shows you how to create an entry form and then enter the data that you get from the form into a database table when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="b5d9c-106">Si presuppone che la serie sia stata completata attraverso le [nozioni di base dei moduli HTML in pagine Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-106">It assumes you have completed the series through [Basics of HTML Forms in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span></span>
> 
> <span data-ttu-id="b5d9c-107">Contenuto dell'esercitazione:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="b5d9c-108">Ulteriori informazioni su come elaborare i moduli di immissione.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-108">More about how to process entry forms.</span></span>
> - <span data-ttu-id="b5d9c-109">Come aggiungere (inserire) dati in un database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-109">How to add (insert) data in a database.</span></span>
> - <span data-ttu-id="b5d9c-110">Come verificare che gli utenti abbiano immesso un valore obbligatorio in un modulo (come convalidare l'input dell'utente).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-110">How to make sure that users have entered a required value in a form (how to validate user input).</span></span>
> - <span data-ttu-id="b5d9c-111">Come visualizzare gli errori di convalida.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-111">How to display validation errors.</span></span>
> - <span data-ttu-id="b5d9c-112">Come passare a un'altra pagina dalla pagina corrente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-112">How to jump to another page from the current page.</span></span>
>   
> 
> <span data-ttu-id="b5d9c-113">Funzionalità/tecnologie discusse:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="b5d9c-114">Metodo `Database.Execute` .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-114">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="b5d9c-115">Istruzione SQL `Insert Into`</span><span class="sxs-lookup"><span data-stu-id="b5d9c-115">The SQL `Insert Into` statement</span></span>
> - <span data-ttu-id="b5d9c-116">Helper `Validation`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-116">The `Validation` helper.</span></span>
> - <span data-ttu-id="b5d9c-117">Metodo `Response.Redirect` .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-117">The `Response.Redirect` method.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="b5d9c-118">Scopo dell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="b5d9c-118">What You'll Build</span></span>

<span data-ttu-id="b5d9c-119">Nell'esercitazione precedente, in cui è stato illustrato come creare un database, sono stati immessi i dati del database modificando il database direttamente in WebMatrix, lavorando nell'area di lavoro **database** .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-119">In the tutorial earlier that showed you how to create a database, you entered database data by editing the database directly in WebMatrix, working in the **Database** workspace.</span></span> <span data-ttu-id="b5d9c-120">Nella maggior parte delle app, tuttavia, non si tratta di un modo pratico per inserire i dati nel database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-120">In most apps, that's not a practical way to put data into the database, though.</span></span> <span data-ttu-id="b5d9c-121">Quindi, in questa esercitazione verrà creata un'interfaccia basata sul Web che consente all'utente o a chiunque di immettere i dati e salvarli nel database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-121">So in this tutorial, you'll create a web-based interface that lets you or anyone enter data and save it to the database.</span></span>

<span data-ttu-id="b5d9c-122">Verrà creata una pagina in cui è possibile immettere nuovi filmati.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-122">You'll create a page where you can enter new movies.</span></span> <span data-ttu-id="b5d9c-123">La pagina conterrà un modulo di immissione con campi (caselle di testo) in cui è possibile immettere un titolo, un genere e un anno di film.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-123">The page will contain an entry form that has fields (text boxes) where you can enter a movie title, genre, and year.</span></span> <span data-ttu-id="b5d9c-124">La pagina sarà simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-124">The page will look like this page:</span></span>

![Pagina ' Aggiungi film ' nel browser](entering-data/_static/image1.png)

<span data-ttu-id="b5d9c-126">Le caselle di testo saranno elementi HTML `<input>` che saranno simili a questo markup:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-126">The text boxes will be HTML `<input>` elements that will look like this markup:</span></span>

`<input type="text" name="genre" value="" />`

## <a name="creating-the-basic-entry-form"></a><span data-ttu-id="b5d9c-127">Creazione del form di voce di base</span><span class="sxs-lookup"><span data-stu-id="b5d9c-127">Creating the Basic Entry Form</span></span>

<span data-ttu-id="b5d9c-128">Creare una pagina denominata *AddMovie. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-128">Create a page named *AddMovie.cshtml*.</span></span>

<span data-ttu-id="b5d9c-129">Sostituire gli elementi del file con il markup seguente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-129">Replace what's in the file with the following markup.</span></span> <span data-ttu-id="b5d9c-130">Sovrascrivi tutto; si aggiungerà un blocco di codice in alto a breve.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-130">Overwrite everything; you'll add a code block at the top shortly.</span></span>

[!code-cshtml[Main](entering-data/samples/sample1.cshtml)]

<span data-ttu-id="b5d9c-131">In questo esempio viene illustrato il codice HTML tipico per la creazione di un form.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-131">This example shows typical HTML for creating a form.</span></span> <span data-ttu-id="b5d9c-132">USA `<input>` elementi per le caselle di testo e per il pulsante Submit.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-132">It uses `<input>` elements for the text boxes and for the submit button.</span></span> <span data-ttu-id="b5d9c-133">Le didascalie per le caselle di testo vengono create utilizzando gli elementi `<label>` standard.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-133">The captions for the text boxes are created by using standard `<label>` elements.</span></span> <span data-ttu-id="b5d9c-134">Gli elementi `<fieldset>` e `<legend>` inseriscono una finestra di dialogo intorno al modulo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-134">The `<fieldset>` and `<legend>` elements put a nice box around the form.</span></span>

<span data-ttu-id="b5d9c-135">Si noti che in questa pagina l'elemento `<form>` utilizza `post` come valore per l'attributo `method`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-135">Notice that in this page, the `<form>` element uses `post` as the value for the `method` attribute.</span></span> <span data-ttu-id="b5d9c-136">Nell'esercitazione precedente è stato creato un modulo che usava il metodo `get`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-136">In the previous tutorial, you created a form that used the `get` method.</span></span> <span data-ttu-id="b5d9c-137">Questo è stato corretto perché, sebbene il form inviasse valori al server, la richiesta non ha apportato modifiche.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-137">That was correct, because although the form submitted values to the server, the request did not make any changes.</span></span> <span data-ttu-id="b5d9c-138">Tutti i dati sono stati recuperati in modi diversi.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-138">All it did was fetch data in different ways.</span></span> <span data-ttu-id="b5d9c-139">Tuttavia, in questa pagina *verranno* apportate modifiche, in modo da aggiungere nuovi record del database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-139">However, in this page you *will* make changes—you're going to add new database records.</span></span> <span data-ttu-id="b5d9c-140">Pertanto, questo form deve utilizzare il metodo `post`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-140">Therefore, this form should use the `post` method.</span></span> <span data-ttu-id="b5d9c-141">Per ulteriori informazioni sulla differenza tra `GET` e `POST` operazioni, vedere la barra laterale per la[sicurezza dei verbi Get, post e http](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) nell'esercitazione precedente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-141">(For more about the difference between `GET` and `POST` operations, see the[GET, POST, and HTTP Verb Safety](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the previous tutorial.)</span></span>

<span data-ttu-id="b5d9c-142">Si noti che ogni casella di testo contiene un elemento `name` (`title`, `genre``year`).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-142">Note that each text box has a `name` element (`title`, `genre`, `year`).</span></span> <span data-ttu-id="b5d9c-143">Come illustrato nell'esercitazione precedente, questi nomi sono importanti perché è necessario disporre di tali nomi per poter ottenere l'input dell'utente in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-143">As you saw in the previous tutorial, these names are important because you must have those names so you can get the user's input later.</span></span> <span data-ttu-id="b5d9c-144">È possibile utilizzare qualsiasi nome.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-144">You can use any names.</span></span> <span data-ttu-id="b5d9c-145">È utile usare nomi significativi che consentano di ricordare i dati che si stanno utilizzando.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-145">It's helpful to use meaningful names that help you remember what data you're working with.</span></span>

<span data-ttu-id="b5d9c-146">L'attributo `value` di ogni elemento `<input>` contiene un po' di codice Razor, ad esempio `Request.Form["title"]`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-146">The `value` attribute of each `<input>` element contains a bit of Razor code (for example, `Request.Form["title"]`).</span></span> <span data-ttu-id="b5d9c-147">È stata appreso una versione di questo trucco nell'esercitazione precedente per mantenere il valore immesso nella casella di testo, se presente, dopo l'invio del modulo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-147">You learned a version of this trick in the previous tutorial to preserve the value entered into the text box (if any) after the form has been submitted.</span></span>

## <a name="getting-the-form-values"></a><span data-ttu-id="b5d9c-148">Recupero dei valori del modulo</span><span class="sxs-lookup"><span data-stu-id="b5d9c-148">Getting the Form Values</span></span>

<span data-ttu-id="b5d9c-149">Successivamente, si aggiunge il codice che elabora il form.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-149">Next, you add code that processes the form.</span></span> <span data-ttu-id="b5d9c-150">Nella struttura verranno eseguite le operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-150">In outline, you'll do the following:</span></span>

1. <span data-ttu-id="b5d9c-151">Controllare se la pagina è stata inviata (è stato inviato).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-151">Check whether the page is being posted (was submitted).</span></span> <span data-ttu-id="b5d9c-152">Si vuole che il codice venga eseguito solo quando gli utenti hanno fatto clic sul pulsante, non quando la pagina viene eseguita per la prima volta.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-152">You want to your code to run only when users have clicked the button, not when the page first runs.</span></span>
2. <span data-ttu-id="b5d9c-153">Ottenere i valori immessi dall'utente nelle caselle di testo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-153">Get the values that the user entered into the text boxes.</span></span> <span data-ttu-id="b5d9c-154">In questo caso, poiché il form usa il verbo di `POST`, si ottengono i valori del form dalla raccolta di `Request.Form`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-154">In this case, because the form is using the `POST` verb, you get the form values from the `Request.Form` collection.</span></span>
3. <span data-ttu-id="b5d9c-155">Inserire i valori come nuovo record nella tabella del database *Movies* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-155">Insert the values as a new record in the *Movies* database table.</span></span>

<span data-ttu-id="b5d9c-156">Aggiungere il codice seguente all'inizio del file:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-156">At the top of the file, add the following code:</span></span>

[!code-cshtml[Main](entering-data/samples/sample2.cshtml)]

<span data-ttu-id="b5d9c-157">Le prime righe creano variabili (`title`, `genre`e `year`) per mantenere i valori dalle caselle di testo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-157">The first few lines create variables (`title`, `genre`, and `year`) to hold the values from the text boxes.</span></span> <span data-ttu-id="b5d9c-158">La riga `if(IsPost)` verifica che le variabili vengano impostate *solo* quando gli utenti fanno clic sul pulsante **Aggiungi film** , ovvero quando il modulo è stato inviato.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-158">The line `if(IsPost)` makes sure that the variables are set *only* when users click the **Add Movie** button — that is, when the form has been posted.</span></span>

<span data-ttu-id="b5d9c-159">Come illustrato in un'esercitazione precedente, è possibile ottenere il valore di una casella di testo usando un'espressione come `Request.Form["name"]`, dove *nome* è il nome dell'elemento di `<input>`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-159">As you saw in an earlier tutorial, you get the value of a text box by using an expression like `Request.Form["name"]`, where *name* is the name of the `<input>` element.</span></span>

<span data-ttu-id="b5d9c-160">I nomi delle variabili (`title`, `genre`e `year`) sono arbitrari.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-160">The names of the variables (`title`, `genre`, and `year`) are arbitrary.</span></span> <span data-ttu-id="b5d9c-161">Come i nomi assegnati a `<input>` elementi, è possibile chiamarli in qualsiasi modo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-161">Like the names that you assign to `<input>` elements, you can call them anything you like.</span></span> <span data-ttu-id="b5d9c-162">I nomi delle variabili non devono corrispondere agli attributi del nome degli elementi `<input>` nel form. Tuttavia, come per gli elementi `<input>`, è consigliabile usare nomi di variabili che riflettono i dati che contengono.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-162">(The names of the variables don't have to match the name attributes of `<input>` elements on the form.) But as with the `<input>` elements, it's a good idea to use variable names that reflect the data that they contain.</span></span> <span data-ttu-id="b5d9c-163">Quando si scrive codice, i nomi coerenti rendono più semplice ricordare i dati che si stanno usando.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-163">When you write code, consistent names make it easier for you to remember what data you're working with.</span></span>

## <a name="adding-data-to-the-database"></a><span data-ttu-id="b5d9c-164">Aggiunta di dati al database</span><span class="sxs-lookup"><span data-stu-id="b5d9c-164">Adding Data to the Database</span></span>

<span data-ttu-id="b5d9c-165">Nel blocco di codice appena aggiunto, *all'interno* della parentesi graffa di chiusura (`}`) del blocco di `if` (non solo all'interno del blocco di codice), aggiungere il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-165">In the code block you just added, just *inside* the closing brace ( `}` ) of the `if` block (not just inside the code block), add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample3.cs)]

<span data-ttu-id="b5d9c-166">Questo esempio è simile al codice usato in un'esercitazione precedente per recuperare e visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-166">This example is similar to the code you used in a previous tutorial to fetch and display data.</span></span> <span data-ttu-id="b5d9c-167">La riga che inizia con `db =` apre il database, come in precedenza, e la riga successiva definisce un'istruzione SQL, come illustrato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-167">The line that starts with `db =` opens the database, like before, and the next line defines a SQL statement, again as you saw before.</span></span> <span data-ttu-id="b5d9c-168">Tuttavia, questa volta definisce un'istruzione SQL `Insert Into`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-168">However, this time it defines a SQL `Insert Into` statement.</span></span> <span data-ttu-id="b5d9c-169">Nell'esempio seguente viene illustrata la sintassi generale dell'istruzione `Insert Into`:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-169">The following example shows the general syntax of the `Insert Into` statement:</span></span>

`INSERT INTO table (column1, column2, column3, ...) VALUES (value1, value2, value3, ...)`

<span data-ttu-id="b5d9c-170">In altre parole, si specifica la tabella in cui eseguire l'inserimento, quindi si elencano le colonne in cui inserire, quindi si elencano i valori da inserire.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-170">In other words, you specify the table to insert into, then list the columns to insert into, and then list the values to insert.</span></span> <span data-ttu-id="b5d9c-171">Come indicato in precedenza, SQL non fa distinzione tra maiuscole e minuscole, ma alcuni utenti sfruttano le parole chiave per semplificare la lettura del comando.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-171">(As noted before, SQL is not case sensitive but some people capitalize the keywords to make it easier to read the command.)</span></span>

<span data-ttu-id="b5d9c-172">Le colonne in cui si esegue l'inserimento sono già elencate nel comando, `(Title, Genre, Year)`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-172">The columns that you're inserting into are already listed in the command — `(Title, Genre, Year)`.</span></span> <span data-ttu-id="b5d9c-173">La parte interessante è il modo in cui si ottengono i valori dalle caselle di testo nella `VALUES` parte del comando.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-173">The interesting part is how you get the values from the text boxes into the `VALUES` part of the command.</span></span> <span data-ttu-id="b5d9c-174">Anziché i valori effettivi, vengono visualizzati `@0`, `@1`e `@2`, che sono segnaposto del corso.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-174">Instead of actual values, you see `@0`, `@1`, and `@2`, which are of course placeholders.</span></span> <span data-ttu-id="b5d9c-175">Quando si esegue il comando (sulla riga `db.Execute`), si passano i valori ottenuti dalle caselle di testo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-175">When you run the command (on the `db.Execute` line), you pass the values that you got from the text boxes.</span></span>

<span data-ttu-id="b5d9c-176">**Importante**:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-176">**Important!**</span></span> <span data-ttu-id="b5d9c-177">Tenere presente che l'unico modo per includere i dati immessi online da un utente in un'istruzione SQL consiste nell'usare i segnaposto, come si può vedere qui (`VALUES(@0, @1, @2)`).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-177">Remember that the only way you should ever include data entered online by a user in a SQL statement is to use placeholders, as you see here (`VALUES(@0, @1, @2)`).</span></span> <span data-ttu-id="b5d9c-178">Se si concatena l'input dell'utente in un'istruzione SQL, si apre manualmente un attacco SQL injection, come illustrato nelle [nozioni di base del modulo in pagine Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581) (esercitazione precedente).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-178">If you concatenate user input into a SQL statement, you open yourself to a SQL injection attack, as explained in [Form Basics in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (the previous tutorial).</span></span>

<span data-ttu-id="b5d9c-179">Sempre all'interno del blocco `if` aggiungere la riga seguente dopo la riga `db.Execute`:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-179">Still inside the `if` block, add the following line after the `db.Execute` line:</span></span>

[!code-css[Main](entering-data/samples/sample4.css)]

<span data-ttu-id="b5d9c-180">Dopo che il nuovo film è stato inserito nel database, questa riga consente di passare (reindirizzare) alla pagina *Movies* per visualizzare il film appena immesso.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-180">After the new movie has been inserted into the database, this line jumps you (redirects) to the *Movies* page so you can see the movie you just entered.</span></span> <span data-ttu-id="b5d9c-181">L'operatore `~` indica la radice del sito Web.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-181">The `~` operator means "root of the website."</span></span> <span data-ttu-id="b5d9c-182">L'operatore `~` funziona solo nelle pagine ASP.NET, non in genere in HTML.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-182">(The `~` operator works only in ASP.NET pages, not in HTML generally.)</span></span>

<span data-ttu-id="b5d9c-183">Il blocco di codice completo ha un aspetto simile a questo esempio:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-183">The complete code block looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample5.cshtml)]

## <a name="testing-the-insert-command-so-far"></a><span data-ttu-id="b5d9c-184">Test del comando Insert (finora)</span><span class="sxs-lookup"><span data-stu-id="b5d9c-184">Testing the Insert Command (So Far)</span></span>

<span data-ttu-id="b5d9c-185">Non è ancora stato fatto, ma ora è opportuno eseguire il test.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-185">You're not done yet, but now is a good time to test.</span></span>

<span data-ttu-id="b5d9c-186">Nella visualizzazione albero dei file in WebMatrix, fare clic con il pulsante destro del mouse sulla pagina *AddMovie. cshtml* e quindi fare clic su **Avvia nel browser**.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-186">In the tree view of files in WebMatrix, right-click the *AddMovie.cshtml* page and then click **Launch in browser**.</span></span>

![Pagina ' Aggiungi film ' nel browser](entering-data/_static/image2.png)

<span data-ttu-id="b5d9c-188">Se si verifica una pagina diversa nel browser, assicurarsi che l'URL sia `http://localhost:nnnnn/AddMovie`), dove *nnnnn* è il numero di porta in uso.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-188">(If you end up with a different page in the browser, make sure that the URL is `http://localhost:nnnnn/AddMovie`), where *nnnnn* is the port number that you're using.)</span></span>

<span data-ttu-id="b5d9c-189">È stata riportata una pagina di errore?</span><span class="sxs-lookup"><span data-stu-id="b5d9c-189">Did you get an error page?</span></span> <span data-ttu-id="b5d9c-190">In tal caso, leggerlo attentamente e assicurarsi che il codice appaia esattamente quanto elencato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-190">If so, read it carefully and make sure that the code looks exactly what was listed earlier.</span></span>

<span data-ttu-id="b5d9c-191">Immettere un filmato nel formato &mdash; ad esempio, usare "Citizen Kane", "Drama" e "1941".</span><span class="sxs-lookup"><span data-stu-id="b5d9c-191">Enter a movie in the form &mdash; for example, use "Citizen Kane", "Drama", and "1941".</span></span> <span data-ttu-id="b5d9c-192">(O qualsiasi altro). Quindi fare clic su **Aggiungi film**.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-192">(Or whatever.) Then click **Add Movie**.</span></span>

<span data-ttu-id="b5d9c-193">Se tutto va bene, si viene reindirizzati alla pagina dei *film* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-193">If all goes well, you're redirected to the *Movies* page.</span></span> <span data-ttu-id="b5d9c-194">Verificare che sia elencato il nuovo film.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-194">Make sure that your new movie is listed.</span></span>

![Pagina film che mostra il film appena aggiunto](entering-data/_static/image3.png)

## <a name="validating-user-input"></a><span data-ttu-id="b5d9c-196">Convalida dell'input utente</span><span class="sxs-lookup"><span data-stu-id="b5d9c-196">Validating User Input</span></span>

<span data-ttu-id="b5d9c-197">Tornare alla pagina *AddMovie* o eseguirlo di nuovo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-197">Go back to the *AddMovie* page, or run it again.</span></span> <span data-ttu-id="b5d9c-198">Immettere un altro film, ma questa volta immettere solo il titolo &mdash; ad esempio, immettere "cantando in the Rain".</span><span class="sxs-lookup"><span data-stu-id="b5d9c-198">Enter another movie, but this time, enter only the title &mdash; for example, enter "Singin' in the Rain".</span></span> <span data-ttu-id="b5d9c-199">Quindi fare clic su **Aggiungi film**.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-199">Then click **Add Movie**.</span></span>

<span data-ttu-id="b5d9c-200">Si viene reindirizzati di nuovo alla pagina *Movies* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-200">You're redirected to the *Movies* page again.</span></span> <span data-ttu-id="b5d9c-201">È possibile trovare il nuovo film, ma è incompleto.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-201">You can find the new movie, but it's incomplete.</span></span>

![Pagina film che mostra un nuovo film in cui mancano alcuni valori](entering-data/_static/image4.png)

<span data-ttu-id="b5d9c-203">Quando è stata creata la tabella *Movies* , si è dichiarato in modo esplicito che nessuno dei campi potrebbe essere null.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-203">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="b5d9c-204">Qui è disponibile un modulo di immissione per i nuovi film e i campi vengono lasciati vuoti.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-204">Here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="b5d9c-205">Questo è un errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-205">That's an error.</span></span>

<span data-ttu-id="b5d9c-206">In questo caso, il database non ha effettivamente generato o *generato*un errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-206">In this case, the database didn't actually raise (or *throw*) an error.</span></span> <span data-ttu-id="b5d9c-207">Non è stato fornito un genere o un anno, quindi il codice nella pagina *AddMovie* ha trattato tali valori come *stringhe vuote*.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-207">You didn't supply a genre or year, so the code in the *AddMovie* page treated those values as so-called *empty strings*.</span></span> <span data-ttu-id="b5d9c-208">Quando viene eseguito il comando SQL `Insert Into`, i campi genere e anno non contengono dati utili, ma non sono null.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-208">When the SQL `Insert Into` command ran, the genre and year fields didn't have useful data in them, but they weren't null.</span></span>

<span data-ttu-id="b5d9c-209">Ovviamente, non si vuole consentire agli utenti di immettere nel database informazioni di film a metà vuote.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-209">Obviously, you don't want to let users enter half-empty movie information into the database.</span></span> <span data-ttu-id="b5d9c-210">La soluzione consiste nel convalidare l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-210">The solution is to validate the user's input.</span></span> <span data-ttu-id="b5d9c-211">Inizialmente, la convalida assicurerà semplicemente che l'utente abbia immesso un valore per tutti i campi, ovvero che nessuno di essi contenga una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-211">Initially, the validation will simply make sure that the user has entered a value for all of the fields (that is, that none of them contains an empty string).</span></span>

> [!TIP]
> 
> <span data-ttu-id="b5d9c-212">**Stringhe null e vuote**</span><span class="sxs-lookup"><span data-stu-id="b5d9c-212">**Null and Empty Strings**</span></span>
> 
> <span data-ttu-id="b5d9c-213">Nella programmazione esiste una differenza tra nozioni diverse di "nessun valore".</span><span class="sxs-lookup"><span data-stu-id="b5d9c-213">In programming, there's a distinction between different notions of "no value."</span></span> <span data-ttu-id="b5d9c-214">In generale, un valore è *null* se non è mai stato impostato o inizializzato in alcun modo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-214">In general, a value is *null* if it has never been set or initialized in any way.</span></span> <span data-ttu-id="b5d9c-215">Al contrario, una variabile che prevede dati di tipo carattere (stringhe) può essere impostata su una *stringa vuota*.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-215">In contrast, a variable that expects character data (strings) can be set to an *empty string*.</span></span> <span data-ttu-id="b5d9c-216">In tal caso, il valore non è null. è stato appena impostato in modo esplicito su una stringa di caratteri la cui lunghezza è zero.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-216">In that case, the value is not null; it's just been explicitly set to a string of characters whose length is zero.</span></span> <span data-ttu-id="b5d9c-217">Queste due istruzioni mostrano la differenza:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-217">These two statements show the difference:</span></span>
> 
> [!code-csharp[Main](entering-data/samples/sample6.cs)]
> 
> <span data-ttu-id="b5d9c-218">È un po' più complicato, ma il punto importante è che `null` rappresenta un tipo di stato non determinato.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-218">It's a little more complicated than that, but the important point is that `null` represents a sort of undetermined state.</span></span>
> 
> <span data-ttu-id="b5d9c-219">A questo punto è importante capire esattamente quando un valore è null e quando si tratta semplicemente di una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-219">Now and then it's important to understand exactly when a value is null and when it's just an empty string.</span></span> <span data-ttu-id="b5d9c-220">Nel codice per la pagina *AddMovie* è possibile ottenere i valori delle caselle di testo usando `Request.Form["title"]` e così via.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-220">In the code for the *AddMovie* page, you get the values of the text boxes by using `Request.Form["title"]` and so on.</span></span> <span data-ttu-id="b5d9c-221">Quando la pagina viene eseguita per la prima volta (prima di fare clic sul pulsante), il valore di `Request.Form["title"]` è null.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-221">When the page first runs (before you click the button), the value of `Request.Form["title"]` is null.</span></span> <span data-ttu-id="b5d9c-222">Tuttavia, quando si invia il modulo, `Request.Form["title"]` ottiene il valore della casella di testo `title`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-222">But when you submit the form, `Request.Form["title"]` gets the value of the `title` text box.</span></span> <span data-ttu-id="b5d9c-223">Non è ovvio, ma una casella di testo vuota non è null. contiene solo una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-223">It's not obvious, but an empty text box is not null; it just has an empty string in it.</span></span> <span data-ttu-id="b5d9c-224">Quindi, quando il codice viene eseguito in risposta al clic del pulsante, `Request.Form["title"]` contiene una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-224">So when the code runs in response to the button click, `Request.Form["title"]` has an empty string in it.</span></span>
> 
> <span data-ttu-id="b5d9c-225">Perché questa distinzione è importante?</span><span class="sxs-lookup"><span data-stu-id="b5d9c-225">Why is this distinction important?</span></span> <span data-ttu-id="b5d9c-226">Quando è stata creata la tabella *Movies* , si è dichiarato in modo esplicito che nessuno dei campi potrebbe essere null.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-226">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="b5d9c-227">Ma qui è disponibile un modulo di immissione per i nuovi film e i campi vengono lasciati vuoti.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-227">But here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="b5d9c-228">Si prevede ragionevolmente che il database si ripresentasse quando si è tentato di salvare nuovi film che non hanno valori per genere o anno.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-228">You would reasonably expect the database to complain when you tried to save new movies that didn't have values for genre or year.</span></span> <span data-ttu-id="b5d9c-229">Questo è il punto &mdash; anche se si lasciano vuote le caselle di testo, i valori non sono null; sono stringhe vuote.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-229">But that's the point &mdash; even if you leave those text boxes blank, the values aren't null; they're empty strings.</span></span> <span data-ttu-id="b5d9c-230">Di conseguenza, è possibile salvare nuovi filmati nel database con queste colonne vuote &mdash; ma non null.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-230">As a result, you're able to save new movies to the database with these columns empty &mdash; but not null!</span></span> <span data-ttu-id="b5d9c-231">valori &mdash;.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-231">&mdash; values.</span></span> <span data-ttu-id="b5d9c-232">Pertanto, è necessario assicurarsi che gli utenti non invii una stringa vuota, operazione che è possibile eseguire convalidando l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-232">Therefore, you have to make sure that users don't submit an empty string, which you can do by validating the user's input.</span></span>

### <a name="the-validation-helper"></a><span data-ttu-id="b5d9c-233">Helper di convalida</span><span class="sxs-lookup"><span data-stu-id="b5d9c-233">The Validation Helper</span></span>

<span data-ttu-id="b5d9c-234">Pagine Web ASP.NET include un helper &mdash; `Validation` Helper &mdash; che è possibile usare per assicurarsi che gli utenti immettano i dati che soddisfano i requisiti.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-234">ASP.NET Web Pages includes a helper &mdash; the `Validation` helper &mdash; that you can use to make sure that users enter data that meets your requirements.</span></span> <span data-ttu-id="b5d9c-235">Il `Validation` Helper è uno degli helper predefiniti per Pagine Web ASP.NET, pertanto non è necessario installarlo come pacchetto usando NuGet, il modo in cui è stato installato l'helper Gravatar in un'esercitazione precedente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-235">The `Validation` helper is one of the helpers that's built in to ASP.NET Web Pages, so you don't have to install it as a package by using NuGet, the way you installed the Gravatar helper in an earlier tutorial.</span></span>

<span data-ttu-id="b5d9c-236">Per convalidare l'input dell'utente, eseguire le operazioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-236">To validate the user's input, you'll do the following:</span></span>

- <span data-ttu-id="b5d9c-237">Usare il codice per specificare che si desidera richiedere valori nelle caselle di testo nella pagina.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-237">Use code to specify that you want to require values in the text boxes on the page.</span></span>
- <span data-ttu-id="b5d9c-238">Inserire un test nel codice in modo che le informazioni sul film vengano aggiunte al database solo se tutto viene convalidato correttamente.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-238">Put a test into the code so that the movie information is added to the database only if everything validates properly.</span></span>
- <span data-ttu-id="b5d9c-239">Aggiungere codice al markup per visualizzare i messaggi di errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-239">Add code into the markup to display error messages.</span></span>

<span data-ttu-id="b5d9c-240">Nel blocco di codice nella pagina *AddMovie* , a destra in alto prima delle dichiarazioni di variabili, aggiungere il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-240">In the code block in the *AddMovie* page, right up at the top before the variable declarations, add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample7.cs)]

<span data-ttu-id="b5d9c-241">Chiamare `Validation.RequireField` una volta per ogni campo (`<input>` elemento) in cui si desidera richiedere una voce.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-241">You call `Validation.RequireField` once for each field (`<input>` element) where you want to require an entry.</span></span> <span data-ttu-id="b5d9c-242">È anche possibile aggiungere un messaggio di errore personalizzato per ogni chiamata, come si può vedere qui.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-242">You can also add a custom error message for each call, like you see here.</span></span> <span data-ttu-id="b5d9c-243">I messaggi sono stati variati solo per mostrare che è possibile inserire qualsiasi elemento.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-243">(We varied the messages just to show that you can put anything you like there.)</span></span>

<span data-ttu-id="b5d9c-244">Se si verifica un problema, si desidera impedire l'inserimento delle nuove informazioni sul film nel database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-244">If there's a problem, you want to prevent the new movie information from being inserted into the database.</span></span> <span data-ttu-id="b5d9c-245">Nel blocco `if(IsPost)` usare `&&` (AND logico) per aggiungere un'altra condizione che verifica `Validation.IsValid()`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-245">In the `if(IsPost)` block, use `&&` (logical AND) to add another condition that tests `Validation.IsValid()`.</span></span> <span data-ttu-id="b5d9c-246">Al termine, l'intero blocco `if(IsPost)` sarà simile a questo codice:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-246">When you're done, the whole `if(IsPost)` block looks like this code:</span></span>

[!code-csharp[Main](entering-data/samples/sample8.cs)]

<span data-ttu-id="b5d9c-247">Se si verifica un errore di convalida con uno dei campi registrati usando l'helper `Validation`, il metodo `Validation.IsValid` restituisce false.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-247">If there's a validation error with any of the fields that you registered by using the `Validation` helper, the `Validation.IsValid` method returns false.</span></span> <span data-ttu-id="b5d9c-248">In tal caso, non verrà eseguito alcuno del codice del blocco, quindi non verranno inserite voci di film non valide nel database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-248">And in that case, none of the code in that block will run, so no invalid movie entries will be inserted into the database.</span></span> <span data-ttu-id="b5d9c-249">Ovviamente, non si è reindirizzati alla pagina dei *film* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-249">And of course you're not redirected to the *Movies* page.</span></span>

<span data-ttu-id="b5d9c-250">Il blocco di codice completo, incluso il codice di convalida, ha ora un aspetto simile a questo esempio:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-250">The complete code block, including the validation code, now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample9.cshtml?highlight=10)]

## <a name="displaying-validation-errors"></a><span data-ttu-id="b5d9c-251">Visualizzazione degli errori di convalida</span><span class="sxs-lookup"><span data-stu-id="b5d9c-251">Displaying Validation Errors</span></span>

<span data-ttu-id="b5d9c-252">L'ultimo passaggio consiste nel visualizzare eventuali messaggi di errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-252">The last step is to display any error messages.</span></span> <span data-ttu-id="b5d9c-253">È possibile visualizzare singoli messaggi per ogni errore di convalida oppure è possibile visualizzare un riepilogo o entrambi.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-253">You can display individual messages for each validation error, or you can display a summary, or both.</span></span> <span data-ttu-id="b5d9c-254">Per questa esercitazione verranno eseguite entrambe le operazioni in modo che sia possibile visualizzarne il funzionamento.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-254">For this tutorial, you'll do both so that you can see how it works.</span></span>

<span data-ttu-id="b5d9c-255">Accanto a ogni elemento `<input>` che si sta convalidando, chiamare il metodo `Html.ValidationMessage` e passargli il nome dell'elemento `<input>` che si sta convalidando.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-255">Next to each `<input>` element that you're validating, call the `Html.ValidationMessage` method and pass it the name of the `<input>` element you're validating.</span></span> <span data-ttu-id="b5d9c-256">Inserire il metodo `Html.ValidationMessage` nel punto in cui si desidera che venga visualizzato il messaggio di errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-256">You put the `Html.ValidationMessage` method right where you want the error message to appear.</span></span> <span data-ttu-id="b5d9c-257">Quando la pagina viene eseguita, il metodo `Html.ValidationMessage` esegue il rendering di un elemento `<span>` in cui verrà visualizzato l'errore di convalida.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-257">When the page runs, the `Html.ValidationMessage` method renders a `<span>` element where the validation error will go.</span></span> <span data-ttu-id="b5d9c-258">Se non è presente alcun errore, viene eseguito il rendering dell'elemento `<span>`, ma non è presente alcun testo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-258">(If there's no error, the `<span>` element is rendered, but there's no text in it.)</span></span>

<span data-ttu-id="b5d9c-259">Modificare il markup nella pagina in modo che includa un `Html.ValidationMessage` metodo per ognuno dei tre elementi `<input>` nella pagina, come nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-259">Change the markup in the page so that it includes an `Html.ValidationMessage` method for each of the three `<input>` elements on the page, like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample10.cshtml?highlight=3,8,13)]

<span data-ttu-id="b5d9c-260">Per verificare il funzionamento del riepilogo, aggiungere anche il markup e il codice seguenti subito dopo l'elemento `<h1>Add a Movie</h1>` nella pagina:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-260">To see how the summary works, also add the following markup and code right after the `<h1>Add a Movie</h1>` element on the page:</span></span>

[!code-cshtml[Main](entering-data/samples/sample11.cshtml)]

<span data-ttu-id="b5d9c-261">Per impostazione predefinita, il metodo `Html.ValidationSummary` Visualizza tutti i messaggi di convalida in un elenco (un elemento `<ul>` all'interno di un elemento `<div>`).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-261">By default, the `Html.ValidationSummary` method displays all the validation messages in a list (a `<ul>` element that's inside a `<div>` element).</span></span> <span data-ttu-id="b5d9c-262">Come per il metodo `Html.ValidationMessage`, viene sempre eseguito il rendering del markup per il riepilogo di convalida. Se non sono presenti errori, non viene eseguito il rendering degli elementi dell'elenco.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-262">As with the `Html.ValidationMessage` method, the markup for the validation summary is always rendered; if there are no errors, no list items are rendered.</span></span>

<span data-ttu-id="b5d9c-263">Il riepilogo può essere un modo alternativo per visualizzare i messaggi di convalida anziché utilizzare il metodo `Html.ValidationMessage` per visualizzare ogni errore specifico del campo.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-263">The summary can be an alternative way to display validation messages instead of by using the `Html.ValidationMessage` method to display each field-specific error.</span></span> <span data-ttu-id="b5d9c-264">In alternativa, è possibile usare un riepilogo e i dettagli.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-264">Or you can use both a summary and the details.</span></span> <span data-ttu-id="b5d9c-265">In alternativa, è possibile usare il metodo `Html.ValidationSummary` per visualizzare un errore generico e quindi usare singole chiamate `Html.ValidationMessage` per visualizzare i dettagli.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-265">Or you can use the `Html.ValidationSummary` method to display a generic error and then use individual `Html.ValidationMessage` calls to display details.</span></span>

<span data-ttu-id="b5d9c-266">La pagina completa ha ora un aspetto simile a questo esempio:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-266">The complete page now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample12.cshtml)]

<span data-ttu-id="b5d9c-267">È tutto.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-267">That's it.</span></span> <span data-ttu-id="b5d9c-268">È ora possibile testare la pagina aggiungendo un film senza uscire da uno o più campi.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-268">You can now test the page by adding a movie but leaving out one or more of the fields.</span></span> <span data-ttu-id="b5d9c-269">Quando si esegue questa operazione, viene visualizzato il messaggio di errore seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-269">When you do, you see the following error display:</span></span>

![Aggiungi pagina di film con messaggi di errore di convalida](entering-data/_static/image5.png)

## <a name="styling-the-validation-error-messages"></a><span data-ttu-id="b5d9c-271">Applicazione di stili ai messaggi di errore di convalida</span><span class="sxs-lookup"><span data-stu-id="b5d9c-271">Styling the Validation Error Messages</span></span>

<span data-ttu-id="b5d9c-272">Si noterà che sono presenti messaggi di errore, ma che non si distinguono molto bene.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-272">You can see that there are error messages, but they don't really stand out very well.</span></span> <span data-ttu-id="b5d9c-273">Tuttavia, esiste un modo semplice per applicare uno stile ai messaggi di errore.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-273">There's an easy way to style the error messages, though.</span></span>

<span data-ttu-id="b5d9c-274">Per applicare uno stile ai singoli messaggi di errore visualizzati da `Html.ValidationMessage`, creare una classe di stile CSS denominata `field-validation-error`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-274">To style the individual error messages that are displayed by `Html.ValidationMessage`, create a CSS style class named `field-validation-error`.</span></span> <span data-ttu-id="b5d9c-275">Per definire la ricerca del riepilogo di convalida, creare una classe di stile CSS denominata `validation-summary-errors`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-275">To define the look for the validation summary, create a CSS style class named `validation-summary-errors`.</span></span>

<span data-ttu-id="b5d9c-276">Per verificare il funzionamento di questa tecnica, aggiungere un elemento `<style>` nella sezione `<head>` della pagina.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-276">To see how this technique works, add a `<style>` element inside the `<head>` section of the page.</span></span> <span data-ttu-id="b5d9c-277">Definire quindi le classi di stile denominate `field-validation-error` e `validation-summary-errors` che contengono le regole seguenti:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-277">Then define style classes named `field-validation-error` and `validation-summary-errors` that contain the following rules:</span></span>

[!code-cshtml[Main](entering-data/samples/sample13.cshtml?highlight=4-17)]

<span data-ttu-id="b5d9c-278">In genere è probabile che le informazioni sullo stile vengano inserite in un file *CSS* separato, ma per semplicità è possibile inserirle nella pagina per il momento.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-278">Normally you'd probably put style information into a separate *.css* file, but for simplicity you can put them in the page for now.</span></span> <span data-ttu-id="b5d9c-279">(Più avanti in questo set di esercitazioni, le regole CSS verranno spostate in un file *CSS* separato).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-279">(Later in this tutorial set, you'll move the CSS rules to a separate *.css* file.)</span></span>

<span data-ttu-id="b5d9c-280">Se si verifica un errore di convalida, il metodo `Html.ValidationMessage` esegue il rendering di un elemento `<span>` che include `class="field-validation-error"`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-280">If there's a validation error, the `Html.ValidationMessage` method renders a `<span>` element that includes `class="field-validation-error"`.</span></span> <span data-ttu-id="b5d9c-281">Aggiungendo una definizione di stile per la classe, è possibile configurare l'aspetto del messaggio.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-281">By adding a style definition for that class, you can configure what the message looks like.</span></span> <span data-ttu-id="b5d9c-282">Se sono presenti errori, il metodo `ValidationSummary` esegue in modo analogo il rendering dell'attributo `class="validation-summary-errors"`.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-282">If there are errors, the `ValidationSummary` method likewise dynamically renders the attribute `class="validation-summary-errors"`.</span></span>

<span data-ttu-id="b5d9c-283">Eseguire di nuovo la pagina e uscire intenzionalmente da un paio di campi.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-283">Run the page again and deliberately leave out a couple of the fields.</span></span> <span data-ttu-id="b5d9c-284">Gli errori sono ora più evidenti.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-284">The errors are now more noticeable.</span></span> <span data-ttu-id="b5d9c-285">In realtà, si tratta di un aspetto esagerato, ma solo per mostrare cosa è possibile fare.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-285">(In fact, they're overdone, but that's just to show what you can do.)</span></span>

![Aggiungere la pagina di film che Mostra gli errori di convalida con stile](entering-data/_static/image6.png)

## <a name="adding-a-link-to-the-movies-page"></a><span data-ttu-id="b5d9c-287">Aggiunta di un collegamento alla pagina Movies</span><span class="sxs-lookup"><span data-stu-id="b5d9c-287">Adding a Link to the Movies Page</span></span>

<span data-ttu-id="b5d9c-288">Un passaggio finale consiste nel passare alla pagina *AddMovie* dall'elenco dei film originali.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-288">One final step is to make it convenient to get to the *AddMovie* page from the original movie listing.</span></span>

<span data-ttu-id="b5d9c-289">Aprire di nuovo la pagina *Movies* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-289">Open the *Movies* page again.</span></span> <span data-ttu-id="b5d9c-290">Dopo il tag di chiusura `</div>` che segue l'helper `WebGrid`, aggiungere il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-290">After the closing `</div>` tag that follows the `WebGrid` helper, add the following markup:</span></span>

[!code-cshtml[Main](entering-data/samples/sample14.cshtml)]

<span data-ttu-id="b5d9c-291">Come si è visto in precedenza, ASP.NET interpreta l'operatore `~` come radice del sito Web.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-291">As you saw before, ASP.NET interprets the `~` operator as the root of the website.</span></span> <span data-ttu-id="b5d9c-292">Non è necessario usare l'operatore `~`; è possibile utilizzare il markup `<a href="./AddMovie">Add a movie</a>` o un altro modo per definire il percorso che il linguaggio HTML riconosce.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-292">You don't have to use the `~` operator; you could use the markup `<a href="./AddMovie">Add a movie</a>` or some other way to define the path that HTML understands.</span></span> <span data-ttu-id="b5d9c-293">Tuttavia, l'operatore `~` è un approccio generale valido quando si creano collegamenti per le pagine Razor, perché rende più flessibile il sito: se si sposta la pagina corrente in una sottocartella, il collegamento verrà comunque indirizzato alla pagina *AddMovie* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-293">But the `~` operator is a good general approach when you create links for Razor pages, because it makes the site more flexible — if you move the current page to a subfolder, the link will still go to the *AddMovie* page.</span></span> <span data-ttu-id="b5d9c-294">Tenere presente che l'operatore `~` funziona solo in pagine con *estensione cshtml* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-294">(Remember that the `~` operator only works in *.cshtml* pages.</span></span> <span data-ttu-id="b5d9c-295">ASP.NET lo riconosce, ma non è HTML standard.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-295">ASP.NET understands it, but it's not standard HTML.)</span></span>

<span data-ttu-id="b5d9c-296">Al termine, eseguire la pagina *Movies* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-296">When you're done, run the *Movies* page.</span></span> <span data-ttu-id="b5d9c-297">L'aspetto sarà simile a questa pagina:</span><span class="sxs-lookup"><span data-stu-id="b5d9c-297">It will look like this page:</span></span>

![Pagina filmati con collegamento alla pagina "Aggiungi film"](entering-data/_static/image7.png)

<span data-ttu-id="b5d9c-299">Fare clic sul collegamento **Aggiungi un film** per assicurarsi che vada alla pagina *AddMovie* .</span><span class="sxs-lookup"><span data-stu-id="b5d9c-299">Click the **Add a movie** link to make sure that it goes to the *AddMovie* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="b5d9c-300">Prossimi</span><span class="sxs-lookup"><span data-stu-id="b5d9c-300">Coming Up Next</span></span>

<span data-ttu-id="b5d9c-301">Nell'esercitazione successiva si apprenderà come consentire agli utenti di modificare i dati già presenti nel database.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-301">In the next tutorial, you'll learn how to let users edit data that's already in the database.</span></span>

## <a name="complete-listing-for-addmovie-page"></a><span data-ttu-id="b5d9c-302">Elenco completo per la pagina AddMovie</span><span class="sxs-lookup"><span data-stu-id="b5d9c-302">Complete Listing for AddMovie Page</span></span>

[!code-cshtml[Main](entering-data/samples/sample15.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="b5d9c-303">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="b5d9c-303">Additional Resources</span></span>

- [<span data-ttu-id="b5d9c-304">Introduzione alla programmazione Web ASP.NET con la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="b5d9c-304">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="b5d9c-305">[Istruzione SQL INSERT INTO](http://www.w3schools.com/sql/sql_insert.asp) nel sito di W3Schools</span><span class="sxs-lookup"><span data-stu-id="b5d9c-305">[SQL INSERT INTO Statement](http://www.w3schools.com/sql/sql_insert.asp) on the W3Schools site</span></span>
- <span data-ttu-id="b5d9c-306">[Convalida dell'input utente nei siti pagine Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=253002).</span><span class="sxs-lookup"><span data-stu-id="b5d9c-306">[Validating User Input in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span></span> <span data-ttu-id="b5d9c-307">Altre informazioni sull'uso di `Validation` helper.</span><span class="sxs-lookup"><span data-stu-id="b5d9c-307">More information about working with the `Validation` helper.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="b5d9c-308">[Precedente](form-basics.md)
> [Successivo](updating-data.md)</span><span class="sxs-lookup"><span data-stu-id="b5d9c-308">[Previous](form-basics.md)
[Next](updating-data.md)</span></span>
