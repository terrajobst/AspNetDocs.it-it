---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Introduzione a Pagine Web ASP.NET-visualizzazione dei dati | Microsoft Docs
author: Rick-Anderson
description: Questa esercitazione illustra come creare un database in WebMatrix e come visualizzare i dati del database in una pagina quando si usa Pagine Web ASP.NET (Razor). Si presuppone y...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 9e665ca8dd064c23a8b8bd3593014969d0c3da48
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78525221"
---
# <a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="ee602-104">Introduzione a Pagine Web ASP.NET-visualizzazione dei dati</span><span class="sxs-lookup"><span data-stu-id="ee602-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>

<span data-ttu-id="ee602-105">di [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="ee602-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="ee602-106">Questa esercitazione illustra come creare un database in WebMatrix e come visualizzare i dati del database in una pagina quando si usa Pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="ee602-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="ee602-107">Si presuppone che la serie sia stata completata tramite [Introduzione alla programmazione pagine Web ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="ee602-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="ee602-108">Contenuto dell'esercitazione:</span><span class="sxs-lookup"><span data-stu-id="ee602-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="ee602-109">Come usare gli strumenti di WebMatrix per creare un database e le tabelle di database.</span><span class="sxs-lookup"><span data-stu-id="ee602-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="ee602-110">Come usare gli strumenti di WebMatrix per aggiungere dati a un database.</span><span class="sxs-lookup"><span data-stu-id="ee602-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="ee602-111">Come visualizzare i dati dal database in una pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="ee602-112">Come eseguire i comandi SQL in Pagine Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="ee602-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="ee602-113">Come personalizzare l'helper `WebGrid` per modificare la visualizzazione dei dati e per aggiungere il paging e l'ordinamento.</span><span class="sxs-lookup"><span data-stu-id="ee602-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="ee602-114">Funzionalità/tecnologie discusse:</span><span class="sxs-lookup"><span data-stu-id="ee602-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="ee602-115">Strumenti di database di WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="ee602-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="ee602-116">`WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="ee602-116">`WebGrid` helper.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="ee602-117">Scopo dell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="ee602-117">What You'll Build</span></span>

<span data-ttu-id="ee602-118">Nell'esercitazione precedente sono stati introdotti i Pagine Web ASP.NET (file con*estensione cshtml* ), le nozioni di base di sintassi Razor e degli helper.</span><span class="sxs-lookup"><span data-stu-id="ee602-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="ee602-119">In questa esercitazione si inizierà a creare l'applicazione Web effettiva che verrà usata per il resto della serie.</span><span class="sxs-lookup"><span data-stu-id="ee602-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="ee602-120">L'app è una semplice applicazione di film che consente di visualizzare, aggiungere, modificare ed eliminare informazioni sui filmati.</span><span class="sxs-lookup"><span data-stu-id="ee602-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="ee602-121">Al termine di questa esercitazione, sarà possibile visualizzare un elenco di film simile a questa pagina:</span><span class="sxs-lookup"><span data-stu-id="ee602-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Visualizzazione WebGrid che include i parametri impostati sui nomi delle classi CSS](displaying-data/_static/image1.png)

<span data-ttu-id="ee602-123">Tuttavia, per iniziare, è necessario creare un database.</span><span class="sxs-lookup"><span data-stu-id="ee602-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="ee602-124">Una breve introduzione ai database</span><span class="sxs-lookup"><span data-stu-id="ee602-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="ee602-125">Questa esercitazione fornirà solo la più breve introduzione ai database.</span><span class="sxs-lookup"><span data-stu-id="ee602-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="ee602-126">Se si ha esperienza nel database, è possibile ignorare questa breve sezione.</span><span class="sxs-lookup"><span data-stu-id="ee602-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="ee602-127">Un database contiene una o più tabelle contenenti informazioni &mdash; ad esempio tabelle per clienti, ordini e fornitori oppure per studenti, insegnanti, classi e voti.</span><span class="sxs-lookup"><span data-stu-id="ee602-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="ee602-128">Strutturalmente, una tabella di database è simile a un foglio di calcolo.</span><span class="sxs-lookup"><span data-stu-id="ee602-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="ee602-129">Immaginate una tipica Rubrica.</span><span class="sxs-lookup"><span data-stu-id="ee602-129">Imagine a typical address book.</span></span> <span data-ttu-id="ee602-130">Per ogni voce della Rubrica, ovvero per ogni persona, sono disponibili diverse informazioni, ad esempio il nome, il cognome, l'indirizzo, l'indirizzo di posta elettronica e il numero di telefono.</span><span class="sxs-lookup"><span data-stu-id="ee602-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Tabella di database di esempio come griglia semplice](displaying-data/_static/image2.png)

<span data-ttu-id="ee602-132">(Le righe sono talvolta denominate *record*e le colonne sono talvolta denominate *campi*).</span><span class="sxs-lookup"><span data-stu-id="ee602-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="ee602-133">Per la maggior parte delle tabelle di database, la tabella deve includere una colonna contenente un valore univoco, ad esempio un numero di cliente, un numero di conto e così via.</span><span class="sxs-lookup"><span data-stu-id="ee602-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="ee602-134">Questo valore è noto come *chiave primaria*della tabella e viene usato per identificare ogni riga nella tabella.</span><span class="sxs-lookup"><span data-stu-id="ee602-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="ee602-135">Nell'esempio, la colonna ID è la chiave primaria per la rubrica mostrata nell'esempio precedente.</span><span class="sxs-lookup"><span data-stu-id="ee602-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="ee602-136">Gran parte delle operazioni eseguite nelle applicazioni Web consiste nel leggere le informazioni dal database e visualizzarle in una pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="ee602-137">Spesso si raccolgono informazioni dagli utenti e le si aggiunge a un database oppure si modificano i record già presenti nel database.</span><span class="sxs-lookup"><span data-stu-id="ee602-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="ee602-138">Queste operazioni verranno illustrate nel corso di questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="ee602-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="ee602-139">Il lavoro del database può essere estremamente complesso e può richiedere conoscenze specializzate.</span><span class="sxs-lookup"><span data-stu-id="ee602-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="ee602-140">Per questa esercitazione, tuttavia, è necessario comprendere solo i concetti di base, che verranno tutti spiegati Man mano che si procede.</span><span class="sxs-lookup"><span data-stu-id="ee602-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="ee602-141">Creazione di un database</span><span class="sxs-lookup"><span data-stu-id="ee602-141">Creating a Database</span></span>

<span data-ttu-id="ee602-142">WebMatrix include strumenti che semplificano la creazione di un database e la creazione di tabelle nel database.</span><span class="sxs-lookup"><span data-stu-id="ee602-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="ee602-143">La struttura di un database viene definita *schema*del database. Per questa esercitazione verrà creato un database in cui è presente una sola tabella &mdash; Movies.</span><span class="sxs-lookup"><span data-stu-id="ee602-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="ee602-144">Aprire WebMatrix, se non è già stato fatto, e aprire il sito WebPagesMovies creato nell'esercitazione precedente.</span><span class="sxs-lookup"><span data-stu-id="ee602-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="ee602-145">Nel riquadro sinistro fare clic sull'area di lavoro **database** .</span><span class="sxs-lookup"><span data-stu-id="ee602-145">In the left pane, click the **Database** workspace.</span></span>

![Scheda area di lavoro database WebMatrix](displaying-data/_static/image3.png)

<span data-ttu-id="ee602-147">La barra multifunzione cambia per visualizzare le attività correlate al database.</span><span class="sxs-lookup"><span data-stu-id="ee602-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="ee602-148">Sulla barra multifunzione fare clic su **nuovo database**.</span><span class="sxs-lookup"><span data-stu-id="ee602-148">In the ribbon, click **New Database**.</span></span>

![Pulsante "nuovo database" nella barra multifunzione di WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="ee602-150">WebMatrix crea un database SQL Server CE (un file con *estensione sdf* ) con lo stesso nome del sito &mdash; *WebPagesMovies. sdf*.</span><span class="sxs-lookup"><span data-stu-id="ee602-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="ee602-151">Questa operazione non verrà eseguita qui, ma è possibile rinominare il file in qualsiasi elemento, purché disponga di un'estensione *SDF* .</span><span class="sxs-lookup"><span data-stu-id="ee602-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="ee602-152">Creazione di una tabella</span><span class="sxs-lookup"><span data-stu-id="ee602-152">Creating a Table</span></span>

<span data-ttu-id="ee602-153">Sulla barra multifunzione fare clic su **nuova tabella**.</span><span class="sxs-lookup"><span data-stu-id="ee602-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="ee602-154">WebMatrix apre Progettazione tabelle in una nuova scheda. se l'opzione nuova tabella non è disponibile, assicurarsi che il nuovo database sia selezionato nella visualizzazione albero a sinistra.</span><span class="sxs-lookup"><span data-stu-id="ee602-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Pulsante ' nuova tabella ' nella barra multifunzione di WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="ee602-156">Nella casella di testo nella parte superiore, in cui la filigrana indica "immettere il nome della tabella", immettere "Movies".</span><span class="sxs-lookup"><span data-stu-id="ee602-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Immissione di un nome di tabella in Progettazione database WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="ee602-158">Il riquadro sotto il nome della tabella è il punto in cui si definiscono le singole colonne.</span><span class="sxs-lookup"><span data-stu-id="ee602-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="ee602-159">Per la tabella *Movies* in questa esercitazione verranno create solo alcune colonne, ovvero *ID*, *title*, *genre*e *year*.</span><span class="sxs-lookup"><span data-stu-id="ee602-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="ee602-160">Nella casella **nome** immettere "ID".</span><span class="sxs-lookup"><span data-stu-id="ee602-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="ee602-161">Se si immette un valore, vengono attivati tutti i controlli per la nuova colonna.</span><span class="sxs-lookup"><span data-stu-id="ee602-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="ee602-162">Premere TAB per l'elenco **tipo di dati** e scegliere **int**. Questo valore specifica che la colonna ID conterrà dati Integer (numero).</span><span class="sxs-lookup"><span data-stu-id="ee602-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="ee602-163">Non è più possibile chiamarlo qui (molto), ma è possibile usare i movimenti standard della tastiera di Windows per spostarsi in questa griglia.</span><span class="sxs-lookup"><span data-stu-id="ee602-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="ee602-164">Ad esempio, è possibile digitare una tabulazione tra i campi, è sufficiente iniziare a digitare per selezionare un elemento in un elenco e così via.</span><span class="sxs-lookup"><span data-stu-id="ee602-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>

<span data-ttu-id="ee602-165">Scheda oltre la casella **valore predefinito** (ovvero lasciare vuoto).</span><span class="sxs-lookup"><span data-stu-id="ee602-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="ee602-166">Nella casella di controllo **è la chiave primaria** e selezionarla.</span><span class="sxs-lookup"><span data-stu-id="ee602-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="ee602-167">Questa opzione indica al database che la colonna *ID* conterrà i dati che identificano le singole righe.</span><span class="sxs-lookup"><span data-stu-id="ee602-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="ee602-168">Ovvero ogni riga avrà un valore univoco nella colonna ID che è possibile utilizzare per trovare la riga.</span><span class="sxs-lookup"><span data-stu-id="ee602-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="ee602-169">Scegliere l'opzione **è Identity** .</span><span class="sxs-lookup"><span data-stu-id="ee602-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="ee602-170">Questa opzione indica al database di generare automaticamente il successivo numero sequenziale per ogni nuova riga.</span><span class="sxs-lookup"><span data-stu-id="ee602-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="ee602-171">L'opzione **is Identity** funziona solo se è stata selezionata anche l'opzione **is primary key** .</span><span class="sxs-lookup"><span data-stu-id="ee602-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="ee602-172">Fare clic sulla riga della griglia successiva oppure premere TAB due volte per terminare la riga corrente.</span><span class="sxs-lookup"><span data-stu-id="ee602-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="ee602-173">Il gesto salva la riga corrente e avvia quella successiva.</span><span class="sxs-lookup"><span data-stu-id="ee602-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="ee602-174">Si noti che la colonna **valore predefinito** ora indica **null**.</span><span class="sxs-lookup"><span data-stu-id="ee602-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="ee602-175">(Null è il valore predefinito per il valore predefinito, quindi è necessario pronunciare).</span><span class="sxs-lookup"><span data-stu-id="ee602-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="ee602-176">Al termine della definizione della nuova colonna **ID** , la finestra di progettazione sarà simile a quella illustrata di seguito:</span><span class="sxs-lookup"><span data-stu-id="ee602-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Progettazione database WebMatrix dopo la definizione della colonna ID per la tabella Movies](displaying-data/_static/image7.png)

<span data-ttu-id="ee602-178">Per creare la colonna successiva, fare clic nella casella nella colonna **nome** .</span><span class="sxs-lookup"><span data-stu-id="ee602-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="ee602-179">Immettere "title" per la colonna e quindi selezionare **nvarchar** come valore del **tipo di dati** .</span><span class="sxs-lookup"><span data-stu-id="ee602-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="ee602-180">La parte "var" di **nvarchar** indica al database che i dati per questa colonna saranno una stringa le cui dimensioni possono variare da record a record.</span><span class="sxs-lookup"><span data-stu-id="ee602-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="ee602-181">Il prefisso "n" rappresenta "National", che indica che il campo può contenere dati di tipo carattere per qualsiasi alfabeto o sistema di scrittura, ovvero che il campo contenga dati Unicode.</span><span class="sxs-lookup"><span data-stu-id="ee602-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="ee602-182">Quando si sceglie **nvarchar**, viene visualizzata un'altra casella in cui è possibile immettere la lunghezza massima per il campo.</span><span class="sxs-lookup"><span data-stu-id="ee602-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="ee602-183">Immettere 50, presumendo che nessun titolo di film che verrà usato in questa esercitazione sia più lungo di 50 caratteri.</span><span class="sxs-lookup"><span data-stu-id="ee602-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="ee602-184">Ignorare il **valore predefinito** e deselezionare l'opzione **Consenti valori null** .</span><span class="sxs-lookup"><span data-stu-id="ee602-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="ee602-185">Non si desidera che il database consenta l'immissione di filmati nel database che non dispongono di un titolo.</span><span class="sxs-lookup"><span data-stu-id="ee602-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="ee602-186">Al termine della riga successiva, la finestra di progettazione sarà simile a quella illustrata di seguito:</span><span class="sxs-lookup"><span data-stu-id="ee602-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Progettazione database WebMatrix dopo la definizione della colonna title per la tabella Movies](displaying-data/_static/image8.png)

<span data-ttu-id="ee602-188">Ripetere questi passaggi per creare una colonna denominata "genre", ad eccezione della lunghezza, impostarla su solo 30.</span><span class="sxs-lookup"><span data-stu-id="ee602-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="ee602-189">Creare un'altra colonna denominata "Year".</span><span class="sxs-lookup"><span data-stu-id="ee602-189">Create another column named "Year."</span></span> <span data-ttu-id="ee602-190">Per il tipo di dati, scegliere **nchar** (non **nvarchar**) e impostare la lunghezza su 4.</span><span class="sxs-lookup"><span data-stu-id="ee602-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="ee602-191">Per l'anno, si userà un numero a 4 cifre, ad esempio "1995" o "2010", pertanto non è necessaria una colonna di dimensioni variabili.</span><span class="sxs-lookup"><span data-stu-id="ee602-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="ee602-192">Ecco come appare la progettazione completata:</span><span class="sxs-lookup"><span data-stu-id="ee602-192">Here's what the finished design looks like:</span></span>

![Progettazione database WebMatrix dopo che tutti i campi sono stati definiti per la tabella Movies](displaying-data/_static/image9.png)

<span data-ttu-id="ee602-194">Premere CTRL + S oppure fare clic sul pulsante **Salva** nella barra di accesso rapido.</span><span class="sxs-lookup"><span data-stu-id="ee602-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="ee602-195">Chiudere la finestra di progettazione database chiudendo la scheda.</span><span class="sxs-lookup"><span data-stu-id="ee602-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="ee602-196">Aggiunta di alcuni dati di esempio</span><span class="sxs-lookup"><span data-stu-id="ee602-196">Adding Some Example Data</span></span>

<span data-ttu-id="ee602-197">Più avanti in questa serie di esercitazioni verrà creata una pagina in cui è possibile immettere nuovi film in un modulo.</span><span class="sxs-lookup"><span data-stu-id="ee602-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="ee602-198">Per il momento, tuttavia, è possibile aggiungere alcuni dati di esempio che è possibile visualizzare in una pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="ee602-199">Nell'area di lavoro **database** in WebMatrix si noti che è presente un albero che mostra il file con *estensione sdf* creato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="ee602-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="ee602-200">Aprire il nodo per il nuovo file con *estensione sdf* , quindi aprire il nodo **tabelle** .</span><span class="sxs-lookup"><span data-stu-id="ee602-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Area di lavoro database WebMatrix con albero aperto alla tabella Movies](displaying-data/_static/image10.png)

<span data-ttu-id="ee602-202">Fare clic con il pulsante destro del mouse sul nodo **Movies** , quindi scegliere **dati**.</span><span class="sxs-lookup"><span data-stu-id="ee602-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="ee602-203">WebMatrix apre una griglia in cui è possibile immettere i dati per la tabella *Movies* :</span><span class="sxs-lookup"><span data-stu-id="ee602-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Griglia di immissione di database in WebMatrix (vuota)](displaying-data/_static/image11.png)

<span data-ttu-id="ee602-205">Fare clic sulla colonna **title** e immettere "When Harry Met Sally".</span><span class="sxs-lookup"><span data-stu-id="ee602-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="ee602-206">Passare alla colonna **genre** (è possibile usare il tasto TAB) e immettere "Romantic Comedy".</span><span class="sxs-lookup"><span data-stu-id="ee602-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="ee602-207">Passare alla colonna **year** e immettere "1989":</span><span class="sxs-lookup"><span data-stu-id="ee602-207">Move to the **Year** column and enter "1989":</span></span>

![Griglia di immissione di database in WebMatrix con un record](displaying-data/_static/image12.png)

<span data-ttu-id="ee602-209">Premere INVIO. WebMatrix Salva il nuovo film.</span><span class="sxs-lookup"><span data-stu-id="ee602-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="ee602-210">Si noti che la colonna **ID** è stata compilata.</span><span class="sxs-lookup"><span data-stu-id="ee602-210">Notice that the **ID** column has been filled in.</span></span>

![Griglia di immissione di database in WebMatrix con un record e un ID generato automaticamente](displaying-data/_static/image13.png)

<span data-ttu-id="ee602-212">Immettere un altro film (ad esempio, "Gone with the Wind", "Drama", "1939").</span><span class="sxs-lookup"><span data-stu-id="ee602-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="ee602-213">La colonna ID viene nuovamente compilata:</span><span class="sxs-lookup"><span data-stu-id="ee602-213">The ID column is filled in again:</span></span>

![Griglia di immissione di database in WebMatrix con due record e ID generati automaticamente](displaying-data/_static/image14.png)

<span data-ttu-id="ee602-215">Immettere un terzo film (ad esempio, "Ghostbusters", "Comedy").</span><span class="sxs-lookup"><span data-stu-id="ee602-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="ee602-216">Come esperimento, lasciare vuota la colonna **year** , quindi premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="ee602-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="ee602-217">Poiché è stata deselezionata l'opzione **Consenti valori null** , nel database viene visualizzato un errore:</span><span class="sxs-lookup"><span data-stu-id="ee602-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Viene visualizzato l'errore "dati non validi" Se il valore di una colonna obbligatoria viene lasciato vuoto](displaying-data/_static/image15.png)

<span data-ttu-id="ee602-219">Fare clic su **OK** per tornare indietro e correggere la voce (l'anno per "Ghostbusters" è 1984), quindi premere INVIO.</span><span class="sxs-lookup"><span data-stu-id="ee602-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="ee602-220">Compilare diversi filmati fino a 8 o così via.</span><span class="sxs-lookup"><span data-stu-id="ee602-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="ee602-221">L'immissione di 8 rende più semplice l'utilizzo del paging in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="ee602-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="ee602-222">Tuttavia, se il numero è troppo elevato, è possibile immetterne solo alcuni per il momento. I dati effettivi non sono importanti.</span><span class="sxs-lookup"><span data-stu-id="ee602-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Griglia di immissione di database in WebMatrix con entrambi i record](displaying-data/_static/image16.png)

<span data-ttu-id="ee602-224">Se sono stati immessi tutti i film senza errori, i valori ID sono sequenziali.</span><span class="sxs-lookup"><span data-stu-id="ee602-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="ee602-225">Se si è tentato di salvare un record di film incompleto, i numeri ID potrebbero non essere sequenziali.</span><span class="sxs-lookup"><span data-stu-id="ee602-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="ee602-226">In caso affermativo, è accettabile.</span><span class="sxs-lookup"><span data-stu-id="ee602-226">If so, that's okay.</span></span> <span data-ttu-id="ee602-227">I numeri non hanno un significato intrinseco e l'unica cosa importante è che siano univoci nella tabella *Movies* .</span><span class="sxs-lookup"><span data-stu-id="ee602-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="ee602-228">Chiudere la scheda che contiene la finestra di progettazione del database.</span><span class="sxs-lookup"><span data-stu-id="ee602-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="ee602-229">A questo punto è possibile attivare la visualizzazione dei dati in una pagina Web.</span><span class="sxs-lookup"><span data-stu-id="ee602-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="ee602-230">Visualizzazione di dati in una pagina tramite l'helper WebGrid</span><span class="sxs-lookup"><span data-stu-id="ee602-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="ee602-231">Per visualizzare i dati in una pagina, si userà l'helper `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="ee602-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="ee602-232">Questo helper produce una visualizzazione in una griglia o in una tabella (righe e colonne).</span><span class="sxs-lookup"><span data-stu-id="ee602-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="ee602-233">Come si vedrà, sarà possibile affinare la griglia con la formattazione e altre funzionalità.</span><span class="sxs-lookup"><span data-stu-id="ee602-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="ee602-234">Per eseguire la griglia, è necessario scrivere alcune righe di codice.</span><span class="sxs-lookup"><span data-stu-id="ee602-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="ee602-235">Queste poche righe fungeranno da modello per quasi tutte le operazioni di accesso ai dati eseguite in questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="ee602-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="ee602-236">Sono disponibili molte opzioni per la visualizzazione dei dati in una pagina. il `WebGrid` Helper è solo uno.</span><span class="sxs-lookup"><span data-stu-id="ee602-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="ee602-237">È stato scelto per questa esercitazione perché è il modo più semplice per visualizzare i dati e perché è ragionevolmente flessibile.</span><span class="sxs-lookup"><span data-stu-id="ee602-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="ee602-238">Nel prossimo set di esercitazioni verrà illustrato come usare un metodo più "manuale" per lavorare con i dati nella pagina, che offre un controllo più diretto su come visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>

<span data-ttu-id="ee602-239">Nel riquadro sinistro in WebMatrix fare clic sull'area di lavoro **file** .</span><span class="sxs-lookup"><span data-stu-id="ee602-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="ee602-240">Il nuovo database creato si trova nella cartella *App\_data* .</span><span class="sxs-lookup"><span data-stu-id="ee602-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="ee602-241">Se la cartella non esiste già, WebMatrix la crea per il nuovo database.</span><span class="sxs-lookup"><span data-stu-id="ee602-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="ee602-242">È possibile che la cartella sia già presente se gli helper sono stati installati in precedenza.</span><span class="sxs-lookup"><span data-stu-id="ee602-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="ee602-243">Nella visualizzazione albero selezionare la radice del sito Web.</span><span class="sxs-lookup"><span data-stu-id="ee602-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="ee602-244">È necessario selezionare la radice del sito Web; in caso contrario, il nuovo file potrebbe essere aggiunto alla cartella app\_data.</span><span class="sxs-lookup"><span data-stu-id="ee602-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="ee602-245">Sulla barra multifunzione fare clic su **nuovo**.</span><span class="sxs-lookup"><span data-stu-id="ee602-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="ee602-246">Nella casella **scegliere un tipo di file** scegliere **cshtml**.</span><span class="sxs-lookup"><span data-stu-id="ee602-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="ee602-247">Nella casella **nome** assegnare un nome alla nuova pagina "Movies. cshtml":</span><span class="sxs-lookup"><span data-stu-id="ee602-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![Finestra di dialogo ' scegliere un tipo di file ' che mostra la pagina ' filmati '](displaying-data/_static/image17.png)

<span data-ttu-id="ee602-249">Fare clic sul pulsante **OK** .</span><span class="sxs-lookup"><span data-stu-id="ee602-249">Click the **OK** button.</span></span> <span data-ttu-id="ee602-250">WebMatrix apre un nuovo file con alcuni elementi di scheletro.</span><span class="sxs-lookup"><span data-stu-id="ee602-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="ee602-251">Prima di tutto verrà scritto il codice per ottenere i dati dal database.</span><span class="sxs-lookup"><span data-stu-id="ee602-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="ee602-252">Si aggiungerà quindi il markup alla pagina per visualizzare effettivamente i dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="ee602-253">Scrittura del codice di query dei dati</span><span class="sxs-lookup"><span data-stu-id="ee602-253">Writing the Data Query Code</span></span>

<span data-ttu-id="ee602-254">Immettere il codice seguente nella parte superiore della pagina tra il `@{` e il `}` caratteri.</span><span class="sxs-lookup"><span data-stu-id="ee602-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="ee602-255">Assicurarsi di immettere questo codice tra le parentesi graffe di apertura e di chiusura.</span><span class="sxs-lookup"><span data-stu-id="ee602-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="ee602-256">La prima riga apre il database creato in precedenza, che è sempre il primo passaggio prima di eseguire un'operazione con il database.</span><span class="sxs-lookup"><span data-stu-id="ee602-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="ee602-257">Si indica il nome del metodo di `Database.Open` del database da aprire.</span><span class="sxs-lookup"><span data-stu-id="ee602-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="ee602-258">Si noti che non è incluso *. sdf* nel nome.</span><span class="sxs-lookup"><span data-stu-id="ee602-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="ee602-259">Il metodo `Open` presuppone che stia cercando un file con estensione *SDF* , ovvero *WebPagesMovies. sdf*, e che il file *SDF* si trovi nella cartella *app\_data* .</span><span class="sxs-lookup"><span data-stu-id="ee602-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="ee602-260">(In precedenza si è notato che la cartella *App\_data* è riservata; questo scenario è uno dei punti in cui ASP.NET crea presupposti per tale nome.)</span><span class="sxs-lookup"><span data-stu-id="ee602-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="ee602-261">Quando il database viene aperto, viene inserito un riferimento a esso nella variabile denominata `db`.</span><span class="sxs-lookup"><span data-stu-id="ee602-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="ee602-262">Che può essere denominato qualsiasi elemento. La variabile `db` rappresenta la modalità di interazione con il database.</span><span class="sxs-lookup"><span data-stu-id="ee602-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="ee602-263">La seconda riga recupera effettivamente i dati del database usando il metodo `Query`.</span><span class="sxs-lookup"><span data-stu-id="ee602-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="ee602-264">Si noti che questo codice funziona: la variabile `db` dispone di un riferimento al database aperto e si richiama il metodo `Query` utilizzando la variabile `db` (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="ee602-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="ee602-265">La query stessa è un'istruzione SQL `Select`.</span><span class="sxs-lookup"><span data-stu-id="ee602-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="ee602-266">Per informazioni di base su SQL, vedere la spiegazione in un secondo momento. Nell'istruzione `Movies` identifica la tabella in cui eseguire la query.</span><span class="sxs-lookup"><span data-stu-id="ee602-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="ee602-267">Il carattere `*` specifica che la query deve restituire tutte le colonne della tabella.</span><span class="sxs-lookup"><span data-stu-id="ee602-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="ee602-268">È anche possibile elencare le colonne singolarmente, separate da virgole.</span><span class="sxs-lookup"><span data-stu-id="ee602-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="ee602-269">I risultati della query, se presenti, vengono restituiti e resi disponibili nella variabile `selectedData`.</span><span class="sxs-lookup"><span data-stu-id="ee602-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="ee602-270">Anche in questo caso, la variabile può essere denominata qualsiasi elemento.</span><span class="sxs-lookup"><span data-stu-id="ee602-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="ee602-271">Infine, la terza riga indica a ASP.NET che si desidera utilizzare un'istanza del `WebGrid` helper.</span><span class="sxs-lookup"><span data-stu-id="ee602-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="ee602-272">Per creare (*creare un'istanza*) l'oggetto helper, usare la parola chiave `new` e passare i risultati della query tramite la variabile `selectedData`.</span><span class="sxs-lookup"><span data-stu-id="ee602-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="ee602-273">Il nuovo oggetto `WebGrid`, insieme ai risultati della query sul database, viene reso disponibile nella variabile `grid`.</span><span class="sxs-lookup"><span data-stu-id="ee602-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="ee602-274">È necessario che venga visualizzato un minuto per visualizzare effettivamente i dati nella pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="ee602-275">In questa fase, il database è stato aperto, sono stati ottenuti i dati desiderati ed è stato preparato il `WebGrid` helper con tali dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="ee602-276">Il passaggio successivo consiste nel creare il markup nella pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="ee602-277">**Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="ee602-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="ee602-278">SQL è un linguaggio usato nella maggior parte dei database relazionali per la gestione dei dati in un database.</span><span class="sxs-lookup"><span data-stu-id="ee602-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="ee602-279">Sono inclusi i comandi che consentono di recuperare i dati e aggiornarli e che consentono di creare, modificare e gestire i dati nelle tabelle di database.</span><span class="sxs-lookup"><span data-stu-id="ee602-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="ee602-280">SQL è diverso rispetto a un linguaggio di programmazione C#(ad esempio).</span><span class="sxs-lookup"><span data-stu-id="ee602-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="ee602-281">Con SQL, si indica al database il risultato desiderato e si tratta del processo del database per capire come ottenere i dati o eseguire l'attività.</span><span class="sxs-lookup"><span data-stu-id="ee602-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="ee602-282">Di seguito sono riportati alcuni esempi di comandi SQL e le operazioni eseguite:</span><span class="sxs-lookup"><span data-stu-id="ee602-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="ee602-283">La prima istruzione `Select` ottiene tutte le colonne (specificate da `*`) dalla tabella *Movies* .</span><span class="sxs-lookup"><span data-stu-id="ee602-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="ee602-284">La seconda istruzione `Select` recupera le colonne ID, Name e Price dai record della tabella *Product* il cui valore di colonna Price è maggiore di 10.</span><span class="sxs-lookup"><span data-stu-id="ee602-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="ee602-285">Il comando restituisce i risultati in ordine alfabetico in base ai valori della colonna Name.</span><span class="sxs-lookup"><span data-stu-id="ee602-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="ee602-286">Se nessun record corrisponde ai criteri di prezzo, il comando restituisce un set vuoto.</span><span class="sxs-lookup"><span data-stu-id="ee602-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="ee602-287">Questo comando inserisce un nuovo record nella tabella *Product* , impostando la colonna Name su "croissant", la colonna Description su "a Flaky Delight" e il prezzo 1,99.</span><span class="sxs-lookup"><span data-stu-id="ee602-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="ee602-288">Si noti che quando si specifica un valore non numerico, il valore è racchiuso tra virgolette singole (non virgolette doppie, come in C#).</span><span class="sxs-lookup"><span data-stu-id="ee602-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="ee602-289">Si usano queste virgolette intorno ai valori di testo o di data, ma non intorno ai numeri.</span><span class="sxs-lookup"><span data-stu-id="ee602-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="ee602-290">Questo comando Elimina i record nella tabella *Product* la cui colonna Data di scadenza è precedente all'1 gennaio 2008.</span><span class="sxs-lookup"><span data-stu-id="ee602-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="ee602-291">Il comando presuppone che la tabella *Product* includa una colonna di questo tipo, ovviamente. La data viene immessa nel formato MM/gg/aaaa, ma deve essere immessa nel formato usato per le impostazioni locali.</span><span class="sxs-lookup"><span data-stu-id="ee602-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="ee602-292">I comandi `Insert` e `Delete` non restituiscono set di risultati.</span><span class="sxs-lookup"><span data-stu-id="ee602-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="ee602-293">Restituiscono invece un numero che indica il numero di record interessati dal comando.</span><span class="sxs-lookup"><span data-stu-id="ee602-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="ee602-294">Per alcune di queste operazioni (ad esempio l'inserimento e l'eliminazione di record), il processo che richiede l'operazione deve disporre delle autorizzazioni appropriate nel database.</span><span class="sxs-lookup"><span data-stu-id="ee602-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="ee602-295">Per questo motivo, per i database di produzione spesso è necessario fornire un nome utente e una password quando ci si connette al database.</span><span class="sxs-lookup"><span data-stu-id="ee602-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="ee602-296">Sono disponibili dozzine di comandi SQL, ma tutti seguono un modello come i comandi visualizzati qui.</span><span class="sxs-lookup"><span data-stu-id="ee602-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="ee602-297">È possibile utilizzare i comandi SQL per creare tabelle di database, contare il numero di record in una tabella, calcolare i prezzi ed eseguire molte altre operazioni.</span><span class="sxs-lookup"><span data-stu-id="ee602-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>

### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="ee602-298">Aggiunta di markup per visualizzare i dati</span><span class="sxs-lookup"><span data-stu-id="ee602-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="ee602-299">All'interno dell'elemento `<head>`, impostare il contenuto dell'elemento `<title>` su "Movies":</span><span class="sxs-lookup"><span data-stu-id="ee602-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="ee602-300">All'interno dell'elemento `<body>` della pagina aggiungere quanto segue:</span><span class="sxs-lookup"><span data-stu-id="ee602-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="ee602-301">È tutto.</span><span class="sxs-lookup"><span data-stu-id="ee602-301">That's it.</span></span> <span data-ttu-id="ee602-302">La variabile `grid` è il valore creato quando è stato creato l'oggetto `WebGrid` nel codice in precedenza.</span><span class="sxs-lookup"><span data-stu-id="ee602-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="ee602-303">Nella visualizzazione albero di WebMatrix fare clic con il pulsante destro del mouse sulla pagina e scegliere **Avvia nel browser**.</span><span class="sxs-lookup"><span data-stu-id="ee602-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="ee602-304">Verrà visualizzata una pagina simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="ee602-304">You'll see something like this page:</span></span>

![Output dell'helper WebGrid predefinito dalla tabella Movies](displaying-data/_static/image18.png)

<span data-ttu-id="ee602-306">Fare clic sul collegamento di un'intestazione di colonna per ordinare in base a tale colonna.</span><span class="sxs-lookup"><span data-stu-id="ee602-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="ee602-307">La possibilità di ordinare facendo clic su un'intestazione è una funzionalità incorporata nell'helper **WebGrid** .</span><span class="sxs-lookup"><span data-stu-id="ee602-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="ee602-308">Il metodo `GetHtml`, come suggerisce il nome, genera il markup che Visualizza i dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="ee602-309">Per impostazione predefinita, il metodo `GetHtml` genera un elemento HTML `<table>`.</span><span class="sxs-lookup"><span data-stu-id="ee602-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="ee602-310">Se si desidera, è possibile verificare il rendering osservando l'origine della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="ee602-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="ee602-311">Modifica dell'aspetto della griglia</span><span class="sxs-lookup"><span data-stu-id="ee602-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="ee602-312">L'uso dell'helper `WebGrid` come è stato appena fatto è semplice, ma la visualizzazione risultante è semplice.</span><span class="sxs-lookup"><span data-stu-id="ee602-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="ee602-313">L'helper `WebGrid` dispone di tutti i tipi di opzioni che consentono di controllare la modalità di visualizzazione dei dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="ee602-314">In questa esercitazione sono disponibili troppe quantità di informazioni, ma in questa sezione verranno illustrate alcune di queste opzioni.</span><span class="sxs-lookup"><span data-stu-id="ee602-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="ee602-315">Nelle esercitazioni successive della serie verranno descritte alcune opzioni aggiuntive.</span><span class="sxs-lookup"><span data-stu-id="ee602-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="ee602-316">Specifica di singole colonne da visualizzare</span><span class="sxs-lookup"><span data-stu-id="ee602-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="ee602-317">Per iniziare, è possibile specificare che si desidera visualizzare solo determinate colonne.</span><span class="sxs-lookup"><span data-stu-id="ee602-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="ee602-318">Per impostazione predefinita, come si è visto, nella griglia vengono visualizzate tutte e quattro le colonne della tabella *Movies* .</span><span class="sxs-lookup"><span data-stu-id="ee602-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="ee602-319">Nel file *Movies. cshtml* sostituire il markup `@grid.GetHtml()` appena aggiunto con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="ee602-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="ee602-320">Per indicare all'helper le colonne da visualizzare, è necessario includere un parametro `columns` per il metodo `GetHtml` e passare una raccolta di colonne.</span><span class="sxs-lookup"><span data-stu-id="ee602-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="ee602-321">Nella raccolta specificare ogni colonna da includere.</span><span class="sxs-lookup"><span data-stu-id="ee602-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="ee602-322">È possibile specificare una singola colonna da visualizzare includendo un oggetto `grid.Column` e passare il nome della colonna di dati desiderata.</span><span class="sxs-lookup"><span data-stu-id="ee602-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="ee602-323">Queste colonne devono essere incluse nei risultati della query SQL: l'helper `WebGrid` non è in grado di visualizzare colonne non restituite dalla query.</span><span class="sxs-lookup"><span data-stu-id="ee602-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="ee602-324">Avviare di nuovo la pagina *Movies. cshtml* nel browser. questa volta si ottiene una visualizzazione simile alla seguente (si noti che non viene visualizzata alcuna colonna ID):</span><span class="sxs-lookup"><span data-stu-id="ee602-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Visualizzazione WebGrid che mostra solo le colonne selezionate](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="ee602-326">Modifica dell'aspetto della griglia</span><span class="sxs-lookup"><span data-stu-id="ee602-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="ee602-327">Sono disponibili altre opzioni per la visualizzazione delle colonne, alcune delle quali verranno analizzate nelle esercitazioni successive di questo set.</span><span class="sxs-lookup"><span data-stu-id="ee602-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="ee602-328">Per il momento, in questa sezione verranno presentati i modi in cui è possibile applicare uno stile alla griglia nel suo complesso.</span><span class="sxs-lookup"><span data-stu-id="ee602-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="ee602-329">All'interno della sezione `<head>` della pagina, immediatamente prima del tag di chiusura `</head>` aggiungere il seguente elemento `<style>`:</span><span class="sxs-lookup"><span data-stu-id="ee602-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="ee602-330">Questo markup CSS definisce le classi denominate `grid`, `head`e così via.</span><span class="sxs-lookup"><span data-stu-id="ee602-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="ee602-331">È anche possibile inserire queste definizioni di stile in un file *CSS* separato e collegarlo alla pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="ee602-332">Questa operazione verrà eseguita più avanti in questo set di esercitazioni. Tuttavia, per semplificare le operazioni di questa esercitazione, si trovano all'interno della stessa pagina in cui vengono visualizzati i dati.</span><span class="sxs-lookup"><span data-stu-id="ee602-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="ee602-333">Ora è possibile ottenere l'helper `WebGrid` per usare queste classi di stile.</span><span class="sxs-lookup"><span data-stu-id="ee602-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="ee602-334">L'helper ha un certo numero di proprietà (ad esempio, `tableStyle`) a questo scopo. a tale scopo, si assegna un nome di classe di stile CSS e il nome della classe viene sottoposto a rendering come parte del markup di cui viene eseguito il rendering dall'helper.</span><span class="sxs-lookup"><span data-stu-id="ee602-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="ee602-335">Modificare il markup `grid.GetHtml` in modo che ora appaia come questo codice:</span><span class="sxs-lookup"><span data-stu-id="ee602-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="ee602-336">Le novità sono l'aggiunta dei parametri `tableStyle`, `headerStyle`e `alternatingRowStyle` al metodo `GetHtml`.</span><span class="sxs-lookup"><span data-stu-id="ee602-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="ee602-337">Questi parametri sono stati impostati sui nomi degli stili CSS aggiunti un momento fa.</span><span class="sxs-lookup"><span data-stu-id="ee602-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="ee602-338">Eseguire la pagina. questa volta viene visualizzata una griglia con un aspetto molto meno semplice rispetto a prima:</span><span class="sxs-lookup"><span data-stu-id="ee602-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Visualizzazione WebGrid che include i parametri impostati sui nomi delle classi CSS](displaying-data/_static/image20.png)

<span data-ttu-id="ee602-340">Per visualizzare il metodo generato dal `GetHtml`, è possibile esaminare l'origine della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="ee602-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="ee602-341">Non verranno esaminati in dettaglio, ma l'aspetto importante è che, specificando parametri come `tableStyle`, è stata causata dalla griglia la generazione di tag HTML come il seguente:</span><span class="sxs-lookup"><span data-stu-id="ee602-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="ee602-342">Al tag `<table>` è stato aggiunto un attributo `class` che fa riferimento a una delle regole CSS aggiunte in precedenza.</span><span class="sxs-lookup"><span data-stu-id="ee602-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="ee602-343">Questo codice illustra il modello di base &mdash; parametri diversi per il metodo `GetHtml` consentono di fare riferimento a classi CSS che il metodo genera quindi insieme al markup.</span><span class="sxs-lookup"><span data-stu-id="ee602-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="ee602-344">Le operazioni eseguite con le classi CSS sono le stesse.</span><span class="sxs-lookup"><span data-stu-id="ee602-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="ee602-345">Aggiunta di paging</span><span class="sxs-lookup"><span data-stu-id="ee602-345">Adding Paging</span></span>

<span data-ttu-id="ee602-346">Come ultima attività per questa esercitazione, verrà aggiunto il paging alla griglia.</span><span class="sxs-lookup"><span data-stu-id="ee602-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="ee602-347">Attualmente non è possibile visualizzare tutti i film in una sola volta.</span><span class="sxs-lookup"><span data-stu-id="ee602-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="ee602-348">Tuttavia, se sono state aggiunte centinaia di film, la visualizzazione della pagina potrebbe essere molto estesa.</span><span class="sxs-lookup"><span data-stu-id="ee602-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="ee602-349">Nel codice della pagina modificare la riga che crea l'oggetto `WebGrid` nel codice seguente:</span><span class="sxs-lookup"><span data-stu-id="ee602-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="ee602-350">L'unica differenza rispetto a prima è che è stato aggiunto un parametro di `rowsPerPage` impostato su 3.</span><span class="sxs-lookup"><span data-stu-id="ee602-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="ee602-351">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="ee602-351">Run the page.</span></span> <span data-ttu-id="ee602-352">Nella griglia vengono visualizzate 3 righe alla volta e i collegamenti di navigazione che consentono di spostarsi tra i film nel database:</span><span class="sxs-lookup"><span data-stu-id="ee602-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Visualizzazione WebGrid con paging](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="ee602-354">Prossimi</span><span class="sxs-lookup"><span data-stu-id="ee602-354">Coming Up Next</span></span>

<span data-ttu-id="ee602-355">Nell'esercitazione successiva si apprenderà come usare Razor e C# il codice per ottenere l'input dell'utente in un modulo.</span><span class="sxs-lookup"><span data-stu-id="ee602-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="ee602-356">Si aggiungerà una casella di ricerca alla pagina Movies per poter trovare i film in base al titolo o al genere.</span><span class="sxs-lookup"><span data-stu-id="ee602-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="ee602-357">Elenco completo per la pagina dei filmati</span><span class="sxs-lookup"><span data-stu-id="ee602-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="ee602-358">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="ee602-358">Additional Resources</span></span>

- [<span data-ttu-id="ee602-359">Introduzione alla programmazione Web ASP.NET con la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="ee602-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="ee602-360">[Precedente](intro-to-web-pages-programming.md)
> [Successivo](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="ee602-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
