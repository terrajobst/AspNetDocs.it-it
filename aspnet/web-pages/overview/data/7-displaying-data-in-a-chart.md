---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Visualizzazione dei dati in un grafico con ASP.NET Web Pages (Razor) | Microsoft Docs
author: microsoft
description: In questo capitolo viene illustrato come visualizzare i dati in un grafico. Nei capitoli precedenti, è stato descritto come visualizzare i dati manualmente e in una griglia. In questo capitolo viene...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 6dad67d4e3d38d57a761c567d937d714a3184ea9
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 05/06/2019
ms.locfileid: "65133707"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="b7061-105">Visualizzazione dei dati in un grafico con ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="b7061-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="b7061-106">by [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="b7061-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="b7061-107">Questo articolo illustra come usare un grafico per visualizzare i dati in un sito Web ASP.NET Web Pages (Razor) utilizzando il `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="b7061-108">**Si apprenderà**:</span><span class="sxs-lookup"><span data-stu-id="b7061-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="b7061-109">Come visualizzare i dati in un grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="b7061-110">Viene descritto come applicare uno stile a grafici di utilizzo dei temi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="b7061-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="b7061-111">Come salvare i grafici e come memorizzarli nella cache per migliorare le prestazioni.</span><span class="sxs-lookup"><span data-stu-id="b7061-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="b7061-112">Queste sono le funzionalità introdotte nell'articolo di programmazione ASP.NET:</span><span class="sxs-lookup"><span data-stu-id="b7061-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="b7061-113">Il `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="b7061-114">Le informazioni contenute in questo articolo si applicano a 1.0 di pagine Web ASP.NET e Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="b7061-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="b7061-115">L'Helper grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-115">The Chart Helper</span></span>

<span data-ttu-id="b7061-116">Quando si desidera visualizzare i dati sotto forma di grafico, è possibile usare `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="b7061-117">Il `Chart` helper può eseguire il rendering di un'immagine che visualizza i dati in un'ampia gamma di tipi di grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="b7061-118">Supporta numerose opzioni per la formattazione e l'assegnazione di etichette.</span><span class="sxs-lookup"><span data-stu-id="b7061-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="b7061-119">Il `Chart` helper può eseguire il rendering di più di 30 tipi di grafici, inclusi tutti i tipi di grafici che è possibile avere familiarità con da Microsoft Excel o altri strumenti di &#8212; grafici ad area, i grafici a barre, istogrammi, grafici a linee e grafici a torta, insieme a informazioni grafici speciali, ad esempio i grafici azionari.</span><span class="sxs-lookup"><span data-stu-id="b7061-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="b7061-120">**Grafico ad aree** ![descrizione: Immagine del tipo di grafico ad Area](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="b7061-121">**Grafico a barre** ![descrizione: Immagine del tipo di grafico a barre](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="b7061-122">**Istogramma** ![descrizione: Immagine del tipo di grafico](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="b7061-123">**Grafico a linee** ![descrizione: Immagine del tipo di grafico a linee](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="b7061-124">**Grafico a torta** ![descrizione: Immagine del tipo di grafico a torta](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="b7061-125">**Grafico azionario** ![descrizione: Immagine del tipo di grafico azionario](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="b7061-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="b7061-126">Elementi del grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-126">Chart Elements</span></span>

<span data-ttu-id="b7061-127">I grafici mostrano i dati e altri elementi quali legende, assi, serie e così via.</span><span class="sxs-lookup"><span data-stu-id="b7061-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="b7061-128">La figura seguente vengono illustrati molti degli elementi del grafico che è possibile personalizzare quando si usa il `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="b7061-129">Questo articolo illustra come impostare alcune (non tutti) di questi elementi.</span><span class="sxs-lookup"><span data-stu-id="b7061-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descrizione: Immagine che mostra gli elementi del grafico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="b7061-131">Creazione di un grafico dai dati</span><span class="sxs-lookup"><span data-stu-id="b7061-131">Creating a Chart from Data</span></span>

<span data-ttu-id="b7061-132">I dati visualizzati in un grafico possono essere da una matrice, dai risultati restituiti da un database o da dati che si trova in un file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="b7061-133">Utilizzando una matrice</span><span class="sxs-lookup"><span data-stu-id="b7061-133">Using an Array</span></span>

<span data-ttu-id="b7061-134">Come illustrato in [Introduzione a ASP.NET Web Pages di programmazione utilizzando la sintassi Razor](https://go.microsoft.com/fwlink/?LinkId=202890), una matrice consente di archiviare una raccolta di elementi simili in una singola variabile.</span><span class="sxs-lookup"><span data-stu-id="b7061-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="b7061-135">È possibile utilizzare matrici per contenere i dati che si desidera includere nel grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="b7061-136">Questa procedura viene illustrato come creare un grafico dai dati nell'array, utilizzando il tipo di grafico predefinito.</span><span class="sxs-lookup"><span data-stu-id="b7061-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="b7061-137">Viene inoltre illustrato come visualizzare il grafico all'interno della pagina.</span><span class="sxs-lookup"><span data-stu-id="b7061-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="b7061-138">Creare un nuovo file denominato *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="b7061-139">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="b7061-140">Prima di tutto, il codice crea un nuovo grafico e imposta la larghezza e altezza.</span><span class="sxs-lookup"><span data-stu-id="b7061-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="b7061-141">Specificare il titolo del grafico utilizzando il `AddTitle` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="b7061-142">Per aggiungere dati, si utilizza il `AddSeries` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="b7061-143">In questo esempio, si utilizza il `name`, `xValue`, e `yValues` i parametri del `AddSeries` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="b7061-144">Il `name` parametro viene visualizzato nella legenda del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="b7061-145">Il `xValue` parametro contiene una matrice di dati che viene visualizzati lungo l'asse orizzontale del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="b7061-146">Il `yValues` parametro contiene una matrice di dati utilizzato per tracciare i punti verticali del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="b7061-147">Il `Write` metodo esegue effettivamente il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="b7061-148">In questo caso, dato che non è stato specificato un tipo di grafico, il `Chart` helper esegue il rendering relativo grafico predefinita, in cui un istogramma.</span><span class="sxs-lookup"><span data-stu-id="b7061-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="b7061-149">Eseguire la pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-149">Run the page in the browser.</span></span> <span data-ttu-id="b7061-150">Il browser viene visualizzato il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="b7061-151">Usando una query sul Database per i dati del grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="b7061-152">Se le informazioni di che cui tracciare il grafico si trova in un database, è possibile eseguire una query sul database e quindi usare i dati dai risultati per creare il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="b7061-153">Questa procedura viene illustrato come leggere e visualizzare i dati dal database creato in questo articolo [Introduzione all'uso di un Database nei siti di ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="b7061-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="b7061-154">Aggiungere un *App\_dati* cartella nella radice del sito Web se la cartella non esiste già.</span><span class="sxs-lookup"><span data-stu-id="b7061-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="b7061-155">Nel *App\_Data* cartella, aggiungere il file di database denominato *SmallBakery.sdf* che è descritti [Introduzione all'uso di un Database nei siti di ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="b7061-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="b7061-156">Creare un nuovo file denominato *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="b7061-157">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="b7061-158">Il codice viene aperto il database SmallBakery prima di tutto e lo assegna a una variabile denominata `db`.</span><span class="sxs-lookup"><span data-stu-id="b7061-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="b7061-159">Questa variabile rappresenta un `Database` oggetto che può essere utilizzato per leggere e scrivere nel database.</span><span class="sxs-lookup"><span data-stu-id="b7061-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="b7061-160">Successivamente, il codice esegue una query SQL per ottenere il nome e il prezzo di ogni prodotto.</span><span class="sxs-lookup"><span data-stu-id="b7061-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="b7061-161">Il codice crea un nuovo grafico e passa la query di database ad esso tramite la chiamata del grafico `DataBindTable` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="b7061-162">Questo metodo accetta due parametri: la `dataSource` parametro è per i dati dalla query e il `xField` parametro consente di impostare la colonna di dati utilizzata per l'asse x del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="b7061-163">Come alternativa all'uso di `DataBindTable` metodo, è possibile usare il `AddSeries` metodo del `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="b7061-164">Il `AddSeries` metodo consente di impostare il `xValue` e `yValues` parametri.</span><span class="sxs-lookup"><span data-stu-id="b7061-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="b7061-165">Ad esempio, invece di usare il `DataBindTable` metodo simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="b7061-166">È possibile usare il `AddSeries` metodo simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="b7061-167">Entrambi eseguono il rendering gli stessi risultati.</span><span class="sxs-lookup"><span data-stu-id="b7061-167">Both render the same results.</span></span> <span data-ttu-id="b7061-168">Il `AddSeries` metodo è più flessibile perché è possibile specificare il tipo di grafico e i dati in modo più esplicito, ma il `DataBindTable` metodo è più facile da usare se non è necessaria la flessibilità aggiuntiva.</span><span class="sxs-lookup"><span data-stu-id="b7061-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="b7061-169">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="b7061-170">Utilizzo di dati XML</span><span class="sxs-lookup"><span data-stu-id="b7061-170">Using XML Data</span></span>

<span data-ttu-id="b7061-171">La terza opzione per la creazione di grafici consiste nell'utilizzare un file XML dei dati per il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="b7061-172">Ciò richiede che il file XML dispone anche di un file di schema (*XSD* file) che descrive la struttura XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="b7061-173">Questa procedura viene illustrato come leggere i dati da un file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="b7061-174">Nel *App\_Data* cartella, creare un nuovo file XML denominato *Data. XML*.</span><span class="sxs-lookup"><span data-stu-id="b7061-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="b7061-175">Sostituire il codice XML esistente con il codice seguente, ovvero alcuni dati XML relativi ai dipendenti di una società fittizia.</span><span class="sxs-lookup"><span data-stu-id="b7061-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="b7061-176">Nel *App\_Data* cartella, creare un nuovo file XML denominato *XSD*.</span><span class="sxs-lookup"><span data-stu-id="b7061-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="b7061-177">(Si noti che l'estensione in questo caso si *XSD*.)</span><span class="sxs-lookup"><span data-stu-id="b7061-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="b7061-178">Sostituire il codice XML esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="b7061-179">Nella radice del sito Web, creare un nuovo file denominato *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="b7061-180">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="b7061-181">Il codice crea prima un `DataSet` oggetto.</span><span class="sxs-lookup"><span data-stu-id="b7061-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="b7061-182">Questo oggetto viene usato per gestire i dati che viene letto dal file XML e organizzandoli però in base alle informazioni nel file di schema.</span><span class="sxs-lookup"><span data-stu-id="b7061-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="b7061-183">(Si noti che nella parte superiore del codice inclusa l'istruzione `using SystemData`.</span><span class="sxs-lookup"><span data-stu-id="b7061-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="b7061-184">Questa operazione è necessaria per poter lavorare con i `DataSet` oggetto.</span><span class="sxs-lookup"><span data-stu-id="b7061-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="b7061-185">Per altre informazioni, vedere [ &quot;Using&quot; istruzioni e nomi completi](#SB_UsingStatements) più avanti in questo articolo.)</span><span class="sxs-lookup"><span data-stu-id="b7061-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="b7061-186">Successivamente, il codice crea un `DataView` oggetto in base al set di dati.</span><span class="sxs-lookup"><span data-stu-id="b7061-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="b7061-187">La visualizzazione di dati fornisce un oggetto che il grafico è possibile associare a &#8212; , leggere e creare un tracciato.</span><span class="sxs-lookup"><span data-stu-id="b7061-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="b7061-188">Il grafico viene associato a dati mediante la `AddSeries` metodo, come illustrato in precedenza durante la creazione di grafici di dati della matrice, con la differenza che questa volta il `xValue` e `yValues` parametri vengono impostati i `DataView` oggetto.</span><span class="sxs-lookup"><span data-stu-id="b7061-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="b7061-189">In questo esempio illustra inoltre come specificare un particolare tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="b7061-190">Quando i dati vengono aggiunti nel `AddSeries` metodo, il `chartType` parametro viene impostato anche per visualizzare un grafico a torta.</span><span class="sxs-lookup"><span data-stu-id="b7061-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="b7061-191">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="b7061-192">Istruzioni "Using" e nomi completi</span><span class="sxs-lookup"><span data-stu-id="b7061-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="b7061-193">.NET Framework basata su ASP.NET Web Pages con sintassi Razor è costituito da molte migliaia di componenti (classi).</span><span class="sxs-lookup"><span data-stu-id="b7061-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="b7061-194">Per rendere più gestibile a funzionare con tutte queste classi, sono organizzati nelle *spazi dei nomi*, che sono pressoché simile alle raccolte.</span><span class="sxs-lookup"><span data-stu-id="b7061-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="b7061-195">Ad esempio, il `System.Web` dello spazio dei nomi contiene classi che supportano la comunicazione browser/server, il `System.Xml` dello spazio dei nomi contiene classi che consentono di creare e leggere i file XML, e il `System.Data` dello spazio dei nomi contiene classi che consentono di lavorare con i dati.</span><span class="sxs-lookup"><span data-stu-id="b7061-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="b7061-196">Per poter accedere a qualsiasi classe fornita in .NET Framework, deve conoscere non solo il nome della classe, ma anche lo spazio dei nomi della classe nel codice.</span><span class="sxs-lookup"><span data-stu-id="b7061-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="b7061-197">Ad esempio, per poter usare il `Chart` helper, il codice deve trovare la `System.Web.Helpers.Chart` (classe), che combina lo spazio dei nomi (`System.Web.Helpers`) con il nome della classe (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="b7061-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="b7061-198">Questo è noto come la classe *completi* nome &#8212; relativo percorso completo e non ambiguo all'interno di vastness di .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="b7061-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="b7061-199">Nel codice, ciò sarebbe simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="b7061-200">Tuttavia, è difficile (e soggetta a errori) desidera utilizzare questi nomi lunghi, completo, ogni volta che si vuole fare riferimento a una classe o helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="b7061-201">Pertanto, per renderlo più semplice usare nomi di classe, è possibile *importare* gli spazi dei nomi si è interessati, ovvero in genere è una semplice manciata tra i molti spazi dei nomi in .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="b7061-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="b7061-202">Se è stato importato uno spazio dei nomi, è possibile usare solo un nome di classe (`Chart`) anziché il nome completo (`System.Web.Helpers.Chart`).</span><span class="sxs-lookup"><span data-stu-id="b7061-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="b7061-203">Quando il codice viene eseguito e viene rilevato un nome di classe, è possibile esaminare solo gli spazi dei nomi che è stato importato per trovare quella classe.</span><span class="sxs-lookup"><span data-stu-id="b7061-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="b7061-204">Quando si usa ASP.NET Web Pages con sintassi Razor per creare pagine web, è in genere usare lo stesso set di classi ogni volta che, tra cui il `WebPage` classe, gli helper diversi e così via.</span><span class="sxs-lookup"><span data-stu-id="b7061-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="b7061-205">Per salvare il lavoro di importazione di spazi dei nomi rilevante ogni volta che si crea un sito Web, ASP.NET è configurato in modo che importato automaticamente un set di spazi dei nomi core per ogni sito Web.</span><span class="sxs-lookup"><span data-stu-id="b7061-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="b7061-206">Ecco perché ancora stato disponeva di spazi dei nomi o l'importazione fino a questo punto; tutte le classi di cui che è lavorato con sono negli spazi dei nomi già importati automaticamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="b7061-207">Tuttavia, talvolta è necessario lavorare con una classe che non si trova in uno spazio dei nomi che viene importato automaticamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="b7061-208">In tal caso, è possibile usare nome completo di tale classe oppure è possibile importare manualmente lo spazio dei nomi che contiene la classe.</span><span class="sxs-lookup"><span data-stu-id="b7061-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="b7061-209">Per importare uno spazio dei nomi, si utilizza il `using` istruzione (`import` in Visual Basic), come illustrato in precedenza in un esempio dell'articolo.</span><span class="sxs-lookup"><span data-stu-id="b7061-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="b7061-210">Ad esempio, il `DataSet` classe si trova nel `System.Data` dello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="b7061-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="b7061-211">Il `System.Data` dello spazio dei nomi non è automaticamente disponibile alle pagine Razor di ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="b7061-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="b7061-212">Pertanto, per lavorare con i `DataSet` classe utilizzando il relativo nome completo, è possibile usare codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="b7061-213">Se è necessario utilizzare il `DataSet` classe più volte è possibile importare uno spazio dei nomi simile al seguente e quindi usare solo il nome della classe nel codice:</span><span class="sxs-lookup"><span data-stu-id="b7061-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="b7061-214">È possibile aggiungere `using` istruzioni per qualsiasi altro .NET Framework gli spazi dei nomi che si desidera fare riferimento.</span><span class="sxs-lookup"><span data-stu-id="b7061-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="b7061-215">Tuttavia, come indicato, non dovrai eseguire questa operazione, spesso, in quanto la maggior parte delle classi che verranno usate sono negli spazi dei nomi che vengono importati automaticamente da ASP.NET per l'utilizzo in *. cshtml* e *vbhtml* pagine.</span><span class="sxs-lookup"><span data-stu-id="b7061-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="b7061-216">Visualizzazione di grafici all'interno di una pagina Web</span><span class="sxs-lookup"><span data-stu-id="b7061-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="b7061-217">Negli esempi visti finora, si crea un grafico e quindi viene eseguito il rendering grafico direttamente al browser come un oggetto grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="b7061-218">In molti casi, tuttavia, si desidera visualizzare un grafico come parte di una pagina, non solo da solo nel browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="b7061-219">A tale scopo è necessario un processo in due passaggi.</span><span class="sxs-lookup"><span data-stu-id="b7061-219">To do that requires a two-step process.</span></span> <span data-ttu-id="b7061-220">Il primo passaggio consiste nel creare una pagina che viene generato l'errore al grafico, come abbiamo già visto.</span><span class="sxs-lookup"><span data-stu-id="b7061-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="b7061-221">Il secondo passaggio consiste nel visualizzare l'immagine risultante in un'altra pagina.</span><span class="sxs-lookup"><span data-stu-id="b7061-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="b7061-222">Per visualizzare l'immagine, si utilizza un elemento HTML `<img>` elemento, nello stesso modo, si farebbe per visualizzare tutte le immagini.</span><span class="sxs-lookup"><span data-stu-id="b7061-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="b7061-223">Tuttavia, anziché fare riferimento a un *jpg* o *PNG* file, il `<img>` i riferimenti agli elementi il *. cshtml* file che contiene il `Chart` helper che Crea il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="b7061-224">Quando si esegue la pagina di visualizzazione, il `<img>` elemento ottiene l'output del `Chart` helper ed esegue il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="b7061-225">Creare un file denominato *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="b7061-226">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="b7061-227">Il codice Usa il `<img>` elemento per visualizzare il grafico creato in precedenza nel *ChartArrayBasic.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="b7061-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="b7061-228">Eseguire la pagina web in un browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-228">Run the web page in a browser.</span></span> <span data-ttu-id="b7061-229">Il *ShowChart.cshtml* file consente di visualizzare l'immagine del grafico in base al codice contenuto nel *ChartArrayBasic.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="b7061-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="b7061-230">Applicazione di stili a un grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-230">Styling a Chart</span></span>

<span data-ttu-id="b7061-231">Il `Chart` helper supporta una vasta gamma di opzioni che consentono di personalizzare l'aspetto del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="b7061-232">È possibile impostare colori, tipi di carattere, i bordi e così via.</span><span class="sxs-lookup"><span data-stu-id="b7061-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="b7061-233">Un modo semplice per personalizzare l'aspetto di un grafico consiste nell'utilizzare un *tema*.</span><span class="sxs-lookup"><span data-stu-id="b7061-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="b7061-234">I temi sono raccolte di informazioni che specificano come eseguire il rendering di un grafico mediante i tipi di carattere, colori, etichette, tavolozze, bordi e gli effetti.</span><span class="sxs-lookup"><span data-stu-id="b7061-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="b7061-235">Si noti che lo stile di un grafico non indica il tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="b7061-236">La tabella seguente elenca i temi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="b7061-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="b7061-237">Tema</span><span class="sxs-lookup"><span data-stu-id="b7061-237">Theme</span></span> | <span data-ttu-id="b7061-238">Descrizione</span><span class="sxs-lookup"><span data-stu-id="b7061-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="b7061-239">Consente di visualizzare le colonne rosso su sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="b7061-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="b7061-240">Consente di visualizzare blu colonne su uno sfondo blu sfumato.</span><span class="sxs-lookup"><span data-stu-id="b7061-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="b7061-241">Consente di visualizzare blu colonne su uno sfondo sfumato verde.</span><span class="sxs-lookup"><span data-stu-id="b7061-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="b7061-242">Consente di visualizzare colonne arancione in un Sfumatura sfondo giallo.</span><span class="sxs-lookup"><span data-stu-id="b7061-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="b7061-243">Consente di visualizzare le colonne di colore rosso 3D su sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="b7061-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="b7061-244">È possibile specificare il tema da usare quando si crea un nuovo grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="b7061-245">Creare un nuovo file denominato *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="b7061-246">Sostituire il contenuto esistente nella pagina con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="b7061-247">Questo codice è lo stesso dell'esempio precedente che usa il database per i dati, ma aggiunge il `theme` parametro durante la creazione di `Chart` oggetto.</span><span class="sxs-lookup"><span data-stu-id="b7061-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="b7061-248">Di seguito viene illustrato il codice modificato:</span><span class="sxs-lookup"><span data-stu-id="b7061-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="b7061-249">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-249">Run the page in a browser.</span></span> <span data-ttu-id="b7061-250">Si vedranno gli stessi dati come prima, ma l'aspetto del grafico più raffinato:</span><span class="sxs-lookup"><span data-stu-id="b7061-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="b7061-251">Salvataggio di un grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-251">Saving a Chart</span></span>

<span data-ttu-id="b7061-252">Quando si usa il `Chart` helper come descritto finora in questo articolo, l'helper crea nuovamente il grafico da zero ogni volta che viene richiamato.</span><span class="sxs-lookup"><span data-stu-id="b7061-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="b7061-253">Se necessario, il codice per il grafico esegue una nuova query del database o legge nuovamente il file XML per ottenere i dati.</span><span class="sxs-lookup"><span data-stu-id="b7061-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="b7061-254">In alcuni casi, questa operazione può essere un'operazione complessa, ad esempio se il database che sta eseguendo la query è di grandi dimensioni o se il file XML contiene una grande quantità di dati.</span><span class="sxs-lookup"><span data-stu-id="b7061-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="b7061-255">Anche se il grafico non richiede una notevole quantità di dati, il processo di creazione dinamica di un'immagine occupa risorse server e se molte persone richiedono le pagine che consentono di visualizzare il grafico, può esserci un impatto sulle prestazioni del sito Web.</span><span class="sxs-lookup"><span data-stu-id="b7061-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="b7061-256">Per ridurre il potenziale impatto sulle prestazioni della creazione di un grafico, è possibile creare una la prima volta è necessario e quindi salvare il file.</span><span class="sxs-lookup"><span data-stu-id="b7061-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="b7061-257">Quando il grafico è necessario anche in questo caso, anziché rigenerarlo, puoi semplicemente recuperare la versione salvata ed eseguirne il rendering.</span><span class="sxs-lookup"><span data-stu-id="b7061-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="b7061-258">È possibile salvare un grafico nei modi seguenti:</span><span class="sxs-lookup"><span data-stu-id="b7061-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="b7061-259">Memorizzare nella cache il grafico nella memoria del computer (nel server).</span><span class="sxs-lookup"><span data-stu-id="b7061-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="b7061-260">Salvare il grafico come file di immagine.</span><span class="sxs-lookup"><span data-stu-id="b7061-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="b7061-261">Salvare il grafico come file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-261">Save the chart as an XML file.</span></span> <span data-ttu-id="b7061-262">Questa opzione consente di modificare il grafico prima di salvarla.</span><span class="sxs-lookup"><span data-stu-id="b7061-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="b7061-263">La memorizzazione nella cache un grafico</span><span class="sxs-lookup"><span data-stu-id="b7061-263">Caching a Chart</span></span>

<span data-ttu-id="b7061-264">Dopo aver creato un grafico, può memorizzarlo nella cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="b7061-265">La memorizzazione nella cache un grafico significa che non deve necessariamente essere ricreati se deve essere visualizzata nuovamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="b7061-266">Quando si salva un grafico nella cache, occorre assegnargli una chiave che deve essere univoca per il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="b7061-267">I grafici salvati nella cache potrebbero essere rimosso se il server in memoria sia insufficiente.</span><span class="sxs-lookup"><span data-stu-id="b7061-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="b7061-268">Inoltre, la cache viene cancellata se l'applicazione viene riavviato per qualsiasi motivo.</span><span class="sxs-lookup"><span data-stu-id="b7061-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="b7061-269">Pertanto, il metodo standard per lavorare con un grafico memorizzato nella cache è sempre prima di tutto verificare se è disponibile nella cache e in caso contrario, quindi per creare o crearlo nuovamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="b7061-270">Nella radice del sito Web, creare un file denominato *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="b7061-271">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="b7061-272">Il `<img>` tag include un `src` che punta all'attributo il *ChartSaveToCache.cshtml* file e passa una chiave per la pagina come una stringa di query.</span><span class="sxs-lookup"><span data-stu-id="b7061-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="b7061-273">La chiave contiene il valore &quot;myChartKey&quot;.</span><span class="sxs-lookup"><span data-stu-id="b7061-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="b7061-274">Il *ChartSaveToCache.cshtml* file contiene il `Chart` helper che crea il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="b7061-275">Questa pagina verrà creata più avanti.</span><span class="sxs-lookup"><span data-stu-id="b7061-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="b7061-276">Alla fine della pagina è presente un collegamento a una pagina denominata *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="b7061-277">Che è una pagina che si creeranno anche a breve.</span><span class="sxs-lookup"><span data-stu-id="b7061-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="b7061-278">È necessario il *ClearCache.cshtml* solo per testare la memorizzazione nella cache per questo esempio, non è un collegamento o una pagina in cui è in genere necessario includere quando si lavora con i grafici memorizzato nella cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="b7061-279">Nella radice del sito Web, creare un nuovo file denominato *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="b7061-280">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="b7061-281">Il codice di verifica innanzitutto se qualsiasi elemento è stato passato come il valore della chiave nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="b7061-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="b7061-282">Se, pertanto, il codice tenta di leggere un grafico dalla cache chiamando il `GetFromCache` metodo e passando la chiave.</span><span class="sxs-lookup"><span data-stu-id="b7061-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="b7061-283">Se si scopre che non c'è nulla nella cache con la chiave (che verrà eseguite la prima volta che viene richiesto il grafico), il codice crea il grafico come di consueto.</span><span class="sxs-lookup"><span data-stu-id="b7061-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="b7061-284">Al termine il grafico, il codice lo salva nella cache chiamando `SaveToCache`.</span><span class="sxs-lookup"><span data-stu-id="b7061-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="b7061-285">Questo metodo richiede una chiave (in modo che il grafico può essere richiesta in un secondo momento) e la quantità di tempo che il grafico deve essere salvato nella cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="b7061-286">(L'ora esatta che è un grafico nella cache dipendono frequenza pensavi potrebbero modificare i dati rappresentati.) Il `SaveToCache` metodo richiede anche un `slidingExpiration` parametro &#8212; se questa opzione è impostata su true, il timeout del contatore viene reimpostato ogni volta che si accede nel grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="b7061-287">In questo caso, in effetti significa che voce della cache del grafico scade due minuti dopo l'ultima volta che qualcuno accede al grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="b7061-288">(L'alternativa per la scadenza variabile è la scadenza assoluta, vale a dire che la voce della cache verrebbe scadono esattamente 2 minuti dopo che è stato inserito nella cache, indipendentemente dalla frequenza era stato eseguito l'accesso).</span><span class="sxs-lookup"><span data-stu-id="b7061-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="b7061-289">Infine, il codice Usa il `WriteFromCache` metodo per recuperare ed eseguire il rendering del grafico dalla cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="b7061-290">Si noti che questo metodo è di fuori di `if` blocco che controlla la cache, perché otterrà il grafico dalla cache se il grafico è stato presente per iniziare o doveva essere generati e salvati nella cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="b7061-291">Si noti che nell'esempio di `AddTitle` metodo include un timestamp.</span><span class="sxs-lookup"><span data-stu-id="b7061-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="b7061-292">(Aggiunge la data e ora correnti &#8212; `DateTime.Now` &#8212; per il titolo.)</span><span class="sxs-lookup"><span data-stu-id="b7061-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="b7061-293">Creare una nuova pagina denominata *ClearCache.cshtml* e sostituirne il contenuto con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="b7061-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="b7061-294">Questa pagina Usa la `WebCache` helper per rimuovere il piano memorizzato nella cache *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="b7061-295">Come indicato in precedenza, non è in genere necessario avere una pagina simile alla seguente.</span><span class="sxs-lookup"><span data-stu-id="b7061-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="b7061-296">Si sta creando qui solo per renderne più semplice testare la memorizzazione nella cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="b7061-297">Eseguire la *ShowCachedChart.cshtml* pagina web in un browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="b7061-298">La pagina viene visualizzata l'immagine del grafico in base al codice contenuto nel *ChartSaveToCache.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="b7061-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="b7061-299">Prendere nota di Cosa dice il timestamp del titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descrizione: Immagine del grafico di base con un timestamp del titolo del grafico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="b7061-301">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-301">Close the browser.</span></span>
8. <span data-ttu-id="b7061-302">Eseguire la *ShowCachedChart.cshtml* nuovamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="b7061-303">Si noti che il timestamp è lo stesso come in precedenza, che indica che il grafico non è stato rigenerato, ma invece è stato letto dalla cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="b7061-304">Nelle *ShowCachedChart.cshtml*, fare clic sui **Cancella cache** collegamento.</span><span class="sxs-lookup"><span data-stu-id="b7061-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="b7061-305">Verrà visualizzata *ClearCache.cshtml*, che segnala che è stata cancellata la cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="b7061-306">Fare clic sui **tornare al ShowCachedChart.cshtml** collegare o rieseguire *ShowCachedChart.cshtml* da WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="b7061-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="b7061-307">Si noti che questa volta il timestamp è stato modificato, perché la cache è stata cancellata.</span><span class="sxs-lookup"><span data-stu-id="b7061-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="b7061-308">Pertanto, il codice era necessario rigenerare il grafico e riportarla in cache.</span><span class="sxs-lookup"><span data-stu-id="b7061-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="b7061-309">Salvataggio di un grafico come File di immagine</span><span class="sxs-lookup"><span data-stu-id="b7061-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="b7061-310">È anche possibile salvare un grafico come file di immagine (ad esempio un *jpg* file) sul server.</span><span class="sxs-lookup"><span data-stu-id="b7061-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="b7061-311">È quindi possibile usare il file di immagine si farebbe con qualsiasi immagine.</span><span class="sxs-lookup"><span data-stu-id="b7061-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="b7061-312">Il vantaggio è il file è archiviato anziché essere salvato in una cache temporanea.</span><span class="sxs-lookup"><span data-stu-id="b7061-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="b7061-313">È possibile salvare una nuova immagine del grafico in momenti diversi (ad esempio, ogni ora) e quindi mantenere un record permanente delle modifiche che si verificano nel corso del tempo.</span><span class="sxs-lookup"><span data-stu-id="b7061-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="b7061-314">Si noti che è necessario assicurarsi che l'applicazione web dispone dell'autorizzazione per salvare un file nella cartella nel server in cui si desidera inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="b7061-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="b7061-315">Nella radice del sito Web, creare una cartella denominata  *\_ChartFiles* se non esiste già.</span><span class="sxs-lookup"><span data-stu-id="b7061-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="b7061-316">Nella radice del sito Web, creare un nuovo file denominato *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="b7061-317">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="b7061-318">Il codice controlla per verificare se il *jpg* file esiste, chiamare il `File.Exists` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="b7061-319">Se il file non esiste, il codice crea un nuovo `Chart` da una matrice.</span><span class="sxs-lookup"><span data-stu-id="b7061-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="b7061-320">Questa volta, il codice chiama il `Save` metodo e passa il `path` parametro per specificare il percorso del file e il nome di file in cui salvare il grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="b7061-321">Nel corpo della pagina, un `<img>` elemento utilizza il percorso in modo che punti la *jpg* file da visualizzare.</span><span class="sxs-lookup"><span data-stu-id="b7061-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="b7061-322">Eseguire la *ChartSave.cshtml* file.</span><span class="sxs-lookup"><span data-stu-id="b7061-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="b7061-323">Tornare a WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="b7061-323">Return to WebMatrix.</span></span> <span data-ttu-id="b7061-324">Si noti che un file di immagine denominato *chart01.jpg* è stata salvata nel  *\_ChartFiles* cartella.</span><span class="sxs-lookup"><span data-stu-id="b7061-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="b7061-325">Salvataggio di un grafico come File XML</span><span class="sxs-lookup"><span data-stu-id="b7061-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="b7061-326">Infine, è possibile salvare un grafico come file XML nel server.</span><span class="sxs-lookup"><span data-stu-id="b7061-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="b7061-327">Un vantaggio dell'uso di questo metodo tramite la memorizzazione nella cache il grafico o salvataggio del grafico in un file è che è possibile modificare il codice XML prima di visualizzare il grafico se si desidera.</span><span class="sxs-lookup"><span data-stu-id="b7061-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="b7061-328">L'applicazione deve disporre delle autorizzazioni di lettura/scrittura per la cartella nel server in cui si desidera inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="b7061-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="b7061-329">Nella radice del sito Web, creare un nuovo file denominato *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b7061-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="b7061-330">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="b7061-331">Questo codice è simile al codice che hai visto in precedenza per l'archiviazione di un grafico nella cache, ad eccezione del fatto che usa un file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="b7061-332">Il codice controlla se esiste il file XML chiamando la `File.Exists` (metodo).</span><span class="sxs-lookup"><span data-stu-id="b7061-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="b7061-333">Se il file esiste, il codice crea un nuovo `Chart` dell'oggetto e passa il nome del file come il `themePath` parametro.</span><span class="sxs-lookup"><span data-stu-id="b7061-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="b7061-334">Ciò consente di creare il grafico basato su qualsiasi oggetto presente in file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="b7061-335">Se il file XML non esiste già, il codice crea un grafico come normale e quindi chiama `SaveXml` per salvarlo.</span><span class="sxs-lookup"><span data-stu-id="b7061-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="b7061-336">Il grafico viene rappresentato tramite il `Write` metodo, come si è visto prima.</span><span class="sxs-lookup"><span data-stu-id="b7061-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="b7061-337">Come con la pagina che illustra la memorizzazione nella cache, questo codice include un timestamp del titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="b7061-338">Creare una nuova pagina denominata *ChartDisplayXMLChart.cshtml* e aggiungervi il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="b7061-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="b7061-339">Eseguire la *ChartDisplayXMLChart.cshtml* pagina.</span><span class="sxs-lookup"><span data-stu-id="b7061-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="b7061-340">Il grafico viene visualizzato.</span><span class="sxs-lookup"><span data-stu-id="b7061-340">The chart is displayed.</span></span> <span data-ttu-id="b7061-341">Prendere nota del timestamp nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="b7061-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="b7061-342">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="b7061-342">Close the browser.</span></span>
6. <span data-ttu-id="b7061-343">In WebMatrix, fare doppio clic il  *\_ChartFiles* cartella, fare clic su **Aggiorna**e quindi aprire la cartella.</span><span class="sxs-lookup"><span data-stu-id="b7061-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="b7061-344">Il *XMLChart.xml* file in questa cartella è stato creato dal `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="b7061-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descrizione: La cartella _ChartFiles che mostra il file XMLChart.xml creato dall'helper grafico.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="b7061-346">Eseguire la *ChartDisplayXMLChart.cshtml* pagina nuovamente.</span><span class="sxs-lookup"><span data-stu-id="b7061-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="b7061-347">Il grafico mostra lo stesso timestamp come la prima volta che è stata eseguita la pagina.</span><span class="sxs-lookup"><span data-stu-id="b7061-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="b7061-348">Ciò avviene perché il grafico viene generato dal file XML salvato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="b7061-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="b7061-349">In WebMatrix aprire il  *\_ChartFiles* cartella ed eliminare le *XMLChart.xml* file.</span><span class="sxs-lookup"><span data-stu-id="b7061-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="b7061-350">Eseguire la *ChartDisplayXMLChart.cshtml* pagina ancora una volta.</span><span class="sxs-lookup"><span data-stu-id="b7061-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="b7061-351">Questa volta, il timestamp viene aggiornato, in quanto il `Chart` helper era necessario ricreare il file XML.</span><span class="sxs-lookup"><span data-stu-id="b7061-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="b7061-352">Se si desidera, verificare i  *\_ChartFiles* cartella e notare che il file XML è nuovo.</span><span class="sxs-lookup"><span data-stu-id="b7061-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="b7061-353">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="b7061-353">Additional Resources</span></span>

- [<span data-ttu-id="b7061-354">Introduzione all'uso di un Database in ASP.NET Web Pages siti</span><span class="sxs-lookup"><span data-stu-id="b7061-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="b7061-355">Tramite la memorizzazione nella cache in ASP.NET Web Pages siti per migliorare le prestazioni</span><span class="sxs-lookup"><span data-stu-id="b7061-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="b7061-356">[Classe grafico](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (riferimenti alle API di pagine Web ASP.NET su MSDN)</span><span class="sxs-lookup"><span data-stu-id="b7061-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
