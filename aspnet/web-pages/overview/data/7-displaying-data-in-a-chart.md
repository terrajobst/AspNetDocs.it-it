---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Visualizzazione di dati in un grafico con Pagine Web ASP.NET (Razor) | Microsoft Docs
author: microsoft
description: In questo capitolo viene illustrato come visualizzare i dati in un grafico. Nei capitoli precedenti si è appreso come visualizzare i dati manualmente e in una griglia. Questo capitolo illustra...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 6dad67d4e3d38d57a761c567d937d714a3184ea9
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78627491"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="7871e-105">Visualizzazione di dati in un grafico con Pagine Web ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="7871e-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="7871e-106">[Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="7871e-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="7871e-107">Questo articolo illustra come usare un grafico per visualizzare i dati in un sito Web Pagine Web ASP.NET (Razor) usando l'helper `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="7871e-108">**Cosa si**apprenderà:</span><span class="sxs-lookup"><span data-stu-id="7871e-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="7871e-109">Visualizzazione dei dati in un grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="7871e-110">Come applicare uno stile ai grafici usando i temi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="7871e-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="7871e-111">Come salvare i grafici e come memorizzarli nella cache per ottenere prestazioni migliori.</span><span class="sxs-lookup"><span data-stu-id="7871e-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="7871e-112">Queste sono le funzionalità di programmazione di ASP.NET introdotte nell'articolo:</span><span class="sxs-lookup"><span data-stu-id="7871e-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="7871e-113">Helper `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="7871e-114">Le informazioni contenute in questo articolo si applicano a Pagine Web ASP.NET 1,0 e Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="7871e-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="7871e-115">Helper del grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-115">The Chart Helper</span></span>

<span data-ttu-id="7871e-116">Quando si desidera visualizzare i dati in forma grafica, è possibile utilizzare `Chart` helper.</span><span class="sxs-lookup"><span data-stu-id="7871e-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="7871e-117">L'helper `Chart` può eseguire il rendering di un'immagine che Visualizza i dati in un'ampia gamma di tipi di grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="7871e-118">Supporta molte opzioni per la formattazione e l'assegnazione di etichette.</span><span class="sxs-lookup"><span data-stu-id="7871e-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="7871e-119">L'helper `Chart` può eseguire il rendering di più di 30 tipi di grafici, inclusi tutti i tipi di grafici che potrebbero essere noti da Microsoft Excel o altri strumenti &#8212; , grafici a barre, istogrammi, grafici a linee e grafici a torta, oltre a grafici più specializzati, ad esempio grafici azionari.</span><span class="sxs-lookup"><span data-stu-id="7871e-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="7871e-120">**Grafico ad area** ![Descrizione: immagine del tipo di grafico ad area](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="7871e-121">**Grafico a barre** ![Descrizione: immagine del tipo di grafico a barre](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="7871e-122">\*\*Istogramma ![Descrizione** : immagine del tipo di istogramma](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="7871e-123">**Grafico a linee** ![Descrizione: immagine del tipo di grafico a linee](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="7871e-124">**Grafico a torta** ![Descrizione: immagine del tipo di grafico a torta](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="7871e-125">**Grafico azionario** ![Descrizione: immagine del tipo di grafico azionario](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="7871e-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="7871e-126">Elementi del grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-126">Chart Elements</span></span>

<span data-ttu-id="7871e-127">I grafici mostrano i dati e altri elementi, ad esempio legende, assi, serie e così via.</span><span class="sxs-lookup"><span data-stu-id="7871e-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="7871e-128">Nell'immagine seguente vengono illustrati molti degli elementi del grafico che è possibile personalizzare quando si utilizza l'helper `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="7871e-129">Questo articolo illustra come impostare alcuni (non tutti) di questi elementi.</span><span class="sxs-lookup"><span data-stu-id="7871e-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descrizione: immagine che Mostra gli elementi del grafico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="7871e-131">Creazione di un grafico dai dati</span><span class="sxs-lookup"><span data-stu-id="7871e-131">Creating a Chart from Data</span></span>

<span data-ttu-id="7871e-132">I dati visualizzati in un grafico possono provenire da una matrice, dai risultati restituiti da un database o dai dati contenuti in un file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="7871e-133">Uso di una matrice</span><span class="sxs-lookup"><span data-stu-id="7871e-133">Using an Array</span></span>

<span data-ttu-id="7871e-134">Come illustrato in [Introduzione alla programmazione pagine Web ASP.NET usando la sintassi Razor](https://go.microsoft.com/fwlink/?LinkId=202890), una matrice consente di archiviare una raccolta di elementi simili in una singola variabile.</span><span class="sxs-lookup"><span data-stu-id="7871e-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="7871e-135">È possibile utilizzare le matrici per contenere i dati che si desidera includere nel grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="7871e-136">In questa procedura viene illustrato come creare un grafico da dati in matrici utilizzando il tipo di grafico predefinito.</span><span class="sxs-lookup"><span data-stu-id="7871e-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="7871e-137">Viene inoltre illustrato come visualizzare il grafico all'interno della pagina.</span><span class="sxs-lookup"><span data-stu-id="7871e-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="7871e-138">Creare un nuovo file denominato *ChartArrayBasic. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="7871e-139">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="7871e-140">Il codice crea innanzitutto un nuovo grafico e ne imposta la larghezza e l'altezza.</span><span class="sxs-lookup"><span data-stu-id="7871e-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="7871e-141">Per specificare il titolo del grafico, utilizzare il metodo `AddTitle`.</span><span class="sxs-lookup"><span data-stu-id="7871e-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="7871e-142">Per aggiungere dati, usare il metodo `AddSeries`.</span><span class="sxs-lookup"><span data-stu-id="7871e-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="7871e-143">In questo esempio vengono usati i parametri `name`, `xValue`e `yValues` del metodo `AddSeries`.</span><span class="sxs-lookup"><span data-stu-id="7871e-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="7871e-144">Il parametro `name` viene visualizzato nella legenda del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="7871e-145">Il parametro `xValue` contiene una matrice di dati visualizzata lungo l'asse orizzontale del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="7871e-146">Il parametro `yValues` contiene una matrice di dati utilizzata per tracciare i punti verticali del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="7871e-147">Il metodo `Write` esegue effettivamente il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="7871e-148">In questo caso, poiché non è stato specificato un tipo di grafico, il `Chart` Helper esegue il rendering del grafico predefinito, che è un istogramma.</span><span class="sxs-lookup"><span data-stu-id="7871e-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="7871e-149">Eseguire la pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-149">Run the page in the browser.</span></span> <span data-ttu-id="7871e-150">Nel browser viene visualizzato il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="7871e-151">Utilizzo di una query di database per i dati del grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="7871e-152">Se le informazioni che si desidera includere nel grafico si trova in un database, è possibile eseguire una query sul database e quindi utilizzare i dati dei risultati per creare il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="7871e-153">In questa procedura viene illustrato come leggere e visualizzare i dati del database creato nell'articolo [Introduzione all'utilizzo di un database in siti pagine Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="7871e-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="7871e-154">Aggiungere una cartella *App\_data* alla radice del sito Web se la cartella non esiste già.</span><span class="sxs-lookup"><span data-stu-id="7871e-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="7871e-155">Nella cartella *App\_data* aggiungere il file di database denominato *SmallBakery. sdf* , descritto in [Introduzione all'uso di un database in pagine Web ASP.NET siti](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="7871e-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="7871e-156">Creare un nuovo file denominato *ChartDataQuery. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="7871e-157">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="7871e-158">Il codice apre innanzitutto il database SmallBakery e lo assegna a una variabile denominata `db`.</span><span class="sxs-lookup"><span data-stu-id="7871e-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="7871e-159">Questa variabile rappresenta un oggetto `Database` che può essere utilizzato per leggere e scrivere nel database.</span><span class="sxs-lookup"><span data-stu-id="7871e-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="7871e-160">Successivamente, il codice esegue una query SQL per ottenere il nome e il prezzo di ogni prodotto.</span><span class="sxs-lookup"><span data-stu-id="7871e-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="7871e-161">Il codice crea un nuovo grafico e lo passa alla query del database chiamando il metodo `DataBindTable` del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="7871e-162">Questo metodo accetta due parametri: il `dataSource` parametro è per i dati della query e il `xField` parametro consente di impostare la colonna di dati utilizzata per l'asse x del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="7871e-163">In alternativa all'uso del metodo `DataBindTable`, è possibile usare il metodo `AddSeries` dell'helper `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="7871e-164">Il metodo `AddSeries` consente di impostare i parametri `xValue` e `yValues`.</span><span class="sxs-lookup"><span data-stu-id="7871e-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="7871e-165">Ad esempio, invece di usare il metodo `DataBindTable` come segue:</span><span class="sxs-lookup"><span data-stu-id="7871e-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="7871e-166">È possibile usare il metodo `AddSeries` come il seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="7871e-167">Entrambi eseguono il rendering degli stessi risultati.</span><span class="sxs-lookup"><span data-stu-id="7871e-167">Both render the same results.</span></span> <span data-ttu-id="7871e-168">Il metodo `AddSeries` è più flessibile perché è possibile specificare il tipo di grafico e i dati in modo più esplicito, ma il metodo `DataBindTable` è più semplice da usare se non è necessaria la flessibilità aggiuntiva.</span><span class="sxs-lookup"><span data-stu-id="7871e-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="7871e-169">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="7871e-170">Utilizzo di dati XML</span><span class="sxs-lookup"><span data-stu-id="7871e-170">Using XML Data</span></span>

<span data-ttu-id="7871e-171">La terza opzione per la creazione di grafici consiste nell'utilizzo di un file XML come dati per il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="7871e-172">A tale scopo, è necessario che il file XML includa anche un file di schema (file*xsd* ) che descriva la struttura XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="7871e-173">In questa procedura viene illustrato come leggere i dati da un file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="7871e-174">Nella cartella *App\_data* creare un nuovo file XML denominato *Data. XML*.</span><span class="sxs-lookup"><span data-stu-id="7871e-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="7871e-175">Sostituire il codice XML esistente con il codice seguente, che è costituito da dati XML sui dipendenti in una società fittizia.</span><span class="sxs-lookup"><span data-stu-id="7871e-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="7871e-176">Nella cartella *App\_data* creare un nuovo file XML denominato *Data. xsd*.</span><span class="sxs-lookup"><span data-stu-id="7871e-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="7871e-177">(Si noti che l'estensione questa volta è *. xsd*).</span><span class="sxs-lookup"><span data-stu-id="7871e-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="7871e-178">Sostituire il codice XML esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="7871e-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="7871e-179">Nella radice del sito Web creare un nuovo file denominato *ChartDataXML. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="7871e-180">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="7871e-181">Il codice crea prima un oggetto `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="7871e-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="7871e-182">Questo oggetto viene utilizzato per gestire i dati letti dal file XML e organizzarli in base alle informazioni contenute nel file di schema.</span><span class="sxs-lookup"><span data-stu-id="7871e-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="7871e-183">Si noti che nella parte superiore del codice è inclusa l'istruzione `using SystemData`.</span><span class="sxs-lookup"><span data-stu-id="7871e-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="7871e-184">Questa operazione è necessaria per poter utilizzare l'oggetto `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="7871e-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="7871e-185">Per ulteriori informazioni, vedere [&quot;utilizzo di istruzioni&quot; e nomi completi](#SB_UsingStatements) più avanti in questo articolo.</span><span class="sxs-lookup"><span data-stu-id="7871e-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="7871e-186">Successivamente, il codice crea un oggetto `DataView` in base al set di dati.</span><span class="sxs-lookup"><span data-stu-id="7871e-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="7871e-187">La visualizzazione dati fornisce un oggetto a &#8212; cui è possibile associare il grafico, ovvero Read e Plot.</span><span class="sxs-lookup"><span data-stu-id="7871e-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="7871e-188">Il grafico viene associato ai dati utilizzando il metodo `AddSeries`, come illustrato in precedenza durante la creazione di grafici dei dati della matrice, ad eccezione del fatto che questa volta i parametri `xValue` e `yValues` sono impostati sull'oggetto `DataView`.</span><span class="sxs-lookup"><span data-stu-id="7871e-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="7871e-189">In questo esempio viene inoltre illustrato come specificare un determinato tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="7871e-190">Quando i dati vengono aggiunti nel metodo `AddSeries`, viene impostato anche il parametro `chartType` per visualizzare un grafico a torta.</span><span class="sxs-lookup"><span data-stu-id="7871e-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="7871e-191">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="7871e-192">Istruzioni "using" e nomi completi</span><span class="sxs-lookup"><span data-stu-id="7871e-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="7871e-193">Il .NET Framework che Pagine Web ASP.NET con sintassi Razor si basa su è costituito da molte migliaia di componenti (classi).</span><span class="sxs-lookup"><span data-stu-id="7871e-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="7871e-194">Per renderlo gestibile da usare con tutte queste classi, sono organizzati in *spazi dei nomi*, che sono simili alle librerie.</span><span class="sxs-lookup"><span data-stu-id="7871e-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="7871e-195">Lo spazio dei nomi `System.Web`, ad esempio, contiene classi che supportano la comunicazione browser/server, lo spazio dei nomi `System.Xml` contiene le classi utilizzate per creare e leggere i file XML e lo spazio dei nomi `System.Data` contiene classi che consentono di utilizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="7871e-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="7871e-196">Per accedere a una determinata classe nell'.NET Framework, il codice deve essere a conoscenza non solo del nome della classe, ma anche dello spazio dei nomi in cui si trova la classe.</span><span class="sxs-lookup"><span data-stu-id="7871e-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="7871e-197">Ad esempio, per poter usare l'helper `Chart`, il codice deve trovare la classe `System.Web.Helpers.Chart`, che combina lo spazio dei nomi (`System.Web.Helpers`) con il nome della classe (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="7871e-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="7871e-198">Questa operazione è nota come nome &#8212; completo della classe la cui *posizione è completa* e non ambigua entro la vastità del .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="7871e-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="7871e-199">Nel codice, si avrà un aspetto simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="7871e-200">Tuttavia, è complesso (ed è soggetta a errori) usare questi nomi lunghi e completi ogni volta che si vuole fare riferimento a una classe o un helper.</span><span class="sxs-lookup"><span data-stu-id="7871e-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="7871e-201">Pertanto, per semplificare l'utilizzo dei nomi di classe, è possibile *importare* gli spazi dei nomi a cui si è interessati, che in genere è semplicemente un piccolo tra i molti spazi dei nomi nel .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="7871e-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="7871e-202">Se è stato importato uno spazio dei nomi, è possibile usare solo un nome di classe (`Chart`) anziché il nome completo (`System.Web.Helpers.Chart`).</span><span class="sxs-lookup"><span data-stu-id="7871e-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="7871e-203">Quando il codice viene eseguito e rileva un nome di classe, può esaminare solo gli spazi dei nomi importati per trovare tale classe.</span><span class="sxs-lookup"><span data-stu-id="7871e-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="7871e-204">Quando si utilizza Pagine Web ASP.NET con sintassi Razor per creare pagine Web, in genere si utilizza ogni volta lo stesso set di classi, tra cui la classe `WebPage`, i vari helper e così via.</span><span class="sxs-lookup"><span data-stu-id="7871e-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="7871e-205">Per evitare di dover importare gli spazi dei nomi rilevanti ogni volta che si crea un sito Web, ASP.NET viene configurato in modo da importare automaticamente un set di spazi dei nomi di base per ogni sito Web.</span><span class="sxs-lookup"><span data-stu-id="7871e-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="7871e-206">Questo è il motivo per cui non è stato necessario gestire gli spazi dei nomi o importare fino a questo momento; tutte le classi con cui si è lavorato si trovano in spazi dei nomi già importati.</span><span class="sxs-lookup"><span data-stu-id="7871e-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="7871e-207">Tuttavia, a volte è necessario usare una classe che non si trova in uno spazio dei nomi importato automaticamente.</span><span class="sxs-lookup"><span data-stu-id="7871e-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="7871e-208">In tal caso, è possibile usare il nome completo della classe oppure è possibile importare manualmente lo spazio dei nomi che contiene la classe.</span><span class="sxs-lookup"><span data-stu-id="7871e-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="7871e-209">Per importare uno spazio dei nomi, è possibile usare l'istruzione `using` (`import` in Visual Basic), come illustrato in precedenza nell'articolo.</span><span class="sxs-lookup"><span data-stu-id="7871e-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="7871e-210">Ad esempio, la classe `DataSet` si trova nello spazio dei nomi `System.Data`.</span><span class="sxs-lookup"><span data-stu-id="7871e-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="7871e-211">Lo spazio dei nomi `System.Data` non è automaticamente disponibile per ASP.NET Razor Pages.</span><span class="sxs-lookup"><span data-stu-id="7871e-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="7871e-212">Per lavorare con la classe `DataSet` usando il nome completo, è quindi possibile usare codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="7871e-213">Se è necessario usare la classe `DataSet` ripetutamente, è possibile importare uno spazio dei nomi simile al seguente e quindi usare solo il nome della classe nel codice:</span><span class="sxs-lookup"><span data-stu-id="7871e-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="7871e-214">È possibile aggiungere istruzioni `using` per qualsiasi altro spazio dei nomi .NET Framework a cui si desidera fare riferimento.</span><span class="sxs-lookup"><span data-stu-id="7871e-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="7871e-215">Tuttavia, come indicato, non è necessario eseguire questa operazione spesso, perché la maggior parte delle classi che verranno utilizzate si trovano in spazi dei nomi importati automaticamente da ASP.NET per l'uso nelle pagine *. cshtml* e *. vbhtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="7871e-216">Visualizzazione di grafici all'interno di una pagina Web</span><span class="sxs-lookup"><span data-stu-id="7871e-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="7871e-217">Negli esempi visualizzati fino a questo punto si crea un grafico e quindi il grafico viene sottoposto a rendering direttamente nel browser sotto forma di grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="7871e-218">In molti casi, tuttavia, si desidera visualizzare un grafico come parte di una pagina, non solo da solo nel browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="7871e-219">A tale scopo, è necessario un processo in due passaggi.</span><span class="sxs-lookup"><span data-stu-id="7871e-219">To do that requires a two-step process.</span></span> <span data-ttu-id="7871e-220">Il primo passaggio consiste nel creare una pagina che genera il grafico, come già visto.</span><span class="sxs-lookup"><span data-stu-id="7871e-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="7871e-221">Il secondo passaggio consiste nel visualizzare l'immagine risultante in un'altra pagina.</span><span class="sxs-lookup"><span data-stu-id="7871e-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="7871e-222">Per visualizzare l'immagine, è possibile usare un elemento HTML `<img>`, nello stesso modo in cui si visualizza un'immagine.</span><span class="sxs-lookup"><span data-stu-id="7871e-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="7871e-223">Tuttavia, anziché fare riferimento a un file con *estensione jpg* o *png* , l'elemento `<img>` fa riferimento al file con *estensione cshtml* che contiene l'helper `Chart` che crea il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="7871e-224">Quando viene eseguita la pagina di visualizzazione, l'elemento `<img>` Ottiene l'output dell'helper `Chart` ed esegue il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="7871e-225">Creare un file denominato *ShowChart. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="7871e-226">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="7871e-227">Il codice usa l'elemento `<img>` per visualizzare il grafico creato in precedenza nel file *ChartArrayBasic. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="7871e-228">Eseguire la pagina Web in un browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-228">Run the web page in a browser.</span></span> <span data-ttu-id="7871e-229">Il file *ShowChart. cshtml* Visualizza l'immagine del grafico in base al codice contenuto nel file *ChartArrayBasic. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="7871e-230">Applicazione di stili a un grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-230">Styling a Chart</span></span>

<span data-ttu-id="7871e-231">L'helper `Chart` supporta un numero elevato di opzioni che consentono di personalizzare l'aspetto del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="7871e-232">È possibile impostare i colori, i tipi di carattere, i bordi e così via.</span><span class="sxs-lookup"><span data-stu-id="7871e-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="7871e-233">Un modo semplice per personalizzare l'aspetto di un grafico consiste nell'usare un *tema*.</span><span class="sxs-lookup"><span data-stu-id="7871e-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="7871e-234">I temi sono raccolte di informazioni che specificano come eseguire il rendering di un grafico utilizzando caratteri, colori, etichette, tavolozze ed effetti.</span><span class="sxs-lookup"><span data-stu-id="7871e-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="7871e-235">Si noti che lo stile di un grafico non indica il tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="7871e-236">Nella tabella seguente sono elencati i temi predefiniti.</span><span class="sxs-lookup"><span data-stu-id="7871e-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="7871e-237">Tema</span><span class="sxs-lookup"><span data-stu-id="7871e-237">Theme</span></span> | <span data-ttu-id="7871e-238">Description</span><span class="sxs-lookup"><span data-stu-id="7871e-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="7871e-239">Visualizza le colonne rosse su uno sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="7871e-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="7871e-240">Visualizza le colonne blu su uno sfondo sfumato blu.</span><span class="sxs-lookup"><span data-stu-id="7871e-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="7871e-241">Visualizza le colonne blu su uno sfondo sfumato verde.</span><span class="sxs-lookup"><span data-stu-id="7871e-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="7871e-242">Visualizza le colonne arancioni su uno sfondo sfumato giallo.</span><span class="sxs-lookup"><span data-stu-id="7871e-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="7871e-243">Consente di visualizzare le colonne rosse tridimensionali in uno sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="7871e-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="7871e-244">È possibile specificare il tema da utilizzare quando si crea un nuovo grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="7871e-245">Creare un nuovo file denominato *ChartStyleGreen. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="7871e-246">Sostituire il contenuto esistente nella pagina con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="7871e-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="7871e-247">Questo codice è identico a quello dell'esempio precedente che usa il database per i dati, ma aggiunge il parametro `theme` quando crea l'oggetto `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="7871e-248">Di seguito viene illustrato il codice modificato:</span><span class="sxs-lookup"><span data-stu-id="7871e-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="7871e-249">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-249">Run the page in a browser.</span></span> <span data-ttu-id="7871e-250">Vengono visualizzati gli stessi dati di prima, ma il grafico sembra più lucido:</span><span class="sxs-lookup"><span data-stu-id="7871e-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="7871e-251">Salvataggio di un grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-251">Saving a Chart</span></span>

<span data-ttu-id="7871e-252">Quando si usa l'helper `Chart` come si è visto finora in questo articolo, l'helper ricrea il grafico da zero ogni volta che viene richiamato.</span><span class="sxs-lookup"><span data-stu-id="7871e-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="7871e-253">Se necessario, anche il codice per il grafico esegue una query sul database o legge di nuovo il file XML per ottenere i dati.</span><span class="sxs-lookup"><span data-stu-id="7871e-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="7871e-254">In alcuni casi, questa operazione può essere un'operazione complessa, ad esempio se il database su cui si sta eseguendo una query è di grandi dimensioni o se il file XML contiene una grande quantità di dati.</span><span class="sxs-lookup"><span data-stu-id="7871e-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="7871e-255">Anche se il grafico non implica una grande quantità di dati, il processo di creazione dinamica di un'immagine richiede risorse del server e, se molte persone richiedono la pagina o le pagine che visualizzano il grafico, può influito negativamente sulle prestazioni del sito Web.</span><span class="sxs-lookup"><span data-stu-id="7871e-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="7871e-256">Per ridurre il potenziale impatto sulle prestazioni della creazione di un grafico, è possibile creare un grafico la prima volta che è necessario e quindi salvarlo.</span><span class="sxs-lookup"><span data-stu-id="7871e-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="7871e-257">Quando il grafico è nuovamente necessario, anziché rigenerarlo, è possibile recuperare solo la versione salvata ed eseguirne il rendering.</span><span class="sxs-lookup"><span data-stu-id="7871e-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="7871e-258">È possibile salvare un grafico nei modi seguenti:</span><span class="sxs-lookup"><span data-stu-id="7871e-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="7871e-259">Memorizzare nella cache il grafico nella memoria del computer (sul server).</span><span class="sxs-lookup"><span data-stu-id="7871e-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="7871e-260">Salvare il grafico come file di immagine.</span><span class="sxs-lookup"><span data-stu-id="7871e-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="7871e-261">Salvare il grafico come file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-261">Save the chart as an XML file.</span></span> <span data-ttu-id="7871e-262">Questa opzione consente di modificare il grafico prima di salvarlo.</span><span class="sxs-lookup"><span data-stu-id="7871e-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="7871e-263">Memorizzazione nella cache di un grafico</span><span class="sxs-lookup"><span data-stu-id="7871e-263">Caching a Chart</span></span>

<span data-ttu-id="7871e-264">Dopo aver creato un grafico, è possibile memorizzarlo nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="7871e-265">La memorizzazione nella cache di un grafico significa che non è necessario ricrearla se deve essere visualizzata nuovamente.</span><span class="sxs-lookup"><span data-stu-id="7871e-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="7871e-266">Quando si salva un grafico nella cache, viene assegnata una chiave che deve essere univoca per tale grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="7871e-267">I grafici salvati nella cache potrebbero essere rimossi se la memoria del server è insufficiente.</span><span class="sxs-lookup"><span data-stu-id="7871e-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="7871e-268">Inoltre, la cache viene cancellata se l'applicazione viene riavviata per qualsiasi motivo.</span><span class="sxs-lookup"><span data-stu-id="7871e-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="7871e-269">Pertanto, il modo standard per utilizzare un grafico memorizzato nella cache consiste nel controllare sempre prima se è disponibile nella cache e, in caso contrario, per crearlo o ricrearlo.</span><span class="sxs-lookup"><span data-stu-id="7871e-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="7871e-270">Alla radice del sito Web, creare un file denominato *ShowCachedChart. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="7871e-271">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="7871e-272">Il tag `<img>` include un attributo `src` che punta al file *ChartSaveToCache. cshtml* e passa una chiave alla pagina come stringa di query.</span><span class="sxs-lookup"><span data-stu-id="7871e-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="7871e-273">La chiave contiene il valore &quot;myChartKey&quot;.</span><span class="sxs-lookup"><span data-stu-id="7871e-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="7871e-274">Il file *ChartSaveToCache. cshtml* contiene il `Chart` helper che consente di creare il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="7871e-275">Questa pagina verrà creata in un secondo momento.</span><span class="sxs-lookup"><span data-stu-id="7871e-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="7871e-276">Alla fine della pagina è presente un collegamento a una pagina denominata *ClearCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="7871e-277">Si tratta di una pagina che verrà creata anche a breve.</span><span class="sxs-lookup"><span data-stu-id="7871e-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="7871e-278">È necessario *ClearCache. cshtml* solo per testare la memorizzazione nella cache per questo esempio: non si tratta di un collegamento o di una pagina che in genere si include quando si utilizzano i grafici memorizzati nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="7871e-279">Alla radice del sito Web, creare un nuovo file denominato *ChartSaveToCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="7871e-280">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="7871e-281">Il codice controlla prima di tutto se è stato passato un valore di chiave nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="7871e-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="7871e-282">In tal caso, il codice tenta di leggere un grafico dalla cache chiamando il metodo `GetFromCache` e passandolo alla chiave.</span><span class="sxs-lookup"><span data-stu-id="7871e-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="7871e-283">Se si scopre che nella cache non sono presenti elementi nella cache (che si verificano la prima volta che il grafico viene richiesto), il codice crea il grafico come di consueto.</span><span class="sxs-lookup"><span data-stu-id="7871e-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="7871e-284">Al termine del grafico, il codice lo salva nella cache chiamando `SaveToCache`.</span><span class="sxs-lookup"><span data-stu-id="7871e-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="7871e-285">Questo metodo richiede una chiave (in modo che il grafico possa essere richiesto in seguito) e la quantità di tempo durante il quale il grafico deve essere salvato nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="7871e-286">(Il momento esatto in cui si memorizza la cache di un grafico dipende dalla frequenza con cui si ritiene che i dati che rappresenta potrebbero cambiare). Il `SaveToCache` metodo richiede anche un parametro &#8212; `slidingExpiration` se è impostato su true, il contatore di timeout viene reimpostato ogni volta che viene eseguito l'accesso al grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="7871e-287">In questo caso, significa che la voce della cache del grafico scade 2 minuti dopo l'ultima volta in cui un utente ha eseguito l'accesso al grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="7871e-288">L'alternativa alla scadenza scorrevole è la scadenza assoluta, vale a dire che la voce della cache scadrà esattamente 2 minuti dopo che è stata inserita nella cache, indipendentemente dalla frequenza con cui è stato eseguito l'accesso.</span><span class="sxs-lookup"><span data-stu-id="7871e-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="7871e-289">Infine, il codice utilizza il metodo `WriteFromCache` per recuperare ed eseguire il rendering del grafico dalla cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="7871e-290">Si noti che questo metodo si trova all'esterno del blocco `if` che controlla la cache, perché otterrà il grafico dalla cache, indipendentemente dal fatto che il grafico dovesse iniziare o debba essere generato e salvato nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="7871e-291">Si noti che nell'esempio il metodo `AddTitle` include un timestamp.</span><span class="sxs-lookup"><span data-stu-id="7871e-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="7871e-292">Aggiunge la data e l'ora &#8212; correnti `DateTime.Now` &#8212; al titolo.</span><span class="sxs-lookup"><span data-stu-id="7871e-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="7871e-293">Creare una nuova pagina denominata *ClearCache. cshtml* e sostituirne il contenuto con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="7871e-294">Questa pagina usa l'helper `WebCache` per rimuovere il grafico memorizzato nella cache di *ChartSaveToCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="7871e-295">Come indicato in precedenza, non è in genere necessario avere una pagina simile alla seguente.</span><span class="sxs-lookup"><span data-stu-id="7871e-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="7871e-296">Che verrà creato qui solo per semplificare la verifica della memorizzazione nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="7871e-297">Eseguire la pagina Web *ShowCachedChart. cshtml* in un browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="7871e-298">Nella pagina viene visualizzata l'immagine del grafico in base al codice contenuto nel file *ChartSaveToCache. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="7871e-299">Prendere nota del timestamp indicato nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descrizione: immagine del grafico di base con timestamp nel titolo del grafico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="7871e-301">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-301">Close the browser.</span></span>
8. <span data-ttu-id="7871e-302">Eseguire di nuovo *ShowCachedChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="7871e-303">Si noti che il timestamp è identico a quello precedente, che indica che il grafico non è stato rigenerato, ma è stato invece letto dalla cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="7871e-304">In *ShowCachedChart. cshtml*fare clic sul collegamento **Cancella cache** .</span><span class="sxs-lookup"><span data-stu-id="7871e-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="7871e-305">In questo modo si passa a *ClearCache. cshtml*, che indica che la cache è stata cancellata.</span><span class="sxs-lookup"><span data-stu-id="7871e-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="7871e-306">Fare clic sul collegamento **Return to ShowCachedChart. cshtml** oppure eseguire nuovamente *ShowCachedChart. cshtml* da WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="7871e-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="7871e-307">Si noti che questa volta il timestamp è stato modificato, perché la cache è stata cancellata.</span><span class="sxs-lookup"><span data-stu-id="7871e-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="7871e-308">Pertanto, il codice doveva rigenerare il grafico e inserirlo nuovamente nella cache.</span><span class="sxs-lookup"><span data-stu-id="7871e-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="7871e-309">Salvataggio di un grafico come file di immagine</span><span class="sxs-lookup"><span data-stu-id="7871e-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="7871e-310">È inoltre possibile salvare un grafico come file di immagine, ad esempio un file con *estensione jpg* , nel server.</span><span class="sxs-lookup"><span data-stu-id="7871e-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="7871e-311">È quindi possibile usare il file di immagine in modo analogo a qualsiasi immagine.</span><span class="sxs-lookup"><span data-stu-id="7871e-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="7871e-312">Il vantaggio è che il file viene archiviato anziché salvato in una cache temporanea.</span><span class="sxs-lookup"><span data-stu-id="7871e-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="7871e-313">È possibile salvare una nuova immagine del grafico in momenti diversi (ad esempio, ogni ora) e quindi conservare un record permanente delle modifiche che si verificano nel tempo.</span><span class="sxs-lookup"><span data-stu-id="7871e-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="7871e-314">Si noti che è necessario assicurarsi che l'applicazione Web disponga dell'autorizzazione per salvare un file nella cartella nel server in cui si desidera inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="7871e-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="7871e-315">Alla radice del sito Web, creare una cartella denominata *\_ChartFiles* , se non esiste già.</span><span class="sxs-lookup"><span data-stu-id="7871e-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="7871e-316">Alla radice del sito Web, creare un nuovo file denominato *ChartSave. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="7871e-317">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="7871e-318">Il codice verifica prima di tutto se il file *. jpg* esiste chiamando il metodo `File.Exists`.</span><span class="sxs-lookup"><span data-stu-id="7871e-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="7871e-319">Se il file non esiste, il codice crea un nuovo `Chart` da una matrice.</span><span class="sxs-lookup"><span data-stu-id="7871e-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="7871e-320">Questa volta, il codice chiama il metodo `Save` e passa il parametro `path` per specificare il percorso e il nome del file in cui salvare il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="7871e-321">Nel corpo della pagina un elemento `<img>` usa il percorso per puntare al file *jpg* da visualizzare.</span><span class="sxs-lookup"><span data-stu-id="7871e-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="7871e-322">Eseguire il file *ChartSave. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="7871e-323">Tornare a WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="7871e-323">Return to WebMatrix.</span></span> <span data-ttu-id="7871e-324">Si noti che nel *\_cartella ChartFiles* è stato salvato un file di immagine denominato *Chart01. jpg* .</span><span class="sxs-lookup"><span data-stu-id="7871e-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="7871e-325">Salvataggio di un grafico come file XML</span><span class="sxs-lookup"><span data-stu-id="7871e-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="7871e-326">Infine, è possibile salvare un grafico come file XML nel server.</span><span class="sxs-lookup"><span data-stu-id="7871e-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="7871e-327">Un vantaggio dell'utilizzo di questo metodo rispetto alla memorizzazione nella cache del grafico o al salvataggio del grafico in un file è la possibilità di modificare il codice XML prima di visualizzare il grafico, se lo si desidera.</span><span class="sxs-lookup"><span data-stu-id="7871e-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="7871e-328">L'applicazione deve disporre delle autorizzazioni di lettura/scrittura per la cartella nel server in cui si vuole inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="7871e-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="7871e-329">Alla radice del sito Web, creare un nuovo file denominato *ChartSaveXml. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="7871e-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="7871e-330">Sostituire il contenuto esistente con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="7871e-331">Questo codice è simile al codice illustrato in precedenza per l'archiviazione di un grafico nella cache, ad eccezione del fatto che usa un file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="7871e-332">Il codice verifica prima di tutto se il file XML esiste chiamando il metodo `File.Exists`.</span><span class="sxs-lookup"><span data-stu-id="7871e-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="7871e-333">Se il file esiste, il codice crea un nuovo oggetto `Chart` e passa il nome del file come parametro di `themePath`.</span><span class="sxs-lookup"><span data-stu-id="7871e-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="7871e-334">Il grafico verrà creato in base a qualsiasi contenuto nel file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="7871e-335">Se il file XML non esiste già, il codice crea un grafico come il normale e quindi chiama `SaveXml` per salvarlo.</span><span class="sxs-lookup"><span data-stu-id="7871e-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="7871e-336">Viene eseguito il rendering del grafico utilizzando il metodo `Write`, come illustrato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="7871e-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="7871e-337">Come nella pagina che mostra la memorizzazione nella cache, questo codice include un timestamp nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="7871e-338">Creare una nuova pagina denominata *ChartDisplayXMLChart. cshtml* e aggiungervi il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="7871e-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="7871e-339">Eseguire la pagina *ChartDisplayXMLChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="7871e-340">Viene visualizzato il grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-340">The chart is displayed.</span></span> <span data-ttu-id="7871e-341">Prendere nota del timestamp nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="7871e-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="7871e-342">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="7871e-342">Close the browser.</span></span>
6. <span data-ttu-id="7871e-343">In WebMatrix fare clic con il pulsante destro del mouse sulla cartella *\_ChartFiles* , scegliere **Aggiorna**, quindi aprire la cartella.</span><span class="sxs-lookup"><span data-stu-id="7871e-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="7871e-344">Il file *xmlchart. XML* in questa cartella è stato creato dall'helper `Chart`.</span><span class="sxs-lookup"><span data-stu-id="7871e-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descrizione: la _ChartFiles cartella che mostra il file xmlchart. XML creato dall'helper del grafico.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="7871e-346">Eseguire di nuovo la pagina *ChartDisplayXMLChart. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="7871e-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="7871e-347">Il grafico mostra lo stesso timestamp della prima volta che è stata eseguita la pagina.</span><span class="sxs-lookup"><span data-stu-id="7871e-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="7871e-348">Questo perché il grafico viene generato dal codice XML salvato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="7871e-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="7871e-349">In WebMatrix aprire la cartella *\_ChartFiles* ed eliminare il file *xmlchart. XML* .</span><span class="sxs-lookup"><span data-stu-id="7871e-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="7871e-350">Eseguire la pagina *ChartDisplayXMLChart. cshtml* ancora una volta.</span><span class="sxs-lookup"><span data-stu-id="7871e-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="7871e-351">Questa volta, il timestamp viene aggiornato, perché il `Chart` Helper ha dovuto ricreare il file XML.</span><span class="sxs-lookup"><span data-stu-id="7871e-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="7871e-352">Se lo si desidera, controllare la cartella *\_ChartFiles* e notare che il file XML è di nuovo.</span><span class="sxs-lookup"><span data-stu-id="7871e-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="7871e-353">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="7871e-353">Additional Resources</span></span>

- [<span data-ttu-id="7871e-354">Introduzione all'utilizzo di un database in siti Pagine Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="7871e-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="7871e-355">Uso della memorizzazione nella cache nei siti Pagine Web ASP.NET per migliorare le prestazioni</span><span class="sxs-lookup"><span data-stu-id="7871e-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="7871e-356">[Classe Chart](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (informazioni di riferimento sull'API pagine Web ASP.NET su MSDN)</span><span class="sxs-lookup"><span data-stu-id="7871e-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
