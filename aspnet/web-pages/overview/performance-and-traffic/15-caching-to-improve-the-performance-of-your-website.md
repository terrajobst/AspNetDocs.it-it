---
uid: web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
title: Memorizzazione dei dati nella cache in un sito di Pagine Web ASP.NET (Razor) per ottenere prestazioni migliori | Microsoft Docs
author: Rick-Anderson
description: È possibile velocizzare il sito Web con l'archivio it, ovvero la cache, ovvero i risultati dei dati che in genere richiederanno molto tempo per recuperare o elaborare un...
ms.author: riande
ms.date: 02/14/2014
ms.assetid: 961e525b-7700-469e-8a68-d7010b6fb68c
msc.legacyurl: /web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
msc.type: authoredcontent
ms.openlocfilehash: 01796d3ca699a6af5d9162b22a926551435c2040
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78641519"
---
# <a name="caching-data-in-an-aspnet-web-pages-razor-site-for-better-performance"></a><span data-ttu-id="92af6-103">Memorizzazione dei dati nella cache in un sito di Pagine Web ASP.NET (Razor) per ottenere prestazioni migliori</span><span class="sxs-lookup"><span data-stu-id="92af6-103">Caching Data in an ASP.NET Web Pages (Razor) Site for Better Performance</span></span>

<span data-ttu-id="92af6-104">di [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="92af6-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="92af6-105">Questo articolo illustra come usare un helper per memorizzare nella cache le informazioni per ottenere prestazioni più veloci in un sito Web di Pagine Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="92af6-105">This article explains how to use a helper to cache information for faster performance in an ASP.NET Web Pages (Razor) website.</span></span> <span data-ttu-id="92af6-106">È possibile velocizzare il sito Web con l'archivio &#8212; it, memorizzando &#8212; nella cache i risultati dei dati che in genere richiederanno molto tempo per il recupero o l'elaborazione e che non cambiano spesso.</span><span class="sxs-lookup"><span data-stu-id="92af6-106">You can speed up your website by having it store &#8212; that is, cache &#8212; the results of data that ordinarily would take considerable time to retrieve or process and that does not change often.</span></span>
> 
> <span data-ttu-id="92af6-107">**Cosa si apprenderà:**</span><span class="sxs-lookup"><span data-stu-id="92af6-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="92af6-108">Come usare la memorizzazione nella cache per migliorare la velocità di risposta del sito Web.</span><span class="sxs-lookup"><span data-stu-id="92af6-108">How to use caching to improve the responsiveness of your website.</span></span>
> 
> <span data-ttu-id="92af6-109">Queste sono le funzionalità di ASP.NET introdotte nell'articolo:</span><span class="sxs-lookup"><span data-stu-id="92af6-109">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="92af6-110">Helper `WebCache`.</span><span class="sxs-lookup"><span data-stu-id="92af6-110">The `WebCache` helper.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="92af6-111">Versioni del software usate nell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="92af6-111">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="92af6-112">Pagine Web ASP.NET (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="92af6-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="92af6-113">Questa esercitazione funziona anche con Pagine Web ASP.NET 2.</span><span class="sxs-lookup"><span data-stu-id="92af6-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>

<span data-ttu-id="92af6-114">Ogni volta che un utente richiede una pagina dal sito, il server Web deve eseguire alcune operazioni per poter soddisfare la richiesta.</span><span class="sxs-lookup"><span data-stu-id="92af6-114">Every time someone requests a page from your site, the web server has to do some work in order to fulfill the request.</span></span> <span data-ttu-id="92af6-115">Per alcune pagine, il server potrebbe dover eseguire attività che accettano (comparativamente) molto tempo, ad esempio il recupero di dati da un database.</span><span class="sxs-lookup"><span data-stu-id="92af6-115">For some of your pages, the server might have to perform tasks that take a (comparatively) long time, such as retrieving data from a database.</span></span> <span data-ttu-id="92af6-116">Anche se queste attività non richiedono molto tempo in termini assoluti, se il sito presenta una grande quantità di traffico, un'intera serie di richieste singole che fanno sì che il server Web esegua l'attività complicata o lenta può aggiungere molto lavoro.</span><span class="sxs-lookup"><span data-stu-id="92af6-116">Even if these tasks don't take long in absolute terms, if your site experiences a lot of traffic, a whole series of individual requests that cause the web server to perform the complicated or slow task can add up to a lot of work.</span></span> <span data-ttu-id="92af6-117">Questo può influire negativamente sulle prestazioni del sito.</span><span class="sxs-lookup"><span data-stu-id="92af6-117">This can ultimately affect the performance of the site.</span></span>

<span data-ttu-id="92af6-118">Un modo per migliorare le prestazioni del sito Web in circostanze come questa è la memorizzazione dei dati nella cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-118">One way to improve the performance of your website in circumstances like this is to cache data.</span></span> <span data-ttu-id="92af6-119">Se il sito riceve richieste ripetute per le stesse informazioni e non è necessario modificare le informazioni per ogni persona e non è sensibile al tempo, anziché rieseguire il recupero o il ricalcolo, è possibile recuperare i dati una volta e quindi archiviare i risultati.</span><span class="sxs-lookup"><span data-stu-id="92af6-119">If your site gets repeated requests for the same information, and the information does not need to be modified for each person, and it's not time sensitive, instead of re-fetching or recalculating it, you can fetch the data once and then store the results.</span></span> <span data-ttu-id="92af6-120">Al successivo arrivo di una richiesta per tali informazioni, è sufficiente estrarla dalla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-120">The next time a request comes in for that information, you just get it out of the cache.</span></span>

<span data-ttu-id="92af6-121">In generale, si memorizzano nella cache le informazioni che non cambiano di frequente.</span><span class="sxs-lookup"><span data-stu-id="92af6-121">In general, you cache information that doesn't change frequently.</span></span> <span data-ttu-id="92af6-122">Quando si inseriscono le informazioni nella cache, questa viene archiviata in memoria sul server Web.</span><span class="sxs-lookup"><span data-stu-id="92af6-122">When you put information in the cache, it's stored in memory on the web server.</span></span> <span data-ttu-id="92af6-123">È possibile specificare la durata della memorizzazione nella cache, da secondi a giorni.</span><span class="sxs-lookup"><span data-stu-id="92af6-123">You can specify how long it should be cached, from seconds to days.</span></span> <span data-ttu-id="92af6-124">Al termine del periodo di memorizzazione nella cache, le informazioni vengono rimosse automaticamente dalla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-124">When the caching period expires, the information is automatically removed from the cache.</span></span>

> [!NOTE]
> <span data-ttu-id="92af6-125">Le voci della cache potrebbero essere rimosse per motivi diversi dalla scadenza.</span><span class="sxs-lookup"><span data-stu-id="92af6-125">Entries in the cache might be removed for reasons other than that they've expired.</span></span> <span data-ttu-id="92af6-126">Ad esempio, è possibile che il server Web venga temporaneamente esaurito in memoria e un modo per recuperare la memoria è la generazione di voci dalla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-126">For example, the web server might temporarily run low on memory, and one way it can reclaim memory is by throwing entries out of the cache.</span></span> <span data-ttu-id="92af6-127">Come si vedrà, anche se sono state inserite informazioni nella cache, è necessario verificare che siano ancora disponibili quando necessario.</span><span class="sxs-lookup"><span data-stu-id="92af6-127">As you'll see, even if you've put information into the cache, you have to check to be sure it's still there when you need it.</span></span>

<span data-ttu-id="92af6-128">Si supponga che il sito Web abbia una pagina che visualizza la temperatura e le previsioni meteorologiche correnti.</span><span class="sxs-lookup"><span data-stu-id="92af6-128">Imagine your website has a page that displays the current temperature and weather forecast.</span></span> <span data-ttu-id="92af6-129">Per ottenere questo tipo di informazioni, è possibile inviare una richiesta a un servizio esterno.</span><span class="sxs-lookup"><span data-stu-id="92af6-129">To get this type of information, you might send a request to an external service.</span></span> <span data-ttu-id="92af6-130">Poiché queste informazioni non cambiano molto (ad esempio, in un periodo di due ore) e poiché le chiamate esterne richiedono tempo e larghezza di banda, è un buon candidato per la memorizzazione nella cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-130">Since this information doesn't change much (within a two-hour time period, for example) and since external calls require time and bandwidth, it's a good candidate for caching.</span></span>

## <a name="adding-caching-to-a-page"></a><span data-ttu-id="92af6-131">Aggiunta della memorizzazione nella cache a una pagina</span><span class="sxs-lookup"><span data-stu-id="92af6-131">Adding Caching to a Page</span></span>

<span data-ttu-id="92af6-132">ASP.NET include un helper `WebCache` che semplifica l'aggiunta di Caching al sito e l'aggiunta di dati alla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-132">ASP.NET includes a `WebCache` helper that makes it easy to add caching to your site and add data to the cache.</span></span> <span data-ttu-id="92af6-133">In questa procedura verrà creata una pagina che memorizza nella cache l'ora corrente.</span><span class="sxs-lookup"><span data-stu-id="92af6-133">In this procedure, you'll create a page that caches the current time.</span></span> <span data-ttu-id="92af6-134">Questo non è un esempio reale, dal momento che l'ora corrente è qualcosa che cambia spesso e che non è più complesso da calcolare.</span><span class="sxs-lookup"><span data-stu-id="92af6-134">This isn't a real-world example, since the current time is something that does change often, and that moreover isn't complex to calculate.</span></span> <span data-ttu-id="92af6-135">Tuttavia, si tratta di un modo efficace per illustrare il funzionamento della memorizzazione nella cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-135">However, it's a good way to illustrate caching in action.</span></span>

1. <span data-ttu-id="92af6-136">Aggiungere una nuova pagina denominata *WebCache. cshtml* al sito Web.</span><span class="sxs-lookup"><span data-stu-id="92af6-136">Add a new page named *WebCache.cshtml* to the website.</span></span>
2. <span data-ttu-id="92af6-137">Aggiungere il codice e il markup seguenti alla pagina:</span><span class="sxs-lookup"><span data-stu-id="92af6-137">Add the following code and markup to the page:</span></span>

    [!code-cshtml[Main](15-caching-to-improve-the-performance-of-your-website/samples/sample1.cshtml)]

    <span data-ttu-id="92af6-138">Quando si memorizzano nella cache i dati, è possibile inserirli nella cache usando un nome univoco nel sito Web.</span><span class="sxs-lookup"><span data-stu-id="92af6-138">When you cache data, you put it into the cache using a name this is unique across the website.</span></span> <span data-ttu-id="92af6-139">In questo caso, si userà una voce della cache denominata `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="92af6-139">In this case, you'll use a cache entry named `CachedTime`.</span></span> <span data-ttu-id="92af6-140">Questa è la `cacheItemKey` illustrata nell'esempio di codice.</span><span class="sxs-lookup"><span data-stu-id="92af6-140">This is the `cacheItemKey` shown in the code example.</span></span>

    <span data-ttu-id="92af6-141">Il codice legge prima la voce della cache `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="92af6-141">The code first reads the `CachedTime` cache entry.</span></span> <span data-ttu-id="92af6-142">Se viene restituito un valore, ovvero se la voce della cache non è null, il codice imposta semplicemente il valore della variabile temporale sui dati della cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-142">If a value is returned (that is, if the cache entry isn't null), the code just sets the value of the time variable to the cache data.</span></span>

    <span data-ttu-id="92af6-143">Tuttavia, se la voce della cache non esiste (ovvero è null), il codice imposta il valore di ora, lo aggiunge alla cache e imposta un valore di scadenza su un minuto.</span><span class="sxs-lookup"><span data-stu-id="92af6-143">However, if the cache entry doesn't exist (that is, it's null), the code sets the time value, adds it to the cache, and sets an expiration value to one minute.</span></span> <span data-ttu-id="92af6-144">Dopo un minuto, la voce della cache viene eliminata.</span><span class="sxs-lookup"><span data-stu-id="92af6-144">After one minute, the cache entry is discarded.</span></span> <span data-ttu-id="92af6-145">(Il valore di scadenza predefinito per un elemento nella cache è 20 minuti). Il comando `WebCache.Set(cacheItemKey, time, 1, false)` Mostra come aggiungere il valore dell'ora corrente alla cache e impostare la relativa scadenza su 1 minuto.</span><span class="sxs-lookup"><span data-stu-id="92af6-145">(The default expiration value for an item in the cache is 20 minutes.) The command `WebCache.Set(cacheItemKey, time, 1, false)` shows how to add the current time value to the cache and set its expiration to 1 minute.</span></span> <span data-ttu-id="92af6-146">L'impostazione del parametro *slidingExpiration* su `false` indica che l'ora di scadenza non viene rinnovata ogni volta che viene richiesta.</span><span class="sxs-lookup"><span data-stu-id="92af6-146">Setting the *slidingExpiration* parameter to `false` means the expiration time is not renewed each time it is requested.</span></span> <span data-ttu-id="92af6-147">Scadrà esattamente 1 minuto dopo che è stato originariamente aggiunto alla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-147">It will expire exactly 1 minute after it was originally added to the cache.</span></span> <span data-ttu-id="92af6-148">Se si imposta questo valore su `true` l'ora di scadenza di 1 minuto viene reimpostata ogni volta che il valore viene richiesto dalla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-148">If you set this value to `true` the 1 minute expiration time is reset each time the value is requested from the cache.</span></span>

    <span data-ttu-id="92af6-149">Questo codice illustra il modello da usare sempre quando si memorizzano nella cache i dati.</span><span class="sxs-lookup"><span data-stu-id="92af6-149">This code illustrates the pattern you should always use when you cache data.</span></span> <span data-ttu-id="92af6-150">Prima di ottenere un elemento dalla cache, verificare sempre prima se il metodo `WebCache.Get` ha restituito null.</span><span class="sxs-lookup"><span data-stu-id="92af6-150">Before you get something out of the cache, always check first whether the `WebCache.Get` method has returned null.</span></span> <span data-ttu-id="92af6-151">Tenere presente che la voce della cache potrebbe essere scaduta o potrebbe essere stata rimossa per altri motivi, pertanto qualsiasi voce specificata non sarà mai garantita nella cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-151">Remember that the cache entry might have expired or might have been removed for some other reason, so any given entry is never guaranteed to be in the cache.</span></span>
3. <span data-ttu-id="92af6-152">Eseguire *WebCache. cshtml* in un browser.</span><span class="sxs-lookup"><span data-stu-id="92af6-152">Run *WebCache.cshtml* in a browser.</span></span> <span data-ttu-id="92af6-153">Assicurarsi che la pagina sia selezionata nell'area di lavoro **file** prima di eseguirla. La prima volta che si richiede la pagina, i dati temporali non sono nella cache e il codice deve aggiungere il valore di ora alla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-153">(Make sure the page is selected in the **Files** workspace before you run it.) The first time you request the page, the time data isn't in the cache, and the code has to add the time value to the cache.</span></span>

    ![cache-1](15-caching-to-improve-the-performance-of-your-website/_static/image1.jpg)
4. <span data-ttu-id="92af6-155">Aggiornare *WebCache. cshtml* nel browser.</span><span class="sxs-lookup"><span data-stu-id="92af6-155">Refresh *WebCache.cshtml* in the browser.</span></span> <span data-ttu-id="92af6-156">Questa volta, i dati temporali sono nella cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-156">This time, the time data is in the cache.</span></span> <span data-ttu-id="92af6-157">Si noti che l'ora non è stata modificata dall'ultima volta in cui è stata visualizzata la pagina.</span><span class="sxs-lookup"><span data-stu-id="92af6-157">Notice that the time hasn't changed since the last time you viewed the page.</span></span>

    ![cache-2](15-caching-to-improve-the-performance-of-your-website/_static/image2.jpg)
5. <span data-ttu-id="92af6-159">Attendere un minuto prima che la cache venga svuotata, quindi aggiornare la pagina.</span><span class="sxs-lookup"><span data-stu-id="92af6-159">Wait one minute for the cache to be emptied, and then refresh the page.</span></span> <span data-ttu-id="92af6-160">La pagina indica di nuovo che i dati relativi all'ora non sono stati trovati nella cache e l'ora aggiornata viene aggiunta alla cache.</span><span class="sxs-lookup"><span data-stu-id="92af6-160">The page again indicates that the time data wasn't found in the cache, and the updated time is added to the cache.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="92af6-161">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="92af6-161">Additional Resources</span></span>

- [<span data-ttu-id="92af6-162">Visualizzazione dei dati in un grafico</span><span class="sxs-lookup"><span data-stu-id="92af6-162">Displaying Data in a Chart</span></span>](https://go.microsoft.com/fwlink/?LinkId=202895)
- <span data-ttu-id="92af6-163">Informazioni di [riferimento sulle API WebCache](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span><span class="sxs-lookup"><span data-stu-id="92af6-163">[WebCache API reference](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span></span>
