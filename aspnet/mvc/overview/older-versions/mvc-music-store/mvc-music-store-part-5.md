---
uid: mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-5
title: 'Parte 5: modificare moduli e modelli | Microsoft Docs'
author: jongalloway
description: Questa serie di esercitazioni illustra tutti i passaggi necessari per compilare l'applicazione di esempio ASP.NET MVC Music Store. Nella parte 5 viene illustrata la modifica di moduli e modelli.
ms.author: riande
ms.date: 04/21/2011
ms.assetid: 6b09413a-6d6a-425a-87c9-629f91b91b28
msc.legacyurl: /mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-5
msc.type: authoredcontent
ms.openlocfilehash: 20b99cbe57b5dfa623205838a5929733a6c2d70d
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78559549"
---
# <a name="part-5-edit-forms-and-templating"></a><span data-ttu-id="6471d-104">Parte 5: modificare moduli e modelli</span><span class="sxs-lookup"><span data-stu-id="6471d-104">Part 5: Edit Forms and Templating</span></span>

<span data-ttu-id="6471d-105">di [Jon Galloway](https://github.com/jongalloway)</span><span class="sxs-lookup"><span data-stu-id="6471d-105">by [Jon Galloway](https://github.com/jongalloway)</span></span>

> <span data-ttu-id="6471d-106">Music Store MVC è un'applicazione di esercitazione che introduce e spiega passo passo come usare ASP.NET MVC e Visual Studio per lo sviluppo Web.</span><span class="sxs-lookup"><span data-stu-id="6471d-106">The MVC Music Store is a tutorial application that introduces and explains step-by-step how to use ASP.NET MVC and Visual Studio for web development.</span></span>  
>   
> <span data-ttu-id="6471d-107">Music Store MVC è un semplice esempio di implementazione di un archivio per un negozio che vende album musicali online e implementa l'amministrazione di base del sito, l'accesso dell'utente e la funzionalità del carrello acquisti.</span><span class="sxs-lookup"><span data-stu-id="6471d-107">The MVC Music Store is a lightweight sample store implementation which sells music albums online, and implements basic site administration, user sign-in, and shopping cart functionality.</span></span>
> 
> <span data-ttu-id="6471d-108">Questa serie di esercitazioni illustra tutti i passaggi necessari per compilare l'applicazione di esempio ASP.NET MVC Music Store.</span><span class="sxs-lookup"><span data-stu-id="6471d-108">This tutorial series details all of the steps taken to build the ASP.NET MVC Music Store sample application.</span></span> <span data-ttu-id="6471d-109">Nella parte 5 viene illustrata la modifica di moduli e modelli.</span><span class="sxs-lookup"><span data-stu-id="6471d-109">Part 5 covers Edit Forms and Templating.</span></span>

<span data-ttu-id="6471d-110">Nel capitolo precedente sono stati caricati dati dal database e visualizzati.</span><span class="sxs-lookup"><span data-stu-id="6471d-110">In the past chapter, we were loading data from our database and displaying it.</span></span> <span data-ttu-id="6471d-111">In questo capitolo verrà anche abilitata la modifica dei dati.</span><span class="sxs-lookup"><span data-stu-id="6471d-111">In this chapter, we'll also enable editing the data.</span></span>

## <a name="creating-the-storemanagercontroller"></a><span data-ttu-id="6471d-112">Creazione di StoreManagerController</span><span class="sxs-lookup"><span data-stu-id="6471d-112">Creating the StoreManagerController</span></span>

<span data-ttu-id="6471d-113">Si inizierà con la creazione di un nuovo controller denominato **StoreManagerController**.</span><span class="sxs-lookup"><span data-stu-id="6471d-113">We'll begin by creating a new controller called **StoreManagerController**.</span></span> <span data-ttu-id="6471d-114">Per questo controller, verranno sfruttate le funzionalità di ponteggi disponibili nell'aggiornamento degli strumenti di ASP.NET MVC 3.</span><span class="sxs-lookup"><span data-stu-id="6471d-114">For this controller, we will be taking advantage of the Scaffolding features available in the ASP.NET MVC 3 Tools Update.</span></span> <span data-ttu-id="6471d-115">Impostare le opzioni per la finestra di dialogo Aggiungi controller, come illustrato di seguito.</span><span class="sxs-lookup"><span data-stu-id="6471d-115">Set the options for the Add Controller dialog as shown below.</span></span>

![](mvc-music-store-part-5/_static/image1.png)

<span data-ttu-id="6471d-116">Quando si fa clic sul pulsante Aggiungi, si noterà che il meccanismo di ponteggi ASP.NET MVC 3 esegue una notevole quantità di lavoro:</span><span class="sxs-lookup"><span data-stu-id="6471d-116">When you click the Add button, you'll see that the ASP.NET MVC 3 scaffolding mechanism does a good amount of work for you:</span></span>

- <span data-ttu-id="6471d-117">Crea il nuovo StoreManagerController con una variabile di Entity Framework locale</span><span class="sxs-lookup"><span data-stu-id="6471d-117">It creates the new StoreManagerController with a local Entity Framework variable</span></span>
- <span data-ttu-id="6471d-118">Aggiunge una cartella StoreManager alla cartella Views del progetto</span><span class="sxs-lookup"><span data-stu-id="6471d-118">It adds a StoreManager folder to the project's Views folder</span></span>
- <span data-ttu-id="6471d-119">Aggiunge la vista Create. cshtml, DELETE. cshtml, details. cshtml, Edit. cshtml e index. cshtml, fortemente tipizzata con la classe album</span><span class="sxs-lookup"><span data-stu-id="6471d-119">It adds Create.cshtml, Delete.cshtml, Details.cshtml, Edit.cshtml, and Index.cshtml view, strongly typed to the Album class</span></span>

![](mvc-music-store-part-5/_static/image2.png)

<span data-ttu-id="6471d-120">La nuova classe controller StoreManager include le azioni del controller CRUD (creazione, lettura, aggiornamento, eliminazione) che sanno come usare la classe del modello album e usano il contesto Entity Framework per l'accesso al database.</span><span class="sxs-lookup"><span data-stu-id="6471d-120">The new StoreManager controller class includes CRUD (create, read, update, delete) controller actions which know how to work with the Album model class and use our Entity Framework context for database access.</span></span>

## <a name="modifying-a-scaffolded-view"></a><span data-ttu-id="6471d-121">Modifica di una vista con impalcatura</span><span class="sxs-lookup"><span data-stu-id="6471d-121">Modifying a Scaffolded View</span></span>

<span data-ttu-id="6471d-122">È importante ricordare che, mentre questo codice è stato generato per noi, è il codice MVC ASP.NET standard, proprio come abbiamo scritto in questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-122">It's important to remember that, while this code was generated for us, it's standard ASP.NET MVC code, just like we've been writing throughout this tutorial.</span></span> <span data-ttu-id="6471d-123">Il suo scopo è quello di risparmiare tempo dedicato alla scrittura di codice del controller standard e alla creazione manuale delle visualizzazioni fortemente tipizzate, ma questo non è il tipo di codice generato, che potrebbe essere stato visualizzato preceduto da avvisi terribili nei commenti su come non modificare il codice.</span><span class="sxs-lookup"><span data-stu-id="6471d-123">It's intended to save you the time you'd spend on writing boilerplate controller code and creating the strongly typed views manually, but this isn't the kind of generated code you may have seen prefaced with dire warnings in comments about how you mustn't change the code.</span></span> <span data-ttu-id="6471d-124">Si tratta del codice e si prevede di modificarlo.</span><span class="sxs-lookup"><span data-stu-id="6471d-124">This is your code, and you're expected to change it.</span></span>

<span data-ttu-id="6471d-125">Si inizia quindi con una modifica rapida alla visualizzazione dell'indice StoreManager (/Views/StoreManager/Index.cshtml).</span><span class="sxs-lookup"><span data-stu-id="6471d-125">So, let's start with a quick edit to the StoreManager Index view (/Views/StoreManager/Index.cshtml).</span></span> <span data-ttu-id="6471d-126">Questa vista consente di visualizzare una tabella che elenca gli album presenti nell'archivio con i collegamenti modifica/dettagli/Elimina e include le proprietà pubbliche dell'album.</span><span class="sxs-lookup"><span data-stu-id="6471d-126">This view will display a table which lists the Albums in our store with Edit / Details / Delete links, and includes the Album's public properties.</span></span> <span data-ttu-id="6471d-127">Il campo AlbumArtUrl verrà rimosso perché non è molto utile in questa visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-127">We'll remove the AlbumArtUrl field, as it's not very useful in this display.</span></span> <span data-ttu-id="6471d-128">Nella sezione &lt;tabella&gt; del codice di visualizzazione rimuovere gli elementi &lt;TH&gt; e &lt;TD&gt; che racchiudono i riferimenti a AlbumArtUrl, come indicato dalle righe evidenziate di seguito:</span><span class="sxs-lookup"><span data-stu-id="6471d-128">In &lt;table&gt; section of the view code, remove the &lt;th&gt; and &lt;td&gt; elements surrounding AlbumArtUrl references, as indicated by the highlighted lines below:</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample1.cshtml)]

<span data-ttu-id="6471d-129">Il codice di visualizzazione modificato verrà visualizzato come segue:</span><span class="sxs-lookup"><span data-stu-id="6471d-129">The modified view code will appear as follows:</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample2.cshtml)]

## <a name="a-first-look-at-the-store-manager"></a><span data-ttu-id="6471d-130">Una prima occhiata al gestore di Store</span><span class="sxs-lookup"><span data-stu-id="6471d-130">A first look at the Store Manager</span></span>

<span data-ttu-id="6471d-131">A questo punto, eseguire l'applicazione e passare a/StoreManager/.</span><span class="sxs-lookup"><span data-stu-id="6471d-131">Now run the application and browse to /StoreManager/.</span></span> <span data-ttu-id="6471d-132">Viene visualizzato l'indice di gestione archivi appena modificato, che mostra un elenco degli album nello Store con i collegamenti da modificare, dettagli ed eliminare.</span><span class="sxs-lookup"><span data-stu-id="6471d-132">This displays the Store Manager Index we just modified, showing a list of the albums in the store with links to Edit, Details, and Delete.</span></span>

![](mvc-music-store-part-5/_static/image3.png)

<span data-ttu-id="6471d-133">Facendo clic sul collegamento modifica viene visualizzato un modulo di modifica con i campi per l'album, inclusi i menu a discesa per genere e artista.</span><span class="sxs-lookup"><span data-stu-id="6471d-133">Clicking the Edit link displays an edit form with fields for the Album, including dropdowns for Genre and Artist.</span></span>

![](mvc-music-store-part-5/_static/image4.png)

<span data-ttu-id="6471d-134">Fare clic sul collegamento "Torna all'elenco" nella parte inferiore, quindi fare clic sul collegamento Dettagli per un album.</span><span class="sxs-lookup"><span data-stu-id="6471d-134">Click the "Back to List" link at the bottom, then click on the Details link for an Album.</span></span> <span data-ttu-id="6471d-135">Verranno visualizzate le informazioni dettagliate per un singolo album.</span><span class="sxs-lookup"><span data-stu-id="6471d-135">This displays the detail information for an individual Album.</span></span>

![](mvc-music-store-part-5/_static/image5.png)

<span data-ttu-id="6471d-136">Fare nuovamente clic sul collegamento Torna a elenco, quindi fare clic su un collegamento Elimina.</span><span class="sxs-lookup"><span data-stu-id="6471d-136">Again, click the Back to List link, then click on a Delete link.</span></span> <span data-ttu-id="6471d-137">Viene visualizzata una finestra di dialogo di conferma che mostra i dettagli dell'album e chiede se si è certi di voler eliminarla.</span><span class="sxs-lookup"><span data-stu-id="6471d-137">This displays a confirmation dialog, showing the album details and asking if we're sure we want to delete it.</span></span>

![](mvc-music-store-part-5/_static/image6.png)

<span data-ttu-id="6471d-138">Facendo clic sul pulsante Elimina nella parte inferiore, l'album verrà eliminato e tornerà alla pagina di indice, che mostra l'album eliminato.</span><span class="sxs-lookup"><span data-stu-id="6471d-138">Clicking the Delete button at the bottom will delete the album and return you to the Index page, which shows the album deleted.</span></span>

<span data-ttu-id="6471d-139">Il gestore dello Store non è stato eseguito, ma è presente un controller funzionante e il codice di visualizzazione per le operazioni CRUD da cui iniziare.</span><span class="sxs-lookup"><span data-stu-id="6471d-139">We're not done with the Store Manager, but we have working controller and view code for the CRUD operations to start from.</span></span>

## <a name="looking-at-the-store-manager-controller-code"></a><span data-ttu-id="6471d-140">Esame del codice del controller di Store Manager</span><span class="sxs-lookup"><span data-stu-id="6471d-140">Looking at the Store Manager Controller code</span></span>

<span data-ttu-id="6471d-141">Il controller di gestione archivi contiene una quantità di codice adeguata.</span><span class="sxs-lookup"><span data-stu-id="6471d-141">The Store Manager Controller contains a good amount of code.</span></span> <span data-ttu-id="6471d-142">Passiamo dall'alto verso il basso.</span><span class="sxs-lookup"><span data-stu-id="6471d-142">Let's go through this from top to bottom.</span></span> <span data-ttu-id="6471d-143">Il controller include alcuni spazi dei nomi standard per un controller MVC, oltre a un riferimento allo spazio dei nomi Models.</span><span class="sxs-lookup"><span data-stu-id="6471d-143">The controller includes some standard namespaces for an MVC controller, as well as a reference to our Models namespace.</span></span> <span data-ttu-id="6471d-144">Il controller dispone di un'istanza privata di MusicStoreEntities, usata da ogni azione del controller per l'accesso ai dati.</span><span class="sxs-lookup"><span data-stu-id="6471d-144">The controller has a private instance of MusicStoreEntities, used by each of the controller actions for data access.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample3.cs)]

### <a name="store-manager-index-and-details-actions"></a><span data-ttu-id="6471d-145">Azioni di indice e dettagli di gestione archivi</span><span class="sxs-lookup"><span data-stu-id="6471d-145">Store Manager Index and Details actions</span></span>

<span data-ttu-id="6471d-146">La visualizzazione index recupera un elenco di album, incluse le informazioni sul genere e sull'artista a cui viene fatto riferimento di ogni album, come illustrato in precedenza quando si utilizza il metodo browse Store.</span><span class="sxs-lookup"><span data-stu-id="6471d-146">The index view retrieves a list of Albums, including each album's referenced Genre and Artist information, as we previously saw when working on the Store Browse method.</span></span> <span data-ttu-id="6471d-147">La vista index segue i riferimenti agli oggetti collegati, in modo che sia possibile visualizzare il nome del genere e il nome dell'artista di ogni album, in modo che il controller sia efficiente ed esegue una query per queste informazioni nella richiesta originale.</span><span class="sxs-lookup"><span data-stu-id="6471d-147">The Index view is following the references to the linked objects so that it can display each album's Genre name and Artist name, so the controller is being efficient and querying for this information in the original request.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample4.cs)]

<span data-ttu-id="6471d-148">L'azione del controller dei dettagli del controller StoreManager funziona esattamente come l'azione dettagli controller del negozio che è stata scritta in precedenza. esegue una query per l'album per ID usando il metodo Find () e quindi lo restituisce alla visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-148">The StoreManager Controller's Details controller action works exactly the same as the Store Controller Details action we wrote previously - it queries for the Album by ID using the Find() method, then returns it to the view.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample5.cs)]

### <a name="the-create-action-methods"></a><span data-ttu-id="6471d-149">Metodi di azione di creazione</span><span class="sxs-lookup"><span data-stu-id="6471d-149">The Create Action Methods</span></span>

<span data-ttu-id="6471d-150">I metodi di azione di creazione sono leggermente diversi da quelli che abbiamo visto finora, perché gestiscono l'input del modulo.</span><span class="sxs-lookup"><span data-stu-id="6471d-150">The Create action methods are a little different from ones we've seen so far, because they handle form input.</span></span> <span data-ttu-id="6471d-151">Quando un utente visita per la prima volta/StoreManager/Create/, viene visualizzato un modulo vuoto.</span><span class="sxs-lookup"><span data-stu-id="6471d-151">When a user first visits /StoreManager/Create/ they will be shown an empty form.</span></span> <span data-ttu-id="6471d-152">Questa pagina HTML conterrà un &lt;form&gt; elemento che contiene gli elementi di input elenco a discesa e casella di testo in cui possono immettere i dettagli dell'album.</span><span class="sxs-lookup"><span data-stu-id="6471d-152">This HTML page will contain a &lt;form&gt; element that contains dropdown and textbox input elements where they can enter the album's details.</span></span>

<span data-ttu-id="6471d-153">Dopo che l'utente ha compilato i valori dei moduli di album, può premere il pulsante "Salva" per inviare di nuovo le modifiche all'applicazione per salvare il database.</span><span class="sxs-lookup"><span data-stu-id="6471d-153">After the user fills in the Album form values, they can press the "Save" button to submit these changes back to our application to save within the database.</span></span> <span data-ttu-id="6471d-154">Quando l'utente preme il pulsante "Save" (Salva) il modulo &lt;&gt; eseguirà un postback HTTP all'URL/StoreManager/Create/e invierà i valori&gt; &lt;form come parte di HTTP-POST.</span><span class="sxs-lookup"><span data-stu-id="6471d-154">When the user presses the "save" button the &lt;form&gt; will perform an HTTP-POST back to the /StoreManager/Create/ URL and submit the &lt;form&gt; values as part of the HTTP-POST.</span></span>

<span data-ttu-id="6471d-155">ASP.NET MVC consente di suddividere facilmente la logica di questi due scenari di chiamata URL consentendo di implementare due metodi di azione di "creazione" distinti all'interno della classe StoreManagerController, uno per gestire la ricerca HTTP-GET iniziale all'URL/StoreManager/Create/e l'altro per gestire il POST HTTP delle modifiche inviate.</span><span class="sxs-lookup"><span data-stu-id="6471d-155">ASP.NET MVC allows us to easily split up the logic of these two URL invocation scenarios by enabling us to implement two separate "Create" action methods within our StoreManagerController class – one to handle the initial HTTP-GET browse to the /StoreManager/Create/ URL, and the other to handle the HTTP-POST of the submitted changes.</span></span>

### <a name="passing-information-to-a-view-using-viewbag"></a><span data-ttu-id="6471d-156">Passaggio di informazioni a una visualizzazione mediante ViewBag</span><span class="sxs-lookup"><span data-stu-id="6471d-156">Passing information to a View using ViewBag</span></span>

<span data-ttu-id="6471d-157">Il ViewBag è stato usato in precedenza in questa esercitazione, ma non è stato parlato.</span><span class="sxs-lookup"><span data-stu-id="6471d-157">We've used the ViewBag earlier in this tutorial, but haven't talked much about it.</span></span> <span data-ttu-id="6471d-158">ViewBag consente di passare informazioni alla visualizzazione senza usare un oggetto modello fortemente tipizzato.</span><span class="sxs-lookup"><span data-stu-id="6471d-158">The ViewBag allows us to pass information to the view without using a strongly typed model object.</span></span> <span data-ttu-id="6471d-159">In questo caso, l'azione Edit HTTP-GET controller deve passare un elenco di generi e artisti al modulo per popolare gli elenchi a discesa e il modo più semplice per farlo consiste nel restituirli come elementi ViewBag.</span><span class="sxs-lookup"><span data-stu-id="6471d-159">In this case, our Edit HTTP-GET controller action needs to pass both a list of Genres and Artists to the form to populate the dropdowns, and the simplest way to do that is to return them as ViewBag items.</span></span>

<span data-ttu-id="6471d-160">ViewBag è un oggetto dinamico, ovvero è possibile digitare ViewBag. foo o ViewBag. YourNameHere senza scrivere codice per definire tali proprietà.</span><span class="sxs-lookup"><span data-stu-id="6471d-160">The ViewBag is a dynamic object, meaning that you can type ViewBag.Foo or ViewBag.YourNameHere without writing code to define those properties.</span></span> <span data-ttu-id="6471d-161">In questo caso, il codice del controller usa ViewBag. GenreId e ViewBag. ArtistId in modo che i valori a discesa inviati con il modulo saranno GenreId e ArtistId, ovvero le proprietà degli album che verranno impostate.</span><span class="sxs-lookup"><span data-stu-id="6471d-161">In this case, the controller code uses ViewBag.GenreId and ViewBag.ArtistId so that the dropdown values submitted with the form will be GenreId and ArtistId, which are the Album properties they will be setting.</span></span>

<span data-ttu-id="6471d-162">Questi valori a discesa vengono restituiti al form utilizzando l'oggetto Select, compilato solo a tale scopo.</span><span class="sxs-lookup"><span data-stu-id="6471d-162">These dropdown values are returned to the form using the SelectList object, which is built just for that purpose.</span></span> <span data-ttu-id="6471d-163">Questa operazione viene eseguita usando un codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="6471d-163">This is done using code like this:</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample6.cs)]

<span data-ttu-id="6471d-164">Come si può notare dal codice del metodo di azione, vengono usati tre parametri per creare questo oggetto:</span><span class="sxs-lookup"><span data-stu-id="6471d-164">As you can see from the action method code, three parameters are being used to create this object:</span></span>

- <span data-ttu-id="6471d-165">Elenco di elementi che verranno visualizzati nell'elenco a discesa.</span><span class="sxs-lookup"><span data-stu-id="6471d-165">The list of items the dropdown will be displaying.</span></span> <span data-ttu-id="6471d-166">Si noti che non si tratta semplicemente di una stringa. si passa un elenco di generi.</span><span class="sxs-lookup"><span data-stu-id="6471d-166">Note that this isn't just a string - we're passing a list of Genres.</span></span>
- <span data-ttu-id="6471d-167">Il parametro successivo passato all'oggetto Select è il valore selezionato.</span><span class="sxs-lookup"><span data-stu-id="6471d-167">The next parameter being passed to the SelectList is the Selected Value.</span></span> <span data-ttu-id="6471d-168">Questo è il modo in cui l'elenco di selezione sa come preselezionare un elemento nell'elenco.</span><span class="sxs-lookup"><span data-stu-id="6471d-168">This how the SelectList knows how to pre-select an item in the list.</span></span> <span data-ttu-id="6471d-169">Questa operazione sarà più semplice da comprendere quando si esamina il modulo di modifica, che è molto simile.</span><span class="sxs-lookup"><span data-stu-id="6471d-169">This will be easier to understand when we look at the Edit form, which is pretty similar.</span></span>
- <span data-ttu-id="6471d-170">Il parametro finale è la proprietà da visualizzare.</span><span class="sxs-lookup"><span data-stu-id="6471d-170">The final parameter is the property to be displayed.</span></span> <span data-ttu-id="6471d-171">In questo caso, indica che la proprietà Genre.Name è quella che verrà visualizzata all'utente.</span><span class="sxs-lookup"><span data-stu-id="6471d-171">In this case, this is indicating that the Genre.Name property is what will be shown to the user.</span></span>

<span data-ttu-id="6471d-172">Tenendo presente questo aspetto, l'azione di creazione HTTP-GET è piuttosto semplice: due SelectLists vengono aggiunti a ViewBag e nessun oggetto modello viene passato al form (poiché non è stato ancora creato).</span><span class="sxs-lookup"><span data-stu-id="6471d-172">With that in mind, then, the HTTP-GET Create action is pretty simple - two SelectLists are added to the ViewBag, and no model object is passed to the form (since it hasn't been created yet).</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample7.cs)]

### <a name="html-helpers-to-display-the-drop-downs-in-the-create-view"></a><span data-ttu-id="6471d-173">Helper HTML per visualizzare gli elenchi a discesa nella visualizzazione crea</span><span class="sxs-lookup"><span data-stu-id="6471d-173">HTML Helpers to display the Drop Downs in the Create View</span></span>

<span data-ttu-id="6471d-174">Poiché si è parlato di come i valori a discesa vengono passati alla visualizzazione, esaminiamo brevemente la visualizzazione per visualizzare il modo in cui tali valori vengono visualizzati.</span><span class="sxs-lookup"><span data-stu-id="6471d-174">Since we've talked about how the drop down values are passed to the view, let's take a quick look at the view to see how those values are displayed.</span></span> <span data-ttu-id="6471d-175">Nel codice di visualizzazione (/Views/StoreManager/Create.cshtml) verrà visualizzata la chiamata seguente per visualizzare l'elenco a discesa genere.</span><span class="sxs-lookup"><span data-stu-id="6471d-175">In the view code (/Views/StoreManager/Create.cshtml), you'll see the following call is made to display the Genre drop down.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample8.cshtml)]

<span data-ttu-id="6471d-176">Si tratta di un metodo di utilità (Helper) HTML che esegue un'attività di visualizzazione comune.</span><span class="sxs-lookup"><span data-stu-id="6471d-176">This is known as an HTML Helper - a utility method which performs a common view task.</span></span> <span data-ttu-id="6471d-177">Gli helper HTML sono molto utili per mantenere conciso e leggibile il codice di visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-177">HTML Helpers are very useful in keeping our view code concise and readable.</span></span> <span data-ttu-id="6471d-178">L'helper HTML. DropDownList viene fornito da ASP.NET MVC, ma come vedremo in seguito, è possibile creare i propri helper per visualizzare il codice che verrà riutilizzato nell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-178">The Html.DropDownList helper is provided by ASP.NET MVC, but as we'll see later it's possible to create our own helpers for view code we'll reuse in our application.</span></span>

<span data-ttu-id="6471d-179">Alla chiamata HTML. DropDownList è sufficiente indicare due cose: dove ottenere l'elenco da visualizzare e il valore, se presente, che deve essere preselezionato.</span><span class="sxs-lookup"><span data-stu-id="6471d-179">The Html.DropDownList call just needs to be told two things - where to get the list to display, and what value (if any) should be pre-selected.</span></span> <span data-ttu-id="6471d-180">Il primo parametro, GenreId, indica al DropDownList di cercare un valore denominato GenreId nel modello o ViewBag.</span><span class="sxs-lookup"><span data-stu-id="6471d-180">The first parameter, GenreId, tells the DropDownList to look for a value named GenreId in either the model or ViewBag.</span></span> <span data-ttu-id="6471d-181">Il secondo parametro viene usato per indicare il valore da mostrare come inizialmente selezionato nell'elenco a discesa.</span><span class="sxs-lookup"><span data-stu-id="6471d-181">The second parameter is used to indicate the value to show as initially selected in the drop down list.</span></span> <span data-ttu-id="6471d-182">Poiché questo modulo è un modulo di creazione, non esiste alcun valore da preselezionare e viene passato String. Empty.</span><span class="sxs-lookup"><span data-stu-id="6471d-182">Since this form is a Create form, there's no value to be preselected and String.Empty is passed.</span></span>

### <a name="handling-the-posted-form-values"></a><span data-ttu-id="6471d-183">Gestione dei valori del modulo inviato</span><span class="sxs-lookup"><span data-stu-id="6471d-183">Handling the Posted Form values</span></span>

<span data-ttu-id="6471d-184">Come illustrato in precedenza, sono disponibili due metodi di azione associati a ogni form.</span><span class="sxs-lookup"><span data-stu-id="6471d-184">As we discussed before, there are two action methods associated with each form.</span></span> <span data-ttu-id="6471d-185">Il primo gestisce la richiesta HTTP-GET e visualizza il form.</span><span class="sxs-lookup"><span data-stu-id="6471d-185">The first handles the HTTP-GET request and displays the form.</span></span> <span data-ttu-id="6471d-186">Il secondo gestisce la richiesta HTTP-POST, che contiene i valori del modulo inviati.</span><span class="sxs-lookup"><span data-stu-id="6471d-186">The second handles the HTTP-POST request, which contains the submitted form values.</span></span> <span data-ttu-id="6471d-187">Si noti che l'azione del controller ha un attributo [HttpPost], che indica a ASP.NET MVC che deve rispondere solo alle richieste HTTP-POST.</span><span class="sxs-lookup"><span data-stu-id="6471d-187">Notice that controller action has an [HttpPost] attribute, which tells ASP.NET MVC that it should only respond to HTTP-POST requests.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample9.cs)]

<span data-ttu-id="6471d-188">Questa azione ha quattro responsabilità:</span><span class="sxs-lookup"><span data-stu-id="6471d-188">This action has four responsibilities:</span></span>

- 1. <span data-ttu-id="6471d-189">Leggere i valori del modulo</span><span class="sxs-lookup"><span data-stu-id="6471d-189">Read the form values</span></span>
- 2. <span data-ttu-id="6471d-190">Controllare se i valori del modulo superano tutte le regole di convalida</span><span class="sxs-lookup"><span data-stu-id="6471d-190">Check if the form values pass any validation rules</span></span>
- 3. <span data-ttu-id="6471d-191">Se l'invio del modulo è valido, salvare i dati e visualizzare l'elenco aggiornato</span><span class="sxs-lookup"><span data-stu-id="6471d-191">If the form submission is valid, save the data and display the updated list</span></span>
- 4. <span data-ttu-id="6471d-192">Se l'invio del modulo non è valido, visualizzare nuovamente il modulo con errori di convalida</span><span class="sxs-lookup"><span data-stu-id="6471d-192">If the form submission is not valid, redisplay the form with validation errors</span></span>

#### <a name="reading-form-values-with-model-binding"></a><span data-ttu-id="6471d-193">Lettura di valori di form con associazione di modelli</span><span class="sxs-lookup"><span data-stu-id="6471d-193">Reading Form Values with Model Binding</span></span>

<span data-ttu-id="6471d-194">L'azione del controller sta elaborando un invio del modulo che include i valori per GenreId e ArtistId (dall'elenco a discesa) e i valori della casella di testo per title, Price e AlbumArtUrl.</span><span class="sxs-lookup"><span data-stu-id="6471d-194">The controller action is processing a form submission that includes values for GenreId and ArtistId (from the drop down list) and textbox values for Title, Price, and AlbumArtUrl.</span></span> <span data-ttu-id="6471d-195">Sebbene sia possibile accedere direttamente ai valori dei moduli, un approccio migliore consiste nell'usare le funzionalità di associazione di modelli incorporate in ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="6471d-195">While it's possible to directly access form values, a better approach is to use the Model Binding capabilities built into ASP.NET MVC.</span></span> <span data-ttu-id="6471d-196">Quando un'azione del controller accetta un tipo di modello come parametro, ASP.NET MVC tenterà di popolare un oggetto di quel tipo usando gli input del form (nonché i valori di route e QueryString).</span><span class="sxs-lookup"><span data-stu-id="6471d-196">When a controller action takes a model type as a parameter, ASP.NET MVC will attempt to populate an object of that type using form inputs (as well as route and querystring values).</span></span> <span data-ttu-id="6471d-197">Questa operazione viene eseguita cercando valori i cui nomi corrispondono alle proprietà dell'oggetto modello, ad esempio quando si imposta il valore GenreId dell'oggetto del nuovo album, Cerca un input con il nome GenreId.</span><span class="sxs-lookup"><span data-stu-id="6471d-197">It does this by looking for values whose names match properties of the model object, e.g. when setting the new Album object's GenreId value, it looks for an input with the name GenreId.</span></span> <span data-ttu-id="6471d-198">Quando si creano viste usando i metodi standard in ASP.NET MVC, i moduli verranno sempre sottoposti a rendering usando i nomi di proprietà come nomi dei campi di input, quindi i nomi dei campi corrisponderanno.</span><span class="sxs-lookup"><span data-stu-id="6471d-198">When you create views using the standard methods in ASP.NET MVC, the forms will always be rendered using property names as input field names, so this the field names will just match up.</span></span>

#### <a name="validating-the-model"></a><span data-ttu-id="6471d-199">Convalida del modello</span><span class="sxs-lookup"><span data-stu-id="6471d-199">Validating the Model</span></span>

<span data-ttu-id="6471d-200">Il modello viene convalidato con una semplice chiamata a ModelState. IsValid.</span><span class="sxs-lookup"><span data-stu-id="6471d-200">The model is validated with a simple call to ModelState.IsValid.</span></span> <span data-ttu-id="6471d-201">Non è stata ancora aggiunta alcuna regola di convalida alla classe album. questa operazione verrà eseguita in un po', quindi il controllo non ha molto da fare.</span><span class="sxs-lookup"><span data-stu-id="6471d-201">We haven't added any validation rules to our Album class yet - we'll do that in a bit - so right now this check doesn't have much to do.</span></span> <span data-ttu-id="6471d-202">Ciò che è importante è che il controllo ModelStat. IsValid verrà adattato alle regole di convalida inserite sul modello, quindi le modifiche future alle regole di convalida non richiederanno aggiornamenti per il codice dell'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="6471d-202">What's important is that this ModelStat.IsValid check will adapt to the validation rules we put on our model, so future changes to validation rules won't require any updates to the controller action code.</span></span>

#### <a name="saving-the-submitted-values"></a><span data-ttu-id="6471d-203">Salvataggio dei valori inviati</span><span class="sxs-lookup"><span data-stu-id="6471d-203">Saving the submitted values</span></span>

<span data-ttu-id="6471d-204">Se l'invio del form supera la convalida, è il momento di salvare i valori nel database.</span><span class="sxs-lookup"><span data-stu-id="6471d-204">If the form submission passes validation, it's time to save the values to the database.</span></span> <span data-ttu-id="6471d-205">Con Entity Framework, che richiede solo l'aggiunta del modello alla raccolta album e la chiamata a SaveChanges.</span><span class="sxs-lookup"><span data-stu-id="6471d-205">With Entity Framework, that just requires adding the model to the Albums collection and calling SaveChanges.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample10.cs)]

<span data-ttu-id="6471d-206">Entity Framework genera i comandi SQL appropriati per salvare in modo permanente il valore.</span><span class="sxs-lookup"><span data-stu-id="6471d-206">Entity Framework generates the appropriate SQL commands to persist the value.</span></span> <span data-ttu-id="6471d-207">Dopo aver salvato i dati, viene reindirizzato di nuovo all'elenco di album per poter visualizzare l'aggiornamento.</span><span class="sxs-lookup"><span data-stu-id="6471d-207">After saving the data, we redirect back to the list of Albums so we can see our update.</span></span> <span data-ttu-id="6471d-208">Questa operazione viene eseguita restituendo RedirectToAction con il nome dell'azione del controller che si desidera visualizzare.</span><span class="sxs-lookup"><span data-stu-id="6471d-208">This is done by returning RedirectToAction with the name of the controller action we want displayed.</span></span> <span data-ttu-id="6471d-209">In questo caso, si tratta del metodo index.</span><span class="sxs-lookup"><span data-stu-id="6471d-209">In this case, that's the Index method.</span></span>

#### <a name="displaying-invalid-form-submissions-with-validation-errors"></a><span data-ttu-id="6471d-210">Visualizzazione di invii di form non validi con errori di convalida</span><span class="sxs-lookup"><span data-stu-id="6471d-210">Displaying invalid form submissions with Validation Errors</span></span>

<span data-ttu-id="6471d-211">Nel caso di input del form non valido, i valori a discesa vengono aggiunti a ViewBag (come nel caso di HTTP-GET) e i valori del modello associato vengono passati di nuovo alla visualizzazione per la visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-211">In the case of invalid form input, the dropdown values are added to the ViewBag (as in the HTTP-GET case) and the bound model values are passed back to the view for display.</span></span> <span data-ttu-id="6471d-212">Gli errori di convalida vengono visualizzati automaticamente utilizzando l'helper HTML @Html.ValidationMessageFor.</span><span class="sxs-lookup"><span data-stu-id="6471d-212">Validation errors are automatically displayed using the @Html.ValidationMessageFor HTML Helper.</span></span>

#### <a name="testing-the-create-form"></a><span data-ttu-id="6471d-213">Test del modulo di creazione</span><span class="sxs-lookup"><span data-stu-id="6471d-213">Testing the Create Form</span></span>

<span data-ttu-id="6471d-214">Per testarlo, eseguire l'applicazione e passare a/StoreManager/Create/. verrà visualizzato il formato vuoto restituito dal metodo StoreController create HTTP-GET.</span><span class="sxs-lookup"><span data-stu-id="6471d-214">To test this out, run the application and browse to /StoreManager/Create/ - this will show you the blank form which was returned by the StoreController Create HTTP-GET method.</span></span>

<span data-ttu-id="6471d-215">Inserire alcuni valori, quindi fare clic sul pulsante Crea per inviare il modulo.</span><span class="sxs-lookup"><span data-stu-id="6471d-215">Fill in some values and click the Create button to submit the form.</span></span>

![](mvc-music-store-part-5/_static/image7.png)

![](mvc-music-store-part-5/_static/image8.png)

### <a name="handling-edits"></a><span data-ttu-id="6471d-216">Gestione delle modifiche</span><span class="sxs-lookup"><span data-stu-id="6471d-216">Handling Edits</span></span>

<span data-ttu-id="6471d-217">La coppia modifica azione (HTTP-GET e HTTP-POST) è molto simile ai metodi di azione di creazione appena esaminati.</span><span class="sxs-lookup"><span data-stu-id="6471d-217">The Edit action pair (HTTP-GET and HTTP-POST) are very similar to the Create action methods we just looked at.</span></span> <span data-ttu-id="6471d-218">Poiché lo scenario di modifica prevede l'uso di un album esistente, il metodo Edit HTTP-GET carica l'album in base al parametro "ID", passato tramite la route.</span><span class="sxs-lookup"><span data-stu-id="6471d-218">Since the edit scenario involves working with an existing album, the Edit HTTP-GET method loads the Album based on the "id" parameter, passed in via the route.</span></span> <span data-ttu-id="6471d-219">Questo codice per il recupero di un album da AlbumId è identico a quello osservato in precedenza nell'azione del controller details.</span><span class="sxs-lookup"><span data-stu-id="6471d-219">This code for retrieving an album by AlbumId is the same as we've previously looked at in the Details controller action.</span></span> <span data-ttu-id="6471d-220">Come per il metodo Create/HTTP-GET, i valori a discesa vengono restituiti tramite il ViewBag.</span><span class="sxs-lookup"><span data-stu-id="6471d-220">As with the Create / HTTP-GET method, the drop down values are returned via the ViewBag.</span></span> <span data-ttu-id="6471d-221">In questo modo è possibile restituire un album come oggetto modello alla vista (fortemente tipizzata per la classe album), passando i dati aggiuntivi, ad esempio un elenco di generi, tramite il ViewBag.</span><span class="sxs-lookup"><span data-stu-id="6471d-221">This allows us to return an Album as our model object to the view (which is strongly typed to the Album class) while passing additional data (e.g. a list of Genres) via the ViewBag.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample11.cs)]

<span data-ttu-id="6471d-222">L'azione modifica HTTP-POST è molto simile all'azione crea HTTP-POST.</span><span class="sxs-lookup"><span data-stu-id="6471d-222">The Edit HTTP-POST action is very similar to the Create HTTP-POST action.</span></span> <span data-ttu-id="6471d-223">L'unica differenza è che invece di aggiungere un nuovo album al database. Raccolta di album, viene rilevata l'istanza corrente dell'album usando DB. Voce (album) e impostazione dello stato su modificato.</span><span class="sxs-lookup"><span data-stu-id="6471d-223">The only difference is that instead of adding a new album to the db.Albums collection, we're finding the current instance of the Album using db.Entry(album) and setting its state to Modified.</span></span> <span data-ttu-id="6471d-224">Indica Entity Framework che si sta modificando un album esistente anziché crearne uno nuovo.</span><span class="sxs-lookup"><span data-stu-id="6471d-224">This tells Entity Framework that we are modifying an existing album as opposed to creating a new one.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample12.cs)]

<span data-ttu-id="6471d-225">Per eseguire il test, è possibile eseguire l'applicazione e passare a/StoreManger/e quindi fare clic sul collegamento Edit (modifica) per un album.</span><span class="sxs-lookup"><span data-stu-id="6471d-225">We can test this out by running the application and browsing to /StoreManger/, then clicking the Edit link for an album.</span></span>

![](mvc-music-store-part-5/_static/image9.png)

<span data-ttu-id="6471d-226">Viene visualizzato il modulo di modifica illustrato dal metodo Edit HTTP-GET.</span><span class="sxs-lookup"><span data-stu-id="6471d-226">This displays the Edit form shown by the Edit HTTP-GET method.</span></span> <span data-ttu-id="6471d-227">Inserire alcuni valori, quindi fare clic sul pulsante Salva.</span><span class="sxs-lookup"><span data-stu-id="6471d-227">Fill in some values and click the Save button.</span></span>

![](mvc-music-store-part-5/_static/image10.png)

<span data-ttu-id="6471d-228">Viene eseguito il post del modulo, vengono salvati i valori e viene restituito all'elenco degli album, mostrando che i valori sono stati aggiornati.</span><span class="sxs-lookup"><span data-stu-id="6471d-228">This posts the form, saves the values, and returns us to the Album list, showing that the values were updated.</span></span>

![](mvc-music-store-part-5/_static/image11.png)

### <a name="handling-deletion"></a><span data-ttu-id="6471d-229">Gestione dell'eliminazione</span><span class="sxs-lookup"><span data-stu-id="6471d-229">Handling Deletion</span></span>

<span data-ttu-id="6471d-230">L'eliminazione segue lo stesso modello di modifica e creazione, usando un'azione del controller per visualizzare il modulo di conferma e un'altra azione del controller per gestire l'invio del modulo.</span><span class="sxs-lookup"><span data-stu-id="6471d-230">Deletion follows the same pattern as Edit and Create, using one controller action to display the confirmation form, and another controller action to handle the form submission.</span></span>

<span data-ttu-id="6471d-231">L'azione HTTP-GET Delete Controller è esattamente identica alla precedente azione del controller dei dettagli di gestione archivi.</span><span class="sxs-lookup"><span data-stu-id="6471d-231">The HTTP-GET Delete controller action is exactly the same as our previous Store Manager Details controller action.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample13.cs)]

<span data-ttu-id="6471d-232">Viene visualizzato un modulo fortemente tipizzato in un tipo di album, usando il modello di contenuto della visualizzazione Delete.</span><span class="sxs-lookup"><span data-stu-id="6471d-232">We display a form that's strongly typed to an Album type, using the Delete view content template.</span></span>

![](mvc-music-store-part-5/_static/image12.png)

<span data-ttu-id="6471d-233">Il modello Delete Mostra tutti i campi del modello, ma è possibile semplificare l'attività.</span><span class="sxs-lookup"><span data-stu-id="6471d-233">The Delete template shows all the fields for the model, but we can simplify that down quite a bit.</span></span> <span data-ttu-id="6471d-234">Modificare il codice di visualizzazione in/Views/StoreManager/Delete.cshtml nel modo seguente.</span><span class="sxs-lookup"><span data-stu-id="6471d-234">Change the view code in /Views/StoreManager/Delete.cshtml to the following.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample14.cshtml)]

<span data-ttu-id="6471d-235">Viene visualizzata una conferma di eliminazione semplificata.</span><span class="sxs-lookup"><span data-stu-id="6471d-235">This displays a simplified Delete confirmation.</span></span>

![](mvc-music-store-part-5/_static/image13.png)

<span data-ttu-id="6471d-236">Se si fa clic sul pulsante Elimina, viene eseguito il postback del form al server, che esegue l'azione DeleteConfirmed.</span><span class="sxs-lookup"><span data-stu-id="6471d-236">Clicking the Delete button causes the form to be posted back to the server, which executes the DeleteConfirmed action.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample15.cs)]

<span data-ttu-id="6471d-237">L'azione HTTP-POST Delete controller esegue le azioni seguenti:</span><span class="sxs-lookup"><span data-stu-id="6471d-237">Our HTTP-POST Delete Controller Action takes the following actions:</span></span>

- 1. <span data-ttu-id="6471d-238">Carica l'album in base all'ID</span><span class="sxs-lookup"><span data-stu-id="6471d-238">Loads the Album by ID</span></span>
- 2. <span data-ttu-id="6471d-239">Elimina l'album e salva le modifiche</span><span class="sxs-lookup"><span data-stu-id="6471d-239">Deletes it the album and save changes</span></span>
- 3. <span data-ttu-id="6471d-240">Reindirizza all'indice, mostrando che l'album è stato rimosso dall'elenco</span><span class="sxs-lookup"><span data-stu-id="6471d-240">Redirects to the Index, showing that the Album was removed from the list</span></span>

<span data-ttu-id="6471d-241">Per testare questo problema, eseguire l'applicazione e passare a/StoreManager.</span><span class="sxs-lookup"><span data-stu-id="6471d-241">To test this, run the application and browse to /StoreManager.</span></span> <span data-ttu-id="6471d-242">Selezionare un album dall'elenco e fare clic sul collegamento Elimina.</span><span class="sxs-lookup"><span data-stu-id="6471d-242">Select an album from the list and click the Delete link.</span></span>

![](mvc-music-store-part-5/_static/image14.png)

<span data-ttu-id="6471d-243">Verrà visualizzata la schermata di conferma dell'eliminazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-243">This displays our Delete confirmation screen.</span></span>

![](mvc-music-store-part-5/_static/image15.png)

<span data-ttu-id="6471d-244">Facendo clic sul pulsante Elimina è possibile rimuovere l'album e tornare alla pagina di indice di Store Manager, che indica che l'album è stato eliminato.</span><span class="sxs-lookup"><span data-stu-id="6471d-244">Clicking the Delete button removes the album and returns us to the Store Manager Index page, which shows that the album has been deleted.</span></span>

![](mvc-music-store-part-5/_static/image16.png)

### <a name="using-a-custom-html-helper-to-truncate-text"></a><span data-ttu-id="6471d-245">Uso di un helper HTML personalizzato per troncare il testo</span><span class="sxs-lookup"><span data-stu-id="6471d-245">Using a custom HTML Helper to truncate text</span></span>

<span data-ttu-id="6471d-246">Nella pagina di indice di Store Manager è presente un potenziale problema.</span><span class="sxs-lookup"><span data-stu-id="6471d-246">We've got one potential issue with our Store Manager Index page.</span></span> <span data-ttu-id="6471d-247">Le proprietà di un titolo di album e di un nome di artista possono avere una lunghezza sufficiente a generare la formattazione della tabella.</span><span class="sxs-lookup"><span data-stu-id="6471d-247">Our Album Title and Artist Name properties can both be long enough that they could throw off our table formatting.</span></span> <span data-ttu-id="6471d-248">Verrà creato un helper HTML personalizzato che consente di troncare facilmente queste e altre proprietà nelle visualizzazioni.</span><span class="sxs-lookup"><span data-stu-id="6471d-248">We'll create a custom HTML Helper to allow us to easily truncate these and other properties in our Views.</span></span>

![](mvc-music-store-part-5/_static/image17.png)

<span data-ttu-id="6471d-249">La sintassi di @helper di Razor ha reso molto semplice creare funzioni di supporto personalizzate da usare nelle visualizzazioni.</span><span class="sxs-lookup"><span data-stu-id="6471d-249">Razor's @helper syntax has made it pretty easy to create your own helper functions for use in your views.</span></span> <span data-ttu-id="6471d-250">Aprire la visualizzazione/Views/StoreManager/Index.cshtml e aggiungere il codice seguente subito dopo la riga di @model.</span><span class="sxs-lookup"><span data-stu-id="6471d-250">Open the /Views/StoreManager/Index.cshtml view and add the following code directly after the @model line.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample16.cshtml)]

<span data-ttu-id="6471d-251">Questo metodo helper accetta una stringa e la lunghezza massima consentita.</span><span class="sxs-lookup"><span data-stu-id="6471d-251">This helper method takes a string and a maximum length to allow.</span></span> <span data-ttu-id="6471d-252">Se il testo fornito è più breve della lunghezza specificata, l'helper lo restituisce così com'è.</span><span class="sxs-lookup"><span data-stu-id="6471d-252">If the text supplied is shorter than the length specified, the helper outputs it as-is.</span></span> <span data-ttu-id="6471d-253">Se è più lungo, tronca il testo e ne esegue il rendering "..." per il resto.</span><span class="sxs-lookup"><span data-stu-id="6471d-253">If it is longer, then it truncates the text and renders "…" for the remainder.</span></span>

<span data-ttu-id="6471d-254">Ora è possibile usare l'helper Truncate per assicurarsi che le proprietà titolo album e nome artista siano inferiori a 25 caratteri.</span><span class="sxs-lookup"><span data-stu-id="6471d-254">Now we can use our Truncate helper to ensure that both the Album Title and Artist Name properties are less than 25 characters.</span></span> <span data-ttu-id="6471d-255">Il codice di visualizzazione completo che usa il nuovo Helper truncate viene visualizzato di seguito.</span><span class="sxs-lookup"><span data-stu-id="6471d-255">The complete view code using our new Truncate helper appears below.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample17.cshtml)]

<span data-ttu-id="6471d-256">A questo punto, quando si Esplora l'URL di/StoreManager/, gli album e i titoli vengono conservati al di sotto delle lunghezze massime.</span><span class="sxs-lookup"><span data-stu-id="6471d-256">Now when we browse the /StoreManager/ URL, the albums and titles are kept below our maximum lengths.</span></span>

![](mvc-music-store-part-5/_static/image18.png)

<span data-ttu-id="6471d-257">Nota: viene illustrato il caso semplice di creazione e utilizzo di un helper in una sola visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="6471d-257">Note: This shows the simple case of creating and using a helper in one view.</span></span> <span data-ttu-id="6471d-258">Per altre informazioni sulla creazione di helper che è possibile usare in tutto il sito, vedere il post di Blog seguente: [http://bit.ly/mvc3-helper-options](http://bit.ly/mvc3-helper-options)</span><span class="sxs-lookup"><span data-stu-id="6471d-258">To learn more about creating helpers that you can use throughout your site, see my blog post: [http://bit.ly/mvc3-helper-options](http://bit.ly/mvc3-helper-options)</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="6471d-259">[Precedente](mvc-music-store-part-4.md)
> [Successivo](mvc-music-store-part-6.md)</span><span class="sxs-lookup"><span data-stu-id="6471d-259">[Previous](mvc-music-store-part-4.md)
[Next](mvc-music-store-part-6.md)</span></span>
