---
uid: mvc/overview/older-versions-1/security/preventing-javascript-injection-attacks-vb
title: Prevenzione degli attacchi injection JavaScript (VB) | Microsoft Docs
author: StephenWalther
description: Impedisci l'esecuzione degli attacchi intrusivi in JavaScript e degli attacchi tramite script intersito. In questa esercitazione, Stephen Walther spiega come è possibile...
ms.author: riande
ms.date: 08/19/2008
ms.assetid: 9274a72e-34dd-4dae-8452-ed733ae71377
msc.legacyurl: /mvc/overview/older-versions-1/security/preventing-javascript-injection-attacks-vb
msc.type: authoredcontent
ms.openlocfilehash: dfe09085f26c62c566649bc6f570aa25367a0f07
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78624096"
---
# <a name="preventing-javascript-injection-attacks-vb"></a><span data-ttu-id="0468e-104">Prevenzione degli attacchi injection JavaScript (VB)</span><span class="sxs-lookup"><span data-stu-id="0468e-104">Preventing JavaScript Injection Attacks (VB)</span></span>

<span data-ttu-id="0468e-105">di [Stephen Walther](https://github.com/StephenWalther)</span><span class="sxs-lookup"><span data-stu-id="0468e-105">by [Stephen Walther](https://github.com/StephenWalther)</span></span>

[<span data-ttu-id="0468e-106">Scaricare PDF</span><span class="sxs-lookup"><span data-stu-id="0468e-106">Download PDF</span></span>](https://download.microsoft.com/download/8/4/8/84843d8d-1575-426c-bcb5-9d0c42e51416/ASPNET_MVC_Tutorial_06_VB.pdf)

> <span data-ttu-id="0468e-107">Impedisci l'esecuzione degli attacchi intrusivi in JavaScript e degli attacchi tramite script intersito.</span><span class="sxs-lookup"><span data-stu-id="0468e-107">Prevent JavaScript Injection Attacks and Cross-Site Scripting Attacks from happening to you.</span></span> <span data-ttu-id="0468e-108">In questa esercitazione Stephen Walther spiega come è possibile sconfiggere facilmente questi tipi di attacchi mediante la codifica HTML del contenuto.</span><span class="sxs-lookup"><span data-stu-id="0468e-108">In this tutorial, Stephen Walther explains how you can easily defeat these types of attacks by HTML encoding your content.</span></span>

<span data-ttu-id="0468e-109">L'obiettivo di questa esercitazione è spiegare come è possibile impedire gli attacchi intrusivi JavaScript nelle applicazioni MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0468e-109">The goal of this tutorial is to explain how you can prevent JavaScript injection attacks in your ASP.NET MVC applications.</span></span> <span data-ttu-id="0468e-110">Questa esercitazione illustra due approcci per difendere il sito Web da un attacco JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-110">This tutorial discusses two approaches to defending your website against a JavaScript injection attack.</span></span> <span data-ttu-id="0468e-111">Si apprenderà come impedire gli attacchi injection JavaScript codificando i dati visualizzati.</span><span class="sxs-lookup"><span data-stu-id="0468e-111">You learn how to prevent JavaScript injection attacks by encoding the data that you display.</span></span> <span data-ttu-id="0468e-112">Si apprenderà anche come impedire gli attacchi injection JavaScript codificando i dati accettati.</span><span class="sxs-lookup"><span data-stu-id="0468e-112">You also learn how to prevent JavaScript injection attacks by encoding the data that you accept.</span></span>

## <a name="what-is-a-javascript-injection-attack"></a><span data-ttu-id="0468e-113">Che cos'è un attacco JavaScript Injection?</span><span class="sxs-lookup"><span data-stu-id="0468e-113">What is a JavaScript Injection Attack?</span></span>

<span data-ttu-id="0468e-114">Ogni volta che si accetta l'input dell'utente e si visualizza nuovamente l'input dell'utente, si apre il sito Web per gli attacchi intrusivi in JavaScript.</span><span class="sxs-lookup"><span data-stu-id="0468e-114">Whenever you accept user input and redisplay the user input, you open your website to JavaScript injection attacks.</span></span> <span data-ttu-id="0468e-115">Si esaminerà un'applicazione concreta che è aperta ad attacchi JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-115">Let's examine a concrete application that is open to JavaScript injection attacks.</span></span>

<span data-ttu-id="0468e-116">Si supponga di aver creato un sito Web per i commenti e suggerimenti dei clienti (vedere la figura 1).</span><span class="sxs-lookup"><span data-stu-id="0468e-116">Imagine that you have created a customer feedback website (see Figure 1).</span></span> <span data-ttu-id="0468e-117">I clienti possono visitare il sito Web e immettere commenti e suggerimenti sulla propria esperienza usando i prodotti.</span><span class="sxs-lookup"><span data-stu-id="0468e-117">Customers can visit the website and enter feedback on their experience using your products.</span></span> <span data-ttu-id="0468e-118">Quando un cliente invia il proprio feedback, i commenti vengono visualizzati nella pagina commenti e suggerimenti.</span><span class="sxs-lookup"><span data-stu-id="0468e-118">When a customer submits their feedback, the feedback is redisplayed on the feedback page.</span></span>

<span data-ttu-id="0468e-119">[Sito Web ![commenti e suggerimenti dei clienti](preventing-javascript-injection-attacks-vb/_static/image2.png)](preventing-javascript-injection-attacks-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="0468e-119">[![Customer Feedback Website](preventing-javascript-injection-attacks-vb/_static/image2.png)](preventing-javascript-injection-attacks-vb/_static/image1.png)</span></span>

<span data-ttu-id="0468e-120">**Figura 01**: sito Web di commenti e suggerimenti dei clienti ([fare clic per visualizzare l'immagine con dimensioni complete](preventing-javascript-injection-attacks-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="0468e-120">**Figure 01**: Customer Feedback Website ([Click to view full-size image](preventing-javascript-injection-attacks-vb/_static/image3.png))</span></span>

<span data-ttu-id="0468e-121">Il sito Web di commenti e suggerimenti dei clienti USA il `controller` nel listato 1.</span><span class="sxs-lookup"><span data-stu-id="0468e-121">The customer feedback website uses the `controller` in Listing 1.</span></span> <span data-ttu-id="0468e-122">Questo `controller` contiene due azioni denominate `Index()` e `Create()`.</span><span class="sxs-lookup"><span data-stu-id="0468e-122">This `controller` contains two actions named `Index()` and `Create()`.</span></span>

<span data-ttu-id="0468e-123">**Listato 1-`HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="0468e-123">**Listing 1 – `HomeController.vb`**</span></span>

[!code-vb[Main](preventing-javascript-injection-attacks-vb/samples/sample1.vb)]

<span data-ttu-id="0468e-124">Il metodo `Index()` Visualizza la visualizzazione `Index`.</span><span class="sxs-lookup"><span data-stu-id="0468e-124">The `Index()` method displays the `Index` view.</span></span> <span data-ttu-id="0468e-125">Questo metodo passa tutti i commenti dei clienti precedenti alla visualizzazione `Index` recuperando il feedback dal database (usando una query di LINQ to SQL).</span><span class="sxs-lookup"><span data-stu-id="0468e-125">This method passes all of the previous customer feedback to the `Index` view by retrieving the feedback from the database (using a LINQ to SQL query).</span></span>

<span data-ttu-id="0468e-126">Il metodo `Create()` crea un nuovo elemento di feedback e lo aggiunge al database.</span><span class="sxs-lookup"><span data-stu-id="0468e-126">The `Create()` method creates a new Feedback item and adds it to the database.</span></span> <span data-ttu-id="0468e-127">Il messaggio immesso dal cliente nel form viene passato al metodo `Create()` nel parametro message.</span><span class="sxs-lookup"><span data-stu-id="0468e-127">The message that the customer enters in the form is passed to the `Create()` method in the message parameter.</span></span> <span data-ttu-id="0468e-128">Viene creato un elemento di feedback e il messaggio viene assegnato alla proprietà `Message` dell'elemento feedback.</span><span class="sxs-lookup"><span data-stu-id="0468e-128">A Feedback item is created and the message is assigned to the Feedback item's `Message` property.</span></span> <span data-ttu-id="0468e-129">L'elemento feedback viene inviato al database con la chiamata al metodo `DataContext.SubmitChanges()`.</span><span class="sxs-lookup"><span data-stu-id="0468e-129">The Feedback item is submitted to the database with the `DataContext.SubmitChanges()` method call.</span></span> <span data-ttu-id="0468e-130">Infine, il visitatore viene reindirizzato di nuovo alla visualizzazione `Index` in cui vengono visualizzati tutti i commenti.</span><span class="sxs-lookup"><span data-stu-id="0468e-130">Finally, the visitor is redirected back to the `Index` view where all of the feedback is displayed.</span></span>

<span data-ttu-id="0468e-131">La visualizzazione `Index` è inclusa nel listato 2.</span><span class="sxs-lookup"><span data-stu-id="0468e-131">The `Index` view is contained in Listing 2.</span></span>

<span data-ttu-id="0468e-132">**Listato 2: `Index.aspx`**</span><span class="sxs-lookup"><span data-stu-id="0468e-132">**Listing 2 – `Index.aspx`**</span></span>

[!code-aspx[Main](preventing-javascript-injection-attacks-vb/samples/sample2.aspx)]

<span data-ttu-id="0468e-133">La visualizzazione `Index` include due sezioni.</span><span class="sxs-lookup"><span data-stu-id="0468e-133">The `Index` view has two sections.</span></span> <span data-ttu-id="0468e-134">La sezione superiore contiene il modulo effettivo per i commenti dei clienti.</span><span class="sxs-lookup"><span data-stu-id="0468e-134">The top section contains the actual customer feedback form.</span></span> <span data-ttu-id="0468e-135">La sezione inferiore contiene un oggetto per. Ogni ciclo esegue il ciclo di tutti gli elementi di commenti e suggerimenti dei clienti precedenti e visualizza le proprietà EntryDate e Message per ogni elemento di feedback.</span><span class="sxs-lookup"><span data-stu-id="0468e-135">The bottom section contains a For..Each loop that loops through all of the previous customer feedback items and displays the EntryDate and Message properties for each feedback item.</span></span>

<span data-ttu-id="0468e-136">Il sito Web di commenti e suggerimenti dei clienti è un sito Web semplice.</span><span class="sxs-lookup"><span data-stu-id="0468e-136">The customer feedback website is a simple website.</span></span> <span data-ttu-id="0468e-137">Sfortunatamente, il sito Web è aperto ad attacchi JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-137">Unfortunately, the website is open to JavaScript injection attacks.</span></span>

<span data-ttu-id="0468e-138">Si supponga di immettere il testo seguente nel modulo feedback del cliente:</span><span class="sxs-lookup"><span data-stu-id="0468e-138">Imagine that you enter the following text into the customer feedback form:</span></span>

[!code-html[Main](preventing-javascript-injection-attacks-vb/samples/sample3.html)]

<span data-ttu-id="0468e-139">Questo testo rappresenta uno script JavaScript che visualizza una finestra di messaggio di avviso.</span><span class="sxs-lookup"><span data-stu-id="0468e-139">This text represents a JavaScript script that displays an alert message box.</span></span> <span data-ttu-id="0468e-140">Dopo che qualcuno ha inviato questo script al modulo feedback, il messaggio <em>Boo!</em> verrà visualizzato ogni volta che qualcuno visita il sito Web di commenti e suggerimenti dei clienti in futuro (vedere la figura 2).</span><span class="sxs-lookup"><span data-stu-id="0468e-140">After someone submits this script into the feedback form, the message <em>Boo!</em>will appear whenever anyone visits the customer feedback website in the future (see Figure 2).</span></span>

<span data-ttu-id="0468e-141">[![injection JavaScript](preventing-javascript-injection-attacks-vb/_static/image5.png)](preventing-javascript-injection-attacks-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="0468e-141">[![JavaScript Injection](preventing-javascript-injection-attacks-vb/_static/image5.png)](preventing-javascript-injection-attacks-vb/_static/image4.png)</span></span>

<span data-ttu-id="0468e-142">**Figura 02**: inserimento di JavaScript ([fare clic per visualizzare l'immagine a dimensione intera](preventing-javascript-injection-attacks-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="0468e-142">**Figure 02**: JavaScript Injection ([Click to view full-size image](preventing-javascript-injection-attacks-vb/_static/image6.png))</span></span>

<span data-ttu-id="0468e-143">A questo punto, la risposta iniziale a attacchi JavaScript Injection potrebbe essere apatia.</span><span class="sxs-lookup"><span data-stu-id="0468e-143">Now, your initial response to JavaScript injection attacks might be apathy.</span></span> <span data-ttu-id="0468e-144">Si potrebbe pensare che gli attacchi di tipo JavaScript Injection siano semplicemente un tipo di attacco di tipo *devisore* .</span><span class="sxs-lookup"><span data-stu-id="0468e-144">You might think that JavaScript injection attacks are simply a type of *defacement* attack.</span></span> <span data-ttu-id="0468e-145">Si potrebbe ritenere che nessuno possa eseguire alcuna operazione realmente male eseguendo un attacco JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-145">You might believe that no one can do anything truly evil by committing a JavaScript injection attack.</span></span>

<span data-ttu-id="0468e-146">Sfortunatamente, un pirata informatico può eseguire alcune operazioni in realtà, inserendo JavaScript in un sito Web.</span><span class="sxs-lookup"><span data-stu-id="0468e-146">Unfortunately, a hacker can do some really, really evil things by injecting JavaScript into a website.</span></span> <span data-ttu-id="0468e-147">È possibile usare un attacco JavaScript Injection per eseguire un attacco XSS (cross-site scripting).</span><span class="sxs-lookup"><span data-stu-id="0468e-147">You can use a JavaScript injection attack to perform a Cross-Site Scripting (XSS) attack.</span></span> <span data-ttu-id="0468e-148">In un attacco di scripting tra siti, si rubano informazioni riservate sull'utente e si inviano le informazioni a un altro sito Web.</span><span class="sxs-lookup"><span data-stu-id="0468e-148">In a Cross-Site Scripting attack, you steal confidential user information and send the information to another website.</span></span>

<span data-ttu-id="0468e-149">Un pirata informatico può ad esempio usare un attacco JavaScript Injection per rubare i valori dei cookie del browser da altri utenti.</span><span class="sxs-lookup"><span data-stu-id="0468e-149">For example, a hacker can use a JavaScript injection attack to steal the values of browser cookies from other users.</span></span> <span data-ttu-id="0468e-150">Se le informazioni riservate, ad esempio le password, i numeri di carta di credito o i numeri di previdenza sociale, vengono archiviate nei cookie del browser, un hacker può usare un attacco JavaScript Injection per rubare queste informazioni.</span><span class="sxs-lookup"><span data-stu-id="0468e-150">If sensitive information -- such as passwords, credit card numbers, or social security numbers – is stored in the browser cookies, then a hacker can use a JavaScript injection attack to steal this information.</span></span> <span data-ttu-id="0468e-151">In alternativa, se un utente immette informazioni riservate in un campo del modulo contenuto in una pagina che è stata compromessa da un attacco JavaScript, l'hacker può usare il codice JavaScript inserito per acquisire i dati del modulo e inviarli a un altro sito Web.</span><span class="sxs-lookup"><span data-stu-id="0468e-151">Or, if a user enters sensitive information in a form field contained in a page that has been compromised with a JavaScript attack, then the hacker can use the injected JavaScript to grab the form data and send it to another website.</span></span>

<span data-ttu-id="0468e-152">*Si è preoccupati*.</span><span class="sxs-lookup"><span data-stu-id="0468e-152">*Please be scared*.</span></span> <span data-ttu-id="0468e-153">Eseguire seriamente attacchi JavaScript Injection e proteggere le informazioni riservate dell'utente.</span><span class="sxs-lookup"><span data-stu-id="0468e-153">Take JavaScript injection attacks seriously and protect your user's confidential information.</span></span> <span data-ttu-id="0468e-154">Nelle due sezioni successive verranno illustrate due tecniche che è possibile usare per difendere le applicazioni MVC ASP.NET da attacchi di attacchi JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-154">In the next two sections, we discuss two techniques that you can use to defend your ASP.NET MVC applications from JavaScript injection attacks.</span></span>

## <a name="approach-1-html-encode-in-the-view"></a><span data-ttu-id="0468e-155">Approccio #1: codifica HTML nella visualizzazione</span><span class="sxs-lookup"><span data-stu-id="0468e-155">Approach #1: HTML Encode in the View</span></span>

<span data-ttu-id="0468e-156">Un metodo semplice per impedire gli attacchi intrusivi JavaScript consiste nel codificare in HTML tutti i dati immessi dagli utenti del sito Web quando si rivisualizzano i dati in una vista.</span><span class="sxs-lookup"><span data-stu-id="0468e-156">One easy method of preventing JavaScript injection attacks is to HTML encode any data entered by website users when you redisplay the data in a view.</span></span> <span data-ttu-id="0468e-157">La vista `Index` aggiornata nel listato 3 segue questo approccio.</span><span class="sxs-lookup"><span data-stu-id="0468e-157">The updated `Index` view in Listing 3 follows this approach.</span></span>

<span data-ttu-id="0468e-158">**Listato 3-`Index.aspx` (codificato in HTML)**</span><span class="sxs-lookup"><span data-stu-id="0468e-158">**Listing 3 – `Index.aspx` (HTML Encoded)**</span></span>

[!code-aspx[Main](preventing-javascript-injection-attacks-vb/samples/sample4.aspx)]

<span data-ttu-id="0468e-159">Si noti che il valore di `feedback.Message` è codificato in HTML prima che il valore venga visualizzato con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="0468e-159">Notice that the value of `feedback.Message` is HTML encoded before the value is displayed with the following code:</span></span>

[!code-aspx[Main](preventing-javascript-injection-attacks-vb/samples/sample5.aspx)]

<span data-ttu-id="0468e-160">Che cosa significa codificare in HTML una stringa?</span><span class="sxs-lookup"><span data-stu-id="0468e-160">What does it mean to HTML encode a string?</span></span> <span data-ttu-id="0468e-161">Quando si codifica in HTML una stringa, i caratteri pericolosi, ad esempio `<` e `>`, vengono sostituiti dai riferimenti alle entità HTML, ad esempio `&lt;` e `&gt;`.</span><span class="sxs-lookup"><span data-stu-id="0468e-161">When you HTML encode a string, dangerous characters such as `<` and `>` are replaced by HTML entity references such as `&lt;` and `&gt;`.</span></span> <span data-ttu-id="0468e-162">Quindi, quando la stringa `<script>alert("Boo!")</script>` è codificata in HTML, viene convertita in `&lt;script&gt;alert(&quot;Boo!&quot;)&lt;/script&gt;`.</span><span class="sxs-lookup"><span data-stu-id="0468e-162">So when the string `<script>alert("Boo!")</script>` is HTML encoded, it gets converted to `&lt;script&gt;alert(&quot;Boo!&quot;)&lt;/script&gt;`.</span></span> <span data-ttu-id="0468e-163">La stringa codificata non viene più eseguita come script JavaScript quando viene interpretata da un browser.</span><span class="sxs-lookup"><span data-stu-id="0468e-163">The encoded string no longer executes as a JavaScript script when interpreted by a browser.</span></span> <span data-ttu-id="0468e-164">Si ottiene invece la pagina innocua nella figura 3.</span><span class="sxs-lookup"><span data-stu-id="0468e-164">Instead, you get the harmless page in Figure 3.</span></span>

<span data-ttu-id="0468e-165">[![attacco JavaScript sconfitto](preventing-javascript-injection-attacks-vb/_static/image8.png)](preventing-javascript-injection-attacks-vb/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="0468e-165">[![Defeated JavaScript Attack](preventing-javascript-injection-attacks-vb/_static/image8.png)](preventing-javascript-injection-attacks-vb/_static/image7.png)</span></span>

<span data-ttu-id="0468e-166">**Figura 03**: attacco JavaScript sconfitto ([fare clic per visualizzare l'immagine con dimensioni complete](preventing-javascript-injection-attacks-vb/_static/image9.png))</span><span class="sxs-lookup"><span data-stu-id="0468e-166">**Figure 03**: Defeated JavaScript Attack ([Click to view full-size image](preventing-javascript-injection-attacks-vb/_static/image9.png))</span></span>

<span data-ttu-id="0468e-167">Si noti che nella visualizzazione `Index` in Listing 3 solo il valore di `feedback.Message` è codificato.</span><span class="sxs-lookup"><span data-stu-id="0468e-167">Notice that in the `Index` view in Listing 3 only the value of `feedback.Message` is encoded.</span></span> <span data-ttu-id="0468e-168">Il valore di `feedback.EntryDate` non è codificato.</span><span class="sxs-lookup"><span data-stu-id="0468e-168">The value of `feedback.EntryDate` is not encoded.</span></span> <span data-ttu-id="0468e-169">È sufficiente codificare i dati immessi da un utente.</span><span class="sxs-lookup"><span data-stu-id="0468e-169">You only need to encode data entered by a user.</span></span> <span data-ttu-id="0468e-170">Poiché il valore di EntryDate è stato generato nel controller, non è necessario codificare questo valore in HTML.</span><span class="sxs-lookup"><span data-stu-id="0468e-170">Because the value of EntryDate was generated in the controller, you don't need to HTML encode this value.</span></span>

## <a name="approach-2-html-encode-in-the-controller"></a><span data-ttu-id="0468e-171">Approccio #2: codifica HTML nel controller</span><span class="sxs-lookup"><span data-stu-id="0468e-171">Approach #2: HTML Encode in the Controller</span></span>

<span data-ttu-id="0468e-172">Anziché i dati di codifica HTML quando si visualizzano i dati in una visualizzazione, è possibile codificare i dati in formato HTML immediatamente prima di inviare i dati al database.</span><span class="sxs-lookup"><span data-stu-id="0468e-172">Instead of HTML encoding data when you display the data in a view, you can HTML encode the data just before you submit the data to the database.</span></span> <span data-ttu-id="0468e-173">Questo secondo approccio viene adottato nel caso del `controller` nel listato 4.</span><span class="sxs-lookup"><span data-stu-id="0468e-173">This second approach is taken in the case of the `controller` in Listing 4.</span></span>

<span data-ttu-id="0468e-174">**Listato 4-`HomeController.cs` (codificato in HTML)**</span><span class="sxs-lookup"><span data-stu-id="0468e-174">**Listing 4 – `HomeController.cs` (HTML Encoded)**</span></span>

[!code-vb[Main](preventing-javascript-injection-attacks-vb/samples/sample6.vb)]

<span data-ttu-id="0468e-175">Si noti che il valore del messaggio è codificato in HTML prima che il valore venga inviato al database all'interno dell'azione `Create()`.</span><span class="sxs-lookup"><span data-stu-id="0468e-175">Notice that the value of Message is HTML encoded before the value is submitted to the database within the `Create()` action.</span></span> <span data-ttu-id="0468e-176">Quando il messaggio viene visualizzato nuovamente nella visualizzazione, il messaggio è codificato in formato HTML e qualsiasi JavaScript inserito nel messaggio non viene eseguito.</span><span class="sxs-lookup"><span data-stu-id="0468e-176">When the Message is redisplayed in the view, the Message is HTML encoded and any JavaScript injected in the Message is not executed.</span></span>

<span data-ttu-id="0468e-177">In genere, è consigliabile preferire il primo approccio descritto in questa esercitazione rispetto a questo secondo approccio.</span><span class="sxs-lookup"><span data-stu-id="0468e-177">Typically, you should favor the first approach discussed in this tutorial over this second approach.</span></span> <span data-ttu-id="0468e-178">Il problema con questo secondo approccio è che si finisce con dati codificati in HTML nel database.</span><span class="sxs-lookup"><span data-stu-id="0468e-178">The problem with this second approach is that you end up with HTML encoded data in your database.</span></span> <span data-ttu-id="0468e-179">In altre parole, i dati del database sono sporchi di caratteri di aspetto buffi.</span><span class="sxs-lookup"><span data-stu-id="0468e-179">In other words, your database data is dirtied with funny looking characters.</span></span>

<span data-ttu-id="0468e-180">Perché questa operazione è negativa?</span><span class="sxs-lookup"><span data-stu-id="0468e-180">Why is this bad?</span></span> <span data-ttu-id="0468e-181">Se è necessario visualizzare i dati del database in un elemento diverso da una pagina Web, si verificano problemi.</span><span class="sxs-lookup"><span data-stu-id="0468e-181">If you ever need to display the database data in something other than a web page, then you will have problems.</span></span> <span data-ttu-id="0468e-182">Non è possibile, ad esempio, visualizzare più facilmente i dati in un Windows Forms Application.</span><span class="sxs-lookup"><span data-stu-id="0468e-182">For example, you can no longer easily display the data in a Windows Forms application.</span></span>

## <a name="summary"></a><span data-ttu-id="0468e-183">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="0468e-183">Summary</span></span>

<span data-ttu-id="0468e-184">Lo scopo di questa esercitazione è quello di spaventare l'utente sulla prospettiva di un attacco JavaScript injection.</span><span class="sxs-lookup"><span data-stu-id="0468e-184">The purpose of this tutorial was to scare you about the prospect of a JavaScript injection attack.</span></span> <span data-ttu-id="0468e-185">Questa esercitazione ha illustrato due approcci per la difesa delle applicazioni MVC ASP.NET da attacchi JavaScript injection: è possibile codificare in HTML i dati inviati dall'utente nella vista oppure è possibile codificare in HTML i dati inviati dall'utente nel controller.</span><span class="sxs-lookup"><span data-stu-id="0468e-185">This tutorial discussed two approaches for defending your ASP.NET MVC applications against JavaScript injection attacks: you can either HTML encode user submitted data in the view or you can HTML encode user submitted data in the controller.</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="0468e-186">Precedente</span><span class="sxs-lookup"><span data-stu-id="0468e-186">Previous</span></span>](authenticating-users-with-windows-authentication-vb.md)
