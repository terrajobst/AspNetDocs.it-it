---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Informazioni sui filtri azione (c#) | Microsoft Docs
author: microsoft
description: L'obiettivo di questa esercitazione è di spiegare i filtri azione. Un filtro azione è un attributo che è possibile applicare un'azione del controller, o un intero controller...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: 8264b48388ee4a6b51515aa2b897ece3b2f3972a
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/09/2019
ms.locfileid: "59380873"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="01eec-104">Informazioni sui filtri per azioni (C#)</span><span class="sxs-lookup"><span data-stu-id="01eec-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="01eec-105">by [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="01eec-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="01eec-106">Scarica il PDF</span><span class="sxs-lookup"><span data-stu-id="01eec-106">Download PDF</span></span>](http://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="01eec-107">L'obiettivo di questa esercitazione è di spiegare i filtri azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="01eec-108">Un filtro azione è un attributo che è possibile applicare un'azione del controller, o un intero controller, che modifica il modo in cui viene eseguita l'azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>


## <a name="understanding-action-filters"></a><span data-ttu-id="01eec-109">Informazioni sui filtri azione</span><span class="sxs-lookup"><span data-stu-id="01eec-109">Understanding Action Filters</span></span>

<span data-ttu-id="01eec-110">L'obiettivo di questa esercitazione è di spiegare i filtri azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="01eec-111">Un filtro azione è un attributo che è possibile applicare un'azione del controller, o un intero controller, che modifica il modo in cui viene eseguita l'azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="01eec-112">Il framework ASP.NET MVC include vari filtri di azione:</span><span class="sxs-lookup"><span data-stu-id="01eec-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="01eec-113">OutputCache – questo filtro delle azioni memorizza nella cache l'output di un'azione del controller per un periodo di tempo specificato.</span><span class="sxs-lookup"><span data-stu-id="01eec-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="01eec-114">HandleError – questo filtro delle azioni di gestione degli errori generati quando viene eseguita un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="01eec-115">Autorizzare: questo filtro delle azioni consente di limitare l'accesso a un particolare utente o ruolo.</span><span class="sxs-lookup"><span data-stu-id="01eec-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="01eec-116">È anche possibile creare i propri filtri azione personalizzati.</span><span class="sxs-lookup"><span data-stu-id="01eec-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="01eec-117">Ad esempio, è possibile creare un filtro azione personalizzato per implementare un sistema di autenticazione personalizzato.</span><span class="sxs-lookup"><span data-stu-id="01eec-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="01eec-118">In alternativa, si potrebbe voler creare un filtro azione che modifica i dati della visualizzazione restituiti da un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="01eec-119">In questa esercitazione descrive come creare un filtro azione fin dall'inizio.</span><span class="sxs-lookup"><span data-stu-id="01eec-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="01eec-120">Creiamo un filtro di azione di Log che registra diverse fasi dell'elaborazione di un'azione di finestra di Output di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="01eec-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="01eec-121">Usando un filtro azioni</span><span class="sxs-lookup"><span data-stu-id="01eec-121">Using an Action Filter</span></span>

<span data-ttu-id="01eec-122">Un filtro azione è un attributo.</span><span class="sxs-lookup"><span data-stu-id="01eec-122">An action filter is an attribute.</span></span> <span data-ttu-id="01eec-123">È possibile applicare la maggior parte dei filtri azione un'azione di singoli controller o un intero controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="01eec-124">Ad esempio, il trattamento dei dati nel listato 1 espone un'azione denominata `Index()` che restituisce l'ora corrente.</span><span class="sxs-lookup"><span data-stu-id="01eec-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="01eec-125">Questa azione è decorata con il `OutputCache` filtro delle azioni.</span><span class="sxs-lookup"><span data-stu-id="01eec-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="01eec-126">Questo filtro, il valore restituito dall'azione da memorizzare nella cache per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="01eec-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

**<span data-ttu-id="01eec-127">Listato 1:</span><span class="sxs-lookup"><span data-stu-id="01eec-127">Listing 1 –</span></span> `Controllers\DataController.cs`**

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="01eec-128">Se viene richiamato più volte il `Index()` azione immettendo l'URL/Data/indice nella barra degli indirizzi del browser e l'aggiornamento di raggiungere il numero di volte, viene visualizzata la stessa ora per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="01eec-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="01eec-129">L'output del `Index()` azione viene memorizzato nella cache per 10 secondi (vedere la figura 1).</span><span class="sxs-lookup"><span data-stu-id="01eec-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>


[![C<span data-ttu-id="01eec-130">tempo inserito nella cache]</span><span class="sxs-lookup"><span data-stu-id="01eec-130">ached time]</span></span>(understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)

<span data-ttu-id="01eec-131">**Figura 01**: Memorizzato nella cache di tempo ([fare clic per visualizzare l'immagine con dimensioni normali](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="01eec-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>


<span data-ttu-id="01eec-132">Nel listato 1, un filtro di singola azione: il `OutputCache` viene applicato il filtro di azione: il `Index()` (metodo).</span><span class="sxs-lookup"><span data-stu-id="01eec-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="01eec-133">Se è necessario, è possibile applicare più filtri di azione per la stessa azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="01eec-134">Ad esempio, è possibile applicare sia la `OutputCache` e `HandleError` filtri dell'azione alla stessa azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="01eec-135">Nel listato 1, il `OutputCache` viene applicato il filtro azione il `Index()` azione.</span><span class="sxs-lookup"><span data-stu-id="01eec-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="01eec-136">È anche possibile applicare questo attributo per il `DataController` classe stessa.</span><span class="sxs-lookup"><span data-stu-id="01eec-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="01eec-137">In tal caso, sarebbe essere memorizzati nella cache il risultato restituito da qualsiasi azione esposta dal controller di per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="01eec-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="01eec-138">I diversi tipi di filtri</span><span class="sxs-lookup"><span data-stu-id="01eec-138">The Different Types of Filters</span></span>

<span data-ttu-id="01eec-139">Il framework ASP.NET MVC supporta quattro tipi diversi di filtri:</span><span class="sxs-lookup"><span data-stu-id="01eec-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="01eec-140">Filtri di autorizzazione: implementa la `IAuthorizationFilter` attributo.</span><span class="sxs-lookup"><span data-stu-id="01eec-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="01eec-141">Filtri azione: implementa la `IActionFilter` attributo.</span><span class="sxs-lookup"><span data-stu-id="01eec-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="01eec-142">Generare filtri: implementa la `IResultFilter` attributo.</span><span class="sxs-lookup"><span data-stu-id="01eec-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="01eec-143">I filtri eccezioni – implementa il `IExceptionFilter` attributo.</span><span class="sxs-lookup"><span data-stu-id="01eec-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="01eec-144">I filtri vengono eseguiti nell'ordine indicato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="01eec-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="01eec-145">Ad esempio, i filtri di autorizzazione vengono sempre eseguiti prima dei filtri azione e i filtri eccezioni vengono sempre eseguiti dopo ogni altro tipo di filtro.</span><span class="sxs-lookup"><span data-stu-id="01eec-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="01eec-146">Filtri di autorizzazione vengono utilizzati per implementare l'autenticazione e autorizzazione per le azioni del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="01eec-147">Ad esempio, il filtro Authorize è un esempio di un filtro di autorizzazione.</span><span class="sxs-lookup"><span data-stu-id="01eec-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="01eec-148">Filtri azione contengono la logica che viene eseguita prima e dopo l'esecuzione di un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="01eec-149">È possibile usare un filtro azione, ad esempio, per modificare i dati di visualizzazione che restituisce un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="01eec-150">I filtri risultato contengono la logica che viene eseguita prima e dopo l'esecuzione di un risultato della visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="01eec-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="01eec-151">Ad esempio, si potrebbe voler modificare un risultato della visualizzazione pulsante destro del mouse prima del rendering della visualizzazione nel browser.</span><span class="sxs-lookup"><span data-stu-id="01eec-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="01eec-152">I filtri eccezioni sono l'ultimo tipo di filtro per l'esecuzione.</span><span class="sxs-lookup"><span data-stu-id="01eec-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="01eec-153">È possibile usare un filtro eccezioni per gestire gli errori generati dalle azioni del controller o i risultati dell'azione controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="01eec-154">È anche possibile usare i filtri eccezioni per registrare gli errori.</span><span class="sxs-lookup"><span data-stu-id="01eec-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="01eec-155">Ogni tipo di filtro viene eseguita in un ordine particolare.</span><span class="sxs-lookup"><span data-stu-id="01eec-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="01eec-156">Se si desidera controllare l'ordine in cui vengono eseguiti i filtri dello stesso tipo è possibile impostare proprietà di ordine del filtro.</span><span class="sxs-lookup"><span data-stu-id="01eec-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="01eec-157">La classe base per tutti i filtri di azione è il `System.Web.Mvc.FilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="01eec-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="01eec-158">Se si desidera implementare un particolare tipo di filtro, quindi è necessario creare una classe che eredita dalla classe di filtro di base e implementa una o più delle `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, o `IExceptionFilter` interfacce.</span><span class="sxs-lookup"><span data-stu-id="01eec-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="01eec-159">La classe ActionFilterAttribute Base</span><span class="sxs-lookup"><span data-stu-id="01eec-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="01eec-160">Per rendere più semplice per implementare un filtro azioni personalizzato, il framework ASP.NET MVC include una base `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="01eec-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="01eec-161">Questa classe implementa sia il `IActionFilter` e `IResultFilter` interfacce ed eredita dal `Filter` classe.</span><span class="sxs-lookup"><span data-stu-id="01eec-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="01eec-162">La terminologia utilizzata in questo caso non è completamente coerenza.</span><span class="sxs-lookup"><span data-stu-id="01eec-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="01eec-163">Tecnicamente, una classe che eredita dalla classe ActionFilterAttribute è sia un filtro azioni che un filtro risultato.</span><span class="sxs-lookup"><span data-stu-id="01eec-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="01eec-164">Tuttavia, nel senso separato, il filtro azioni di word consente di fare riferimento a qualsiasi tipo di filtro nel framework di MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="01eec-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="01eec-165">La base `ActionFilterAttribute` classe dispone dei metodi seguenti che è possibile eseguire l'override:</span><span class="sxs-lookup"><span data-stu-id="01eec-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="01eec-166">OnActionExecuting: questo metodo viene chiamato prima che venga eseguita un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="01eec-167">OnActionExecuted: questo metodo viene chiamato dopo l'esecuzione di un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="01eec-168">OnResultExecuting: questo metodo viene chiamato prima dell'esecuzione di un risultato di azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="01eec-169">OnResultExecuted: questo metodo viene chiamato dopo l'esecuzione di un risultato di azione del controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="01eec-170">Nella sezione successiva, si vedrà come è possibile implementare ognuno di questi metodi diversi.</span><span class="sxs-lookup"><span data-stu-id="01eec-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="01eec-171">Creazione di un filtro di azione di Log</span><span class="sxs-lookup"><span data-stu-id="01eec-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="01eec-172">Per illustrare come creare un filtro azioni personalizzato, si creerà un filtro azioni personalizzato che registra le fasi dell'elaborazione di un'azione del controller per la finestra di Output di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="01eec-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="01eec-173">Nostro `LogActionFilter` è contenuta nel listato 2.</span><span class="sxs-lookup"><span data-stu-id="01eec-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

**<span data-ttu-id="01eec-174">Listato 2:</span><span class="sxs-lookup"><span data-stu-id="01eec-174">Listing 2 –</span></span> `ActionFilters\LogActionFilter.cs`**

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="01eec-175">Nel listato 2, il `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, e `OnResultExecuted()` tutti i metodi chiamano il `Log()` (metodo).</span><span class="sxs-lookup"><span data-stu-id="01eec-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="01eec-176">Il nome del metodo e i dati della route corrente viene passato per il `Log()` (metodo).</span><span class="sxs-lookup"><span data-stu-id="01eec-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="01eec-177">Il `Log()` metodo scrive un messaggio nella finestra di Output di Visual Studio (vedere la figura 2).</span><span class="sxs-lookup"><span data-stu-id="01eec-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>


[![W<span data-ttu-id="01eec-178">riting alla finestra di Output di Visual Studio]</span><span class="sxs-lookup"><span data-stu-id="01eec-178">riting to the Visual Studio Output window]</span></span>(understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)

<span data-ttu-id="01eec-179">**Figura 02**: La scrittura nella finestra di Output di Visual Studio ([fare clic per visualizzare l'immagine con dimensioni normali](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="01eec-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>


<span data-ttu-id="01eec-180">Il controller Home nel listato 3 viene illustrato come applicare il filtro di azione di Log a una classe intero controller.</span><span class="sxs-lookup"><span data-stu-id="01eec-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="01eec-181">Ogni volta che le azioni esposte dal controller Home vengono richiamate, ovvero il `Index()` metodo o il `About()` metodo – le fasi di elaborazione azione vengono registrati nella finestra di Output di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="01eec-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

**<span data-ttu-id="01eec-182">Listato 3:</span><span class="sxs-lookup"><span data-stu-id="01eec-182">Listing 3 –</span></span> `Controllers\HomeController.cs`**

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="01eec-183">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="01eec-183">Summary</span></span>

<span data-ttu-id="01eec-184">In questa esercitazione, sono stati presentati i filtri di azione MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="01eec-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="01eec-185">Si è appreso come i quattro diversi tipi di filtri: filtri di autorizzazione, i filtri azione, i filtri risultato e i filtri eccezioni.</span><span class="sxs-lookup"><span data-stu-id="01eec-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="01eec-186">Si è appreso anche sulla base `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="01eec-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="01eec-187">Infine, si è appreso come implementare un filtro azione semplice.</span><span class="sxs-lookup"><span data-stu-id="01eec-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="01eec-188">È stato creato un filtro di azione di Log che registra le fasi dell'elaborazione di un'azione del controller per la finestra di Output di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="01eec-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="01eec-189">[Precedente](asp-net-mvc-routing-overview-cs.md)
> [Successivo](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="01eec-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
