---
uid: web-forms/overview/data-access/advanced-data-access-scenarios/creating-stored-procedures-and-user-defined-functions-with-managed-code-vb
title: Creazione di stored procedure e funzioni definite dall'utente con codice gestito (VB) | Microsoft Docs
author: rick-anderson
description: Microsoft SQL Server 2005 si integra con .NET Common Language Runtime per consentire agli sviluppatori di creare oggetti di database tramite codice gestito. Questa esercitazione...
ms.author: riande
ms.date: 08/03/2007
ms.assetid: 8be9a51b-ea6b-46c7-bfa2-476d9b14c24c
msc.legacyurl: /web-forms/overview/data-access/advanced-data-access-scenarios/creating-stored-procedures-and-user-defined-functions-with-managed-code-vb
msc.type: authoredcontent
ms.openlocfilehash: 0ac5f71d519689a9dc84fb82a04196d520cca6e1
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78551303"
---
# <a name="creating-stored-procedures-and-user-defined-functions-with-managed-code-vb"></a><span data-ttu-id="81555-104">Creazione di stored procedure e funzioni definite dall'utente con codice gestito (VB)</span><span class="sxs-lookup"><span data-stu-id="81555-104">Creating Stored Procedures and User-Defined Functions with Managed Code (VB)</span></span>

<span data-ttu-id="81555-105">di [Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="81555-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="81555-106">[Scarica codice](https://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_75_VB.zip) o [Scarica PDF](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/datatutorial75vb1.pdf)</span><span class="sxs-lookup"><span data-stu-id="81555-106">[Download Code](https://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_75_VB.zip) or [Download PDF](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/datatutorial75vb1.pdf)</span></span>

> <span data-ttu-id="81555-107">Microsoft SQL Server 2005 si integra con .NET Common Language Runtime per consentire agli sviluppatori di creare oggetti di database tramite codice gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-107">Microsoft SQL Server 2005 integrates with the .NET Common Language Runtime to allow developers to create database objects through managed code.</span></span> <span data-ttu-id="81555-108">In questa esercitazione viene illustrato come creare stored procedure gestite e funzioni gestite definite dall'utente con il Visual Basic C# o il codice.</span><span class="sxs-lookup"><span data-stu-id="81555-108">This tutorial shows how to create managed stored procedures and managed user-defined functions with your Visual Basic or C# code.</span></span> <span data-ttu-id="81555-109">Viene inoltre illustrato il modo in cui queste edizioni di Visual Studio consentono di eseguire il debug di tali oggetti di database gestiti.</span><span class="sxs-lookup"><span data-stu-id="81555-109">We also see how these editions of Visual Studio allow you to debug such managed database objects.</span></span>

## <a name="introduction"></a><span data-ttu-id="81555-110">Introduzione</span><span class="sxs-lookup"><span data-stu-id="81555-110">Introduction</span></span>

<span data-ttu-id="81555-111">I database come Microsoft SQL Server 2005 usano [Transact-Structured Query Language (T-SQL)](http://en.wikipedia.org/wiki/Transact-SQL) per l'inserimento, la modifica e il recupero dei dati.</span><span class="sxs-lookup"><span data-stu-id="81555-111">Databases like Microsoft s SQL Server 2005 use the [Transact-Structured Query Language (T-SQL)](http://en.wikipedia.org/wiki/Transact-SQL) for inserting, modifying, and retrieving data.</span></span> <span data-ttu-id="81555-112">La maggior parte dei sistemi di database include costrutti per raggruppare una serie di istruzioni SQL che possono essere eseguite come una singola unità riutilizzabile.</span><span class="sxs-lookup"><span data-stu-id="81555-112">Most database systems include constructs for grouping a series of SQL statements that can then be executed as a single, reusable unit.</span></span> <span data-ttu-id="81555-113">Le stored procedure sono un esempio.</span><span class="sxs-lookup"><span data-stu-id="81555-113">Stored procedures are one example.</span></span> <span data-ttu-id="81555-114">Un altro è costituito da *funzioni definite dall'utente*(UDF), un costrutto che verrà esaminato più dettagliatamente nel passaggio 9.</span><span class="sxs-lookup"><span data-stu-id="81555-114">Another is *User-Defined Functions*(UDFs), a construct that we will examine in greater detail in Step 9.</span></span>

<span data-ttu-id="81555-115">SQL è progettato per l'utilizzo di set di dati.</span><span class="sxs-lookup"><span data-stu-id="81555-115">At its core, SQL is designed for working with sets of data.</span></span> <span data-ttu-id="81555-116">Le istruzioni `SELECT`, `UPDATE`e `DELETE` si applicano in modo intrinseco a tutti i record nella tabella corrispondente e sono limitate solo dalle clausole `WHERE`.</span><span class="sxs-lookup"><span data-stu-id="81555-116">The `SELECT`, `UPDATE`, and `DELETE` statements inherently apply to all records in the corresponding table and are only limited by their `WHERE` clauses.</span></span> <span data-ttu-id="81555-117">Esistono tuttavia diverse funzionalità del linguaggio progettate per l'utilizzo di un record alla volta e per la modifica di dati scalari.</span><span class="sxs-lookup"><span data-stu-id="81555-117">Yet there are many language features designed for working with one record at a time and for manipulating scalar data.</span></span> <span data-ttu-id="81555-118">[`CURSOR` s](http://www.sqlteam.com/item.asp?ItemID=553) consentono di eseguire il ciclo di un set di record uno alla volta.</span><span class="sxs-lookup"><span data-stu-id="81555-118">[`CURSOR` s](http://www.sqlteam.com/item.asp?ItemID=553) allow for a set of records to be looped through one at a time.</span></span> <span data-ttu-id="81555-119">Funzioni di modifica delle stringhe come `LEFT`, `CHARINDEX`e `PATINDEX` funzionano con dati scalari.</span><span class="sxs-lookup"><span data-stu-id="81555-119">String manipulation functions like `LEFT`, `CHARINDEX`, and `PATINDEX` work with scalar data.</span></span> <span data-ttu-id="81555-120">SQL include anche istruzioni del flusso di controllo come `IF` e `WHILE`.</span><span class="sxs-lookup"><span data-stu-id="81555-120">SQL also includes control flow statements like `IF` and `WHILE`.</span></span>

<span data-ttu-id="81555-121">Prima di Microsoft SQL Server 2005, le stored procedure e le funzioni definite dall'utente potevano essere definite solo come una raccolta di istruzioni T-SQL.</span><span class="sxs-lookup"><span data-stu-id="81555-121">Prior to Microsoft SQL Server 2005, stored procedures and UDFs could only be defined as a collection of T-SQL statements.</span></span> <span data-ttu-id="81555-122">SQL Server 2005, tuttavia, è stato progettato per fornire l'integrazione con [Common Language Runtime (CLR)](https://msdn.microsoft.com/netframework/aa497266.aspx), ovvero il runtime utilizzato da tutti gli assembly .NET.</span><span class="sxs-lookup"><span data-stu-id="81555-122">SQL Server 2005, however, was designed to provide integration with the [Common Language Runtime (CLR)](https://msdn.microsoft.com/netframework/aa497266.aspx), which is the runtime used by all .NET assemblies.</span></span> <span data-ttu-id="81555-123">Di conseguenza, le stored procedure e le funzioni definite dall'utente in un database SQL Server 2005 possono essere create utilizzando codice gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-123">Consequently, the stored procedures and UDFs in a SQL Server 2005 database can be created using managed code.</span></span> <span data-ttu-id="81555-124">Ovvero, è possibile creare un stored procedure o una funzione definita dall'utente come metodo in una classe Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="81555-124">That is, you can create a stored procedure or UDF as a method in a Visual Basic class.</span></span> <span data-ttu-id="81555-125">In questo modo, le stored procedure e le funzioni definite dall'utente utilizzeranno le funzionalità nell'.NET Framework e dalle classi personalizzate.</span><span class="sxs-lookup"><span data-stu-id="81555-125">This enables these stored procedures and UDFs to utilize functionality in the .NET Framework and from your own custom classes.</span></span>

<span data-ttu-id="81555-126">In questa esercitazione verrà esaminato come creare stored procedure gestite e funzioni definite dall'utente e come integrarle nel database Northwind.</span><span class="sxs-lookup"><span data-stu-id="81555-126">In this tutorial we will examine how to create managed stored procedures and User-Defined Functions and how to integrate them into our Northwind database.</span></span> <span data-ttu-id="81555-127">Inizia subito.</span><span class="sxs-lookup"><span data-stu-id="81555-127">Let s get started!</span></span>

> [!NOTE]
> <span data-ttu-id="81555-128">Gli oggetti di database gestiti offrono alcuni vantaggi rispetto alle rispettive controparti SQL.</span><span class="sxs-lookup"><span data-stu-id="81555-128">Managed database objects offer some advantages over their SQL counterparts.</span></span> <span data-ttu-id="81555-129">La ricchezza e la familiarità del linguaggio e la possibilità di riutilizzare il codice e la logica esistenti costituiscono i vantaggi principali.</span><span class="sxs-lookup"><span data-stu-id="81555-129">Language richness and familiarity and the ability to reuse existing code and logic are the main advantages.</span></span> <span data-ttu-id="81555-130">Gli oggetti di database gestiti, tuttavia, potrebbero essere meno efficienti quando si utilizzano set di dati che non implicano una logica procedurale.</span><span class="sxs-lookup"><span data-stu-id="81555-130">But managed database objects are likely to be less efficient when working with sets of data that do not involve much procedural logic.</span></span> <span data-ttu-id="81555-131">Per una discussione più approfondita sui vantaggi derivanti dall'utilizzo di codice gestito rispetto a T-SQL, vedere i [vantaggi dell'utilizzo di codice gestito per creare oggetti di database](https://msdn.microsoft.com/library/k2e1fb36(VS.80).aspx).</span><span class="sxs-lookup"><span data-stu-id="81555-131">For a more thorough discussion on the advantages of using managed code versus T-SQL, check out the [Advantages of Using Managed Code to Create Database Objects](https://msdn.microsoft.com/library/k2e1fb36(VS.80).aspx).</span></span>

## <a name="step-1-moving-the-northwind-database-out-ofapp_data"></a><span data-ttu-id="81555-132">Passaggio 1: trasferimento del database Northwind fuori dalla`App_Data`</span><span class="sxs-lookup"><span data-stu-id="81555-132">Step 1: Moving the Northwind Database Out of`App_Data`</span></span>

<span data-ttu-id="81555-133">Tutte le esercitazioni finora hanno usato un file di database Microsoft SQL Server 2005 Express Edition nella cartella `App_Data` dell'applicazione Web.</span><span class="sxs-lookup"><span data-stu-id="81555-133">All of our tutorials thus far have used a Microsoft SQL Server 2005 Express Edition database file in the web application s `App_Data` folder.</span></span> <span data-ttu-id="81555-134">L'inserimento del database in `App_Data` semplifica la distribuzione e l'esecuzione di queste esercitazioni quando tutti i file si trovano all'interno di una directory e non richiede alcun passaggio di configurazione aggiuntivo per il test dell'esercitazione.</span><span class="sxs-lookup"><span data-stu-id="81555-134">Placing the database in `App_Data` simplified distributing and running these tutorials as all of the files were located within one directory and required no additional configuration steps to test the tutorial.</span></span>

<span data-ttu-id="81555-135">Per questa esercitazione, tuttavia, spostare il database Northwind fuori dalla `App_Data` e registrarlo in modo esplicito con l'istanza di database SQL Server 2005 Express Edition.</span><span class="sxs-lookup"><span data-stu-id="81555-135">For this tutorial, however, let s move the Northwind database out of `App_Data` and explicitly register it with the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="81555-136">Sebbene sia possibile eseguire i passaggi per questa esercitazione con il database nella cartella `App_Data`, un certo numero di passaggi è molto più semplice registrando in modo esplicito il database con l'istanza di database SQL Server 2005 Express Edition.</span><span class="sxs-lookup"><span data-stu-id="81555-136">While we can perform the steps for this tutorial with the database in the `App_Data` folder, a number of the steps are made much simpler by explicitly registering the database with the SQL Server 2005 Express Edition database instance.</span></span>

<span data-ttu-id="81555-137">Il download di questa esercitazione include i due file di database-`NORTHWND.MDF` e `NORTHWND_log.LDF`-inseriti in una cartella denominata `DataFiles`.</span><span class="sxs-lookup"><span data-stu-id="81555-137">The download for this tutorial has the two database files - `NORTHWND.MDF` and `NORTHWND_log.LDF` - placed in a folder named `DataFiles`.</span></span> <span data-ttu-id="81555-138">Se si segue con la propria implementazione delle esercitazioni, chiudere Visual Studio e spostare i file `NORTHWND.MDF` e `NORTHWND_log.LDF` dalla cartella `App_Data` del sito Web a una cartella esterna al sito Web.</span><span class="sxs-lookup"><span data-stu-id="81555-138">If you are following along with your own implementation of the tutorials, close Visual Studio and move the `NORTHWND.MDF` and `NORTHWND_log.LDF` files from the website s `App_Data` folder to a folder outside of the website.</span></span> <span data-ttu-id="81555-139">Una volta che i file di database sono stati spostati in un'altra cartella, è necessario registrare il database Northwind con l'istanza di database SQL Server 2005 Express Edition.</span><span class="sxs-lookup"><span data-stu-id="81555-139">Once the database files have been moved to another folder we need to register the Northwind database with the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="81555-140">Questa operazione può essere eseguita da SQL Server Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-140">This can be done from SQL Server Management Studio.</span></span> <span data-ttu-id="81555-141">Se nel computer è installata un'edizione non Express di SQL Server 2005, probabilmente è già installato Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-141">If you have a non-Express Edition of SQL Server 2005 installed on your computer then you likely already have Management Studio installed.</span></span> <span data-ttu-id="81555-142">Se si ha solo SQL Server 2005 Express Edition nel computer, è necessario scaricare e installare [Microsoft SQL Server Management Studio Express](https://www.microsoft.com/downloads/details.aspx?displaylang=en&amp;FamilyID=C243A5AE-4BD1-4E3D-94B8-5A0F62BF7796).</span><span class="sxs-lookup"><span data-stu-id="81555-142">If you only have SQL Server 2005 Express Edition on your computer then take a moment to download and install [Microsoft SQL Server Management Studio Express](https://www.microsoft.com/downloads/details.aspx?displaylang=en&amp;FamilyID=C243A5AE-4BD1-4E3D-94B8-5A0F62BF7796).</span></span>

<span data-ttu-id="81555-143">Avviare SQL Server Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-143">Launch SQL Server Management Studio.</span></span> <span data-ttu-id="81555-144">Come illustrato nella figura 1, Management Studio inizia chiedendo a quale server connettersi.</span><span class="sxs-lookup"><span data-stu-id="81555-144">As Figure 1 shows, Management Studio starts by asking what server to connect to.</span></span> <span data-ttu-id="81555-145">Immettere localhost\SQLExpress per il nome del server, scegliere autenticazione di Windows nell'elenco a discesa autenticazione, quindi fare clic su Connetti.</span><span class="sxs-lookup"><span data-stu-id="81555-145">Enter localhost\SQLExpress for the server name, choose Windows Authentication in the Authentication drop-down list, and click Connect.</span></span>

![Connettersi all'istanza di database appropriata](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image1.png)

<span data-ttu-id="81555-147">**Figura 1**: connettersi all'istanza di database appropriata</span><span class="sxs-lookup"><span data-stu-id="81555-147">**Figure 1**: Connect to the Appropriate Database Instance</span></span>

<span data-ttu-id="81555-148">Una volta stabilita la connessione, nella finestra Esplora oggetti vengono elencate le informazioni relative all'istanza del database SQL Server 2005 Express Edition, inclusi i relativi database, le informazioni di sicurezza, le opzioni di gestione e così via.</span><span class="sxs-lookup"><span data-stu-id="81555-148">Once you ve connected, the Object Explorer window will list information about the SQL Server 2005 Express Edition database instance, including its databases, security information, management options, and so forth.</span></span>

<span data-ttu-id="81555-149">È necessario alleghi il database Northwind nella cartella `DataFiles` (o in qualsiasi punto in cui è stato spostato) nell'istanza del database SQL Server 2005 Express Edition.</span><span class="sxs-lookup"><span data-stu-id="81555-149">We need to attach the Northwind database in the `DataFiles` folder (or wherever you may have moved it) to the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="81555-150">Fare clic con il pulsante destro del mouse sulla cartella database e scegliere l'opzione Connetti dal menu di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="81555-150">Right-click on the Databases folder and choose the Attach option from the context menu.</span></span> <span data-ttu-id="81555-151">Verrà visualizzata la finestra di dialogo Connetti database.</span><span class="sxs-lookup"><span data-stu-id="81555-151">This will bring up the Attach Databases dialog box.</span></span> <span data-ttu-id="81555-152">Fare clic sul pulsante Aggiungi, eseguire il drill-down fino al file di `NORTHWND.MDF` appropriato e fare clic su OK.</span><span class="sxs-lookup"><span data-stu-id="81555-152">Click the Add button, drill down to the appropriate `NORTHWND.MDF` file, and click OK.</span></span> <span data-ttu-id="81555-153">A questo punto la schermata dovrebbe essere simile alla figura 2.</span><span class="sxs-lookup"><span data-stu-id="81555-153">At this point your screen should look similar to Figure 2.</span></span>

<span data-ttu-id="81555-154">[![connettersi all'istanza di database appropriata](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image3.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image2.png)</span><span class="sxs-lookup"><span data-stu-id="81555-154">[![Connect to the Appropriate Database Instance](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image3.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image2.png)</span></span>

<span data-ttu-id="81555-155">**Figura 2**: connettersi all'istanza di database appropriata ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="81555-155">**Figure 2**: Connect to the Appropriate Database Instance ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image4.png))</span></span>

> [!NOTE]
> <span data-ttu-id="81555-156">Quando ci si connette all'istanza di SQL Server 2005 Express Edition tramite Management Studio la finestra di dialogo Collega database non consente di eseguire il drill-down nelle directory dei profili utente, ad esempio i documenti.</span><span class="sxs-lookup"><span data-stu-id="81555-156">When connecting to the SQL Server 2005 Express Edition instance through Management Studio the Attach Databases dialog box does not allow you to drill down into user profile directories, such as My Documents.</span></span> <span data-ttu-id="81555-157">Assicurarsi pertanto di inserire i file `NORTHWND.MDF` e `NORTHWND_log.LDF` in una directory del profilo non utente.</span><span class="sxs-lookup"><span data-stu-id="81555-157">Therefore, make sure to place the `NORTHWND.MDF` and `NORTHWND_log.LDF` files in a non-user profile directory.</span></span>

<span data-ttu-id="81555-158">Fare clic sul pulsante OK per alleghi il database.</span><span class="sxs-lookup"><span data-stu-id="81555-158">Click the OK button to attach the database.</span></span> <span data-ttu-id="81555-159">La finestra di dialogo Connetti database verrà chiusa e il Esplora oggetti dovrebbe ora elencare il database appena collegato.</span><span class="sxs-lookup"><span data-stu-id="81555-159">The Attach Databases dialog box will close and the Object Explorer should now list the just-attached database.</span></span> <span data-ttu-id="81555-160">È probabile che il database Northwind abbia un nome come `9FE54661B32FDD967F51D71D0D5145CC_LINE ARTICLES\DATATUTORIALS\VOLUME 3\CSHARP\73\ASPNET_DATA_TUTORIAL_75_CS\APP_DATA\NORTHWND.MDF`.</span><span class="sxs-lookup"><span data-stu-id="81555-160">Chances are the Northwind database has a name like `9FE54661B32FDD967F51D71D0D5145CC_LINE ARTICLES\DATATUTORIALS\VOLUME 3\CSHARP\73\ASPNET_DATA_TUTORIAL_75_CS\APP_DATA\NORTHWND.MDF`.</span></span> <span data-ttu-id="81555-161">Rinominare il database in Northwind facendo clic con il pulsante destro del mouse sul database e scegliendo Rinomina.</span><span class="sxs-lookup"><span data-stu-id="81555-161">Rename the database to Northwind by right-clicking on the database and choosing Rename.</span></span>

![Rinominare il database in Northwind](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image5.png)

<span data-ttu-id="81555-163">**Figura 3**: rinominare il database in Northwind</span><span class="sxs-lookup"><span data-stu-id="81555-163">**Figure 3**: Rename the Database to Northwind</span></span>

## <a name="step-2-creating-a-new-solution-and-sql-server-project-in-visual-studio"></a><span data-ttu-id="81555-164">Passaggio 2: creazione di una nuova soluzione e SQL Server progetto in Visual Studio</span><span class="sxs-lookup"><span data-stu-id="81555-164">Step 2: Creating a New Solution and SQL Server Project in Visual Studio</span></span>

<span data-ttu-id="81555-165">Per creare stored procedure o funzioni definite dall'utente gestite in SQL Server 2005, le stored procedure e la logica UDF vengono scritte come Visual Basic codice in una classe.</span><span class="sxs-lookup"><span data-stu-id="81555-165">To create managed stored procedures or UDFs in SQL Server 2005 we will write the stored procedure and UDF logic as Visual Basic code in a class.</span></span> <span data-ttu-id="81555-166">Una volta scritto il codice, è necessario compilare questa classe in un assembly (un file `.dll`), registrare l'assembly con il database SQL Server, quindi creare un stored procedure o un oggetto UDF nel database che punti al metodo corrispondente nell'assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-166">Once the code has been written, we will need to compile this class into an assembly (a `.dll` file), register the assembly with the SQL Server database, and then create a stored procedure or UDF object in the database that points to the corresponding method in the assembly.</span></span> <span data-ttu-id="81555-167">Questi passaggi possono essere eseguiti manualmente.</span><span class="sxs-lookup"><span data-stu-id="81555-167">These steps can all be performed manually.</span></span> <span data-ttu-id="81555-168">È possibile creare il codice in qualsiasi editor di testo, compilarlo dalla riga di comando usando il compilatore di Visual Basic (`vbc.exe`), registrarlo con il database usando il comando [`CREATE ASSEMBLY`](https://msdn.microsoft.com/library/ms189524.aspx) o Management Studio e aggiungere l'oggetto stored procedure o UDF attraverso metodi simili.</span><span class="sxs-lookup"><span data-stu-id="81555-168">We can create the code in any text editor, compile it from the command line using the Visual Basic compiler (`vbc.exe`), register it with the database using the [`CREATE ASSEMBLY`](https://msdn.microsoft.com/library/ms189524.aspx) command or from Management Studio, and add the stored procedure or UDF object through similar means.</span></span> <span data-ttu-id="81555-169">Fortunatamente, le versioni Professional e Team System di Visual Studio includono un tipo di progetto SQL Server che automatizza queste attività.</span><span class="sxs-lookup"><span data-stu-id="81555-169">Fortunately, the Professional and Team Systems versions of Visual Studio include a SQL Server Project type that automates these tasks.</span></span> <span data-ttu-id="81555-170">In questa esercitazione verrà illustrato come usare il tipo di progetto SQL Server per creare un stored procedure gestito e una funzione definita dall'utente.</span><span class="sxs-lookup"><span data-stu-id="81555-170">In this tutorial we will walk through using the SQL Server Project type to create a managed stored procedure and UDF.</span></span>

> [!NOTE]
> <span data-ttu-id="81555-171">Se si usa Visual Web Developer o l'edizione standard di Visual Studio, sarà necessario usare invece l'approccio manuale.</span><span class="sxs-lookup"><span data-stu-id="81555-171">If you are using Visual Web Developer or the Standard edition of Visual Studio, then you will have to use the manual approach instead.</span></span> <span data-ttu-id="81555-172">Il passaggio 13 include istruzioni dettagliate per l'esecuzione manuale di questi passaggi.</span><span class="sxs-lookup"><span data-stu-id="81555-172">Step 13 provides detailed instructions for performing these steps manually.</span></span> <span data-ttu-id="81555-173">Si consiglia di leggere i passaggi da 2 a 12 prima di leggere il passaggio 13, dal momento che questi passaggi includono importanti SQL Server le istruzioni di configurazione che devono essere applicate indipendentemente dalla versione di Visual Studio in uso.</span><span class="sxs-lookup"><span data-stu-id="81555-173">I encourage you to read Steps 2 through 12 before reading Step 13 since these steps include important SQL Server configuration instructions that must be applied regardless of what version of Visual Studio you are using.</span></span>

<span data-ttu-id="81555-174">Per iniziare, aprire Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-174">Start by opening Visual Studio.</span></span> <span data-ttu-id="81555-175">Scegliere nuovo progetto dal menu file per visualizzare la finestra di dialogo nuovo progetto (vedere la figura 4).</span><span class="sxs-lookup"><span data-stu-id="81555-175">From the File menu, choose New Project to display the New Project dialog box (see Figure 4).</span></span> <span data-ttu-id="81555-176">Eseguire il drill-down fino al tipo di progetto di database e quindi, dai modelli elencati a destra, scegliere di creare un nuovo progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-176">Drill down to the Database project type and then, from the Templates listed on the right, choose to create a new SQL Server Project.</span></span> <span data-ttu-id="81555-177">Si è scelto di assegnare un nome al progetto `ManagedDatabaseConstructs` e di posizionarlo all'interno di una soluzione denominata `Tutorial75`.</span><span class="sxs-lookup"><span data-stu-id="81555-177">I have chosen to name this project `ManagedDatabaseConstructs` and placed it within a Solution named `Tutorial75`.</span></span>

<span data-ttu-id="81555-178">[![creare un nuovo progetto SQL Server](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image7.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image6.png)</span><span class="sxs-lookup"><span data-stu-id="81555-178">[![Create a New SQL Server Project](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image7.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image6.png)</span></span>

<span data-ttu-id="81555-179">**Figura 4**: creare un nuovo progetto di SQL Server ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image8.png))</span><span class="sxs-lookup"><span data-stu-id="81555-179">**Figure 4**: Create a New SQL Server Project ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image8.png))</span></span>

<span data-ttu-id="81555-180">Fare clic sul pulsante OK nella finestra di dialogo nuovo progetto per creare la soluzione e il progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-180">Click the OK button in the New Project dialog box to create the Solution and SQL Server Project.</span></span>

<span data-ttu-id="81555-181">Un progetto SQL Server è associato a un database specifico.</span><span class="sxs-lookup"><span data-stu-id="81555-181">A SQL Server Project is tied to a particular database.</span></span> <span data-ttu-id="81555-182">Di conseguenza, dopo aver creato il nuovo progetto di SQL Server viene immediatamente richiesto di specificare queste informazioni.</span><span class="sxs-lookup"><span data-stu-id="81555-182">Consequently, after creating the new SQL Server Project we are immediately asked to specify this information.</span></span> <span data-ttu-id="81555-183">Nella figura 5 viene illustrata la finestra di dialogo nuovo riferimento al database che è stata compilata in modo da puntare al database Northwind registrato nell'istanza del database SQL Server 2005 Express Edition di nuovo nel passaggio 1.</span><span class="sxs-lookup"><span data-stu-id="81555-183">Figure 5 shows the New Database Reference dialog box that has been filled out to point to the Northwind database we registered in the SQL Server 2005 Express Edition database instance back in Step 1.</span></span>

![Associare il progetto SQL Server al database Northwind](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image9.png)

<span data-ttu-id="81555-185">**Figura 5**: associare il progetto SQL Server al database Northwind</span><span class="sxs-lookup"><span data-stu-id="81555-185">**Figure 5**: Associate the SQL Server Project with the Northwind Database</span></span>

<span data-ttu-id="81555-186">Per eseguire il debug delle stored procedure gestite e delle funzioni definite dall'utente create in questo progetto, è necessario abilitare il supporto del debug SQL/CLR per la connessione.</span><span class="sxs-lookup"><span data-stu-id="81555-186">In order to debug the managed stored procedures and UDFs we will create within this project, we need to enable SQL/CLR debugging support for the connection.</span></span> <span data-ttu-id="81555-187">Quando si associa un progetto SQL Server a un nuovo database, come illustrato nella figura 5, Visual Studio chiede se si vuole abilitare il debug SQL/CLR sulla connessione (vedere la figura 6).</span><span class="sxs-lookup"><span data-stu-id="81555-187">Whenever associating a SQL Server Project with a new database (as we did in Figure 5), Visual Studio asks us if we want to enable SQL/CLR debugging on the connection (see Figure 6).</span></span> <span data-ttu-id="81555-188">Scegliere Sì.</span><span class="sxs-lookup"><span data-stu-id="81555-188">Click Yes.</span></span>

![Abilita debug SQL/CLR](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image10.png)

<span data-ttu-id="81555-190">**Figura 6**: abilitare il debug SQL/CLR</span><span class="sxs-lookup"><span data-stu-id="81555-190">**Figure 6**: Enable SQL/CLR Debugging</span></span>

<span data-ttu-id="81555-191">A questo punto il nuovo progetto SQL Server è stato aggiunto alla soluzione.</span><span class="sxs-lookup"><span data-stu-id="81555-191">At this point the new SQL Server Project has been added to the Solution.</span></span> <span data-ttu-id="81555-192">Contiene una cartella denominata `Test Scripts` con un file denominato `Test.sql`, che viene usato per il debug degli oggetti di database gestiti creati nel progetto.</span><span class="sxs-lookup"><span data-stu-id="81555-192">It contains a folder named `Test Scripts` with a file named `Test.sql`, which is used for debugging the managed database objects created in the project.</span></span> <span data-ttu-id="81555-193">Si esaminerà il debug nel passaggio 12.</span><span class="sxs-lookup"><span data-stu-id="81555-193">We will look at debugging in Step 12.</span></span>

<span data-ttu-id="81555-194">È ora possibile aggiungere al progetto nuove stored procedure gestite e funzioni definite dall'utente, ma prima di fare in modo che prima includa l'applicazione Web esistente nella soluzione.</span><span class="sxs-lookup"><span data-stu-id="81555-194">We can now add new managed stored procedures and UDFs to this project, but before we do let s first include our existing web application in the Solution.</span></span> <span data-ttu-id="81555-195">Dal menu file selezionare l'opzione Aggiungi e scegliere sito Web esistente.</span><span class="sxs-lookup"><span data-stu-id="81555-195">From the File menu select the Add option and choose Existing Web Site.</span></span> <span data-ttu-id="81555-196">Passare alla cartella del sito Web appropriata e fare clic su OK.</span><span class="sxs-lookup"><span data-stu-id="81555-196">Browse to the appropriate website folder and click OK.</span></span> <span data-ttu-id="81555-197">Come illustrato nella figura 7, la soluzione verrà aggiornata in modo da includere due progetti: il sito Web e il `ManagedDatabaseConstructs` SQL Server progetto.</span><span class="sxs-lookup"><span data-stu-id="81555-197">As Figure 7 shows, this will update the Solution to include two projects: the website and the `ManagedDatabaseConstructs` SQL Server Project.</span></span>

![Il Esplora soluzioni include ora due progetti](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image11.png)

<span data-ttu-id="81555-199">**Figura 7**: il Esplora soluzioni include ora due progetti</span><span class="sxs-lookup"><span data-stu-id="81555-199">**Figure 7**: The Solution Explorer Now Includes Two Projects</span></span>

<span data-ttu-id="81555-200">Il valore `NORTHWNDConnectionString` in `Web.config` fa riferimento al file `NORTHWND.MDF` nella cartella `App_Data`.</span><span class="sxs-lookup"><span data-stu-id="81555-200">The `NORTHWNDConnectionString` value in `Web.config` currently references the `NORTHWND.MDF` file in the `App_Data` folder.</span></span> <span data-ttu-id="81555-201">Poiché il database è stato rimosso da `App_Data` ed è stato registrato in modo esplicito nell'istanza del database SQL Server 2005 Express Edition, è necessario aggiornare in modo corrispondente il valore di `NORTHWNDConnectionString`.</span><span class="sxs-lookup"><span data-stu-id="81555-201">Since we removed this database from `App_Data` and explicitly registered it in the SQL Server 2005 Express Edition database instance, we need to correspondingly update the `NORTHWNDConnectionString` value.</span></span> <span data-ttu-id="81555-202">Aprire il file `Web.config` nel sito Web e modificare il valore di `NORTHWNDConnectionString` in modo che la stringa di connessione legga: `Data Source=localhost\SQLExpress;Initial Catalog=Northwind;Integrated Security=True`.</span><span class="sxs-lookup"><span data-stu-id="81555-202">Open the `Web.config` file in the website and change the `NORTHWNDConnectionString` value so that the connection string reads: `Data Source=localhost\SQLExpress;Initial Catalog=Northwind;Integrated Security=True`.</span></span> <span data-ttu-id="81555-203">Dopo questa modifica, la sezione `<connectionStrings>` in `Web.config` dovrebbe essere simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="81555-203">After this change, your `<connectionStrings>` section in `Web.config` should look similar to the following:</span></span>

[!code-xml[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample1.xml)]

> [!NOTE]
> <span data-ttu-id="81555-204">Come illustrato nell' [esercitazione precedente](debugging-stored-procedures-vb.md), quando si esegue il debug di un oggetto SQL Server da un'applicazione client, ad esempio un sito Web ASP.NET, è necessario disabilitare il pool di connessioni.</span><span class="sxs-lookup"><span data-stu-id="81555-204">As discussed in the [preceding tutorial](debugging-stored-procedures-vb.md), when debugging a SQL Server object from a client application, such as an ASP.NET website, we need to disable connection pooling.</span></span> <span data-ttu-id="81555-205">La stringa di connessione mostrata in precedenza Disabilita il pool di connessioni (`Pooling=false`).</span><span class="sxs-lookup"><span data-stu-id="81555-205">The connection string shown above disables connection pooling ( `Pooling=false` ).</span></span> <span data-ttu-id="81555-206">Se non si prevede di eseguire il debug delle stored procedure gestite e delle funzioni definite dall'utente dal sito Web ASP.NET, abilitare il pool di connessioni.</span><span class="sxs-lookup"><span data-stu-id="81555-206">If you do not plan on debugging the managed stored procedures and UDFs from the ASP.NET website, enable connection pooling.</span></span>

## <a name="step-3-creating-a-managed-stored-procedure"></a><span data-ttu-id="81555-207">Passaggio 3: creazione di una stored procedure gestita</span><span class="sxs-lookup"><span data-stu-id="81555-207">Step 3: Creating a Managed Stored Procedure</span></span>

<span data-ttu-id="81555-208">Per aggiungere un stored procedure gestito al database Northwind, è necessario innanzitutto creare il stored procedure come metodo nel progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-208">To add a managed stored procedure to the Northwind database we first need to create the stored procedure as a method in the SQL Server Project.</span></span> <span data-ttu-id="81555-209">Dal Esplora soluzioni fare clic con il pulsante destro del mouse sul nome del progetto `ManagedDatabaseConstructs` e scegliere di aggiungere un nuovo elemento.</span><span class="sxs-lookup"><span data-stu-id="81555-209">From the Solution Explorer, right-click on the `ManagedDatabaseConstructs` project name and choose to add a new item.</span></span> <span data-ttu-id="81555-210">Verrà visualizzata la finestra di dialogo Aggiungi nuovo elemento, in cui sono elencati i tipi di oggetti di database gestiti che è possibile aggiungere al progetto.</span><span class="sxs-lookup"><span data-stu-id="81555-210">This will display the Add New Item dialog box, which lists the types of managed database objects that can be added to the project.</span></span> <span data-ttu-id="81555-211">Come illustrato nella figura 8, tra gli altri sono incluse stored procedure e funzioni definite dall'utente.</span><span class="sxs-lookup"><span data-stu-id="81555-211">As Figure 8 shows, this includes stored procedures and User-Defined Functions, among others.</span></span>

<span data-ttu-id="81555-212">Inizia con l'aggiunta di un stored procedure che restituisce semplicemente tutti i prodotti che non sono più disponibili.</span><span class="sxs-lookup"><span data-stu-id="81555-212">Let s start by adding a stored procedure that simply returns all of the products that have been discontinued.</span></span> <span data-ttu-id="81555-213">Denominare il nuovo file di stored procedure `GetDiscontinuedProducts.vb`.</span><span class="sxs-lookup"><span data-stu-id="81555-213">Name the new stored procedure file `GetDiscontinuedProducts.vb`.</span></span>

<span data-ttu-id="81555-214">[![aggiungere una nuova stored procedure denominata GetDiscontinuedProducts. vb](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image13.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image12.png)</span><span class="sxs-lookup"><span data-stu-id="81555-214">[![Add a New Stored Procedure Named GetDiscontinuedProducts.vb](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image13.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image12.png)</span></span>

<span data-ttu-id="81555-215">**Figura 8**: aggiungere una nuova stored Procedure denominata `GetDiscontinuedProducts.vb` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image14.png))</span><span class="sxs-lookup"><span data-stu-id="81555-215">**Figure 8**: Add a New Stored Procedure Named `GetDiscontinuedProducts.vb` ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image14.png))</span></span>

<span data-ttu-id="81555-216">Verrà creato un nuovo file di classe Visual Basic con il contenuto seguente:</span><span class="sxs-lookup"><span data-stu-id="81555-216">This will create a new Visual Basic class file with the following content:</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample2.vb)]

<span data-ttu-id="81555-217">Si noti che il stored procedure viene implementato come metodo di `Shared` all'interno di un file di classe `Partial` denominato `StoredProcedures`.</span><span class="sxs-lookup"><span data-stu-id="81555-217">Note that the stored procedure is implemented as a `Shared` method within a `Partial` class file named `StoredProcedures`.</span></span> <span data-ttu-id="81555-218">Il metodo `GetDiscontinuedProducts` viene inoltre decorato con l' [attributo`SqlProcedure`](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlprocedureattribute.aspx), che contrassegna il metodo come stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-218">Moreover, the `GetDiscontinuedProducts` method is decorated with the [`SqlProcedure` attribute](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlprocedureattribute.aspx), which marks the method as a stored procedure.</span></span>

<span data-ttu-id="81555-219">Il codice seguente crea un oggetto `SqlCommand` e imposta il relativo `CommandText` su una query `SELECT` che restituisce tutte le colonne dalla tabella `Products` per i prodotti il cui campo `Discontinued` è uguale a 1.</span><span class="sxs-lookup"><span data-stu-id="81555-219">The following code creates a `SqlCommand` object and sets its `CommandText` to a `SELECT` query that returns all of the columns from the `Products` table for products whose `Discontinued` field equals 1.</span></span> <span data-ttu-id="81555-220">Esegue quindi il comando e invia i risultati all'applicazione client.</span><span class="sxs-lookup"><span data-stu-id="81555-220">It then executes the command and sends the results back to the client application.</span></span> <span data-ttu-id="81555-221">Aggiungere questo codice al metodo `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-221">Add this code to the `GetDiscontinuedProducts` method.</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample3.vb)]

<span data-ttu-id="81555-222">Tutti gli oggetti di database gestiti possono accedere a un [oggetto`SqlContext`](https://msdn.microsoft.com/library/ms131108.aspx) che rappresenta il contesto del chiamante.</span><span class="sxs-lookup"><span data-stu-id="81555-222">All managed database objects have access to a [`SqlContext` object](https://msdn.microsoft.com/library/ms131108.aspx) that represents the context of the caller.</span></span> <span data-ttu-id="81555-223">Il `SqlContext` fornisce l'accesso a un [oggetto`SqlPipe`](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.aspx) tramite la relativa [Proprietà`Pipe`](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlcontext.pipe.aspx).</span><span class="sxs-lookup"><span data-stu-id="81555-223">The `SqlContext` provides access to a [`SqlPipe` object](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.aspx) via its [`Pipe` property](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlcontext.pipe.aspx).</span></span> <span data-ttu-id="81555-224">Questo oggetto `SqlPipe` viene usato per trasferire le informazioni tra il database SQL Server e l'applicazione chiamante.</span><span class="sxs-lookup"><span data-stu-id="81555-224">This `SqlPipe` object is used to ferry information between the SQL Server database and the calling application.</span></span> <span data-ttu-id="81555-225">Come suggerisce il nome, il [metodo`ExecuteAndSend`](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.executeandsend.aspx) esegue un oggetto `SqlCommand` passato e invia i risultati all'applicazione client.</span><span class="sxs-lookup"><span data-stu-id="81555-225">As its name implies, the [`ExecuteAndSend` method](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.executeandsend.aspx) executes a passed-in `SqlCommand` object and sends the results back to the client application.</span></span>

> [!NOTE]
> <span data-ttu-id="81555-226">Gli oggetti di database gestiti sono ideali per stored procedure e funzioni definite dall'utente che utilizzano la logica procedurale invece della logica basata su set.</span><span class="sxs-lookup"><span data-stu-id="81555-226">Managed database objects are best suited for stored procedures and UDFs that use procedural logic rather than set-based logic.</span></span> <span data-ttu-id="81555-227">La logica procedurale comporta l'utilizzo di set di dati per riga o per l'utilizzo di dati scalari.</span><span class="sxs-lookup"><span data-stu-id="81555-227">Procedural logic involves working with sets of data on a row-by-row basis or working with scalar data.</span></span> <span data-ttu-id="81555-228">Il metodo `GetDiscontinuedProducts` appena creato, tuttavia, non implica alcuna logica procedurale.</span><span class="sxs-lookup"><span data-stu-id="81555-228">The `GetDiscontinuedProducts` method we just created, however, involves no procedural logic.</span></span> <span data-ttu-id="81555-229">Pertanto, sarebbe idealmente implementato come stored procedure T-SQL.</span><span class="sxs-lookup"><span data-stu-id="81555-229">Therefore, it would ideally be implemented as a T-SQL stored procedure.</span></span> <span data-ttu-id="81555-230">Viene implementato come stored procedure gestito per illustrare i passaggi necessari per la creazione e la distribuzione di stored procedure gestite.</span><span class="sxs-lookup"><span data-stu-id="81555-230">It is implemented as a managed stored procedure to demonstrate the steps necessary for creating and deploying managed stored procedures.</span></span>

## <a name="step-4-deploying-the-managed-stored-procedure"></a><span data-ttu-id="81555-231">Passaggio 4: distribuzione della stored procedure gestita</span><span class="sxs-lookup"><span data-stu-id="81555-231">Step 4: Deploying the Managed Stored Procedure</span></span>

<span data-ttu-id="81555-232">Con questo codice, è possibile eseguire la distribuzione nel database Northwind.</span><span class="sxs-lookup"><span data-stu-id="81555-232">With this code complete, we are ready to deploy it to the Northwind database.</span></span> <span data-ttu-id="81555-233">La distribuzione di un progetto SQL Server compilerà il codice in un assembly, registrerà l'assembly con il database e creerà gli oggetti corrispondenti nel database e li collegherà ai metodi appropriati nell'assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-233">Deploying a SQL Server Project compiles the code into an assembly, registers the assembly with the database, and creates the corresponding objects in the database, linking them to the appropriate methods in the assembly.</span></span> <span data-ttu-id="81555-234">Il set esatto di attività eseguite dall'opzione Deploy è stato scritto più precisamente nel passaggio 13.</span><span class="sxs-lookup"><span data-stu-id="81555-234">The exact set of tasks performed by the Deploy option is more precisely spelled out in Step 13.</span></span> <span data-ttu-id="81555-235">Fare clic con il pulsante destro del mouse sul nome del progetto `ManagedDatabaseConstructs` nel Esplora soluzioni e scegliere l'opzione Distribuisci.</span><span class="sxs-lookup"><span data-stu-id="81555-235">Right-click on the `ManagedDatabaseConstructs` project name in the Solution Explorer and choose the Deploy option.</span></span> <span data-ttu-id="81555-236">Tuttavia, la distribuzione ha esito negativo con l'errore seguente: sintassi non corretta in prossimità di ' EXTERNAL '.</span><span class="sxs-lookup"><span data-stu-id="81555-236">However, deployment fails with the following error: Incorrect syntax near 'EXTERNAL'.</span></span> <span data-ttu-id="81555-237">Per abilitare tale caratteristica può essere necessario impostare su un valore superiore il livello di compatibilità del database corrente.</span><span class="sxs-lookup"><span data-stu-id="81555-237">You may need to set the compatibility level of the current database to a higher value to enable this feature.</span></span> <span data-ttu-id="81555-238">Vedere la guida per la `sp_dbcmptlevel`di stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-238">See help for the stored procedure `sp_dbcmptlevel`.</span></span>

<span data-ttu-id="81555-239">Questo messaggio di errore si verifica quando si tenta di registrare l'assembly con il database Northwind.</span><span class="sxs-lookup"><span data-stu-id="81555-239">This error message occurs when attempting to register the assembly with the Northwind database.</span></span> <span data-ttu-id="81555-240">Per registrare un assembly con un database SQL Server 2005, è necessario che il livello di compatibilità del database sia impostato su 90.</span><span class="sxs-lookup"><span data-stu-id="81555-240">In order to register an assembly with a SQL Server 2005 database, the database s compatibility level must be set to 90.</span></span> <span data-ttu-id="81555-241">Per impostazione predefinita, i nuovi database SQL Server 2005 hanno un livello di compatibilità pari a 90.</span><span class="sxs-lookup"><span data-stu-id="81555-241">By default, new SQL Server 2005 databases have a compatibility level of 90.</span></span> <span data-ttu-id="81555-242">Tuttavia, i database creati con Microsoft SQL Server 2000 hanno un livello di compatibilità predefinito pari a 80.</span><span class="sxs-lookup"><span data-stu-id="81555-242">However, databases created using Microsoft SQL Server 2000 have a default compatibility level of 80.</span></span> <span data-ttu-id="81555-243">Poiché il database Northwind inizialmente era un database di Microsoft SQL Server 2000, il livello di compatibilità è attualmente impostato su 80 e pertanto deve essere aumentato a 90 per poter registrare gli oggetti di database gestiti.</span><span class="sxs-lookup"><span data-stu-id="81555-243">Since the Northwind database was initially a Microsoft SQL Server 2000 database, its compatibility level is currently set to 80 and therefore needs to be increased to 90 in order to register managed database objects.</span></span>

<span data-ttu-id="81555-244">Per aggiornare il livello di compatibilità del database, aprire una nuova finestra di query in Management Studio e immettere:</span><span class="sxs-lookup"><span data-stu-id="81555-244">To update the database s compatibility level, open a New Query window in Management Studio and enter:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample4.sql)]

<span data-ttu-id="81555-245">Fare clic sull'icona Esegui sulla barra degli strumenti per eseguire la query precedente.</span><span class="sxs-lookup"><span data-stu-id="81555-245">Click the Execute icon in the Toolbar to run the above query.</span></span>

<span data-ttu-id="81555-246">[![aggiornare il livello di compatibilità del database Northwind](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image16.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image15.png)</span><span class="sxs-lookup"><span data-stu-id="81555-246">[![Update the Northwind Database s Compatibility Level](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image16.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image15.png)</span></span>

<span data-ttu-id="81555-247">**Figura 9**: aggiornare il livello di compatibilità del database Northwind ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image17.png))</span><span class="sxs-lookup"><span data-stu-id="81555-247">**Figure 9**: Update the Northwind Database s Compatibility Level ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image17.png))</span></span>

<span data-ttu-id="81555-248">Dopo aver aggiornato il livello di compatibilità, ridistribuire il progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-248">After updating the compatibility level, redeploy the SQL Server Project.</span></span> <span data-ttu-id="81555-249">Questa volta la distribuzione deve essere completata senza errori.</span><span class="sxs-lookup"><span data-stu-id="81555-249">This time the deployment should complete without error.</span></span>

<span data-ttu-id="81555-250">Tornare alla SQL Server Management Studio, fare clic con il pulsante destro del mouse sul database Northwind nella Esplora oggetti e scegliere Aggiorna.</span><span class="sxs-lookup"><span data-stu-id="81555-250">Return to SQL Server Management Studio, right-click on the Northwind database in the Object Explorer, and choose Refresh.</span></span> <span data-ttu-id="81555-251">Successivamente, eseguire il drill-down nella cartella Programmabilità, quindi espandere la cartella assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-251">Next, drill down into the Programmability folder and then expand the Assemblies folder.</span></span> <span data-ttu-id="81555-252">Come illustrato nella figura 10, il database Northwind include ora l'assembly generato dal progetto `ManagedDatabaseConstructs`.</span><span class="sxs-lookup"><span data-stu-id="81555-252">As Figure 10 shows, the Northwind database now includes the assembly generated by the `ManagedDatabaseConstructs` project.</span></span>

![L'assembly ManagedDatabaseConstructs è ora registrato con il database Northwind](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image18.png)

<span data-ttu-id="81555-254">**Figura 10**: l'assembly `ManagedDatabaseConstructs` è ora registrato con il database Northwind</span><span class="sxs-lookup"><span data-stu-id="81555-254">**Figure 10**: The `ManagedDatabaseConstructs` Assembly is Now Registered with the Northwind Database</span></span>

<span data-ttu-id="81555-255">Espandere anche la cartella stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-255">Also expand the Stored Procedures folder.</span></span> <span data-ttu-id="81555-256">Qui viene visualizzato un stored procedure denominato `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-256">There you will see a stored procedure named `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="81555-257">Questo stored procedure è stato creato dal processo di distribuzione e punta al metodo `GetDiscontinuedProducts` nell'assembly di `ManagedDatabaseConstructs`.</span><span class="sxs-lookup"><span data-stu-id="81555-257">This stored procedure was created by the deployment process and points to the `GetDiscontinuedProducts` method in the `ManagedDatabaseConstructs` assembly.</span></span> <span data-ttu-id="81555-258">Quando viene eseguito il `GetDiscontinuedProducts` stored procedure, esegue a sua volta il metodo di `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-258">When the `GetDiscontinuedProducts` stored procedure is executed, it, in turn, executes the `GetDiscontinuedProducts` method.</span></span> <span data-ttu-id="81555-259">Poiché si tratta di una stored procedure gestita, non può essere modificata tramite Management Studio (quindi l'icona del lucchetto accanto al nome del stored procedure).</span><span class="sxs-lookup"><span data-stu-id="81555-259">Since this is a managed stored procedure it cannot be edited through Management Studio (hence the lock icon next to the stored procedure name).</span></span>

![La stored procedure GetDiscontinuedProducts viene elencata nella cartella stored procedure](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image19.png)

<span data-ttu-id="81555-261">**Figura 11**: la stored procedure `GetDiscontinuedProducts` viene elencata nella cartella stored procedure</span><span class="sxs-lookup"><span data-stu-id="81555-261">**Figure 11**: The `GetDiscontinuedProducts` Stored Procedure is Listed in the Stored Procedures Folder</span></span>

<span data-ttu-id="81555-262">È ancora necessario superare un ostacolo prima di poter chiamare il stored procedure gestito: il database è configurato per impedire l'esecuzione del codice gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-262">There is still one more hurdle we have to overcome before we can call the managed stored procedure: the database is configured to prevent execution of managed code.</span></span> <span data-ttu-id="81555-263">Per verificarlo, aprire una nuova finestra di query ed eseguire il `GetDiscontinuedProducts` stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-263">Verify this by opening a new query window and executing the `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="81555-264">Verrà visualizzato il messaggio di errore seguente: l'esecuzione del codice utente nel .NET Framework è disabilitata.</span><span class="sxs-lookup"><span data-stu-id="81555-264">You will receive the following error message: Execution of user code in the .NET Framework is disabled.</span></span> <span data-ttu-id="81555-265">Abilitare l'opzione di configurazione CLR enabled.</span><span class="sxs-lookup"><span data-stu-id="81555-265">Enable �clr enabled configuration option.</span></span>

<span data-ttu-id="81555-266">Per esaminare le informazioni di configurazione del database Northwind, immettere ed eseguire il comando `exec sp_configure` nella finestra query.</span><span class="sxs-lookup"><span data-stu-id="81555-266">To examine the Northwind database s configuration information, enter and execute the command `exec sp_configure` in the query window.</span></span> <span data-ttu-id="81555-267">Ciò indica che l'impostazione CLR enabled è attualmente impostata su 0.</span><span class="sxs-lookup"><span data-stu-id="81555-267">This shows that the clr enabled setting is currently set to 0.</span></span>

<span data-ttu-id="81555-268">[![l'impostazione CLR enabled è attualmente impostata su 0](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image21.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image20.png)</span><span class="sxs-lookup"><span data-stu-id="81555-268">[![The clr enabled Setting is Currently Set to 0](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image21.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image20.png)</span></span>

<span data-ttu-id="81555-269">**Figura 12**: l'impostazione CLR enabled è attualmente impostata su 0 ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image22.png))</span><span class="sxs-lookup"><span data-stu-id="81555-269">**Figure 12**: The clr enabled Setting is Currently Set to 0 ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image22.png))</span></span>

<span data-ttu-id="81555-270">Si noti che per ogni impostazione di configurazione nella figura 12 sono elencati quattro valori: i valori minimo e massimo e i valori di configurazione ed esecuzione.</span><span class="sxs-lookup"><span data-stu-id="81555-270">Note that each configuration setting in Figure 12 has four values listed with it: the minimum and maximum values and the config and run values.</span></span> <span data-ttu-id="81555-271">Per aggiornare il valore di configurazione per l'impostazione CLR enabled, eseguire il comando seguente:</span><span class="sxs-lookup"><span data-stu-id="81555-271">To update the config value for the clr enabled setting, execute the following command:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample5.sql)]

<span data-ttu-id="81555-272">Se si esegue di nuovo il `exec sp_configure` si noterà che l'istruzione precedente ha aggiornato il valore di configurazione di CLR enabled a 1, ma il valore di esecuzione è ancora impostato su 0.</span><span class="sxs-lookup"><span data-stu-id="81555-272">If you re-run the `exec sp_configure` you will see that the above statement updated the clr enabled setting s config value to 1, but that the run value is still set to 0.</span></span> <span data-ttu-id="81555-273">Affinché questa modifica di configurazione abbia effetto, è necessario eseguire il [comando`RECONFIGURE`](https://msdn.microsoft.com/library/ms176069.aspx), che consente di impostare il valore di esecuzione sul valore di configurazione corrente.</span><span class="sxs-lookup"><span data-stu-id="81555-273">For this configuration change to take affect we need to execute the [`RECONFIGURE` command](https://msdn.microsoft.com/library/ms176069.aspx), which will set the run value to the current config value.</span></span> <span data-ttu-id="81555-274">È sufficiente immettere `RECONFIGURE` nella finestra di query e fare clic sull'icona Esegui sulla barra degli strumenti.</span><span class="sxs-lookup"><span data-stu-id="81555-274">Simply enter `RECONFIGURE` in the query window and click the Execute icon in the Toolbar.</span></span> <span data-ttu-id="81555-275">Se si esegue `exec sp_configure` ora verrà visualizzato il valore 1 per il file di configurazione e i valori di esecuzione di CLR enabled.</span><span class="sxs-lookup"><span data-stu-id="81555-275">If you run `exec sp_configure` now you should see a value of 1 for the clr enabled setting s config and run values.</span></span>

<span data-ttu-id="81555-276">Con la configurazione abilitata per CLR completata, è possibile eseguire il stored procedure di `GetDiscontinuedProducts` gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-276">With the clr enabled configuration complete, we are ready to run the managed `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="81555-277">Nella finestra della query immettere ed eseguire il comando `exec` `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-277">In the query window enter and execute the command `exec` `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="81555-278">Richiamando il stored procedure si determina l'esecuzione del codice gestito corrispondente nel `GetDiscontinuedProducts` metodo.</span><span class="sxs-lookup"><span data-stu-id="81555-278">Invoking the stored procedure causes the corresponding managed code in the `GetDiscontinuedProducts` method to execute.</span></span> <span data-ttu-id="81555-279">Questo codice genera una query `SELECT` per restituire tutti i prodotti che non sono più disponibili e restituisce tali dati all'applicazione chiamante, che è SQL Server Management Studio in questa istanza.</span><span class="sxs-lookup"><span data-stu-id="81555-279">This code issues a `SELECT` query to return all products that are discontinued and returns this data to the calling application, which is SQL Server Management Studio in this instance.</span></span> <span data-ttu-id="81555-280">Management Studio riceve questi risultati e li Visualizza nella finestra risultati.</span><span class="sxs-lookup"><span data-stu-id="81555-280">Management Studio receives these results and displays them in the Results window.</span></span>

<span data-ttu-id="81555-281">[![la stored procedure GetDiscontinuedProducts restituisce tutti i prodotti non più disponibili](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image24.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image23.png)</span><span class="sxs-lookup"><span data-stu-id="81555-281">[![The GetDiscontinuedProducts Stored Procedure Returns All Discontinued Products](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image24.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image23.png)</span></span>

<span data-ttu-id="81555-282">**Figura 13**: la stored procedure `GetDiscontinuedProducts` restituisce tutti i prodotti non più disponibili ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image25.png))</span><span class="sxs-lookup"><span data-stu-id="81555-282">**Figure 13**: The `GetDiscontinuedProducts` Stored Procedure Returns All Discontinued Products ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image25.png))</span></span>

## <a name="step-5-creating-managed-stored-procedures-that-accept-input-parameters"></a><span data-ttu-id="81555-283">Passaggio 5: creazione di stored procedure gestite che accettano parametri di input</span><span class="sxs-lookup"><span data-stu-id="81555-283">Step 5: Creating Managed Stored Procedures that Accept Input Parameters</span></span>

<span data-ttu-id="81555-284">Molte delle query e delle stored procedure create in queste esercitazioni hanno usato *parametri*.</span><span class="sxs-lookup"><span data-stu-id="81555-284">Many of the queries and stored procedures we have created throughout these tutorials have used *parameters*.</span></span> <span data-ttu-id="81555-285">Ad esempio, nell'esercitazione [creazione di nuove stored procedure per i TableAdapter del set di dati tipizzati](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md) è stato creato un stored procedure denominato `GetProductsByCategoryID` che accettava un parametro di input denominato `@CategoryID`.</span><span class="sxs-lookup"><span data-stu-id="81555-285">For example, in the [Creating New Stored Procedures for the Typed DataSet s TableAdapters](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md) tutorial we created a stored procedure named `GetProductsByCategoryID` that accepted an input parameter named `@CategoryID`.</span></span> <span data-ttu-id="81555-286">Il stored procedure restituito tutti i prodotti il cui campo `CategoryID` corrisponde al valore del parametro `@CategoryID` fornito.</span><span class="sxs-lookup"><span data-stu-id="81555-286">The stored procedure then returned all products whose `CategoryID` field matched the value of the supplied `@CategoryID` parameter.</span></span>

<span data-ttu-id="81555-287">Per creare un stored procedure gestito che accetta parametri di input, è sufficiente specificare i parametri nella definizione del metodo.</span><span class="sxs-lookup"><span data-stu-id="81555-287">To create a managed stored procedure that accepts input parameters, simply specify those parameters in the method s definition.</span></span> <span data-ttu-id="81555-288">Per illustrare questo problema, aggiungere un'altra stored procedure gestita al progetto `ManagedDatabaseConstructs` denominato `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-288">To illustrate this, let s add another managed stored procedure to the `ManagedDatabaseConstructs` project named `GetProductsWithPriceLessThan`.</span></span> <span data-ttu-id="81555-289">Questo stored procedure gestito accetterà un parametro di input che specifica un prezzo e restituirà tutti i prodotti il cui campo `UnitPrice` è inferiore al valore del parametro.</span><span class="sxs-lookup"><span data-stu-id="81555-289">This managed stored procedure will accept an input parameter specifying a price and will return all products whose `UnitPrice` field is less than the parameter s value.</span></span>

<span data-ttu-id="81555-290">Per aggiungere un nuovo stored procedure al progetto, fare clic con il pulsante destro del mouse sul nome del progetto `ManagedDatabaseConstructs` e scegliere di aggiungere un nuovo stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-290">To add a new stored procedure to the project, right-click on the `ManagedDatabaseConstructs` project name and choose to add a new stored procedure.</span></span> <span data-ttu-id="81555-291">Denominare il file `GetProductsWithPriceLessThan.vb`.</span><span class="sxs-lookup"><span data-stu-id="81555-291">Name the file `GetProductsWithPriceLessThan.vb`.</span></span> <span data-ttu-id="81555-292">Come illustrato nel passaggio 3, viene creato un nuovo file di classe di Visual Basic con un metodo denominato `GetProductsWithPriceLessThan` inserito all'interno della classe `Partial` `StoredProcedures`.</span><span class="sxs-lookup"><span data-stu-id="81555-292">As we saw in Step 3, this will create a new Visual Basic class file with a method named `GetProductsWithPriceLessThan` placed within the `Partial` class `StoredProcedures`.</span></span>

<span data-ttu-id="81555-293">Aggiornare la definizione del metodo `GetProductsWithPriceLessThan` in modo che accetti un parametro di input [`SqlMoney`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.aspx) denominato `price` e scrivere il codice per l'esecuzione e restituire i risultati della query:</span><span class="sxs-lookup"><span data-stu-id="81555-293">Update the `GetProductsWithPriceLessThan` method s definition so that it accepts a [`SqlMoney`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.aspx) input parameter named `price` and write the code to execute and return the query results:</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample6.vb)]

<span data-ttu-id="81555-294">La definizione e il codice del `GetProductsWithPriceLessThan` metodo è molto simile alla definizione e al codice del metodo di `GetDiscontinuedProducts` creato nel passaggio 3.</span><span class="sxs-lookup"><span data-stu-id="81555-294">The `GetProductsWithPriceLessThan` method s definition and code closely resembles the definition and code of the `GetDiscontinuedProducts` method created in Step 3.</span></span> <span data-ttu-id="81555-295">Le uniche differenze sono che il metodo `GetProductsWithPriceLessThan` accetta come parametro di input (`price`), la query `SqlCommand` s include un parametro (`@MaxPrice`) e un parametro viene aggiunto alla raccolta `SqlCommand` s `Parameters` e assegna il valore della variabile `price`.</span><span class="sxs-lookup"><span data-stu-id="81555-295">The only differences are that the `GetProductsWithPriceLessThan` method accepts as input parameter (`price`), the `SqlCommand` s query includes a parameter (`@MaxPrice`), and a parameter is added to the `SqlCommand` s `Parameters` collection is and assigned the value of the `price` variable.</span></span>

<span data-ttu-id="81555-296">Una volta aggiunto il codice, ridistribuire il progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-296">After adding this code, redeploy the SQL Server Project.</span></span> <span data-ttu-id="81555-297">Tornare quindi a SQL Server Management Studio e aggiornare la cartella stored procedure.</span><span class="sxs-lookup"><span data-stu-id="81555-297">Next, return to SQL Server Management Studio and Refresh the Stored Procedures folder.</span></span> <span data-ttu-id="81555-298">Verrà visualizzata una nuova voce `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-298">You should see a new entry, `GetProductsWithPriceLessThan`.</span></span> <span data-ttu-id="81555-299">Da una finestra di query, immettere ed eseguire il comando `exec GetProductsWithPriceLessThan 25`, che elenca tutti i prodotti minori di $25, come illustrato nella figura 14.</span><span class="sxs-lookup"><span data-stu-id="81555-299">From a query window, enter and execute the command `exec GetProductsWithPriceLessThan 25`, which will list all products less than $25, as Figure 14 shows.</span></span>

<span data-ttu-id="81555-300">[vengono visualizzati i prodotti ![sotto $25](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image27.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image26.png)</span><span class="sxs-lookup"><span data-stu-id="81555-300">[![Products Under $25 are Displayed](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image27.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image26.png)</span></span>

<span data-ttu-id="81555-301">**Figura 14**: vengono visualizzati i prodotti sotto $25 ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image28.png))</span><span class="sxs-lookup"><span data-stu-id="81555-301">**Figure 14**: Products Under $25 are Displayed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image28.png))</span></span>

## <a name="step-6-calling-the-managed-stored-procedure-from-the-data-access-layer"></a><span data-ttu-id="81555-302">Passaggio 6: chiamata della stored procedure gestita dal livello di accesso ai dati</span><span class="sxs-lookup"><span data-stu-id="81555-302">Step 6: Calling the Managed Stored Procedure from the Data Access Layer</span></span>

<span data-ttu-id="81555-303">A questo punto è stato aggiunto il `GetDiscontinuedProducts` e `GetProductsWithPriceLessThan` stored procedure gestite al progetto `ManagedDatabaseConstructs` e sono state registrate con il database Northwind SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-303">At this point we have added the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures to the `ManagedDatabaseConstructs` project and have registered them with the Northwind SQL Server database.</span></span> <span data-ttu-id="81555-304">Queste stored procedure gestite sono state richiamate anche da SQL Server Management Studio (vedere figure 13 e 14).</span><span class="sxs-lookup"><span data-stu-id="81555-304">We also invoked these managed stored procedures from SQL Server Management Studio (see Figures 13 and 14).</span></span> <span data-ttu-id="81555-305">Per consentire all'applicazione ASP.NET di usare queste stored procedure gestite, tuttavia, è necessario aggiungerle ai livelli di accesso ai dati e della logica di business nell'architettura.</span><span class="sxs-lookup"><span data-stu-id="81555-305">In order for our ASP.NET application to use these managed stored procedures, however, we need to add them to the Data Access and Business Logic Layers in the architecture.</span></span> <span data-ttu-id="81555-306">In questo passaggio si aggiungeranno due nuovi metodi al `ProductsTableAdapter` nel set di dati tipizzato `NorthwindWithSprocs`, che è stato inizialmente creato nell'esercitazione [creazione di nuove stored procedure per il set di dati tipizzati s TableAdapters](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md) .</span><span class="sxs-lookup"><span data-stu-id="81555-306">In this step we will add two new methods to the `ProductsTableAdapter` in the `NorthwindWithSprocs` Typed DataSet, which was initially created in the [Creating New Stored Procedures for the Typed DataSet s TableAdapters](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md) tutorial.</span></span> <span data-ttu-id="81555-307">Nel passaggio 7 si aggiungono i metodi corrispondenti a BLL.</span><span class="sxs-lookup"><span data-stu-id="81555-307">In Step 7 we will add corresponding methods to the BLL.</span></span>

<span data-ttu-id="81555-308">Aprire il `NorthwindWithSprocs` DataSet tipizzato in Visual Studio e iniziare aggiungendo un nuovo metodo al `ProductsTableAdapter` denominato `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-308">Open the `NorthwindWithSprocs` Typed DataSet in Visual Studio and start by adding a new method to the `ProductsTableAdapter` named `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="81555-309">Per aggiungere un nuovo metodo a un TableAdapter, fare clic con il pulsante destro del mouse sul nome del TableAdapter nella finestra di progettazione e scegliere l'opzione Aggiungi query dal menu di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="81555-309">To add a new method to a TableAdapter, right-click on the TableAdapter s name in the Designer and choose the Add Query option from the context menu.</span></span>

> [!NOTE]
> <span data-ttu-id="81555-310">Poiché il database Northwind è stato spostato dalla cartella `App_Data` all'istanza del database SQL Server 2005 Express Edition, è necessario aggiornare la stringa di connessione corrispondente in Web. config per riflettere questa modifica.</span><span class="sxs-lookup"><span data-stu-id="81555-310">Since we moved the Northwind database from the `App_Data` folder to the SQL Server 2005 Express Edition database instance, it is imperative that the corresponding connection string in Web.config be updated to reflect this change.</span></span> <span data-ttu-id="81555-311">Nel passaggio 2 è stato illustrato l'aggiornamento del valore `NORTHWNDConnectionString` in `Web.config`.</span><span class="sxs-lookup"><span data-stu-id="81555-311">In Step 2 we discussed updating the `NORTHWNDConnectionString` value in `Web.config`.</span></span> <span data-ttu-id="81555-312">Se si dimentica di eseguire questo aggiornamento, verrà visualizzato il messaggio di errore non è stato possibile aggiungere la query.</span><span class="sxs-lookup"><span data-stu-id="81555-312">If you forgot to make this update, then you will see the error message Failed to add query.</span></span> <span data-ttu-id="81555-313">Impossibile trovare la `NORTHWNDConnectionString` di connessione per `Web.config` oggetto in una finestra di dialogo quando si tenta di aggiungere un nuovo metodo al TableAdapter.</span><span class="sxs-lookup"><span data-stu-id="81555-313">Unable to find connection `NORTHWNDConnectionString` for object `Web.config` in a dialog box when attempting to add a new method to the TableAdapter.</span></span> <span data-ttu-id="81555-314">Per correggere l'errore, fare clic su OK, quindi passare a `Web.config` e aggiornare il valore di `NORTHWNDConnectionString` come descritto nel passaggio 2.</span><span class="sxs-lookup"><span data-stu-id="81555-314">To resolve this error, click OK and then go to `Web.config` and update the `NORTHWNDConnectionString` value as discussed in Step 2.</span></span> <span data-ttu-id="81555-315">Quindi provare ad aggiungere nuovamente il metodo al TableAdapter.</span><span class="sxs-lookup"><span data-stu-id="81555-315">Then try re-adding the method to the TableAdapter.</span></span> <span data-ttu-id="81555-316">Questa volta dovrebbe funzionare senza errori.</span><span class="sxs-lookup"><span data-stu-id="81555-316">This time it should work without error.</span></span>

<span data-ttu-id="81555-317">L'aggiunta di un nuovo metodo avvia la configurazione guidata query TableAdapter, che è stata usata più volte nelle esercitazioni precedenti.</span><span class="sxs-lookup"><span data-stu-id="81555-317">Adding a new method launches the TableAdapter Query Configuration wizard, which we have used many times in past tutorials.</span></span> <span data-ttu-id="81555-318">Il primo passaggio richiede di specificare il modo in cui l'oggetto TableAdapter deve accedere al database: tramite un'istruzione SQL ad hoc o tramite un stored procedure nuovo o esistente.</span><span class="sxs-lookup"><span data-stu-id="81555-318">The first step asks us to specify how the TableAdapter should access the database: through an ad-hoc SQL statement or via a new or existing stored procedure.</span></span> <span data-ttu-id="81555-319">Poiché è già stata creata e registrata la `GetDiscontinuedProducts` stored procedure gestita con il database, scegliere l'opzione Usa stored procedure esistente e fare clic su Avanti.</span><span class="sxs-lookup"><span data-stu-id="81555-319">Since we have already created and registered the `GetDiscontinuedProducts` managed stored procedure with the database, choose the Use existing stored procedure option and hit Next.</span></span>

<span data-ttu-id="81555-320">[![scegliere l'opzione Usa stored procedure esistente](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image30.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image29.png)</span><span class="sxs-lookup"><span data-stu-id="81555-320">[![Choose the Use existing stored procedure Option](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image30.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image29.png)</span></span>

<span data-ttu-id="81555-321">**Figura 15**: scegliere l'opzione Usa stored procedure esistente ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image31.png))</span><span class="sxs-lookup"><span data-stu-id="81555-321">**Figure 15**: Choose the Use existing stored procedure Option ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image31.png))</span></span>

<span data-ttu-id="81555-322">Nella schermata successiva viene richiesto di stored procedure il metodo richiamerà.</span><span class="sxs-lookup"><span data-stu-id="81555-322">The next screen prompts us for the stored procedure the method will invoke.</span></span> <span data-ttu-id="81555-323">Scegliere la stored procedure gestita `GetDiscontinuedProducts` dall'elenco a discesa e fare clic su Avanti.</span><span class="sxs-lookup"><span data-stu-id="81555-323">Choose the `GetDiscontinuedProducts` managed stored procedure from the drop-down list and hit Next.</span></span>

<span data-ttu-id="81555-324">[![selezionare la stored procedure gestita GetDiscontinuedProducts](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image33.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image32.png)</span><span class="sxs-lookup"><span data-stu-id="81555-324">[![Select the GetDiscontinuedProducts Managed Stored Procedure](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image33.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image32.png)</span></span>

<span data-ttu-id="81555-325">**Figura 16**: selezionare la stored Procedure gestita `GetDiscontinuedProducts` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image34.png))</span><span class="sxs-lookup"><span data-stu-id="81555-325">**Figure 16**: Select the `GetDiscontinuedProducts` Managed Stored Procedure ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image34.png))</span></span>

<span data-ttu-id="81555-326">Viene quindi richiesto di specificare se il stored procedure restituisce righe, un valore singolo o niente.</span><span class="sxs-lookup"><span data-stu-id="81555-326">We are then asked to specify whether the stored procedure returns rows, a single value, or nothing.</span></span> <span data-ttu-id="81555-327">Poiché `GetDiscontinuedProducts` restituisce il set di righe di prodotto non più utilizzate, scegliere la prima opzione (dati tabulari) e fare clic su Avanti.</span><span class="sxs-lookup"><span data-stu-id="81555-327">Since `GetDiscontinuedProducts` returns the set of discontinued product rows, choose the first option ( Tabular data ) and click Next.</span></span>

<span data-ttu-id="81555-328">[![selezionare l'opzione dati tabulari](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image36.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image35.png)</span><span class="sxs-lookup"><span data-stu-id="81555-328">[![Select the Tabular Data Option](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image36.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image35.png)</span></span>

<span data-ttu-id="81555-329">**Figura 17**: selezionare l'opzione dati tabulari ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image37.png))</span><span class="sxs-lookup"><span data-stu-id="81555-329">**Figure 17**: Select the Tabular Data Option ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image37.png))</span></span>

<span data-ttu-id="81555-330">La schermata finale della procedura guidata consente di specificare i modelli di accesso ai dati usati e i nomi dei metodi risultanti.</span><span class="sxs-lookup"><span data-stu-id="81555-330">The final wizard screen allows us to specify the data access patterns used and the names of the resulting methods.</span></span> <span data-ttu-id="81555-331">Lasciare selezionate entrambe le caselle di controllo e denominare i metodi `FillByDiscontinued` e `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-331">Leave both checkboxes checked and name the methods `FillByDiscontinued` and `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="81555-332">Fare clic su Fine per completare la procedura guidata.</span><span class="sxs-lookup"><span data-stu-id="81555-332">Click Finish to complete the wizard.</span></span>

<span data-ttu-id="81555-333">[![il nome dei metodi FillByDiscontinued e GetDiscontinuedProducts](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image39.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image38.png)</span><span class="sxs-lookup"><span data-stu-id="81555-333">[![Name the Methods FillByDiscontinued and GetDiscontinuedProducts](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image39.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image38.png)</span></span>

<span data-ttu-id="81555-334">**Figura 18**: assegnare un nome ai metodi `FillByDiscontinued` e `GetDiscontinuedProducts` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image40.png))</span><span class="sxs-lookup"><span data-stu-id="81555-334">**Figure 18**: Name the Methods `FillByDiscontinued` and `GetDiscontinuedProducts` ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image40.png))</span></span>

<span data-ttu-id="81555-335">Ripetere questi passaggi per creare i metodi denominati `FillByPriceLessThan` e `GetProductsWithPriceLessThan` nel `ProductsTableAdapter` per il stored procedure gestito `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-335">Repeat these steps to create methods named `FillByPriceLessThan` and `GetProductsWithPriceLessThan` in the `ProductsTableAdapter` for the `GetProductsWithPriceLessThan` managed stored procedure.</span></span>

<span data-ttu-id="81555-336">Nella figura 19 è illustrata una schermata di progettazione DataSet dopo l'aggiunta dei metodi alla `ProductsTableAdapter` per le stored procedure `GetDiscontinuedProducts` e `GetProductsWithPriceLessThan` gestite.</span><span class="sxs-lookup"><span data-stu-id="81555-336">Figure 19 shows a screenshot of the DataSet Designer after adding the methods to the `ProductsTableAdapter` for the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures.</span></span>

<span data-ttu-id="81555-337">[![ProductsTableAdapter include i nuovi metodi aggiunti in questo passaggio](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image42.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image41.png)</span><span class="sxs-lookup"><span data-stu-id="81555-337">[![The ProductsTableAdapter Includes the New Methods Added in this Step](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image42.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image41.png)</span></span>

<span data-ttu-id="81555-338">**Figura 19**: il `ProductsTableAdapter` include i nuovi metodi aggiunti in questo passaggio ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image43.png))</span><span class="sxs-lookup"><span data-stu-id="81555-338">**Figure 19**: The `ProductsTableAdapter` Includes the New Methods Added in this Step ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image43.png))</span></span>

## <a name="step-7-adding-corresponding-methods-to-the-business-logic-layer"></a><span data-ttu-id="81555-339">Passaggio 7: aggiunta di metodi corrispondenti al livello della logica di business</span><span class="sxs-lookup"><span data-stu-id="81555-339">Step 7: Adding Corresponding Methods to the Business Logic Layer</span></span>

<span data-ttu-id="81555-340">Ora che è stato aggiornato il livello di accesso ai dati per includere i metodi per chiamare le stored procedure gestite aggiunte nei passaggi 4 e 5, è necessario aggiungere i metodi corrispondenti al livello della logica di business.</span><span class="sxs-lookup"><span data-stu-id="81555-340">Now that we have updated the Data Access Layer to include methods for calling the managed stored procedures added in Steps 4 and 5, we need to add corresponding methods to the Business Logic Layer.</span></span> <span data-ttu-id="81555-341">Aggiungere i due metodi seguenti alla classe `ProductsBLLWithSprocs`:</span><span class="sxs-lookup"><span data-stu-id="81555-341">Add the following two methods to the `ProductsBLLWithSprocs` class:</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample7.vb)]

<span data-ttu-id="81555-342">Entrambi i metodi chiamano semplicemente il metodo DAL corrispondente e restituiscono l'istanza di `ProductsDataTable`.</span><span class="sxs-lookup"><span data-stu-id="81555-342">Both methods simply call the corresponding DAL method and return the `ProductsDataTable` instance.</span></span> <span data-ttu-id="81555-343">Il markup `DataObjectMethodAttribute` sopra ogni metodo comporta l'inclusione di questi metodi nell'elenco a discesa della scheda Seleziona della configurazione guidata origine dati di ObjectDataSource.</span><span class="sxs-lookup"><span data-stu-id="81555-343">The `DataObjectMethodAttribute` markup above each method causes these methods to be included in the drop-down list in the SELECT tab of the ObjectDataSource s Configure Data Source wizard.</span></span>

## <a name="step-8-invoking-the-managed-stored-procedures-from-the-presentation-layer"></a><span data-ttu-id="81555-344">Passaggio 8: richiamo delle stored procedure gestite dal livello di presentazione</span><span class="sxs-lookup"><span data-stu-id="81555-344">Step 8: Invoking the Managed Stored Procedures from the Presentation Layer</span></span>

<span data-ttu-id="81555-345">Con la logica di business e i livelli di accesso ai dati aumentati per includere il supporto per la chiamata di `GetDiscontinuedProducts` e `GetProductsWithPriceLessThan` stored procedure gestite, ora è possibile visualizzare i risultati di tali stored procedure tramite una pagina ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="81555-345">With the Business Logic and Data Access Layers augmented to include support for calling the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures, we can now display these stored procedures results through an ASP.NET page.</span></span>

<span data-ttu-id="81555-346">Aprire la pagina `ManagedFunctionsAndSprocs.aspx` nella cartella `AdvancedDAL` e, dalla casella degli strumenti, trascinare un controllo GridView nella finestra di progettazione.</span><span class="sxs-lookup"><span data-stu-id="81555-346">Open the `ManagedFunctionsAndSprocs.aspx` page in the `AdvancedDAL` folder and, from the Toolbox, drag a GridView onto the Designer.</span></span> <span data-ttu-id="81555-347">Impostare la proprietà GridView s `ID` su `DiscontinuedProducts` e, dal relativo smart tag, associarla a un nuovo ObjectDataSource denominato `DiscontinuedProductsDataSource`.</span><span class="sxs-lookup"><span data-stu-id="81555-347">Set the GridView s `ID` property to `DiscontinuedProducts` and, from its smart tag, bind it to a new ObjectDataSource named `DiscontinuedProductsDataSource`.</span></span> <span data-ttu-id="81555-348">Configurare ObjectDataSource per eseguire il pull dei dati dal metodo `GetDiscontinuedProducts` della classe `ProductsBLLWithSprocs`.</span><span class="sxs-lookup"><span data-stu-id="81555-348">Configure the ObjectDataSource to pull its data from the `ProductsBLLWithSprocs` class s `GetDiscontinuedProducts` method.</span></span>

<span data-ttu-id="81555-349">[![configurare ObjectDataSource per l'utilizzo della classe ProductsBLLWithSprocs](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image45.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image44.png)</span><span class="sxs-lookup"><span data-stu-id="81555-349">[![Configure the ObjectDataSource to Use the ProductsBLLWithSprocs Class](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image45.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image44.png)</span></span>

<span data-ttu-id="81555-350">**Figura 20**: configurare ObjectDataSource per l'uso della classe `ProductsBLLWithSprocs` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image46.png))</span><span class="sxs-lookup"><span data-stu-id="81555-350">**Figure 20**: Configure the ObjectDataSource to Use the `ProductsBLLWithSprocs` Class ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image46.png))</span></span>

<span data-ttu-id="81555-351">[![scegliere il metodo GetDiscontinuedProducts dall'elenco a discesa nella scheda Seleziona](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image48.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image47.png)</span><span class="sxs-lookup"><span data-stu-id="81555-351">[![Choose the GetDiscontinuedProducts Method from the Drop-Down List in the SELECT Tab](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image48.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image47.png)</span></span>

<span data-ttu-id="81555-352">**Figura 21**: scegliere il metodo `GetDiscontinuedProducts` dall'elenco a discesa nella scheda Seleziona ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image49.png))</span><span class="sxs-lookup"><span data-stu-id="81555-352">**Figure 21**: Choose the `GetDiscontinuedProducts` Method from the Drop-Down List in the SELECT Tab ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image49.png))</span></span>

<span data-ttu-id="81555-353">Poiché questa griglia verrà utilizzata per visualizzare solo le informazioni sul prodotto, impostare gli elenchi a discesa nelle schede Aggiorna, Inserisci ed Elimina su (nessuno), quindi fare clic su fine.</span><span class="sxs-lookup"><span data-stu-id="81555-353">Since this grid will be used to just display product information, set the drop-down lists in the UPDATE, INSERT, and DELETE tabs to (None) and then click Finish.</span></span>

<span data-ttu-id="81555-354">Al termine della procedura guidata, Visual Studio aggiungerà automaticamente un BoundField o CheckBoxField per ogni campo dati nel `ProductsDataTable`.</span><span class="sxs-lookup"><span data-stu-id="81555-354">Upon completing the wizard, Visual Studio will automatically add a BoundField or CheckBoxField for each data field in the `ProductsDataTable`.</span></span> <span data-ttu-id="81555-355">Rimuovere tutti questi campi ad eccezione di `ProductName` e `Discontinued`. a questo punto, il markup dichiarativo di GridView e ObjectDataSource dovrebbe avere un aspetto simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="81555-355">Take a moment to remove all of these fields except for `ProductName` and `Discontinued`, at which point your GridView and ObjectDataSource s declarative markup should look similar to the following:</span></span>

[!code-aspx[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample8.aspx)]

<span data-ttu-id="81555-356">Per visualizzare questa pagina, è possibile usare un browser.</span><span class="sxs-lookup"><span data-stu-id="81555-356">Take a moment to view this page through a browser.</span></span> <span data-ttu-id="81555-357">Quando la pagina viene visitata, ObjectDataSource chiama il metodo `ProductsBLLWithSprocs` Class s `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-357">When the page is visited, the ObjectDataSource calls the `ProductsBLLWithSprocs` class s `GetDiscontinuedProducts` method.</span></span> <span data-ttu-id="81555-358">Come illustrato nel passaggio 7, questo metodo chiama il metodo DAL `ProductsDataTable` della classe s `GetDiscontinuedProducts`, che richiama il stored procedure di `GetDiscontinuedProducts`.</span><span class="sxs-lookup"><span data-stu-id="81555-358">As we saw in Step 7, this method calls down to the DAL s `ProductsDataTable` class s `GetDiscontinuedProducts` method, which invokes the `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="81555-359">Questo stored procedure è un stored procedure gestito ed esegue il codice creato nel passaggio 3, restituendo i prodotti obsoleti.</span><span class="sxs-lookup"><span data-stu-id="81555-359">This stored procedure is a managed stored procedure and executes the code we created in Step 3, returning the discontinued products.</span></span>

<span data-ttu-id="81555-360">I risultati restituiti dal stored procedure gestito vengono assemblati in un `ProductsDataTable` da DAL e quindi restituiti a BLL, che li restituisce al livello di presentazione, in cui sono associati a GridView e visualizzati.</span><span class="sxs-lookup"><span data-stu-id="81555-360">The results returned by the managed stored procedure are packaged up into a `ProductsDataTable` by the DAL and then returned to the BLL, which then returns them to the Presentation Layer where they are bound to the GridView and displayed.</span></span> <span data-ttu-id="81555-361">Come previsto, nella griglia sono elencati i prodotti che non sono più disponibili.</span><span class="sxs-lookup"><span data-stu-id="81555-361">As expected, the grid lists those products that have been discontinued.</span></span>

<span data-ttu-id="81555-362">[![sono elencati i prodotti obsoleti](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image51.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image50.png)</span><span class="sxs-lookup"><span data-stu-id="81555-362">[![The Discontinued Products are Listed](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image51.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image50.png)</span></span>

<span data-ttu-id="81555-363">**Figura 22**: i prodotti obsoleti sono elencati ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image52.png))</span><span class="sxs-lookup"><span data-stu-id="81555-363">**Figure 22**: The Discontinued Products are Listed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image52.png))</span></span>

<span data-ttu-id="81555-364">Per altre procedure, aggiungere una casella di testo e un altro GridView alla pagina.</span><span class="sxs-lookup"><span data-stu-id="81555-364">For further practice, add a TextBox and another GridView to the page.</span></span> <span data-ttu-id="81555-365">Fare in modo che GridView visualizzi i prodotti inferiori alla quantità immessa nella casella di testo chiamando il metodo `ProductsBLLWithSprocs` Class s `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-365">Have this GridView display the products less than the amount entered into the TextBox by calling the `ProductsBLLWithSprocs` class s `GetProductsWithPriceLessThan` method.</span></span>

## <a name="step-9-creating-and-calling-t-sql-udfs"></a><span data-ttu-id="81555-366">Passaggio 9: creazione e chiamata di funzioni definite dall'utente T-SQL</span><span class="sxs-lookup"><span data-stu-id="81555-366">Step 9: Creating and Calling T-SQL UDFs</span></span>

<span data-ttu-id="81555-367">Le funzioni definite dall'utente, o UDF, sono oggetti di database che simulano strettamente la semantica delle funzioni nei linguaggi di programmazione.</span><span class="sxs-lookup"><span data-stu-id="81555-367">User-Defined Functions, or UDFs, are database objects the closely mimic the semantics of functions in programming languages.</span></span> <span data-ttu-id="81555-368">Analogamente a una funzione in Visual Basic, le funzioni definite dall'utente possono includere un numero variabile di parametri di input e restituire un valore di un determinato tipo.</span><span class="sxs-lookup"><span data-stu-id="81555-368">Like a function in Visual Basic, UDFs can include a variable number of input parameters and return a value of a particular type.</span></span> <span data-ttu-id="81555-369">Una funzione definita dall'utente può restituire dati scalari, ovvero una stringa, un numero intero e così via, o dati tabulari.</span><span class="sxs-lookup"><span data-stu-id="81555-369">A UDF can return either scalar data - a string, an integer, and so forth - or tabular data.</span></span> <span data-ttu-id="81555-370">Diamo una rapida occhiata a entrambi i tipi di UDF, a partire da una funzione definita dall'utente che restituisce un tipo di dati scalari.</span><span class="sxs-lookup"><span data-stu-id="81555-370">Let s take a quick look at both types of UDFs, starting with a UDF that returns a scalar data type.</span></span>

<span data-ttu-id="81555-371">La funzione definita dall'utente seguente calcola il valore stimato dell'inventario per un prodotto specifico.</span><span class="sxs-lookup"><span data-stu-id="81555-371">The following UDF calculates the estimated value of the inventory for a particular product.</span></span> <span data-ttu-id="81555-372">A tale scopo, accetta tre parametri di input: i valori `UnitPrice`, `UnitsInStock`e `Discontinued` per un prodotto specifico e restituisce un valore di tipo `money`.</span><span class="sxs-lookup"><span data-stu-id="81555-372">It does so by taking in three input parameters - the `UnitPrice`, `UnitsInStock`, and `Discontinued` values for a particular product - and returns a value of type `money`.</span></span> <span data-ttu-id="81555-373">Consente di calcolare il valore stimato dell'inventario moltiplicando il `UnitPrice` per l'`UnitsInStock`.</span><span class="sxs-lookup"><span data-stu-id="81555-373">It computes the estimated value of the inventory by multiplying the `UnitPrice` by the `UnitsInStock`.</span></span> <span data-ttu-id="81555-374">Per gli elementi sospesi, questo valore è dimezzato.</span><span class="sxs-lookup"><span data-stu-id="81555-374">For discontinued items, this value is halved.</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample9.sql)]

<span data-ttu-id="81555-375">Dopo che questa funzione definita dall'utente è stata aggiunta al database, può essere individuata tramite Management Studio espandendo la cartella Programmabilità, quindi le funzioni e quindi le funzioni valore scalare.</span><span class="sxs-lookup"><span data-stu-id="81555-375">Once this UDF has been added to the database, it can be found through Management Studio by expanding the Programmability folder, then Functions, and then Scalar-value Functions.</span></span> <span data-ttu-id="81555-376">Può essere usato in una query `SELECT` come segue:</span><span class="sxs-lookup"><span data-stu-id="81555-376">It can be used in a `SELECT` query like so:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample10.sql)]

<span data-ttu-id="81555-377">Il `udf_ComputeInventoryValue` UDF è stato aggiunto al database Northwind; La figura 23 Mostra l'output della query `SELECT` precedente quando viene visualizzata tramite Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-377">I have added the `udf_ComputeInventoryValue` UDF to the Northwind database; Figure 23 shows the output of the above `SELECT` query when viewed through Management Studio.</span></span> <span data-ttu-id="81555-378">Si noti inoltre che la funzione definita dall'utente è elencata sotto la cartella funzioni valore scalare nella Esplora oggetti.</span><span class="sxs-lookup"><span data-stu-id="81555-378">Also note that the UDF is listed under the Scalar-value Functions folder in the Object Explorer.</span></span>

<span data-ttu-id="81555-379">[![vengono elencati i valori di inventario di ogni prodotto](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image54.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image53.png)</span><span class="sxs-lookup"><span data-stu-id="81555-379">[![Each Product s Inventory Values is Listed](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image54.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image53.png)</span></span>

<span data-ttu-id="81555-380">**Figura 23**: i valori di inventario di ogni prodotto sono elencati ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image55.png))</span><span class="sxs-lookup"><span data-stu-id="81555-380">**Figure 23**: Each Product s Inventory Values is Listed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image55.png))</span></span>

<span data-ttu-id="81555-381">Le funzioni definite dall'utente possono inoltre restituire dati tabulari.</span><span class="sxs-lookup"><span data-stu-id="81555-381">UDFs can also return tabular data.</span></span> <span data-ttu-id="81555-382">Ad esempio, è possibile creare una funzione definita dall'utente che restituisce i prodotti che appartengono a una categoria specifica:</span><span class="sxs-lookup"><span data-stu-id="81555-382">For example, we can create a UDF that returns products that belong to a particular category:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample11.sql)]

<span data-ttu-id="81555-383">La funzione definita dall'utente `udf_GetProductsByCategoryID` accetta un parametro di input `@CategoryID` e restituisce i risultati della query `SELECT` specificata.</span><span class="sxs-lookup"><span data-stu-id="81555-383">The `udf_GetProductsByCategoryID` UDF accepts a `@CategoryID` input parameter and returns the results of the specified `SELECT` query.</span></span> <span data-ttu-id="81555-384">Una volta creata, è possibile fare riferimento a questa funzione definita dall'utente nella clausola `FROM` (o `JOIN`) di una query di `SELECT`.</span><span class="sxs-lookup"><span data-stu-id="81555-384">Once created, this UDF can be referenced in the `FROM` (or `JOIN`) clause of a `SELECT` query.</span></span> <span data-ttu-id="81555-385">Nell'esempio seguente vengono restituiti i valori di `ProductID`, `ProductName`e `CategoryID` per ogni bevanda.</span><span class="sxs-lookup"><span data-stu-id="81555-385">The following example would return the `ProductID`, `ProductName`, and `CategoryID` values for each of the beverages.</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample12.sql)]

<span data-ttu-id="81555-386">Il `udf_GetProductsByCategoryID` UDF è stato aggiunto al database Northwind; Nella figura 24 viene illustrato l'output della query `SELECT` precedente quando viene visualizzato tramite Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-386">I have added the `udf_GetProductsByCategoryID` UDF to the Northwind database; Figure 24 shows the output of the above `SELECT` query when viewed through Management Studio.</span></span> <span data-ttu-id="81555-387">Le funzioni definite dall'utente che restituiscono dati tabulari sono reperibili nella cartella Esplora oggetti s Table-Value Functions.</span><span class="sxs-lookup"><span data-stu-id="81555-387">UDFs that return tabular data can be found in the Object Explorer s Table-value Functions folder.</span></span>

<span data-ttu-id="81555-388">[![ProductID, ProductName e CategoryID sono elencati per ogni bevanda](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image57.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image56.png)</span><span class="sxs-lookup"><span data-stu-id="81555-388">[![The ProductID, ProductName, and CategoryID are Listed for Each Beverage](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image57.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image56.png)</span></span>

<span data-ttu-id="81555-389">**Figura 24**: le `ProductID`, `ProductName`e `CategoryID` sono elencate per ogni bevanda ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image58.png))</span><span class="sxs-lookup"><span data-stu-id="81555-389">**Figure 24**: The `ProductID`, `ProductName`, and `CategoryID` are Listed for Each Beverage ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image58.png))</span></span>

> [!NOTE]
> <span data-ttu-id="81555-390">Per altre informazioni sulla creazione e sull'uso di UDF, vedere [Introduzione alle funzioni definite dall'utente](http://www.sqlteam.com/item.asp?ItemID=1955).</span><span class="sxs-lookup"><span data-stu-id="81555-390">For more information on creating and using UDFs, check out [Intro to User-Defined Functions](http://www.sqlteam.com/item.asp?ItemID=1955).</span></span> <span data-ttu-id="81555-391">Vedere anche [vantaggi e svantaggi delle funzioni definite dall'utente](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1).</span><span class="sxs-lookup"><span data-stu-id="81555-391">Also check out [Advantages and Drawbacks of User-Defined Functions](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1).</span></span>

## <a name="step-10-creating-a-managed-udf"></a><span data-ttu-id="81555-392">Passaggio 10: creazione di una funzione definita dall'utente gestita</span><span class="sxs-lookup"><span data-stu-id="81555-392">Step 10: Creating a Managed UDF</span></span>

<span data-ttu-id="81555-393">I `udf_ComputeInventoryValue` e `udf_GetProductsByCategoryID` UDF creati negli esempi precedenti sono oggetti di database T-SQL.</span><span class="sxs-lookup"><span data-stu-id="81555-393">The `udf_ComputeInventoryValue` and `udf_GetProductsByCategoryID` UDFs created in the above examples are T-SQL database objects.</span></span> <span data-ttu-id="81555-394">SQL Server 2005 supporta anche UDF gestite, che possono essere aggiunte al progetto `ManagedDatabaseConstructs` esattamente come le stored procedure gestite dei passaggi 3 e 5.</span><span class="sxs-lookup"><span data-stu-id="81555-394">SQL Server 2005 also supports managed UDFs, which can be added to the `ManagedDatabaseConstructs` project just like the managed stored procedures from Steps 3 and 5.</span></span> <span data-ttu-id="81555-395">Per questo passaggio, consente di implementare il `udf_ComputeInventoryValue` UDF nel codice gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-395">For this step, let s implement the `udf_ComputeInventoryValue` UDF in managed code.</span></span>

<span data-ttu-id="81555-396">Per aggiungere una funzione definita dall'utente gestita al progetto `ManagedDatabaseConstructs`, fare clic con il pulsante destro del mouse sul nome del progetto in Esplora soluzioni e scegliere di aggiungere un nuovo elemento.</span><span class="sxs-lookup"><span data-stu-id="81555-396">To add a managed UDF to the `ManagedDatabaseConstructs` project, right-click on the project name in Solution Explorer and choose to Add a New Item.</span></span> <span data-ttu-id="81555-397">Selezionare il modello definito dall'utente dalla finestra di dialogo Aggiungi nuovo elemento e denominare il nuovo file UDF `udf_ComputeInventoryValue_Managed.vb`.</span><span class="sxs-lookup"><span data-stu-id="81555-397">Select the User-Defined Template from the Add New Item dialog box and name the new UDF file `udf_ComputeInventoryValue_Managed.vb`.</span></span>

<span data-ttu-id="81555-398">[![aggiungere una nuova funzione definita dall'utente gestita al progetto ManagedDatabaseConstructs](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image60.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image59.png)</span><span class="sxs-lookup"><span data-stu-id="81555-398">[![Add a New Managed UDF to the ManagedDatabaseConstructs Project](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image60.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image59.png)</span></span>

<span data-ttu-id="81555-399">**Figura 25**: aggiungere una nuova funzione definita dall'utente gestita al progetto `ManagedDatabaseConstructs` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image61.png))</span><span class="sxs-lookup"><span data-stu-id="81555-399">**Figure 25**: Add a New Managed UDF to the `ManagedDatabaseConstructs` Project ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image61.png))</span></span>

<span data-ttu-id="81555-400">Il modello di funzione definito dall'utente crea una classe `Partial` denominata `UserDefinedFunctions` con un metodo il cui nome corrisponde al nome del file di classe (`udf_ComputeInventoryValue_Managed`in questa istanza).</span><span class="sxs-lookup"><span data-stu-id="81555-400">The User-Defined Function template creates a `Partial` class named `UserDefinedFunctions` with a method whose name is the same as the class file s name (`udf_ComputeInventoryValue_Managed`, in this instance).</span></span> <span data-ttu-id="81555-401">Questo metodo viene decorato utilizzando l' [attributo`SqlFunction`](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlfunctionattribute.aspx), che contrassegna il metodo come una funzione definita dall'utente gestita.</span><span class="sxs-lookup"><span data-stu-id="81555-401">This method is decorated using the [`SqlFunction` attribute](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlfunctionattribute.aspx), which flags the method as a managed UDF.</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample13.vb)]

<span data-ttu-id="81555-402">Il metodo `udf_ComputeInventoryValue` restituisce attualmente un [oggetto`SqlString`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlstring.aspx) e non accetta parametri di input.</span><span class="sxs-lookup"><span data-stu-id="81555-402">The `udf_ComputeInventoryValue` method currently returns a [`SqlString` object](https://msdn.microsoft.com/library/system.data.sqltypes.sqlstring.aspx) and does not accept any input parameters.</span></span> <span data-ttu-id="81555-403">È necessario aggiornare la definizione del metodo in modo che accetti tre parametri di input: `UnitPrice`, `UnitsInStock`e `Discontinued` e restituisca un oggetto `SqlMoney`.</span><span class="sxs-lookup"><span data-stu-id="81555-403">We need to update the method definition so that it accepts three input parameters - `UnitPrice`, `UnitsInStock`, and `Discontinued` - and returns a `SqlMoney` object.</span></span> <span data-ttu-id="81555-404">La logica per il calcolo del valore di inventario è identica a quella di T-SQL `udf_ComputeInventoryValue` UDF.</span><span class="sxs-lookup"><span data-stu-id="81555-404">The logic for calculating the inventory value is identical to that in the T-SQL `udf_ComputeInventoryValue` UDF.</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample14.vb)]

<span data-ttu-id="81555-405">Si noti che i parametri di input del metodo UDF sono dei tipi SQL corrispondenti: `SqlMoney` per il campo `UnitPrice`, [`SqlInt16`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlint16.aspx) per `UnitsInStock`e [`SqlBoolean`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlboolean.aspx) per `Discontinued`.</span><span class="sxs-lookup"><span data-stu-id="81555-405">Note that the UDF method s input parameters are of their corresponding SQL types: `SqlMoney` for the `UnitPrice` field, [`SqlInt16`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlint16.aspx) for `UnitsInStock`, and [`SqlBoolean`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlboolean.aspx) for `Discontinued`.</span></span> <span data-ttu-id="81555-406">Questi tipi di dati riflettono i tipi definiti nella tabella `Products`: la colonna `UnitPrice` è di tipo `money`, la colonna `UnitsInStock` di tipo `smallint`e la colonna `Discontinued` di tipo `bit`.</span><span class="sxs-lookup"><span data-stu-id="81555-406">These data types reflect the types defined in the `Products` table: the `UnitPrice` column is of type `money`, the `UnitsInStock` column of type `smallint`, and the `Discontinued` column of type `bit`.</span></span>

<span data-ttu-id="81555-407">Il codice inizia creando un'istanza di `SqlMoney` denominata `inventoryValue` a cui viene assegnato un valore pari a 0.</span><span class="sxs-lookup"><span data-stu-id="81555-407">The code starts by creating a `SqlMoney` instance named `inventoryValue` that is assigned a value of 0.</span></span> <span data-ttu-id="81555-408">La tabella `Products` consente di `NULL` i valori del database nelle colonne `UnitsInPrice` e `UnitsInStock`.</span><span class="sxs-lookup"><span data-stu-id="81555-408">The `Products` table allows for database `NULL` values in the `UnitsInPrice` and `UnitsInStock` columns.</span></span> <span data-ttu-id="81555-409">Pertanto, è necessario verificare prima di tutto se questi valori contengono `NULL` s, operazione eseguita tramite la [proprietà`IsNull`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.isnull.aspx)`SqlMoney` oggetto.</span><span class="sxs-lookup"><span data-stu-id="81555-409">Therefore, we need to first check to see if these values contain `NULL` s, which we do through the `SqlMoney` object s [`IsNull` property](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.isnull.aspx).</span></span> <span data-ttu-id="81555-410">Se sia `UnitPrice` che `UnitsInStock` contengono valori non`NULL`, il `inventoryValue` verrà calcolato come prodotto dei due.</span><span class="sxs-lookup"><span data-stu-id="81555-410">If both `UnitPrice` and `UnitsInStock` contain non-`NULL` values, then we compute the `inventoryValue` to be the product of the two.</span></span> <span data-ttu-id="81555-411">Quindi, se `Discontinued` è true, il valore viene dimezzato.</span><span class="sxs-lookup"><span data-stu-id="81555-411">Then, if `Discontinued` is true, then we halve the value.</span></span>

> [!NOTE]
> <span data-ttu-id="81555-412">L'oggetto `SqlMoney` consente di moltiplicare due istanze di `SqlMoney`.</span><span class="sxs-lookup"><span data-stu-id="81555-412">The `SqlMoney` object only allows two `SqlMoney` instances to be multiplied together.</span></span> <span data-ttu-id="81555-413">Non consente di moltiplicare un'istanza di `SqlMoney` per un numero a virgola mobile letterale.</span><span class="sxs-lookup"><span data-stu-id="81555-413">It does not allow a `SqlMoney` instance to be multiplied by a literal floating-point number.</span></span> <span data-ttu-id="81555-414">Pertanto, per dimezzare `inventoryValue` viene moltiplicato per una nuova istanza di `SqlMoney` con valore 0,5.</span><span class="sxs-lookup"><span data-stu-id="81555-414">Therefore, to halve `inventoryValue` we multiply it by a new `SqlMoney` instance that has the value 0.5.</span></span>

## <a name="step-11-deploying-the-managed-udf"></a><span data-ttu-id="81555-415">Passaggio 11: distribuzione della funzione definita dall'utente gestita</span><span class="sxs-lookup"><span data-stu-id="81555-415">Step 11: Deploying the Managed UDF</span></span>

<span data-ttu-id="81555-416">Ora che la funzione definita dall'utente gestita è stata creata, è possibile distribuirla nel database Northwind.</span><span class="sxs-lookup"><span data-stu-id="81555-416">Now that that the managed UDF has been created, we are ready to deploy it to the Northwind database.</span></span> <span data-ttu-id="81555-417">Come illustrato nel passaggio 4, gli oggetti gestiti in un progetto di SQL Server vengono distribuiti facendo clic con il pulsante destro del mouse sul nome del progetto nel Esplora soluzioni e scegliendo l'opzione Distribuisci dal menu di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="81555-417">As we saw in Step 4, the managed objects in a SQL Server Project are deployed by right-clicking on the project name in the Solution Explorer and choosing the Deploy option from the context menu.</span></span>

<span data-ttu-id="81555-418">Dopo aver distribuito il progetto, tornare a SQL Server Management Studio e aggiornare la cartella funzioni a valori scalari.</span><span class="sxs-lookup"><span data-stu-id="81555-418">Once you have deployed the project, return to SQL Server Management Studio and refresh the Scalar-valued Functions folder.</span></span> <span data-ttu-id="81555-419">Verranno ora visualizzate due voci:</span><span class="sxs-lookup"><span data-stu-id="81555-419">You should now see two entries:</span></span>

- <span data-ttu-id="81555-420">`dbo.udf_ComputeInventoryValue`: la funzione definita dall'utente T-SQL creata nel passaggio 9 e</span><span class="sxs-lookup"><span data-stu-id="81555-420">`dbo.udf_ComputeInventoryValue` - the T-SQL UDF created in Step 9, and</span></span>
- <span data-ttu-id="81555-421">`dbo.udf ComputeInventoryValue_Managed`: la funzione definita dall'utente gestita creata nel passaggio 10 appena distribuito.</span><span class="sxs-lookup"><span data-stu-id="81555-421">`dbo.udf ComputeInventoryValue_Managed` - the managed UDF created in Step 10 that was just deployed.</span></span>

<span data-ttu-id="81555-422">Per testare questa funzione definita dall'utente gestita, eseguire la query seguente dall'interno Management Studio:</span><span class="sxs-lookup"><span data-stu-id="81555-422">To test this managed UDF, execute the following query from within Management Studio:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample15.sql)]

<span data-ttu-id="81555-423">Questo comando usa la funzione definita dall'utente gestita `udf ComputeInventoryValue_Managed` invece della UDF T-SQL `udf_ComputeInventoryValue`, ma l'output è lo stesso.</span><span class="sxs-lookup"><span data-stu-id="81555-423">This command uses the managed `udf ComputeInventoryValue_Managed` UDF instead of the T-SQL `udf_ComputeInventoryValue` UDF, but the output is the same.</span></span> <span data-ttu-id="81555-424">Fare riferimento alla figura 23 per visualizzare una schermata dell'output della funzione definita dall'utente.</span><span class="sxs-lookup"><span data-stu-id="81555-424">Refer back to Figure 23 to see a screenshot of the UDF s output.</span></span>

## <a name="step-12-debugging-the-managed-database-objects"></a><span data-ttu-id="81555-425">Passaggio 12: debug degli oggetti di database gestiti</span><span class="sxs-lookup"><span data-stu-id="81555-425">Step 12: Debugging the Managed Database Objects</span></span>

<span data-ttu-id="81555-426">Nell'esercitazione sulle [stored procedure di debug](debugging-stored-procedures-vb.md) sono state illustrate le tre opzioni per il debug di SQL Server tramite Visual Studio: debug diretto del database, debug delle applicazioni e debug da un progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-426">In the [Debugging Stored Procedures](debugging-stored-procedures-vb.md) tutorial we discussed the three options for debugging SQL Server through Visual Studio: Direct Database Debugging, Application Debugging, and Debugging from a SQL Server Project.</span></span> <span data-ttu-id="81555-427">Non è possibile eseguire il debug di oggetti di database gestiti tramite il debug diretto del database, ma è possibile eseguirne il debug da un'applicazione client e direttamente dal progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-427">Managed database objects cannot be debugged via Direct Database Debugging, but can be debugged from a client application and directly from the SQL Server Project.</span></span> <span data-ttu-id="81555-428">Per il corretto funzionamento del debug, tuttavia, il database SQL Server 2005 deve consentire il debug SQL/CLR.</span><span class="sxs-lookup"><span data-stu-id="81555-428">In order for debugging to work, however, the SQL Server 2005 database must allow SQL/CLR debugging.</span></span> <span data-ttu-id="81555-429">Ricordiamo che quando abbiamo creato il progetto `ManagedDatabaseConstructs` Visual Studio ci ha chiesto se avessimo voluto abilitare il debug SQL/CLR (vedere la figura 6 nel passaggio 2).</span><span class="sxs-lookup"><span data-stu-id="81555-429">Recall that when we first created the `ManagedDatabaseConstructs` project Visual Studio asked us whether we wanted to enable SQL/CLR debugging (see Figure 6 in Step 2).</span></span> <span data-ttu-id="81555-430">Questa impostazione può essere modificata facendo clic con il pulsante destro del mouse sul database dalla finestra Esplora server.</span><span class="sxs-lookup"><span data-stu-id="81555-430">This setting can be modified by right-clicking on the database from the Server Explorer window.</span></span>

![Verificare che il database consenta il debug SQL/CLR](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image62.png)

<span data-ttu-id="81555-432">**Figura 26**: verificare che il database consenta il debug SQL/CLR</span><span class="sxs-lookup"><span data-stu-id="81555-432">**Figure 26**: Ensure that the Database Allows SQL/CLR Debugging</span></span>

<span data-ttu-id="81555-433">Si supponga di voler eseguire il debug del stored procedure gestito `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-433">Imagine that we wanted to debug the `GetProductsWithPriceLessThan` managed stored procedure.</span></span> <span data-ttu-id="81555-434">Per iniziare, è necessario impostare un punto di interruzione nel codice del metodo `GetProductsWithPriceLessThan`.</span><span class="sxs-lookup"><span data-stu-id="81555-434">We would start by setting a breakpoint within the code of the `GetProductsWithPriceLessThan` method.</span></span>

<span data-ttu-id="81555-435">[![impostare un punto di interruzione nel metodo GetProductsWithPriceLessThan](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image64.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image63.png)</span><span class="sxs-lookup"><span data-stu-id="81555-435">[![Set a Breakpoint in the GetProductsWithPriceLessThan Method](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image64.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image63.png)</span></span>

<span data-ttu-id="81555-436">**Figura 27**: impostare un punto di interruzione nel metodo `GetProductsWithPriceLessThan` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image65.png))</span><span class="sxs-lookup"><span data-stu-id="81555-436">**Figure 27**: Set a Breakpoint in the `GetProductsWithPriceLessThan` Method ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image65.png))</span></span>

<span data-ttu-id="81555-437">Si esaminerà prima di tutto il debug degli oggetti di database gestiti dal progetto SQL Server.</span><span class="sxs-lookup"><span data-stu-id="81555-437">Let s first look at debugging the managed database objects from the SQL Server Project.</span></span> <span data-ttu-id="81555-438">Poiché la soluzione include due progetti: il progetto SQL Server `ManagedDatabaseConstructs` insieme al nostro sito Web: per eseguire il debug dal progetto SQL Server è necessario indicare a Visual Studio di avviare il progetto di SQL Server `ManagedDatabaseConstructs` all'avvio del debug.</span><span class="sxs-lookup"><span data-stu-id="81555-438">Since our Solution includes two projects - the `ManagedDatabaseConstructs` SQL Server Project along with our website - in order to debug from the SQL Server Project we need to instruct Visual Studio to launch the `ManagedDatabaseConstructs` SQL Server Project when we start debugging.</span></span> <span data-ttu-id="81555-439">Fare clic con il pulsante destro del mouse sul progetto `ManagedDatabaseConstructs` in Esplora soluzioni e scegliere l'opzione imposta come progetto di avvio dal menu di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="81555-439">Right-click the `ManagedDatabaseConstructs` project in Solution Explorer and choose the Set as StartUp Project option from the context menu.</span></span>

<span data-ttu-id="81555-440">Quando il progetto di `ManagedDatabaseConstructs` viene avviato dal debugger, vengono eseguite le istruzioni SQL nel file `Test.sql`, che si trova nella cartella `Test Scripts`.</span><span class="sxs-lookup"><span data-stu-id="81555-440">When the `ManagedDatabaseConstructs` project is launched from the debugger it executes the SQL statements in the `Test.sql` file, which is located in the `Test Scripts` folder.</span></span> <span data-ttu-id="81555-441">Ad esempio, per testare la `GetProductsWithPriceLessThan` stored procedure gestita, sostituire il contenuto del file di `Test.sql` esistente con l'istruzione seguente, che richiama il stored procedure gestito `GetProductsWithPriceLessThan` passando il valore `@CategoryID` 14,95:</span><span class="sxs-lookup"><span data-stu-id="81555-441">For example, to test the `GetProductsWithPriceLessThan` managed stored procedure, replace the existing `Test.sql` file content with the following statement, which invokes the `GetProductsWithPriceLessThan` managed stored procedure passing in the `@CategoryID` value of 14.95:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample16.sql)]

<span data-ttu-id="81555-442">Dopo aver immesso lo script precedente in `Test.sql`, avviare il debug selezionando il menu debug e scegliendo Avvia debug o premendo F5 o l'icona di riproduzione verde sulla barra degli strumenti.</span><span class="sxs-lookup"><span data-stu-id="81555-442">Once you ve entered the above script into `Test.sql`, start debugging by going to the Debug menu and choosing Start Debugging or by hitting F5 or the green play icon in the Toolbar.</span></span> <span data-ttu-id="81555-443">In questo modo i progetti vengono compilati nella soluzione, vengono distribuiti gli oggetti di database gestiti nel database Northwind, quindi viene eseguito lo script `Test.sql`.</span><span class="sxs-lookup"><span data-stu-id="81555-443">This will build the projects within the Solution, deploy the managed database objects to the Northwind database, and then execute the `Test.sql` script.</span></span> <span data-ttu-id="81555-444">A questo punto, il punto di interruzione verrà raggiunto ed è possibile eseguire il `GetProductsWithPriceLessThan` metodo, esaminare i valori dei parametri di input e così via.</span><span class="sxs-lookup"><span data-stu-id="81555-444">At this point, the breakpoint will be hit and we can step through the `GetProductsWithPriceLessThan` method, examine the values of the input parameters, and so on.</span></span>

<span data-ttu-id="81555-445">[![è stato raggiunto il punto di interruzione nel metodo GetProductsWithPriceLessThan](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image67.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image66.png)</span><span class="sxs-lookup"><span data-stu-id="81555-445">[![The Breakpoint in the GetProductsWithPriceLessThan Method Was Hit](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image67.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image66.png)</span></span>

<span data-ttu-id="81555-446">**Figura 28**: è stato raggiunto il punto di interruzione nel metodo `GetProductsWithPriceLessThan` ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image68.png))</span><span class="sxs-lookup"><span data-stu-id="81555-446">**Figure 28**: The Breakpoint in the `GetProductsWithPriceLessThan` Method Was Hit ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image68.png))</span></span>

<span data-ttu-id="81555-447">Per eseguire il debug di un oggetto di database SQL tramite un'applicazione client, è necessario che il database sia configurato per supportare il debug delle applicazioni.</span><span class="sxs-lookup"><span data-stu-id="81555-447">In order for a SQL database object to be debugged through a client application, it is imperative that the database be configured to support application debugging.</span></span> <span data-ttu-id="81555-448">Fare clic con il pulsante destro del mouse sul database in Esplora server e verificare che l'opzione debug applicazione sia selezionata.</span><span class="sxs-lookup"><span data-stu-id="81555-448">Right-click on the database in Server Explorer and ensure that the Application Debugging option is checked.</span></span> <span data-ttu-id="81555-449">Inoltre, è necessario configurare l'applicazione ASP.NET per l'integrazione con il debugger SQL e per disabilitare il pool di connessioni.</span><span class="sxs-lookup"><span data-stu-id="81555-449">Furthermore, we need to configure the ASP.NET application to integrate with the SQL Debugger and to disable connection pooling.</span></span> <span data-ttu-id="81555-450">Questi passaggi sono stati descritti in dettaglio nel passaggio 2 dell'esercitazione [debug di stored procedure](debugging-stored-procedures-vb.md) .</span><span class="sxs-lookup"><span data-stu-id="81555-450">These steps were discussed in detail in Step 2 of the [Debugging Stored Procedures](debugging-stored-procedures-vb.md) tutorial.</span></span>

<span data-ttu-id="81555-451">Dopo aver configurato l'applicazione e il database di ASP.NET, impostare il sito Web ASP.NET come progetto di avvio e avviare il debug.</span><span class="sxs-lookup"><span data-stu-id="81555-451">Once you have configured the ASP.NET application and database, set the ASP.NET website as the startup project and start debugging.</span></span> <span data-ttu-id="81555-452">Se si visita una pagina che chiama uno degli oggetti gestiti con un punto di interruzione, l'applicazione verrà arrestata e il controllo verrà spostato al debugger, in cui è possibile esaminare il codice come illustrato nella figura 28.</span><span class="sxs-lookup"><span data-stu-id="81555-452">If you visit a page that calls one of the managed objects that has a breakpoint, the application will halt and control will be turned over to the debugger, where you can step through the code as shown in Figure 28.</span></span>

## <a name="step-13-manually-compiling-and-deploying-managed-database-objects"></a><span data-ttu-id="81555-453">Passaggio 13: compilazione e distribuzione manuali di oggetti di database gestiti</span><span class="sxs-lookup"><span data-stu-id="81555-453">Step 13: Manually Compiling and Deploying Managed Database Objects</span></span>

<span data-ttu-id="81555-454">SQL Server progetti semplificano la creazione, la compilazione e la distribuzione di oggetti di database gestiti.</span><span class="sxs-lookup"><span data-stu-id="81555-454">SQL Server Projects make it easy to create, compile, and deploy managed database objects.</span></span> <span data-ttu-id="81555-455">Sfortunatamente, i progetti SQL Server sono disponibili solo nelle edizioni Professional e team Systems di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-455">Unfortunately, SQL Server Projects are only available in the Professional and Team Systems editions of Visual Studio.</span></span> <span data-ttu-id="81555-456">Se si utilizza Visual Web Developer o l'edizione standard di Visual Studio e si desidera utilizzare oggetti di database gestiti, sarà necessario crearli e distribuirli manualmente.</span><span class="sxs-lookup"><span data-stu-id="81555-456">If you are using Visual Web Developer or the Standard Edition of Visual Studio and want to use managed database objects, you will need to manually create and deploy them.</span></span> <span data-ttu-id="81555-457">Questa operazione prevede quattro passaggi:</span><span class="sxs-lookup"><span data-stu-id="81555-457">This involves four steps:</span></span>

1. <span data-ttu-id="81555-458">Creare un file contenente il codice sorgente per l'oggetto di database gestito,</span><span class="sxs-lookup"><span data-stu-id="81555-458">Create a file that contains the source code for the managed database object,</span></span>
2. <span data-ttu-id="81555-459">Compilare l'oggetto in un assembly,</span><span class="sxs-lookup"><span data-stu-id="81555-459">Compile the object into an assembly,</span></span>
3. <span data-ttu-id="81555-460">Registrare l'assembly con il database SQL Server 2005 e</span><span class="sxs-lookup"><span data-stu-id="81555-460">Register the assembly with the SQL Server 2005 database, and</span></span>
4. <span data-ttu-id="81555-461">Creare un oggetto di database in SQL Server che punti al metodo appropriato nell'assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-461">Create a database object in SQL Server that points to the appropriate method in the assembly.</span></span>

<span data-ttu-id="81555-462">Per illustrare queste attività, è possibile creare un nuovo stored procedure gestito che restituisca i prodotti la cui `UnitPrice` è maggiore di un valore specificato.</span><span class="sxs-lookup"><span data-stu-id="81555-462">To illustrate these tasks, let s create a new managed stored procedure that returns those products whose `UnitPrice` is greater than a specified value.</span></span> <span data-ttu-id="81555-463">Creare un nuovo file nel computer denominato `GetProductsWithPriceGreaterThan.vb` e immettere il codice seguente nel file. per eseguire questa operazione, è possibile usare Visual Studio, il blocco note o qualsiasi editor di testo:</span><span class="sxs-lookup"><span data-stu-id="81555-463">Create a new file on your computer named `GetProductsWithPriceGreaterThan.vb` and enter the following code into the file (you can use Visual Studio, Notepad, or any text editor to accomplish this):</span></span>

[!code-vb[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample17.vb)]

<span data-ttu-id="81555-464">Questo codice è quasi identico a quello del `GetProductsWithPriceLessThan` metodo creato nel passaggio 5.</span><span class="sxs-lookup"><span data-stu-id="81555-464">This code is nearly identical to that of the `GetProductsWithPriceLessThan` method created in Step 5.</span></span> <span data-ttu-id="81555-465">Le uniche differenze sono i nomi dei metodi, la clausola `WHERE` e il nome del parametro utilizzato nella query.</span><span class="sxs-lookup"><span data-stu-id="81555-465">The only differences are the method names, the `WHERE` clause, and the parameter name used in the query.</span></span> <span data-ttu-id="81555-466">Tornando al metodo `GetProductsWithPriceLessThan`, la clausola `WHERE` Read: `WHERE UnitPrice < @MaxPrice`.</span><span class="sxs-lookup"><span data-stu-id="81555-466">Back in the `GetProductsWithPriceLessThan` method, the `WHERE` clause read: `WHERE UnitPrice < @MaxPrice`.</span></span> <span data-ttu-id="81555-467">In `GetProductsWithPriceGreaterThan`si usa: `WHERE UnitPrice > @MinPrice`.</span><span class="sxs-lookup"><span data-stu-id="81555-467">Here, in `GetProductsWithPriceGreaterThan`, we use: `WHERE UnitPrice > @MinPrice` .</span></span>

<span data-ttu-id="81555-468">A questo punto è necessario compilare questa classe in un assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-468">We now need to compile this class into an assembly.</span></span> <span data-ttu-id="81555-469">Dalla riga di comando passare alla directory in cui è stato salvato il file di `GetProductsWithPriceGreaterThan.vb` e usare C# il compilatore (`csc.exe`) per compilare il file di classe in un assembly:</span><span class="sxs-lookup"><span data-stu-id="81555-469">From the command line, navigate to the directory where you saved the `GetProductsWithPriceGreaterThan.vb` file and use the C# compiler (`csc.exe`) to compile the class file into an assembly:</span></span>

[!code-console[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample18.cmd)]

<span data-ttu-id="81555-470">Se la cartella che contiene la v `bc.exe` non è presente nel `PATH`del sistema, sarà necessario fare riferimento al percorso completo, `%WINDOWS%\Microsoft.NET\Framework\version\`, come segue:</span><span class="sxs-lookup"><span data-stu-id="81555-470">If the folder containing v `bc.exe` in not in the system s `PATH`, you will have to fully reference its path, `%WINDOWS%\Microsoft.NET\Framework\version\`, like so:</span></span>

[!code-console[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample19.cmd)]

<span data-ttu-id="81555-471">[![compilare GetProductsWithPriceGreaterThan. vb in un assembly](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image70.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image69.png)</span><span class="sxs-lookup"><span data-stu-id="81555-471">[![Compile GetProductsWithPriceGreaterThan.vb Into an Assembly](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image70.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image69.png)</span></span>

<span data-ttu-id="81555-472">**Figura 29**: compilare `GetProductsWithPriceGreaterThan.vb` in un assembly ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image71.png))</span><span class="sxs-lookup"><span data-stu-id="81555-472">**Figure 29**: Compile `GetProductsWithPriceGreaterThan.vb` Into an Assembly ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image71.png))</span></span>

<span data-ttu-id="81555-473">Il flag `/t` specifica che il file di classe Visual Basic deve essere compilato in una DLL, anziché in un eseguibile.</span><span class="sxs-lookup"><span data-stu-id="81555-473">The `/t` flag specifies that the Visual Basic class file should be compiled into a DLL (rather than an executable).</span></span> <span data-ttu-id="81555-474">Il flag `/out` specifica il nome dell'assembly risultante.</span><span class="sxs-lookup"><span data-stu-id="81555-474">The `/out` flag specifies the name of the resulting assembly.</span></span>

> [!NOTE]
> <span data-ttu-id="81555-475">Anziché compilare il file di classe `GetProductsWithPriceGreaterThan.vb` dalla riga di comando, in alternativa è possibile usare [Visual Basic Express Edition](https://msdn.microsoft.com/vstudio/express/vb/) o creare un progetto di libreria di classi separato in Visual Studio Standard Edition.</span><span class="sxs-lookup"><span data-stu-id="81555-475">Rather than compiling the `GetProductsWithPriceGreaterThan.vb` class file from the command line you could alternatively use [Visual Basic Express Edition](https://msdn.microsoft.com/vstudio/express/vb/) or create a separate Class Library project in Visual Studio Standard Edition.</span></span> <span data-ttu-id="81555-476">S Ren Jacob Lauritsen ha gentilmente fornito un progetto Visual Basic Express Edition con codice per il stored procedure di `GetProductsWithPriceGreaterThan` e le due stored procedure gestite e UDF create nei passaggi 3, 5 e 10.</span><span class="sxs-lookup"><span data-stu-id="81555-476">S ren Jacob Lauritsen has kindly provided such a Visual Basic Express Edition project with code for the `GetProductsWithPriceGreaterThan` stored procedure and the two managed stored procedures and UDF created in Steps 3, 5, and 10.</span></span> <span data-ttu-id="81555-477">Il progetto s di s Ren include anche i comandi T-SQL necessari per aggiungere gli oggetti di database corrispondenti.</span><span class="sxs-lookup"><span data-stu-id="81555-477">S ren s project also includes the T-SQL commands needed to add the corresponding database objects.</span></span>

<span data-ttu-id="81555-478">Con il codice compilato in un assembly, è possibile registrare l'assembly all'interno del database SQL Server 2005.</span><span class="sxs-lookup"><span data-stu-id="81555-478">With the code compiled into an assembly, we are ready to register the assembly within the SQL Server 2005 database.</span></span> <span data-ttu-id="81555-479">Questa operazione può essere eseguita tramite T-SQL, usando il comando `CREATE ASSEMBLY`o tramite SQL Server Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-479">This can be performed through T-SQL, using the command `CREATE ASSEMBLY`, or through SQL Server Management Studio.</span></span> <span data-ttu-id="81555-480">Consente di concentrarsi sull'uso di Management Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-480">Let s focus on using Management Studio.</span></span>

<span data-ttu-id="81555-481">In Management Studio espandere la cartella Programmabilità nel database Northwind.</span><span class="sxs-lookup"><span data-stu-id="81555-481">From Management Studio, expand the Programmability folder in the Northwind database.</span></span> <span data-ttu-id="81555-482">Una delle sottocartelle è costituita da assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-482">One of its subfolder is Assemblies.</span></span> <span data-ttu-id="81555-483">Per aggiungere manualmente un nuovo assembly al database, fare clic con il pulsante destro del mouse sulla cartella assembly e scegliere nuovo assembly dal menu di scelta rapida.</span><span class="sxs-lookup"><span data-stu-id="81555-483">To manually add a new Assembly to the database, right-click on the Assemblies folder and choose New Assembly from the context menu.</span></span> <span data-ttu-id="81555-484">Verrà visualizzata la finestra di dialogo nuovo assembly (vedere la figura 30).</span><span class="sxs-lookup"><span data-stu-id="81555-484">This displays the New Assembly dialog box (see Figure 30).</span></span> <span data-ttu-id="81555-485">Fare clic sul pulsante Sfoglia, selezionare l'assembly `ManuallyCreatedDBObjects.dll` appena compilato, quindi fare clic su OK per aggiungere l'assembly al database.</span><span class="sxs-lookup"><span data-stu-id="81555-485">Click on the Browse button, select the `ManuallyCreatedDBObjects.dll` assembly we just compiled, and then click OK to add the Assembly to the database.</span></span> <span data-ttu-id="81555-486">Nell'Esplora oggetti non dovrebbe essere visualizzato l'assembly `ManuallyCreatedDBObjects.dll`.</span><span class="sxs-lookup"><span data-stu-id="81555-486">You should not see the `ManuallyCreatedDBObjects.dll` assembly in the Object Explorer.</span></span>

<span data-ttu-id="81555-487">[![aggiungere l'assembly ManuallyCreatedDBObjects. dll al database](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image73.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image72.png)</span><span class="sxs-lookup"><span data-stu-id="81555-487">[![Add the ManuallyCreatedDBObjects.dll Assembly to the Database](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image73.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image72.png)</span></span>

<span data-ttu-id="81555-488">**Figura 30**: aggiungere l'assembly `ManuallyCreatedDBObjects.dll` al database ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image74.png))</span><span class="sxs-lookup"><span data-stu-id="81555-488">**Figure 30**: Add the `ManuallyCreatedDBObjects.dll` Assembly to the Database ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image74.png))</span></span>

![ManuallyCreatedDBObjects. dll è elencato nell'Esplora oggetti](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image75.png)

<span data-ttu-id="81555-490">**Figura 31**: la `ManuallyCreatedDBObjects.dll` è elencata nell'Esplora oggetti</span><span class="sxs-lookup"><span data-stu-id="81555-490">**Figure 31**: The `ManuallyCreatedDBObjects.dll` is Listed in the Object Explorer</span></span>

<span data-ttu-id="81555-491">Sebbene l'assembly sia stato aggiunto al database Northwind, è ancora necessario associare un stored procedure al metodo `GetProductsWithPriceGreaterThan` nell'assembly.</span><span class="sxs-lookup"><span data-stu-id="81555-491">While we have added the assembly to the Northwind database, we have yet to associate a stored procedure with the `GetProductsWithPriceGreaterThan` method in the assembly.</span></span> <span data-ttu-id="81555-492">A tale scopo, aprire una nuova finestra di query ed eseguire lo script seguente:</span><span class="sxs-lookup"><span data-stu-id="81555-492">To accomplish this, open a new query window and execute the following script:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample20.sql)]

<span data-ttu-id="81555-493">In questo modo viene creato un nuovo stored procedure nel database Northwind denominato `GetProductsWithPriceGreaterThan` e viene associato al metodo gestito `GetProductsWithPriceGreaterThan` (che si trova nella classe `StoredProcedures`, presente nell'assembly `ManuallyCreatedDBObjects`).</span><span class="sxs-lookup"><span data-stu-id="81555-493">This creates a new stored procedure in the Northwind database named `GetProductsWithPriceGreaterThan` and associates it with the managed method `GetProductsWithPriceGreaterThan` (which is in the class `StoredProcedures`, which is in the assembly `ManuallyCreatedDBObjects`).</span></span>

<span data-ttu-id="81555-494">Dopo aver eseguito lo script precedente, aggiornare la cartella stored procedure nel Esplora oggetti.</span><span class="sxs-lookup"><span data-stu-id="81555-494">After executing the above script, refresh the Stored Procedures folder in the Object Explorer.</span></span> <span data-ttu-id="81555-495">Verrà visualizzato un nuovo stored procedure entry-`GetProductsWithPriceGreaterThan`, che contiene un'icona a blocchi accanto.</span><span class="sxs-lookup"><span data-stu-id="81555-495">You should see a new stored procedure entry - `GetProductsWithPriceGreaterThan` - which has a lock icon next to it.</span></span> <span data-ttu-id="81555-496">Per testare questa stored procedure, immettere ed eseguire lo script seguente nella finestra di query:</span><span class="sxs-lookup"><span data-stu-id="81555-496">To test this stored procedure, enter and execute the following script in the query window:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/samples/sample21.sql)]

<span data-ttu-id="81555-497">Come illustrato nella Figura 32, il comando precedente Visualizza le informazioni relative a tali prodotti con una `UnitPrice` maggiore di $24,95.</span><span class="sxs-lookup"><span data-stu-id="81555-497">As Figure 32 shows, the above command displays information for those products with a `UnitPrice` greater than $24.95.</span></span>

<span data-ttu-id="81555-498">[![ManuallyCreatedDBObjects. dll è elencato nell'Esplora oggetti](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image77.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image76.png)</span><span class="sxs-lookup"><span data-stu-id="81555-498">[![The ManuallyCreatedDBObjects.dll is Listed in the Object Explorer](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image77.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image76.png)</span></span>

<span data-ttu-id="81555-499">**Figura 32**: la `ManuallyCreatedDBObjects.dll` è elencata nell'Esplora oggetti ([fare clic per visualizzare l'immagine con dimensioni complete](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image78.png))</span><span class="sxs-lookup"><span data-stu-id="81555-499">**Figure 32**: The `ManuallyCreatedDBObjects.dll` is Listed in the Object Explorer ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-vb/_static/image78.png))</span></span>

## <a name="summary"></a><span data-ttu-id="81555-500">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="81555-500">Summary</span></span>

<span data-ttu-id="81555-501">Microsoft SQL Server 2005 fornisce l'integrazione con CLR (Common Language Runtime), che consente la creazione di oggetti di database tramite codice gestito.</span><span class="sxs-lookup"><span data-stu-id="81555-501">Microsoft SQL Server 2005 provides integration with the Common Language Runtime (CLR), which allows database objects to be created using managed code.</span></span> <span data-ttu-id="81555-502">In precedenza, questi oggetti di database potevano essere creati solo con T-SQL, ma ora è possibile creare questi oggetti usando linguaggi di programmazione .NET come Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="81555-502">Previously, these database objects could only be created using T-SQL, but now we can create these objects using .NET programming languages like Visual Basic.</span></span> <span data-ttu-id="81555-503">In questa esercitazione sono state create due stored procedure gestite e una funzione gestita definita dall'utente.</span><span class="sxs-lookup"><span data-stu-id="81555-503">In this tutorial we created two managed stored procedures and a managed User-Defined Function.</span></span>

<span data-ttu-id="81555-504">Il tipo di progetto SQL Server di Visual Studio facilita la creazione, la compilazione e la distribuzione di oggetti di database gestiti.</span><span class="sxs-lookup"><span data-stu-id="81555-504">Visual Studio s SQL Server Project type facilitates creating, compiling, and deploying managed database objects.</span></span> <span data-ttu-id="81555-505">Offre inoltre un supporto completo per il debug.</span><span class="sxs-lookup"><span data-stu-id="81555-505">Moreover, it offers rich debugging support.</span></span> <span data-ttu-id="81555-506">Tuttavia, SQL Server tipi di progetto sono disponibili solo nelle edizioni Professional e team Systems di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="81555-506">However, SQL Server Project types are only available in the Professional and Team Systems editions of Visual Studio.</span></span> <span data-ttu-id="81555-507">Per coloro che usano Visual Web Developer o l'edizione standard di Visual Studio, i passaggi per la creazione, la compilazione e la distribuzione devono essere eseguiti manualmente, come illustrato nel passaggio 13.</span><span class="sxs-lookup"><span data-stu-id="81555-507">For those using Visual Web Developer or the Standard Edition of Visual Studio, the creation, compilation, and deployment steps must be performed manually, as we saw in Step 13.</span></span>

<span data-ttu-id="81555-508">Buona programmazione!</span><span class="sxs-lookup"><span data-stu-id="81555-508">Happy Programming!</span></span>

## <a name="further-reading"></a><span data-ttu-id="81555-509">Ulteriori informazioni</span><span class="sxs-lookup"><span data-stu-id="81555-509">Further Reading</span></span>

<span data-ttu-id="81555-510">Per ulteriori informazioni sugli argomenti trattati in questa esercitazione, fare riferimento alle risorse seguenti:</span><span class="sxs-lookup"><span data-stu-id="81555-510">For more information on the topics discussed in this tutorial, refer to the following resources:</span></span>

- [<span data-ttu-id="81555-511">Vantaggi e svantaggi delle funzioni definite dall'utente</span><span class="sxs-lookup"><span data-stu-id="81555-511">Advantages and Drawbacks of User-Defined Functions</span></span>](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1)
- [<span data-ttu-id="81555-512">Creazione di oggetti SQL Server 2005 nel codice gestito</span><span class="sxs-lookup"><span data-stu-id="81555-512">Creating SQL Server 2005 Objects in Managed Code</span></span>](https://channel9.msdn.com/Showpost.aspx?postid=142413)
- [<span data-ttu-id="81555-513">Creazione di trigger tramite codice gestito in SQL Server 2005</span><span class="sxs-lookup"><span data-stu-id="81555-513">Creating Triggers Using Managed Code in SQL Server 2005</span></span>](http://www.15seconds.com/issue/041006.htm)
- <span data-ttu-id="81555-514">[Procedura: creare ed eseguire una stored procedure SQL Server CLR](https://msdn.microsoft.com/library/5czye81z(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="81555-514">[How To: Create and Run a CLR SQL Server Stored Procedure](https://msdn.microsoft.com/library/5czye81z(VS.80).aspx)</span></span>
- <span data-ttu-id="81555-515">[Procedura: creare ed eseguire una funzione CLR SQL Server definita dall'utente](https://msdn.microsoft.com/library/w2kae45k(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="81555-515">[How To: Create and Run a CLR SQL Server User-Defined Function](https://msdn.microsoft.com/library/w2kae45k(VS.80).aspx)</span></span>
- <span data-ttu-id="81555-516">[Procedura: modificare lo script `Test.sql` per eseguire oggetti SQL](https://msdn.microsoft.com/library/ms233682(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="81555-516">[How To: Edit the `Test.sql` Script to Run SQL Objects](https://msdn.microsoft.com/library/ms233682(VS.80).aspx)</span></span>
- [<span data-ttu-id="81555-517">Introduzione alle funzioni definite dall'utente</span><span class="sxs-lookup"><span data-stu-id="81555-517">Intro to User Defined Functions</span></span>](http://www.sqlteam.com/item.asp?ItemID=1955)
- [<span data-ttu-id="81555-518">Codice gestito e SQL Server 2005 (video)</span><span class="sxs-lookup"><span data-stu-id="81555-518">Managed Code and SQL Server 2005 (Video)</span></span>](https://channel9.msdn.com/Showpost.aspx?postid=142413)
- <span data-ttu-id="81555-519">[Guida di riferimento a Transact-SQL](https://msdn.microsoft.com/library/aa299742(SQL.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="81555-519">[Transact-SQL Reference](https://msdn.microsoft.com/library/aa299742(SQL.80).aspx)</span></span>
- <span data-ttu-id="81555-520">[Procedura dettagliata: creazione di una stored procedure nel codice gestito](https://msdn.microsoft.com/library/zxsa8hkf(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="81555-520">[Walkthrough: Creating a Stored Procedure in Managed Code](https://msdn.microsoft.com/library/zxsa8hkf(VS.80).aspx)</span></span>

## <a name="about-the-author"></a><span data-ttu-id="81555-521">Informazioni sull'autore</span><span class="sxs-lookup"><span data-stu-id="81555-521">About the Author</span></span>

<span data-ttu-id="81555-522">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), autore di sette ASP/ASP. NET Books e fondatore di [4GuysFromRolla.com](http://www.4guysfromrolla.com), collabora con le tecnologie Web Microsoft a partire da 1998.</span><span class="sxs-lookup"><span data-stu-id="81555-522">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of seven ASP/ASP.NET books and founder of [4GuysFromRolla.com](http://www.4guysfromrolla.com), has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="81555-523">Scott lavora come consulente, trainer e writer indipendenti.</span><span class="sxs-lookup"><span data-stu-id="81555-523">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="81555-524">Il suo ultimo libro è [*Sams Teach Yourself ASP.NET 2,0 in 24 ore*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span><span class="sxs-lookup"><span data-stu-id="81555-524">His latest book is [*Sams Teach Yourself ASP.NET 2.0 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="81555-525">Può essere raggiunto in [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="81555-525">He can be reached at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span> <span data-ttu-id="81555-526">o tramite il suo Blog, disponibile in [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span><span class="sxs-lookup"><span data-stu-id="81555-526">or via his blog, which can be found at [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span></span>

## <a name="special-thanks-to"></a><span data-ttu-id="81555-527">Grazie speciale</span><span class="sxs-lookup"><span data-stu-id="81555-527">Special Thanks To</span></span>

<span data-ttu-id="81555-528">Questa serie di esercitazioni è stata esaminata da molti revisori utili.</span><span class="sxs-lookup"><span data-stu-id="81555-528">This tutorial series was reviewed by many helpful reviewers.</span></span> <span data-ttu-id="81555-529">Il revisore principale di questa esercitazione è stato S Ren Jacob Lauritsen.</span><span class="sxs-lookup"><span data-stu-id="81555-529">Lead reviewer for this tutorial was S ren Jacob Lauritsen.</span></span> <span data-ttu-id="81555-530">Oltre a esaminare questo articolo, S Ren ha creato anche il progetto di Visual C# Express Edition incluso nel download di questo articolo per la compilazione manuale degli oggetti di database gestiti.</span><span class="sxs-lookup"><span data-stu-id="81555-530">In addition to reviewing this article, S ren also created the Visual C# Express Edition project included in this article s download for manually compiling the managed database objects.</span></span> <span data-ttu-id="81555-531">Sei interessato a esaminare i miei prossimi articoli MSDN?</span><span class="sxs-lookup"><span data-stu-id="81555-531">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="81555-532">In tal caso, rilasciare una riga in [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="81555-532">If so, drop me a line at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="81555-533">Precedente</span><span class="sxs-lookup"><span data-stu-id="81555-533">Previous</span></span>](debugging-stored-procedures-vb.md)
