---
uid: aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/the-fix-it-sample-application
title: "Appendice: l'applicazione di esempio Fix it (compilazione di app Cloud reali con Azure) | Microsoft Docs"
author: MikeWasson
description: La creazione di app cloud del mondo reale con l'e-book di Azure si basa su una presentazione sviluppata da Scott Guthrie. Vengono illustrati 13 modelli e procedure che possono essere...
ms.author: riande
ms.date: 06/12/2014
ms.assetid: 1bc333c5-f096-4ea7-b170-779accc21c1a
msc.legacyurl: /aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/the-fix-it-sample-application
msc.type: authoredcontent
ms.openlocfilehash: 896196bdb6a6b0d12a6c798ead510e37dd38a9fc
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 03/06/2020
ms.locfileid: "78583447"
---
# <a name="appendix-the-fix-it-sample-application-building-real-world-cloud-apps-with-azure"></a><span data-ttu-id="daf47-104">Appendice: l'applicazione di esempio Fix it (compilazione di app Cloud reali con Azure)</span><span class="sxs-lookup"><span data-stu-id="daf47-104">Appendix: The Fix It Sample Application (Building Real-World Cloud Apps with Azure)</span></span>

<span data-ttu-id="daf47-105">di [Mike Wasson](https://github.com/MikeWasson), [Rick Anderson](https://twitter.com/RickAndMSFT), [Tom Dykstra](https://github.com/tdykstra)</span><span class="sxs-lookup"><span data-stu-id="daf47-105">by [Mike Wasson](https://github.com/MikeWasson), [Rick Anderson](https://twitter.com/RickAndMSFT), [Tom Dykstra](https://github.com/tdykstra)</span></span>

[<span data-ttu-id="daf47-106">Scaricare il progetto Correggi it</span><span class="sxs-lookup"><span data-stu-id="daf47-106">Download The Fix It Project</span></span>](https://code.msdn.microsoft.com/Fix-It-app-for-Building-cdd80df4)

> <span data-ttu-id="daf47-107">La **creazione di app cloud del mondo reale con** l'e-book di Azure si basa su una presentazione sviluppata da Scott Guthrie.</span><span class="sxs-lookup"><span data-stu-id="daf47-107">The **Building Real World Cloud Apps with Azure** e-book is based on a presentation developed by Scott Guthrie.</span></span> <span data-ttu-id="daf47-108">Vengono illustrati 13 modelli e procedure che consentono di sviluppare correttamente app Web per il cloud.</span><span class="sxs-lookup"><span data-stu-id="daf47-108">It explains 13 patterns and practices that can help you be successful developing web apps for the cloud.</span></span> <span data-ttu-id="daf47-109">Per informazioni sull'e-book, vedere [il primo capitolo](introduction.md).</span><span class="sxs-lookup"><span data-stu-id="daf47-109">For information about the e-book, see [the first chapter](introduction.md).</span></span>

<span data-ttu-id="daf47-110">Questa appendice per la creazione di app Cloud reali con e-book di Azure contiene le sezioni seguenti che forniscono informazioni aggiuntive sull'applicazione di esempio Fix it che è possibile scaricare:</span><span class="sxs-lookup"><span data-stu-id="daf47-110">This appendix to the Building Real World Cloud Apps with Azure e-book contains the following sections that provide additional information about the Fix It sample application that you can download:</span></span>

- [<span data-ttu-id="daf47-111">Problemi noti</span><span class="sxs-lookup"><span data-stu-id="daf47-111">Known issues</span></span>](#knownissues)
- [<span data-ttu-id="daf47-112">Procedure consigliate</span><span class="sxs-lookup"><span data-stu-id="daf47-112">Best practices</span></span>](#bestpractices)
- [<span data-ttu-id="daf47-113">Come eseguire l'app da Visual Studio nel computer locale</span><span class="sxs-lookup"><span data-stu-id="daf47-113">How to run the app from Visual Studio on your local computer</span></span>](#run-in-vs)
- [<span data-ttu-id="daf47-114">Come distribuire l'app di base in app Web del servizio app Azure usando gli script di Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="daf47-114">How to deploy the base app to Azure App Service Web Apps by using the Windows PowerShell scripts</span></span>](#deploybase)
- [<span data-ttu-id="daf47-115">Risoluzione dei problemi relativi agli script di Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="daf47-115">Troubleshooting the Windows PowerShell scripts</span></span>](#troubleshooting)
- [<span data-ttu-id="daf47-116">Come distribuire l'app con l'elaborazione delle code in app Web del servizio app Azure e un servizio cloud di Azure</span><span class="sxs-lookup"><span data-stu-id="daf47-116">How to deploy the app with queue processing to Azure App Service Web Apps and an Azure Cloud Service</span></span>](#deployqueues)

<a id="knownissues"></a>
## <a name="known-issues"></a><span data-ttu-id="daf47-117">Problemi noti</span><span class="sxs-lookup"><span data-stu-id="daf47-117">Known issues</span></span>

<span data-ttu-id="daf47-118">L'app per la correzione è stata originariamente sviluppata per illustrare il più semplice possibile alcuni dei modelli presentati in questo e-book.</span><span class="sxs-lookup"><span data-stu-id="daf47-118">The Fix It app was originally developed in order to illustrate as simply as possible some of the patterns presented in this e-book.</span></span> <span data-ttu-id="daf47-119">Tuttavia, poiché l'e-book riguarda la creazione di app reali, abbiamo sottoposto il codice di correzione a un processo di revisione e test simile a quello che avremmo fatto per il software rilasciato.</span><span class="sxs-lookup"><span data-stu-id="daf47-119">However, since the e-book is about building real-world apps, we subjected the Fix It code to a review and testing process similar to what we'd do for released software.</span></span> <span data-ttu-id="daf47-120">Sono stati rilevati diversi problemi e, come per qualsiasi applicazione reale, alcuni di essi sono stati corretti e alcuni di essi sono stati rilasciati a una versione successiva.</span><span class="sxs-lookup"><span data-stu-id="daf47-120">We found a number of issues, and as with any real-world application, some of them we fixed and some of them we deferred to a later release.</span></span>

<span data-ttu-id="daf47-121">Nell'elenco seguente sono inclusi i problemi che devono essere risolti in un'applicazione di produzione, ma per un motivo o un altro si è deciso di non risolvere nella versione iniziale dell'applicazione di esempio Fix it.</span><span class="sxs-lookup"><span data-stu-id="daf47-121">The following list includes issues that should be addressed in a production application, but for one reason or another we decided not to address in the initial release of the Fix It sample application.</span></span>

### <a name="security"></a><span data-ttu-id="daf47-122">Sicurezza</span><span class="sxs-lookup"><span data-stu-id="daf47-122">Security</span></span>

- <span data-ttu-id="daf47-123">Assicurarsi che non sia possibile assegnare un'attività a un proprietario inesistente.</span><span class="sxs-lookup"><span data-stu-id="daf47-123">Ensure that you can't assign a task to a non-existent owner.</span></span>
- <span data-ttu-id="daf47-124">Assicurarsi che sia possibile visualizzare e modificare solo le attività create o assegnate all'utente.</span><span class="sxs-lookup"><span data-stu-id="daf47-124">Ensure that you can only view and modify tasks that you created or are assigned to you.</span></span>
- <span data-ttu-id="daf47-125">Usare HTTPS per le pagine di accesso e i cookie di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-125">Use HTTPS for sign-in pages and authentication cookies.</span></span>
- <span data-ttu-id="daf47-126">Specificare un limite di tempo per i cookie di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-126">Specify a time limit for authentication cookies.</span></span>

### <a name="input-validation"></a><span data-ttu-id="daf47-127">Convalida dell'input</span><span class="sxs-lookup"><span data-stu-id="daf47-127">Input validation</span></span>

<span data-ttu-id="daf47-128">In generale, un'app di produzione esegue una maggiore convalida dell'input rispetto all'app per la correzione.</span><span class="sxs-lookup"><span data-stu-id="daf47-128">In general, a production app would do more input validation than the Fix It app.</span></span> <span data-ttu-id="daf47-129">Ad esempio, la dimensione dell'immagine o le dimensioni del file di immagine consentite per il caricamento devono essere limitate.</span><span class="sxs-lookup"><span data-stu-id="daf47-129">For example, the image size / image file size allowed for upload should be limited.</span></span>

### <a name="administrator-functionality"></a><span data-ttu-id="daf47-130">Funzionalità amministratore</span><span class="sxs-lookup"><span data-stu-id="daf47-130">Administrator functionality</span></span>

<span data-ttu-id="daf47-131">Un amministratore deve essere in grado di modificare la proprietà per le attività esistenti.</span><span class="sxs-lookup"><span data-stu-id="daf47-131">An administrator should be able to change ownership on existing tasks.</span></span> <span data-ttu-id="daf47-132">Il creatore di un'attività, ad esempio, potrebbe lasciare l'azienda, lasciando che non sia disponibile l'autorizzazione a mantenere l'attività a meno che non sia abilitato l'accesso amministrativo.</span><span class="sxs-lookup"><span data-stu-id="daf47-132">For example, the creator of a task might leave the company, leaving no one with authority to maintain the task unless administrative access is enabled.</span></span>

### <a name="queue-message-processing"></a><span data-ttu-id="daf47-133">Elaborazione messaggi in coda</span><span class="sxs-lookup"><span data-stu-id="daf47-133">Queue message processing</span></span>

<span data-ttu-id="daf47-134">L'elaborazione dei messaggi in coda nell'app per la correzione è stata progettata per essere semplice per illustrare il modello di lavoro incentrato sulle code con una quantità minima di codice.</span><span class="sxs-lookup"><span data-stu-id="daf47-134">Queue message processing in the Fix It app was designed to be simple in order to illustrate the queue-centric work pattern with a minimum amount of code.</span></span> <span data-ttu-id="daf47-135">Questo semplice codice non sarebbe adatto per un'applicazione di produzione effettiva.</span><span class="sxs-lookup"><span data-stu-id="daf47-135">This simple code would not be adequate for an actual production application.</span></span>

- <span data-ttu-id="daf47-136">Il codice non garantisce che ogni messaggio della coda venga elaborato al massimo una volta.</span><span class="sxs-lookup"><span data-stu-id="daf47-136">The code does not guarantee that each queue message will be processed at most once.</span></span> <span data-ttu-id="daf47-137">Quando si riceve un messaggio dalla coda, si verifica un periodo di timeout durante il quale il messaggio è invisibile ad altri listener della coda.</span><span class="sxs-lookup"><span data-stu-id="daf47-137">When you get a message from the queue, there is a timeout period, during which the message is invisible to other queue listeners.</span></span> <span data-ttu-id="daf47-138">Se il timeout scade prima che il messaggio venga eliminato, il messaggio diventa nuovamente visibile.</span><span class="sxs-lookup"><span data-stu-id="daf47-138">If the timeout expires before the message is deleted, the message becomes visible again.</span></span> <span data-ttu-id="daf47-139">Pertanto, se un'istanza del ruolo di lavoro impiega molto tempo per l'elaborazione di un messaggio, è teoricamente possibile che lo stesso messaggio venga elaborato due volte, ottenendo un'attività duplicata nel database.</span><span class="sxs-lookup"><span data-stu-id="daf47-139">Therefore, if a worker role instance spends a long time processing a message, it is theoretically possible for the same message to get processed twice, resulting in a duplicate task in the database.</span></span> <span data-ttu-id="daf47-140">Per altre informazioni su questo problema, vedere [uso delle code di archiviazione di Azure](https://msdn.microsoft.com/library/ff803365.aspx#sec7).</span><span class="sxs-lookup"><span data-stu-id="daf47-140">For more information about this issue, see [Using Azure Storage Queues](https://msdn.microsoft.com/library/ff803365.aspx#sec7).</span></span>
- <span data-ttu-id="daf47-141">La logica di polling della coda può essere più conveniente, suddividendo in batch il recupero dei messaggi.</span><span class="sxs-lookup"><span data-stu-id="daf47-141">The queue polling logic could be more cost-effective, by batching message retrieval.</span></span> <span data-ttu-id="daf47-142">Ogni volta che si chiama [CloudQueue. GetMessageAsync](https://msdn.microsoft.com/library/microsoft.windowsazure.storage.queue.cloudqueue.getmessageasync.aspx), è previsto un costo per la transazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-142">Every time you call [CloudQueue.GetMessageAsync](https://msdn.microsoft.com/library/microsoft.windowsazure.storage.queue.cloudqueue.getmessageasync.aspx), there is a transaction cost.</span></span> <span data-ttu-id="daf47-143">In alternativa, è possibile chiamare [CloudQueue. GetMessagesAsync](https://msdn.microsoft.com/library/microsoft.windowsazure.storage.queue.cloudqueue.getmessagesasync.aspx) (prendere nota del plurale ' s'), che ottiene più messaggi in una singola transazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-143">Instead, you can call [CloudQueue.GetMessagesAsync](https://msdn.microsoft.com/library/microsoft.windowsazure.storage.queue.cloudqueue.getmessagesasync.aspx) (note the plural 's'), which gets multiple messages in a single transaction.</span></span> <span data-ttu-id="daf47-144">I costi delle transazioni per le code di archiviazione di Azure sono molto bassi, quindi l'effetto sui costi non è sostanziale nella maggior parte degli scenari.</span><span class="sxs-lookup"><span data-stu-id="daf47-144">The transaction costs for Azure Storage Queues are very low, so the impact on costs is not substantial in most scenarios.</span></span>
- <span data-ttu-id="daf47-145">Il loop stretto nel codice di elaborazione dei messaggi della coda causa l'affinità della CPU, che non usa in modo efficiente le VM multicore.</span><span class="sxs-lookup"><span data-stu-id="daf47-145">The tight loop in the queue message-processing code causes CPU affinity, which does not utilize multi-core VMs efficiently.</span></span> <span data-ttu-id="daf47-146">Una progettazione migliore usa il parallelismo delle attività per eseguire diverse attività asincrone in parallelo.</span><span class="sxs-lookup"><span data-stu-id="daf47-146">A better design would use task parallelism to run several async tasks in parallel.</span></span>
- <span data-ttu-id="daf47-147">L'elaborazione dei messaggi della coda ha solo una gestione delle eccezioni rudimentale.</span><span class="sxs-lookup"><span data-stu-id="daf47-147">Queue message-processing has only rudimentary exception handling.</span></span> <span data-ttu-id="daf47-148">Ad esempio, il codice non gestisce [i messaggi non elaborabili](https://msdn.microsoft.com/library/ms789028.aspx).</span><span class="sxs-lookup"><span data-stu-id="daf47-148">For example, the code doesn't handle [poison messages](https://msdn.microsoft.com/library/ms789028.aspx).</span></span> <span data-ttu-id="daf47-149">Quando l'elaborazione del messaggio genera un'eccezione, è necessario registrare l'errore ed eliminare il messaggio. in caso contrario, il ruolo di lavoro tenterà di elaborarlo di nuovo e il ciclo continuerà per un periodo illimitato.</span><span class="sxs-lookup"><span data-stu-id="daf47-149">(When message processing causes an exception, you have to log the error and delete the message, or the worker role will try to process it again, and the loop will continue indefinitely.)</span></span>

### <a name="sql-queries-are-unbounded"></a><span data-ttu-id="daf47-150">Query SQL non associate</span><span class="sxs-lookup"><span data-stu-id="daf47-150">SQL queries are unbounded</span></span>

<span data-ttu-id="daf47-151">Il codice corrente di correzione it non impone alcun limite per il numero di righe che possono essere restituite dalle query per le pagine di indice.</span><span class="sxs-lookup"><span data-stu-id="daf47-151">Current Fix It code places no limit on how many rows the queries for Index pages might return.</span></span> <span data-ttu-id="daf47-152">Se nel database viene immesso un volume elevato di attività, le dimensioni degli elenchi ottenuti potrebbero causare problemi di prestazioni.</span><span class="sxs-lookup"><span data-stu-id="daf47-152">If a large volume of tasks is entered into the database, the size of the resulting lists received could cause performance issues.</span></span> <span data-ttu-id="daf47-153">La soluzione consiste nell'implementare il paging.</span><span class="sxs-lookup"><span data-stu-id="daf47-153">The solution is to implement paging.</span></span> <span data-ttu-id="daf47-154">Per un esempio, vedere [ordinamento, filtro e paging con la Entity Framework in un'applicazione MVC ASP.NET](../../../../mvc/overview/getting-started/getting-started-with-ef-using-mvc/sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md).</span><span class="sxs-lookup"><span data-stu-id="daf47-154">For an example, see [Sorting, Filtering, and Paging with the Entity Framework in an ASP.NET MVC Application](../../../../mvc/overview/getting-started/getting-started-with-ef-using-mvc/sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md).</span></span>

### <a name="view-models-recommended"></a><span data-ttu-id="daf47-155">Modelli di visualizzazione consigliati</span><span class="sxs-lookup"><span data-stu-id="daf47-155">View models recommended</span></span>

<span data-ttu-id="daf47-156">L'app Fix it usa la classe di entità FixItTask per passare le informazioni tra il controller e la visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-156">The Fix It app uses the FixItTask entity class to pass information between the controller and the view.</span></span> <span data-ttu-id="daf47-157">Una procedura consigliata consiste nell'usare i modelli di visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-157">A best practice is to use view models.</span></span> <span data-ttu-id="daf47-158">Il modello di dominio, ad esempio la classe di entità FixItTask, è progettato in base a quanto necessario per la persistenza dei dati, mentre un modello di visualizzazione può essere progettato per la presentazione dei dati.</span><span class="sxs-lookup"><span data-stu-id="daf47-158">The domain model (e.g., the FixItTask entity class) is designed around what is needed for data persistence, while a view model can be designed for data presentation.</span></span> <span data-ttu-id="daf47-159">Per altre informazioni, vedere [12 ASP.NET MVC Best Practices](https://codeclimber.net.nz/archive/2009/10/27/12-asp.net-mvc-best-practices.aspx).</span><span class="sxs-lookup"><span data-stu-id="daf47-159">For more information, see [12 ASP.NET MVC Best Practices](https://codeclimber.net.nz/archive/2009/10/27/12-asp.net-mvc-best-practices.aspx).</span></span>

### <a name="secure-image-blob-recommended"></a><span data-ttu-id="daf47-160">BLOB di immagini protette consigliato</span><span class="sxs-lookup"><span data-stu-id="daf47-160">Secure image blob recommended</span></span>

<span data-ttu-id="daf47-161">L'app Fix it archivia le immagini caricate come pubbliche, ovvero chiunque possa trovare l'URL può accedere alle immagini.</span><span class="sxs-lookup"><span data-stu-id="daf47-161">The Fix It app stores uploaded images as public, meaning that anyone who finds the URL can access the images.</span></span> <span data-ttu-id="daf47-162">Le immagini possono essere protette anziché pubbliche.</span><span class="sxs-lookup"><span data-stu-id="daf47-162">The images could be secured instead of public.</span></span>

### <a name="no-powershell-automation-scripts-for-queues"></a><span data-ttu-id="daf47-163">Nessun script di automazione di PowerShell per le code</span><span class="sxs-lookup"><span data-stu-id="daf47-163">No PowerShell automation scripts for queues</span></span>

<span data-ttu-id="daf47-164">Gli script di automazione di PowerShell di esempio sono stati scritti solo per la versione di base di correzione eseguita completamente in app Azure app Web del servizio.</span><span class="sxs-lookup"><span data-stu-id="daf47-164">Sample PowerShell automation scripts were written only for the base version of Fix It that runs entirely in Azure App Service Web Apps.</span></span> <span data-ttu-id="daf47-165">Non sono stati forniti script per la configurazione e la distribuzione nell'app Web e l'ambiente del servizio cloud necessario per l'elaborazione delle code.</span><span class="sxs-lookup"><span data-stu-id="daf47-165">We haven't provided scripts for setting up and deploying to the web app plus Cloud Service environment required for queue processing.</span></span>

### <a name="special-handling-for-html-codes-in-user-input"></a><span data-ttu-id="daf47-166">Gestione speciale per i codici HTML nell'input utente</span><span class="sxs-lookup"><span data-stu-id="daf47-166">Special handling for HTML codes in user input</span></span>

<span data-ttu-id="daf47-167">ASP.NET impedisce automaticamente molti modi in cui gli utenti malintenzionati potrebbero provare gli attacchi di scripting tra siti immettendo lo script nelle caselle di testo di input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="daf47-167">ASP.NET automatically prevents many ways in which malicious users might attempt cross-site scripting attacks by entering script in user input text boxes.</span></span> <span data-ttu-id="daf47-168">E l'helper `DisplayFor` MVC usato per visualizzare i titoli delle attività e le note codificano automaticamente i valori che invia al browser.</span><span class="sxs-lookup"><span data-stu-id="daf47-168">And the MVC `DisplayFor` helper used to display task titles and notes automatically HTML-encodes values that it sends to the browser.</span></span> <span data-ttu-id="daf47-169">Tuttavia, in un'app di produzione è consigliabile adottare misure aggiuntive.</span><span class="sxs-lookup"><span data-stu-id="daf47-169">But in a production app you might want to take additional measures.</span></span> <span data-ttu-id="daf47-170">Per ulteriori informazioni, vedere la pagina relativa [alla convalida delle richieste in ASP.NET](https://msdn.microsoft.com/library/hh882339.aspx).</span><span class="sxs-lookup"><span data-stu-id="daf47-170">For more information, see [Request Validation in ASP.NET](https://msdn.microsoft.com/library/hh882339.aspx).</span></span>

<a id="bestpractices"></a>
## <a name="best-practices"></a><span data-ttu-id="daf47-171">Procedure consigliate</span><span class="sxs-lookup"><span data-stu-id="daf47-171">Best practices</span></span>

<span data-ttu-id="daf47-172">Di seguito sono riportati alcuni problemi che sono stati risolti dopo essere stati individuati nella revisione del codice e i test della versione originale dell'app per la correzione.</span><span class="sxs-lookup"><span data-stu-id="daf47-172">Following are some issues that were fixed after being discovered in code review and testing of the original version of the Fix It app.</span></span> <span data-ttu-id="daf47-173">Alcune sono state causate dal fatto che il codificatore originale non è a conoscenza di una particolare procedura consigliata, alcune semplicemente perché il codice è stato scritto rapidamente e non era destinato al software rilasciato.</span><span class="sxs-lookup"><span data-stu-id="daf47-173">Some were caused by the original coder not being aware of a particular best practice, some simply because the code was written quickly and wasn't intended for released software.</span></span> <span data-ttu-id="daf47-174">In questo articolo vengono elencati i problemi in caso di elementi appresi da questa revisione e test che potrebbero essere utili ad altri utenti che sviluppano anche app Web.</span><span class="sxs-lookup"><span data-stu-id="daf47-174">We're listing the issues here in case there's something we learned from this review and testing that might be helpful to others who are also developing web apps.</span></span>

### <a name="dispose-the-database-repository"></a><span data-ttu-id="daf47-175">Eliminare il repository di database</span><span class="sxs-lookup"><span data-stu-id="daf47-175">Dispose the database repository</span></span>

<span data-ttu-id="daf47-176">La classe `FixItTaskRepository` deve eliminare l'istanza di `DbContext` Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="daf47-176">The `FixItTaskRepository` class must dispose the Entity Framework `DbContext` instance.</span></span> <span data-ttu-id="daf47-177">Questa operazione è stata apportata implementando `IDisposable` nella classe `FixItTaskRepository`:</span><span class="sxs-lookup"><span data-stu-id="daf47-177">We did this by implementing `IDisposable` in the `FixItTaskRepository` class:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample1.cs)]

<span data-ttu-id="daf47-178">Si noti che AutoFac eliminerà automaticamente l'istanza `FixItTaskRepository`, quindi non è necessario eliminarla in modo esplicito.</span><span class="sxs-lookup"><span data-stu-id="daf47-178">Note that AutoFac will automatically dispose the `FixItTaskRepository` instance, so we don't need to explicitly dispose it.</span></span>

<span data-ttu-id="daf47-179">Un'altra opzione consiste nel rimuovere la variabile membro `DbContext` da `FixItTaskRepository`e creare invece una variabile `DbContext` locale all'interno di ogni metodo del repository, all'interno di un'istruzione `using`.</span><span class="sxs-lookup"><span data-stu-id="daf47-179">Another option is to remove the `DbContext` member variable from `FixItTaskRepository`, and instead create a local `DbContext` variable within each repository method, inside a `using` statement.</span></span> <span data-ttu-id="daf47-180">Esempio:</span><span class="sxs-lookup"><span data-stu-id="daf47-180">For example:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample2.cs)]

### <a name="register-singletons-as-such-with-di"></a><span data-ttu-id="daf47-181">Registrare i singleton come tali con DI</span><span class="sxs-lookup"><span data-stu-id="daf47-181">Register singletons as such with DI</span></span>

<span data-ttu-id="daf47-182">Poiché è necessaria una sola istanza della classe `PhotoService` e della classe `Logger`, queste classi devono essere [registrate come istanze singole per l'inserimento di dipendenze](https://code.google.com/p/autofac/wiki/InstanceScope) in *DependenciesConfig.cs*:</span><span class="sxs-lookup"><span data-stu-id="daf47-182">Since only one instance of the `PhotoService` class and `Logger` class is needed, these classes should be [registered as single instances for dependency injection](https://code.google.com/p/autofac/wiki/InstanceScope) in *DependenciesConfig.cs*:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample3.cs?highlight=1,3)]

### <a name="security-dont-show-error-details-to-users"></a><span data-ttu-id="daf47-183">Sicurezza: non visualizzare i dettagli dell'errore per gli utenti</span><span class="sxs-lookup"><span data-stu-id="daf47-183">Security: Don't show error details to users</span></span>

<span data-ttu-id="daf47-184">L'app per la correzione originale non disponeva di una pagina di errore generica e consente di visualizzare tutte le eccezioni fino all'interfaccia utente, pertanto alcune eccezioni come gli errori di connessione al database potrebbero comportare la visualizzazione di una traccia dello stack completa nel browser.</span><span class="sxs-lookup"><span data-stu-id="daf47-184">The original Fix It app didn't have a generic error page and just let all exceptions bubble up to the UI, so some exceptions such as database connection errors could result in a full stack trace being displayed to the browser.</span></span> <span data-ttu-id="daf47-185">Informazioni dettagliate sugli errori possono a volte facilitare gli attacchi da parte di utenti malintenzionati.</span><span class="sxs-lookup"><span data-stu-id="daf47-185">Detailed error information can sometimes facilitate attacks by malicious users.</span></span> <span data-ttu-id="daf47-186">La soluzione consiste nel registrare i dettagli dell'eccezione e visualizzare una pagina di errore per l'utente che non include i dettagli dell'errore.</span><span class="sxs-lookup"><span data-stu-id="daf47-186">The solution is to log the exception details and display an error page to the user that doesn't include error details.</span></span> <span data-ttu-id="daf47-187">La correzione dell'app it era già in registrazione e per visualizzare una pagina di errore è stato aggiunto `<customErrors mode=On>` nel file Web. config.</span><span class="sxs-lookup"><span data-stu-id="daf47-187">The Fix It app was already logging, and in order to display an error page, we added `<customErrors mode=On>` in the Web.config file.</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample4.xml?highlight=2)]

<span data-ttu-id="daf47-188">Per impostazione predefinita, in questo modo *Views\Shared\Error.cshtml* viene visualizzato per individuare eventuali errori.</span><span class="sxs-lookup"><span data-stu-id="daf47-188">By default this causes *Views\Shared\Error.cshtml* to be displayed for errors.</span></span> <span data-ttu-id="daf47-189">È possibile personalizzare *Error. cshtml* o creare una visualizzazione della pagina di errore personalizzata e aggiungere un attributo `defaultRedirect`.</span><span class="sxs-lookup"><span data-stu-id="daf47-189">You can customize *Error.cshtml* or create your own error page view and add a `defaultRedirect` attribute.</span></span> <span data-ttu-id="daf47-190">È inoltre possibile specificare diverse pagine di errore per errori specifici.</span><span class="sxs-lookup"><span data-stu-id="daf47-190">You can also specify different error pages for specific errors.</span></span>

### <a name="security-only-allow-a-task-to-be-edited-by-its-creator"></a><span data-ttu-id="daf47-191">Sicurezza: consente solo la modifica di un'attività da un creatore</span><span class="sxs-lookup"><span data-stu-id="daf47-191">Security: only allow a task to be edited by its creator</span></span>

<span data-ttu-id="daf47-192">La pagina di indice del dashboard Mostra solo le attività create dall'utente connesso, ma un utente malintenzionato potrebbe creare un URL con un ID per l'attività di un altro utente.</span><span class="sxs-lookup"><span data-stu-id="daf47-192">The Dashboard Index page only shows tasks created by the logged-on user, but a malicious user could create a URL with an ID to another user's task.</span></span> <span data-ttu-id="daf47-193">È stato aggiunto il codice in *DashboardController.cs* per restituire 404 in questo caso:</span><span class="sxs-lookup"><span data-stu-id="daf47-193">We added code in *DashboardController.cs* to return a 404 in that case:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample5.cs?highlight=9-14,24-29)]

### <a name="dont-swallow-exceptions"></a><span data-ttu-id="daf47-194">Non ingoiare eccezioni</span><span class="sxs-lookup"><span data-stu-id="daf47-194">Don't swallow exceptions</span></span>

<span data-ttu-id="daf47-195">L'app Fix it originale ha appena restituito null dopo la registrazione di un'eccezione risultante da una query SQL:</span><span class="sxs-lookup"><span data-stu-id="daf47-195">The original Fix It app just returned null after logging an exception that resulted from a SQL query:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample6.cs?highlight=4)]

<span data-ttu-id="daf47-196">In questo modo, l'utente avrà l'aspetto dell'esito positivo della query, ma non ha restituito alcuna riga.</span><span class="sxs-lookup"><span data-stu-id="daf47-196">This would make it look to the user as if the query succeeded but just didn't return any rows.</span></span> <span data-ttu-id="daf47-197">La soluzione consiste nel generare nuovamente l'eccezione dopo l'intercettazione e la registrazione:</span><span class="sxs-lookup"><span data-stu-id="daf47-197">Solution is to re-throw the exception after catching and logging:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample7.cs)]

### <a name="catch-all-exceptions-in-worker-roles"></a><span data-ttu-id="daf47-198">Intercettare tutte le eccezioni nei ruoli di lavoro</span><span class="sxs-lookup"><span data-stu-id="daf47-198">Catch all exceptions in worker roles</span></span>

<span data-ttu-id="daf47-199">Eventuali eccezioni non gestite in un ruolo di lavoro provocheranno il riciclo della macchina virtuale, pertanto si desidera eseguire il wrapping di tutte le operazioni eseguite in un blocco try-catch e gestire tutte le eccezioni.</span><span class="sxs-lookup"><span data-stu-id="daf47-199">Any unhandled exceptions in a worker role will cause the VM to be recycled, so you want to wrap everything you do in a try-catch block and handle all exceptions.</span></span>

### <a name="specify-length-for-string-properties-in-entity-classes"></a><span data-ttu-id="daf47-200">Specificare la lunghezza per le proprietà di stringa nelle classi di entità</span><span class="sxs-lookup"><span data-stu-id="daf47-200">Specify length for string properties in entity classes</span></span>

<span data-ttu-id="daf47-201">Per visualizzare il codice semplice, la versione originale dell'app Fix it non ha specificato lunghezze per i campi dell'entità FixItTask e, di conseguenza, sono stati definiti come varchar (max) nel database.</span><span class="sxs-lookup"><span data-stu-id="daf47-201">In order to display simple code, the original version of the Fix It app didn't specify lengths for the fields of the FixItTask entity, and as a result they were defined as varchar(max) in the database.</span></span> <span data-ttu-id="daf47-202">Di conseguenza, l'interfaccia utente accetta quasi tutte le quantità di input.</span><span class="sxs-lookup"><span data-stu-id="daf47-202">As a result, the UI would accept almost any amount of input.</span></span> <span data-ttu-id="daf47-203">Specificando i limiti delle lunghezze che si applicano sia all'input utente nella pagina Web che alla dimensione della colonna nel database:</span><span class="sxs-lookup"><span data-stu-id="daf47-203">Specifying lengths sets limits that apply both to user input in the web page and column size in the database:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample8.cs?highlight=4,7,10,12,14)]

### <a name="mark-private-members-as-readonly-when-they-arent-expected-to-change"></a><span data-ttu-id="daf47-204">Contrassegnare i membri privati come ReadOnly quando non si prevede di modificare</span><span class="sxs-lookup"><span data-stu-id="daf47-204">Mark private members as readonly when they aren't expected to change</span></span>

<span data-ttu-id="daf47-205">Ad esempio, nella classe `DashboardController` viene creata un'istanza di `FixItTaskRepository` e non si prevede che venga modificata, quindi è stata definita come [ReadOnly](https://msdn.microsoft.com/library/acdd6hb7.aspx).</span><span class="sxs-lookup"><span data-stu-id="daf47-205">For example, in the `DashboardController` class an instance of `FixItTaskRepository` is created and isn't expected to change, so we defined it as [readonly](https://msdn.microsoft.com/library/acdd6hb7.aspx).</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample9.cs?highlight=3)]

### <a name="use-listany-instead-of-listcount-gt-0"></a><span data-ttu-id="daf47-206">Usare l'elenco. Any () anziché list. Count () &gt; 0</span><span class="sxs-lookup"><span data-stu-id="daf47-206">Use list.Any() instead of list.Count() &gt; 0</span></span>

<span data-ttu-id="daf47-207">Se si è interessati al fatto che uno o più elementi in un elenco corrispondano ai criteri specificati, usare il metodo [any](https://msdn.microsoft.com/library/bb534972.aspx) , perché restituisce non appena viene trovato un elemento che consente di adattare i criteri, mentre il metodo `Count` deve sempre scorrere ogni elemento.</span><span class="sxs-lookup"><span data-stu-id="daf47-207">If you all you care about is whether one or more items in a list fit the specified criteria, use the [Any](https://msdn.microsoft.com/library/bb534972.aspx) method, because it returns as soon as an item fitting the criteria is found, whereas the `Count` method always has to iterate through every item.</span></span> <span data-ttu-id="daf47-208">Il file *index. cshtml* del dashboard includeva originariamente il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="daf47-208">The Dashboard *Index.cshtml* file originally had this code:</span></span>

[!code-cshtml[Main](the-fix-it-sample-application/samples/sample10.cshtml)]

<span data-ttu-id="daf47-209">L'operazione è stata modificata in questo modo:</span><span class="sxs-lookup"><span data-stu-id="daf47-209">We changed it to this:</span></span>

[!code-cshtml[Main](the-fix-it-sample-application/samples/sample11.cshtml?highlight=1)]

### <a name="generate-urls-in-mvc-views-using-mvc-helpers"></a><span data-ttu-id="daf47-210">Generare URL nelle visualizzazioni MVC usando gli helper MVC</span><span class="sxs-lookup"><span data-stu-id="daf47-210">Generate URLs in MVC views using MVC helpers</span></span>

<span data-ttu-id="daf47-211">Per il pulsante **Crea un Fix it** nella Home page, l'app Fix it ha codificato un elemento Anchor:</span><span class="sxs-lookup"><span data-stu-id="daf47-211">For the **Create a Fix It** button on the home page, the Fix It app hard coded an anchor element:</span></span>

[!code-cshtml[Main](the-fix-it-sample-application/samples/sample12.cshtml)]

<span data-ttu-id="daf47-212">Per i collegamenti di visualizzazione/azione di questo tipo è preferibile usare l'helper HTML [URL. Action](https://msdn.microsoft.com/library/system.web.mvc.urlhelper.action.aspx) , ad esempio:</span><span class="sxs-lookup"><span data-stu-id="daf47-212">For View/Action links like this it's better to use the [Url.Action](https://msdn.microsoft.com/library/system.web.mvc.urlhelper.action.aspx) HTML helper, for example:</span></span>

[!code-cshtml[Main](the-fix-it-sample-application/samples/sample13.cshtml)]

### <a name="use-taskdelay-instead-of-threadsleep-in-worker-role"></a><span data-ttu-id="daf47-213">Usare Task. Delay anziché thread. Sleep nel ruolo di lavoro</span><span class="sxs-lookup"><span data-stu-id="daf47-213">Use Task.Delay instead of Thread.Sleep in worker role</span></span>

<span data-ttu-id="daf47-214">Il modello nuovo-progetto inserisce `Thread.Sleep` nel codice di esempio per un ruolo di lavoro, ma la causa della sospensione del thread può causare la generazione di thread superflui aggiuntivi da parte del pool di thread.</span><span class="sxs-lookup"><span data-stu-id="daf47-214">The new-project template puts `Thread.Sleep` in the sample code for a worker role, but causing the thread to sleep can cause the thread pool to spawn additional unnecessary threads.</span></span> <span data-ttu-id="daf47-215">È possibile evitare tale operazione utilizzando [Task. Delay](https://msdn.microsoft.com/library/hh139096.aspx) .</span><span class="sxs-lookup"><span data-stu-id="daf47-215">You can avoid that by using [Task.Delay](https://msdn.microsoft.com/library/hh139096.aspx) instead.</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample14.cs?highlight=11)]

### <a name="avoid-async-void"></a><span data-ttu-id="daf47-216">Evitare il void asincrono</span><span class="sxs-lookup"><span data-stu-id="daf47-216">Avoid async void</span></span>

<span data-ttu-id="daf47-217">Se un metodo asincrono non deve restituire un valore, restituire un tipo di `Task` anziché `void`.</span><span class="sxs-lookup"><span data-stu-id="daf47-217">If an async method doesn't need to return a value, return a `Task` type rather than `void`.</span></span>

<span data-ttu-id="daf47-218">Questo esempio è relativo alla classe `FixItQueueManager`:</span><span class="sxs-lookup"><span data-stu-id="daf47-218">This example is from the `FixItQueueManager` class:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample15.cs)]

<span data-ttu-id="daf47-219">Usare `async void` solo per i gestori eventi di primo livello.</span><span class="sxs-lookup"><span data-stu-id="daf47-219">You should use `async void` only for top-level event handlers.</span></span> <span data-ttu-id="daf47-220">Se si definisce un metodo come `async void`, il chiamante non può **attendere** il metodo o intercettare le eccezioni generate dal metodo.</span><span class="sxs-lookup"><span data-stu-id="daf47-220">If you define a method as `async void`, the caller cannot **await** the method or catch any exceptions the method throws.</span></span> <span data-ttu-id="daf47-221">Per ulteriori informazioni, vedere [procedure consigliate nella programmazione asincrona](https://msdn.microsoft.com/magazine/jj991977.aspx).</span><span class="sxs-lookup"><span data-stu-id="daf47-221">For more information, see [Best Practices in Asynchronous Programming](https://msdn.microsoft.com/magazine/jj991977.aspx).</span></span>

### <a name="use-a-cancellation-token-to-break-from-worker-role-loop"></a><span data-ttu-id="daf47-222">Usare un token di annullamento per interrompere il ciclo del ruolo di lavoro</span><span class="sxs-lookup"><span data-stu-id="daf47-222">Use a cancellation token to break from worker role loop</span></span>

<span data-ttu-id="daf47-223">In genere, il metodo **Run** in un ruolo di lavoro contiene un ciclo infinito.</span><span class="sxs-lookup"><span data-stu-id="daf47-223">Typically, the **Run** method on a worker role contains an infinite loop.</span></span> <span data-ttu-id="daf47-224">Quando il ruolo di lavoro viene arrestato, viene chiamato il metodo [RoleEntryPoint. OnStop](https://msdn.microsoft.com/library/windowsazure/microsoft.windowsazure.serviceruntime.roleentrypoint.onstop.aspx) .</span><span class="sxs-lookup"><span data-stu-id="daf47-224">When the worker role is stopping, the [RoleEntryPoint.OnStop](https://msdn.microsoft.com/library/windowsazure/microsoft.windowsazure.serviceruntime.roleentrypoint.onstop.aspx) method is called.</span></span> <span data-ttu-id="daf47-225">È consigliabile usare questo metodo per annullare le operazioni eseguite all'interno del metodo **Run** e uscire normalmente.</span><span class="sxs-lookup"><span data-stu-id="daf47-225">You should use this method to cancel the work that is being done inside the **Run** method and exit gracefully.</span></span> <span data-ttu-id="daf47-226">In caso contrario, il processo potrebbe essere terminato nel corso di un'operazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-226">Otherwise, the process might be terminated in the middle of an operation.</span></span>

### <a name="opt-out-of-automatic-mime-sniffing-procedure"></a><span data-ttu-id="daf47-227">Rifiutare esplicitamente la procedura di sniffing MIME automatico</span><span class="sxs-lookup"><span data-stu-id="daf47-227">Opt out of Automatic MIME Sniffing Procedure</span></span>

<span data-ttu-id="daf47-228">In alcuni casi, Internet Explorer segnala un tipo MIME diverso dal tipo specificato dal server Web.</span><span class="sxs-lookup"><span data-stu-id="daf47-228">In some cases, Internet Explorer reports a MIME type different than the type specified by the web server.</span></span> <span data-ttu-id="daf47-229">Se, ad esempio, Internet Explorer rileva contenuto HTML in un file fornito con l'intestazione di risposta HTTP Content-Type: text/plain, Internet Explorer determina che il rendering del contenuto deve essere eseguito come HTML.</span><span class="sxs-lookup"><span data-stu-id="daf47-229">For instance, if Internet Explorer finds HTML content in a file delivered with the HTTP response header Content-Type: text/plain, Internet Explorer determines that the content should be rendered as HTML.</span></span> <span data-ttu-id="daf47-230">Sfortunatamente, questo "analisi MIME" può causare problemi di sicurezza anche per i server che ospitano contenuto non attendibile.</span><span class="sxs-lookup"><span data-stu-id="daf47-230">Unfortunately, this "MIME-sniffing" can also lead to security problems for servers hosting untrusted content.</span></span> <span data-ttu-id="daf47-231">Per contrastare questo problema, Internet Explorer 8 ha apportato alcune modifiche al codice di determinazione del tipo MIME e consente agli sviluppatori di applicazioni di [rifiutare esplicitamente](https://blogs.msdn.com/b/ie/archive/2008/07/02/ie8-security-part-v-comprehensive-protection.aspx)l'analisi MIME.</span><span class="sxs-lookup"><span data-stu-id="daf47-231">To combat this problem, Internet Explorer 8 has made a number of changes to MIME-type determination code and allows application developers to [opt out of MIME-sniffing](https://blogs.msdn.com/b/ie/archive/2008/07/02/ie8-security-part-v-comprehensive-protection.aspx).</span></span> <span data-ttu-id="daf47-232">Il codice seguente è stato aggiunto al file *Web. config* .</span><span class="sxs-lookup"><span data-stu-id="daf47-232">The following code was added to the *Web.config* file.</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample16.xml?highlight=2-7)]

### <a name="enable-bundling-and-minification"></a><span data-ttu-id="daf47-233">Abilitare la creazione di bundle e minification</span><span class="sxs-lookup"><span data-stu-id="daf47-233">Enable bundling and minification</span></span>

<span data-ttu-id="daf47-234">Quando Visual Studio crea un nuovo progetto Web, la creazione di bundle e minification di file JavaScript non è abilitata per impostazione predefinita.</span><span class="sxs-lookup"><span data-stu-id="daf47-234">When Visual Studio creates a new web project, bundling and minification of JavaScript files is not enabled by default.</span></span> <span data-ttu-id="daf47-235">È stata aggiunta una riga di codice in BundleConfig.cs:</span><span class="sxs-lookup"><span data-stu-id="daf47-235">We added a line of code in BundleConfig.cs:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample17.cs?highlight=9)]

### <a name="set-an-expiration-time-out-for-authentication-cookies"></a><span data-ttu-id="daf47-236">Impostare un timeout di scadenza per i cookie di autenticazione</span><span class="sxs-lookup"><span data-stu-id="daf47-236">Set an expiration time-out for authentication cookies</span></span>

<span data-ttu-id="daf47-237">Per impostazione predefinita, i cookie di autenticazione scadono entro due settimane.</span><span class="sxs-lookup"><span data-stu-id="daf47-237">By default, authentication cookies expire in two weeks.</span></span> <span data-ttu-id="daf47-238">Un tempo più breve è più sicuro.</span><span class="sxs-lookup"><span data-stu-id="daf47-238">A shorter time is more secure.</span></span> <span data-ttu-id="daf47-239">È possibile modificare questa impostazione in *StartupAuth.cs*:</span><span class="sxs-lookup"><span data-stu-id="daf47-239">You can change this setting in *StartupAuth.cs*:</span></span>

[!code-csharp[Main](the-fix-it-sample-application/samples/sample18.cs?highlight=4-5)]

<a id="run-in-vs"></a>
## <a name="how-to-run-the-app-from-visual-studio-on-your-local-computer"></a><span data-ttu-id="daf47-240">Come eseguire l'app da Visual Studio nel computer locale</span><span class="sxs-lookup"><span data-stu-id="daf47-240">How to run the app from Visual Studio on your local computer</span></span>

<span data-ttu-id="daf47-241">Esistono due modi per eseguire l'app Fix it:</span><span class="sxs-lookup"><span data-stu-id="daf47-241">There are two ways to run the Fix It app:</span></span>

- <span data-ttu-id="daf47-242">Eseguire l'applicazione di base che scrive nuove attività direttamente nel database SQL.</span><span class="sxs-lookup"><span data-stu-id="daf47-242">Run the base application that writes new tasks directly to the SQL database.</span></span>
- <span data-ttu-id="daf47-243">Eseguire l'applicazione usando una coda e un servizio back-end per creare attività.</span><span class="sxs-lookup"><span data-stu-id="daf47-243">Run the application using a queue plus a backend service to create tasks.</span></span> <span data-ttu-id="daf47-244">Il modello di coda è descritto nel capitolo [modello di lavoro incentrato sulla coda](queue-centric-work-pattern.md).</span><span class="sxs-lookup"><span data-stu-id="daf47-244">The queue pattern is described in the chapter [Queue-Centric Work Pattern](queue-centric-work-pattern.md).</span></span>

<a id="runbase"></a>
### <a name="run-the-base-application"></a><span data-ttu-id="daf47-245">Eseguire l'applicazione di base</span><span class="sxs-lookup"><span data-stu-id="daf47-245">Run the base application</span></span>

1. <span data-ttu-id="daf47-246">Installare [Visual Studio 2017](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=button+cta&utm_content=download+vs2017).</span><span class="sxs-lookup"><span data-stu-id="daf47-246">Install [Visual Studio 2017](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=button+cta&utm_content=download+vs2017).</span></span>
2. <span data-ttu-id="daf47-247">Installare [Azure SDK per .NET per Visual Studio](https://azure.microsoft.com/downloads/).</span><span class="sxs-lookup"><span data-stu-id="daf47-247">Install the [Azure SDK for .NET for Visual Studio](https://azure.microsoft.com/downloads/).</span></span>
3. <span data-ttu-id="daf47-248">Scaricare il file zip da [MSDN Code Gallery](https://code.msdn.microsoft.com/Fix-It-app-for-Building-cdd80df4).</span><span class="sxs-lookup"><span data-stu-id="daf47-248">Download the .zip file from the [MSDN Code Gallery](https://code.msdn.microsoft.com/Fix-It-app-for-Building-cdd80df4).</span></span>
4. <span data-ttu-id="daf47-249">In Esplora file fare clic con il pulsante destro del mouse sul file con estensione zip e scegliere Proprietà, quindi nella Finestra Proprietà fare clic su Sblocca.</span><span class="sxs-lookup"><span data-stu-id="daf47-249">In File Explorer, right-click the .zip file and click Properties, then in the Properties window click Unblock.</span></span>
5. <span data-ttu-id="daf47-250">Decomprimere il file.</span><span class="sxs-lookup"><span data-stu-id="daf47-250">Unzip the file.</span></span>
6. <span data-ttu-id="daf47-251">Fare doppio clic sul file con estensione sln per avviare Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="daf47-251">Double-click the .sln file to launch Visual Studio.</span></span>
7. <span data-ttu-id="daf47-252">Dal menu **strumenti** fare clic su **Gestione pacchetti NuGet**e quindi su **console di gestione pacchetti**.</span><span class="sxs-lookup"><span data-stu-id="daf47-252">From the **Tools** menu, click **NuGet Package Manager**, then **Package Manager Console**.</span></span>
8. <span data-ttu-id="daf47-253">Nella console di gestione pacchetti (PMC) fare clic su Ripristina.</span><span class="sxs-lookup"><span data-stu-id="daf47-253">In the Package Manager Console (PMC), click Restore.</span></span>
9. <span data-ttu-id="daf47-254">Uscire da Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="daf47-254">Exit Visual Studio.</span></span>
10. <span data-ttu-id="daf47-255">Avviare l' [emulatore di archiviazione di Azure](/azure/storage/common/storage-use-emulator).</span><span class="sxs-lookup"><span data-stu-id="daf47-255">Start the [Azure storage emulator](/azure/storage/common/storage-use-emulator).</span></span>
11. <span data-ttu-id="daf47-256">Riavviare Visual Studio e aprire il file della soluzione chiuso nel passaggio precedente.</span><span class="sxs-lookup"><span data-stu-id="daf47-256">Restart Visual Studio, opening the solution file you closed in the previous step.</span></span>
12. <span data-ttu-id="daf47-257">Verificare che il progetto FixIt sia impostato come progetto di avvio, quindi premere CTRL + F5 per eseguire il progetto.</span><span class="sxs-lookup"><span data-stu-id="daf47-257">Make sure the FixIt project is set as the startup project, and then press CTRL+F5 to run the project.</span></span>

<a id="queueslocal"></a>
### <a name="run-the-application-with-queue-processing"></a><span data-ttu-id="daf47-258">Eseguire l'applicazione con l'elaborazione della coda</span><span class="sxs-lookup"><span data-stu-id="daf47-258">Run the application with queue processing</span></span>

1. <span data-ttu-id="daf47-259">Seguire le istruzioni per [eseguire l'applicazione di base](#runbase), quindi chiudere il browser e chiudere Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="daf47-259">Follow the directions for [Run the base application](#runbase), and then close the browser and close Visual Studio.</span></span>
2. <span data-ttu-id="daf47-260">Avviare Visual Studio con privilegi di amministratore.</span><span class="sxs-lookup"><span data-stu-id="daf47-260">Start Visual Studio with administrator privileges.</span></span> <span data-ttu-id="daf47-261">Si userà l'emulatore di calcolo di Azure e che richiede privilegi di amministratore.</span><span class="sxs-lookup"><span data-stu-id="daf47-261">(You'll be using the Azure compute emulator, and that requires administrator privileges.)</span></span>
3. <span data-ttu-id="daf47-262">Nel file *Web. config* dell'applicazione nel progetto *MyFixIt* (il progetto Web), modificare il valore di `appSettings/UseQueues` su "true":</span><span class="sxs-lookup"><span data-stu-id="daf47-262">In the application *Web.config* file in the *MyFixIt* project (the web project), change the value of `appSettings/UseQueues` to "true":</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample19.cmd?highlight=3)]
4. <span data-ttu-id="daf47-263">Se l' [emulatore di archiviazione di Azure](https://msdn.microsoft.com/library/windowsazure/hh403989.aspx) non è ancora in esecuzione, riavviarlo.</span><span class="sxs-lookup"><span data-stu-id="daf47-263">If the [Azure storage emulator](https://msdn.microsoft.com/library/windowsazure/hh403989.aspx) isn't still running, start it again.</span></span>
5. <span data-ttu-id="daf47-264">Eseguire simultaneamente il progetto Web FixIt e il progetto MyFixItCloudService.</span><span class="sxs-lookup"><span data-stu-id="daf47-264">Run the FixIt web project and the MyFixItCloudService project simultaneously.</span></span>

    <span data-ttu-id="daf47-265">Uso di Visual Studio:</span><span class="sxs-lookup"><span data-stu-id="daf47-265">Using Visual Studio:</span></span>

   1. <span data-ttu-id="daf47-266">Premere **F5** per eseguire il progetto Fixit.</span><span class="sxs-lookup"><span data-stu-id="daf47-266">Press **F5** to run the FixIt project.</span></span>
   2. <span data-ttu-id="daf47-267">In **Esplora soluzioni**fare clic con il pulsante destro del mouse sul progetto MyFixItCloudService, quindi scegliere **debug** > **Avvia nuova istanza**.</span><span class="sxs-lookup"><span data-stu-id="daf47-267">In **Solution Explorer**, right-click the MyFixItCloudService project, and then click **Debug** > **Start New Instance**.</span></span>

    <span data-ttu-id="daf47-268">Uso di Visual Studio 2013 Express per il Web:</span><span class="sxs-lookup"><span data-stu-id="daf47-268">Using Visual Studio 2013 Express for Web:</span></span>

   3. <span data-ttu-id="daf47-269">In Esplora soluzioni fare clic con il pulsante destro del mouse sulla soluzione FixIt e scegliere **Proprietà**.</span><span class="sxs-lookup"><span data-stu-id="daf47-269">In Solution Explorer, right-click the FixIt solution and select **Properties**.</span></span>
   4. <span data-ttu-id="daf47-270">Selezionare **progetti di avvio multipli**.</span><span class="sxs-lookup"><span data-stu-id="daf47-270">Select **Multiple Startup Projects**.</span></span>
   5. <span data-ttu-id="daf47-271">Nell'elenco a discesa **azione** in MyFixIt e MyFixItCloudService selezionare **Avvia**.</span><span class="sxs-lookup"><span data-stu-id="daf47-271">In the **Action** dropdown list under MyFixIt and MyFixItCloudService, select **Start**.</span></span>
   6. <span data-ttu-id="daf47-272">Fare clic su **OK**.</span><span class="sxs-lookup"><span data-stu-id="daf47-272">Click **OK**.</span></span>
   7. <span data-ttu-id="daf47-273">Premere **F5** per eseguire entrambi i progetti.</span><span class="sxs-lookup"><span data-stu-id="daf47-273">Press **F5** to run both projects.</span></span>

      <span data-ttu-id="daf47-274">Quando si esegue il progetto MyFixItCloudService, Visual Studio avvia l'emulatore di calcolo di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-274">When you run the MyFixItCloudService project, Visual Studio starts the Azure compute emulator.</span></span> <span data-ttu-id="daf47-275">A seconda della configurazione del firewall, potrebbe essere necessario consentire l'emulatore tramite il firewall.</span><span class="sxs-lookup"><span data-stu-id="daf47-275">Depending on your firewall configuration, you might need to allow the emulator through the firewall.</span></span>

<a id="deploybase"></a>
## <a name="how-to-deploy-the-base-app-to-azure-app-service-web-apps-by-using-the-windows-powershell-scripts"></a><span data-ttu-id="daf47-276">Come distribuire l'app di base in app Web del servizio app Azure usando gli script di Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="daf47-276">How to deploy the base app to Azure App Service Web Apps by using the Windows PowerShell scripts</span></span>

<span data-ttu-id="daf47-277">Per illustrare il modello di [automazione di tutti gli elementi](automate-everything.md) , l'app Correggi it viene fornita con script che configurano un ambiente in Azure e distribuiscono il progetto nel nuovo ambiente.</span><span class="sxs-lookup"><span data-stu-id="daf47-277">To illustrate the [Automate Everything](automate-everything.md) pattern, the Fix It app is supplied with scripts that set up an environment in Azure and deploy the project to the new environment.</span></span> <span data-ttu-id="daf47-278">Nelle istruzioni seguenti viene illustrato come utilizzare gli script.</span><span class="sxs-lookup"><span data-stu-id="daf47-278">The following instructions explain how to use the scripts.</span></span>

<span data-ttu-id="daf47-279">Se si vuole eseguire in Azure senza usare le code e sono state apportate le modifiche da eseguire localmente con le code, assicurarsi di impostare di nuovo il valore appSetting di UseQueues su false prima di procedere con le istruzioni seguenti.</span><span class="sxs-lookup"><span data-stu-id="daf47-279">If you want to run in Azure without using queues, and you made the changes to run locally with queues, make sure you set the UseQueues appSetting value back to false before proceeding with the following instructions.</span></span>

<span data-ttu-id="daf47-280">Queste istruzioni presuppongono che sia già stato scaricato ed eseguito la soluzione Correggi it localmente e che si disponga di un account Azure o che si disponga di una sottoscrizione di Azure che si è autorizzati a gestire.</span><span class="sxs-lookup"><span data-stu-id="daf47-280">These instructions assume you have already downloaded and run the Fix It solution locally, and that you have an Azure account or have an Azure subscription that you are authorized to manage.</span></span>

1. <span data-ttu-id="daf47-281">Installare la console di **Azure PowerShell** .</span><span class="sxs-lookup"><span data-stu-id="daf47-281">Install the **Azure PowerShell** console.</span></span> <span data-ttu-id="daf47-282">Per istruzioni, vedere [Come installare e configurare Azure PowerShell](https://docs.microsoft.com/powershell/azure/overview?view=azurermps-4.3.1).</span><span class="sxs-lookup"><span data-stu-id="daf47-282">For instructions, see [How to install and configure Azure PowerShell](https://docs.microsoft.com/powershell/azure/overview?view=azurermps-4.3.1).</span></span>

    <span data-ttu-id="daf47-283">Questa console personalizzata è configurata per l'uso con la sottoscrizione di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-283">This customized console is configured to work with your Azure subscription.</span></span> <span data-ttu-id="daf47-284">Il modulo Azure viene installato nella directory *programmi* e viene importato automaticamente a ogni uso della console di Azure PowerShell.</span><span class="sxs-lookup"><span data-stu-id="daf47-284">The Azure module is installed in the *Program Files* directory and is automatically imported on every use of the Azure PowerShell console.</span></span>

    <span data-ttu-id="daf47-285">Se si preferisce lavorare in un programma host diverso, ad esempio Windows PowerShell ISE, assicurarsi di usare il cmdlet [Import-Module](https://go.microsoft.com/fwlink/?LinkID=141553) per importare il modulo di Azure o usare un comando nel modulo di Azure per attivare l'importazione automatica del modulo.</span><span class="sxs-lookup"><span data-stu-id="daf47-285">If you prefer to work in a different host program, such as Windows PowerShell ISE, be sure to use the [Import-Module](https://go.microsoft.com/fwlink/?LinkID=141553) cmdlet to import the Azure module or use a command in the Azure module to trigger automatic importing of the module.</span></span>
2. <span data-ttu-id="daf47-286">Avviare Azure PowerShell con l'opzione **Esegui come amministratore** .</span><span class="sxs-lookup"><span data-stu-id="daf47-286">Start Azure PowerShell with the **Run as administrator** option.</span></span>
3. <span data-ttu-id="daf47-287">Eseguire il cmdlet [Set-ExecutionPolicy](https://go.microsoft.com/fwlink/p/?linkid=293941) per impostare i criteri di esecuzione Azure PowerShell su `RemoteSigned`.</span><span class="sxs-lookup"><span data-stu-id="daf47-287">Run the [Set-ExecutionPolicy](https://go.microsoft.com/fwlink/p/?linkid=293941) cmdlet to set the Azure PowerShell execution policy to `RemoteSigned`.</span></span> <span data-ttu-id="daf47-288">Immettere **Y** (per Sì) per completare la modifica dei criteri.</span><span class="sxs-lookup"><span data-stu-id="daf47-288">Enter **Y** (for Yes) to complete the policy change.</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample20.cmd)]

    <span data-ttu-id="daf47-289">Questa impostazione consente di eseguire script locali che non dispongono di una firma digitale.</span><span class="sxs-lookup"><span data-stu-id="daf47-289">This setting enables you to run local scripts that aren't digitally signed.</span></span> <span data-ttu-id="daf47-290">È anche possibile impostare i criteri di esecuzione su `Unrestricted`, in modo da eliminare la necessità di sbloccare il passaggio in un secondo momento, ma questa operazione non è consigliata per motivi di sicurezza.</span><span class="sxs-lookup"><span data-stu-id="daf47-290">(You can also set the execution policy to `Unrestricted`, which would eliminate the need for the unblock step later, but this is not recommended for security reasons.)</span></span>
4. <span data-ttu-id="daf47-291">Eseguire il cmdlet `Add-AzureAccount` per configurare PowerShell con le credenziali per l'account.</span><span class="sxs-lookup"><span data-stu-id="daf47-291">Run the `Add-AzureAccount` cmdlet to set up PowerShell with credentials for your account.</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample21.cmd)]

    <span data-ttu-id="daf47-292">Queste credenziali scadono dopo un periodo di tempo ed è necessario eseguire nuovamente il cmdlet `Add-AzureAccount`.</span><span class="sxs-lookup"><span data-stu-id="daf47-292">These credentials expire after a period of time and you have to re-run the `Add-AzureAccount` cmdlet.</span></span> <span data-ttu-id="daf47-293">Poiché questo e-book viene scritto, il limite di tempo prima della scadenza delle credenziali è 12 ore.</span><span class="sxs-lookup"><span data-stu-id="daf47-293">As this e-book is being written, the time limit before credentials expire is 12 hours.</span></span>
5. <span data-ttu-id="daf47-294">Se si dispone di più sottoscrizioni, utilizzare il cmdlet Select-AzureSubscription per specificare la sottoscrizione in cui si desidera creare l'ambiente di test.</span><span class="sxs-lookup"><span data-stu-id="daf47-294">If you have multiple subscriptions, use the Select-AzureSubscription cmdlet to specify the subscription you want to create the test environment in.</span></span>
6. <span data-ttu-id="daf47-295">Importare un certificato di gestione per la stessa sottoscrizione di Azure usando i cmdlet `Get-AzurePublishSettingsFile` e `Import-AzurePublishSettingsFile`.</span><span class="sxs-lookup"><span data-stu-id="daf47-295">Import a management certificate for the same Azure subscription by using the `Get-AzurePublishSettingsFile` and `Import-AzurePublishSettingsFile` cmdlets.</span></span> <span data-ttu-id="daf47-296">Il primo di questi cmdlet Scarica un file di certificato e nel secondo si specifica il percorso del file per importarlo.</span><span class="sxs-lookup"><span data-stu-id="daf47-296">The first of these cmdlets downloads a certificate file, and in the second one you specify the location of that file in order to import it.</span></span> > [!IMPORTANT]
   > <span data-ttu-id="daf47-297">Tenere il file scaricato in un percorso sicuro o eliminarlo al termine dell'operazione, perché contiene un certificato che può essere usato per gestire i servizi di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-297">Keep the downloaded file in a safe location or delete it when you're done with it, because it contains a certificate that can be used to manage your Azure services.</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample22.cmd)]

    <span data-ttu-id="daf47-298">Il certificato viene usato per una chiamata API REST che rileva l'indirizzo IP del computer di sviluppo per impostare una regola del firewall nel server di database SQL.</span><span class="sxs-lookup"><span data-stu-id="daf47-298">The certificate is used for a REST API call that detects the development machine's IP address in order to set a firewall rule on the SQL Database server.</span></span>
7. <span data-ttu-id="daf47-299">Eseguire il cmdlet [set-location](https://go.microsoft.com/fwlink/p/?linkid=293912) (gli alias sono `cd`, `chdir`e `sl`) per passare alla directory che contiene gli script.</span><span class="sxs-lookup"><span data-stu-id="daf47-299">Run the [Set-Location](https://go.microsoft.com/fwlink/p/?linkid=293912) cmdlet (aliases are `cd`, `chdir`, and `sl`) to navigate to the directory that contains the scripts.</span></span> <span data-ttu-id="daf47-300">Si trovano nella cartella *automazione* nella cartella della soluzione Correggi it. Inserire il percorso tra virgolette se uno dei nomi di directory contiene spazi.</span><span class="sxs-lookup"><span data-stu-id="daf47-300">(They're located in the *Automation* folder in the Fix It solution folder.) Put the path in quotes if any of the directory names contain spaces.</span></span> <span data-ttu-id="daf47-301">Ad esempio, per passare alla directory `c:\Sample Apps\FixIt\Automation` è possibile immettere il comando seguente:</span><span class="sxs-lookup"><span data-stu-id="daf47-301">For example, to navigate to the `c:\Sample Apps\FixIt\Automation` directory you could enter the following command:</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample23.cmd)]
8. <span data-ttu-id="daf47-302">Per consentire a Windows PowerShell di eseguire questi script, usare il cmdlet [Unblock-file](https://go.microsoft.com/fwlink/p/?linkid=294021) .</span><span class="sxs-lookup"><span data-stu-id="daf47-302">To allow Windows PowerShell to run these scripts, use the [Unblock-File](https://go.microsoft.com/fwlink/p/?linkid=294021) cmdlet.</span></span> <span data-ttu-id="daf47-303">Gli script sono bloccati perché sono stati scaricati da Internet.</span><span class="sxs-lookup"><span data-stu-id="daf47-303">(The scripts are blocked because they were downloaded from the Internet.)</span></span>

    > [!WARNING]
    > <span data-ttu-id="daf47-304">Sicurezza: prima di eseguire `Unblock-File` in un file eseguibile o script, aprire il file nel blocco note, esaminare i comandi e verificare che non contengano codice dannoso.</span><span class="sxs-lookup"><span data-stu-id="daf47-304">Security - Before running `Unblock-File` on any script or executable file, open the file in Notepad, examine the commands, and verify that they do not contain any malicious code.</span></span>

    <span data-ttu-id="daf47-305">Ad esempio, il comando seguente esegue il cmdlet `Unblock-File` in tutti gli script della directory corrente.</span><span class="sxs-lookup"><span data-stu-id="daf47-305">For example, the following command runs the `Unblock-File` cmdlet on all scripts in the current directory.</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample24.cmd)]
9. <span data-ttu-id="daf47-306">Per creare l'app Web per la base (nessuna elaborazione delle code) correggerla, eseguire lo script di creazione dell'ambiente.</span><span class="sxs-lookup"><span data-stu-id="daf47-306">To create the web app for the base (no queues processing) Fix It app, run the environment creation script.</span></span>

    <span data-ttu-id="daf47-307">Il parametro `Name` obbligatorio specifica il nome del database e viene usato anche per l'account di archiviazione creato dallo script.</span><span class="sxs-lookup"><span data-stu-id="daf47-307">The required `Name` parameter specifies the name of the database and is also used for the storage account that the script creates.</span></span> <span data-ttu-id="daf47-308">Il nome deve essere univoco a livello globale all'interno del dominio azurewebsites.net.</span><span class="sxs-lookup"><span data-stu-id="daf47-308">The name must be globally unique within the azurewebsites.net domain.</span></span> <span data-ttu-id="daf47-309">Se si specifica un nome che non è univoco, ad esempio Fixit o test (o anche come nell'esempio fixitdemo), il `New-AzureWebsite` cmdlet ha esito negativo con un errore interno che segnala un conflitto.</span><span class="sxs-lookup"><span data-stu-id="daf47-309">If you specify a name that is not unique, like Fixit or Test (or even as in the example, fixitdemo), the `New-AzureWebsite` cmdlet fails with an Internal Error that reports a conflict.</span></span> <span data-ttu-id="daf47-310">Lo script converte il nome in tutti i casi minuscoli in modo che siano conformi ai requisiti di nome per le app Web, gli account di archiviazione e i database.</span><span class="sxs-lookup"><span data-stu-id="daf47-310">The script converts the name to all lower-case to comply with name requirements for web apps, storage accounts, and databases.</span></span>

    <span data-ttu-id="daf47-311">Il parametro obbligatorio `SqlDatabasePassword` specifica la password per l'account amministratore che verrà creato per il database SQL.</span><span class="sxs-lookup"><span data-stu-id="daf47-311">The required `SqlDatabasePassword` parameter specifies the password for the admin account that will be created for SQL Database.</span></span> <span data-ttu-id="daf47-312">Non includere caratteri XML speciali nella password (&amp; &lt; &gt;;).</span><span class="sxs-lookup"><span data-stu-id="daf47-312">Don't include special XML characters in the password (&amp; &lt; &gt; ;).</span></span> <span data-ttu-id="daf47-313">Si tratta di una limitazione del modo in cui gli script sono stati scritti, non di un limite di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-313">This is a limitation of the way the scripts were written, not a limitation of Azure.</span></span>

    <span data-ttu-id="daf47-314">Se ad esempio si vuole creare un'app Web denominata "fixitdemo" e usare una password di amministratore SQL Server "Passw0rd1", è possibile immettere il comando seguente:</span><span class="sxs-lookup"><span data-stu-id="daf47-314">For example, if you want to create a web app named "fixitdemo" and use a SQL Server administrator password of "Passw0rd1", you could enter the following command:</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample25.cmd)]

    <span data-ttu-id="daf47-315">Il nome deve essere univoco nel dominio azurewebsites.net e la password deve soddisfare i requisiti del database SQL per la complessità della password.</span><span class="sxs-lookup"><span data-stu-id="daf47-315">The name must be unique in the azurewebsites.net domain, and the password must meet SQL Database requirements for password complexity.</span></span> <span data-ttu-id="daf47-316">Il Passw0rd1 di esempio soddisfa i requisiti.</span><span class="sxs-lookup"><span data-stu-id="daf47-316">(The example Passw0rd1 does meet the requirements.)</span></span>

    <span data-ttu-id="daf47-317">Si noti che il comando inizia con ".\".</span><span class="sxs-lookup"><span data-stu-id="daf47-317">Note that the command begins with ".\".</span></span> <span data-ttu-id="daf47-318">Per evitare l'esecuzione dannosa degli script, Windows PowerShell richiede di fornire il percorso completo del file di script quando si esegue uno script.</span><span class="sxs-lookup"><span data-stu-id="daf47-318">To help prevent malicious execution of scripts, Windows PowerShell requires that you provide the fully qualified path to the script file when you run a script.</span></span> <span data-ttu-id="daf47-319">È possibile usare un punto per indicare la directory corrente (".\") o specificare il percorso completo, ad esempio:</span><span class="sxs-lookup"><span data-stu-id="daf47-319">You can use a dot to indicate the current directory (".\") or provide the fully qualified path, such as:</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample26.cmd)]

    <span data-ttu-id="daf47-320">Per ulteriori informazioni sullo script, utilizzare il cmdlet `Get-Help`.</span><span class="sxs-lookup"><span data-stu-id="daf47-320">For more information about the script, use the `Get-Help` cmdlet.</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample27.cmd)]

    <span data-ttu-id="daf47-321">È possibile usare i parametri `Detailed`, `Full`, `Parameters`e `Examples` del cmdlet Get-Help per filtrare la guida restituita.</span><span class="sxs-lookup"><span data-stu-id="daf47-321">You can use the `Detailed`, `Full`, `Parameters`, and `Examples` parameters of the Get-Help cmdlet to filter the help that is returned.</span></span>

    <span data-ttu-id="daf47-322">Se lo script ha esito negativo o genera errori, ad esempio "New-AzureWebsite: call set-AzureSubscription e Select-AzureSubscription First", è possibile che non sia stata completata la configurazione di Azure PowerShell.</span><span class="sxs-lookup"><span data-stu-id="daf47-322">If the script fails or generates errors, such as "New-AzureWebsite : Call Set-AzureSubscription and Select-AzureSubscription first," you might not have completed the configuration of Azure PowerShell.</span></span>

    <span data-ttu-id="daf47-323">Al termine dello script, è possibile usare il portale di gestione di Azure per visualizzare le risorse create, come illustrato nel capitolo [automazione di tutti gli elementi](automate-everything.md) .</span><span class="sxs-lookup"><span data-stu-id="daf47-323">After the script finishes, you can use the Azure Management Portal to see the resources that were created, as shown in the [Automate Everything](automate-everything.md) chapter.</span></span>
10. <span data-ttu-id="daf47-324">Per distribuire il progetto FixIt nel nuovo ambiente Azure, usare lo script *AzureWebsite. ps1* .</span><span class="sxs-lookup"><span data-stu-id="daf47-324">To deploy the FixIt project to the new Azure environment, use the *AzureWebsite.ps1* script.</span></span> <span data-ttu-id="daf47-325">Esempio:</span><span class="sxs-lookup"><span data-stu-id="daf47-325">For example:</span></span>

    [!code-console[Main](the-fix-it-sample-application/samples/sample28.cmd)]

    <span data-ttu-id="daf47-326">Al termine della distribuzione, il browser si apre con la correzione dell'esecuzione in Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-326">When deployment is done, the browser opens with Fix It running in Azure.</span></span>

<a id="troubleshooting"></a>
## <a name="troubleshooting-the-windows-powershell-scripts"></a><span data-ttu-id="daf47-327">Risoluzione dei problemi relativi agli script di Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="daf47-327">Troubleshooting the Windows PowerShell scripts</span></span>

<span data-ttu-id="daf47-328">Gli errori più comuni rilevati durante l'esecuzione di questi script sono correlati alle autorizzazioni.</span><span class="sxs-lookup"><span data-stu-id="daf47-328">The most common errors encountered when running these scripts are related to permissions.</span></span> <span data-ttu-id="daf47-329">Verificare che `Add-AzureAccount` e `Import-AzurePublishSettingsFile` abbiano avuto esito positivo e che siano stati usati per la stessa sottoscrizione di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-329">Make sure that `Add-AzureAccount` and `Import-AzurePublishSettingsFile` were successful and that you used them for the same Azure subscription.</span></span> <span data-ttu-id="daf47-330">Anche se `Add-AzureAccount` ha avuto esito positivo, potrebbe essere necessario eseguirlo di nuovo.</span><span class="sxs-lookup"><span data-stu-id="daf47-330">Even if `Add-AzureAccount` was successful you might have to run it again.</span></span> <span data-ttu-id="daf47-331">Le autorizzazioni aggiunte da `Add-AzureAccount` scadono entro 12 ore.</span><span class="sxs-lookup"><span data-stu-id="daf47-331">The permissions added by `Add-AzureAccount` expire in 12 hours.</span></span>

### <a name="object-reference-not-set-to-an-instance-of-an-object"></a><span data-ttu-id="daf47-332">Riferimento a un oggetto non impostato su un'istanza di oggetto.</span><span class="sxs-lookup"><span data-stu-id="daf47-332">Object reference not set to an instance of an object.</span></span>

<span data-ttu-id="daf47-333">Se lo script restituisce errori, ad esempio "riferimento oggetto non impostato su un'istanza di un oggetto", il che significa che Windows PowerShell non è in grado di trovare un oggetto da elaborare (si tratta di un'eccezione di riferimento null), eseguire il cmdlet `Add-AzureAccount` e riprovare a eseguire lo script.</span><span class="sxs-lookup"><span data-stu-id="daf47-333">If the script returns errors, such as "Object reference not set to an instance of an object," which means that Windows PowerShell can't find an object to process (this is a null reference exception), run the `Add-AzureAccount` cmdlet and try the script again.</span></span>

[!code-console[Main](the-fix-it-sample-application/samples/sample29.cmd)]

### <a name="internalerror-the-server-encountered-an-internal-error"></a><span data-ttu-id="daf47-334">InternalError: si è verificato un errore interno del server.</span><span class="sxs-lookup"><span data-stu-id="daf47-334">InternalError: The server encountered an internal error.</span></span>

<span data-ttu-id="daf47-335">Il cmdlet `New-AzureWebsite` restituisce un errore interno quando il nome non è univoco nel dominio azurewebsites.net.</span><span class="sxs-lookup"><span data-stu-id="daf47-335">The `New-AzureWebsite` cmdlet returns an internal error when the name is not unique in the azurewebsites.net domain.</span></span> <span data-ttu-id="daf47-336">Per risolvere l'errore, usare un valore diverso per il nome, che è nel parametro Name di *New-AzureWebsiteEnv. ps1*.</span><span class="sxs-lookup"><span data-stu-id="daf47-336">To resolve the error, use a different value for the name, which is in the Name parameter of *New-AzureWebsiteEnv.ps1*.</span></span>

[!code-console[Main](the-fix-it-sample-application/samples/sample30.cmd)]

### <a name="restarting-the-script"></a><span data-ttu-id="daf47-337">Riavvio dello script</span><span class="sxs-lookup"><span data-stu-id="daf47-337">Restarting the script</span></span>

<span data-ttu-id="daf47-338">Se è necessario riavviare lo script *New-AzureWebsiteEnv. ps1* perché non è riuscito prima di stampare il messaggio "script completato", potrebbe essere necessario eliminare le risorse create dallo script prima dell'arresto.</span><span class="sxs-lookup"><span data-stu-id="daf47-338">If you need to restart the *New-AzureWebsiteEnv.ps1* script because it failed before it printed the "Script is complete" message, you might want to delete resources that the script created before it stopped.</span></span> <span data-ttu-id="daf47-339">Ad esempio, se lo script ha già creato l'app Web ContosoFixItDemo e si esegue di nuovo lo script con lo stesso nome, lo script avrà esito negativo perché il nome è in uso.</span><span class="sxs-lookup"><span data-stu-id="daf47-339">For example, if the script already created the ContosoFixItDemo web app and you run the script again with the same name, the script will fail because the name is in use.</span></span>

<span data-ttu-id="daf47-340">Per determinare le risorse create dallo script prima dell'arresto, usare i cmdlet seguenti:</span><span class="sxs-lookup"><span data-stu-id="daf47-340">To determine which resources the script created before it stopped, use the following cmdlets:</span></span>

- `Get-AzureWebsite`
- `Get-AzureSqlDatabaseServer`
- <span data-ttu-id="daf47-341">`Get-AzureSqlDatabase`: per eseguire questo cmdlet, inviare tramite pipe il nome del server di database a `Get-AzureSqlDatabase`: `Get-AzureSqlDatabaseServer | Get-AzureSqlDatabase.`</span><span class="sxs-lookup"><span data-stu-id="daf47-341">`Get-AzureSqlDatabase`: To run this cmdlet, pipe the database server name to `Get-AzureSqlDatabase`:   `Get-AzureSqlDatabaseServer | Get-AzureSqlDatabase.`</span></span>

<span data-ttu-id="daf47-342">Per eliminare queste risorse, usare i comandi seguenti.</span><span class="sxs-lookup"><span data-stu-id="daf47-342">To delete these resources, use the following commands.</span></span> <span data-ttu-id="daf47-343">Si noti che se si elimina il server di database, vengono eliminati automaticamente i database associati al server.</span><span class="sxs-lookup"><span data-stu-id="daf47-343">Note that if you delete the database server, you automatically delete the databases associated with the server.</span></span>

- `Get-AzureWebsite -Name <WebsiteName> | Remove-AzureWebsite`
- `Get-AzureSqlDatabase -Name <DatabaseName> -ServerName <DatabaseServerName> | Remove-SqlAzureDatabase`
- `Get-AzureSqlDatabaseServer | Remove-AzureSqlDatabaseServer`

<a id="deployqueues"></a>
## <a name="how-to-deploy-the-app-with-queue-processing-to-azure-app-service-web-apps-and-an-azure-cloud-service"></a><span data-ttu-id="daf47-344">Come distribuire l'app con l'elaborazione delle code in app Web del servizio app Azure e un servizio cloud di Azure</span><span class="sxs-lookup"><span data-stu-id="daf47-344">How to deploy the app with queue processing to Azure App Service Web Apps and an Azure Cloud Service</span></span>

<span data-ttu-id="daf47-345">Per abilitare le code, apportare la modifica seguente nel file MyFixIt\Web.config.</span><span class="sxs-lookup"><span data-stu-id="daf47-345">To enable queues, make the following change in the MyFixIt\Web.config file.</span></span> <span data-ttu-id="daf47-346">In `appSettings`modificare il valore di `UseQueues` su "true":</span><span class="sxs-lookup"><span data-stu-id="daf47-346">Under `appSettings`, change the value of `UseQueues` to "true":</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample31.xml)]

<span data-ttu-id="daf47-347">Distribuire quindi l'applicazione MVC in un'app Web nel servizio app Azure, come descritto in [precedenza](#deploybase).</span><span class="sxs-lookup"><span data-stu-id="daf47-347">Then deploy the MVC application to an web app in Azure App Service, as described [earlier](#deploybase).</span></span>

<span data-ttu-id="daf47-348">Successivamente, creare un nuovo servizio cloud di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-348">Next, create a new Azure cloud service.</span></span> <span data-ttu-id="daf47-349">Gli script inclusi con l'app Correggi it non creano o distribuiscono il servizio cloud, pertanto è necessario usare portale di Azure a tale scopo.</span><span class="sxs-lookup"><span data-stu-id="daf47-349">The scripts included with the Fix It app do not create or deploy the cloud service, so you must use Azure portal for this.</span></span> <span data-ttu-id="daf47-350">Nel portale fare clic su **nuovo** -- **calcolo** - **servizio cloud** -- **creazione rapida**, quindi immettere un URL e un percorso di Data Center.</span><span class="sxs-lookup"><span data-stu-id="daf47-350">In the portal, click **New** -- **Compute** – **Cloud Service** -- **Quick Create**, and then enter a URL and a data center location.</span></span> <span data-ttu-id="daf47-351">Usare lo stesso data center in cui è stata distribuita l'app Web.</span><span class="sxs-lookup"><span data-stu-id="daf47-351">Use the same data center where you deployed the web app.</span></span>

![](the-fix-it-sample-application/_static/image1.png)

<span data-ttu-id="daf47-352">Prima di poter distribuire il servizio cloud, è necessario aggiornare alcuni file di configurazione.</span><span class="sxs-lookup"><span data-stu-id="daf47-352">Before you can deploy the cloud service, you need to update some of the configuration files.</span></span>

<span data-ttu-id="daf47-353">In MyFixIt. WorkerRole\app.config, in `connectionStrings`, sostituire il valore della stringa di connessione `appdb` con la stringa di connessione effettiva per il database SQL.</span><span class="sxs-lookup"><span data-stu-id="daf47-353">In MyFixIt.WorkerRole\app.config, under `connectionStrings`, replace the value of the `appdb` connection string with the actual connection string for the SQL Database.</span></span> <span data-ttu-id="daf47-354">È possibile ottenere la stringa di connessione dal portale.</span><span class="sxs-lookup"><span data-stu-id="daf47-354">You can get the connection string from the portal.</span></span> <span data-ttu-id="daf47-355">Nel portale fare clic su **database sql** - **appdb** - **visualizzare le stringhe di connessione al database SQL per ADO .NET, ODBC, php e JDBC**.</span><span class="sxs-lookup"><span data-stu-id="daf47-355">In the portal, click **SQL Databases** - **appdb** - **View SQL Database connection strings for ADO .Net, ODBC, PHP, and JDBC**.</span></span> <span data-ttu-id="daf47-356">Copiare la stringa di connessione ADO.NET e incollare il valore nel file app. config.</span><span class="sxs-lookup"><span data-stu-id="daf47-356">Copy the ADO.NET connection string and paste the value into the app.config file.</span></span> <span data-ttu-id="daf47-357">Sostituire "{Your\_password\_here}" con la password del database.</span><span class="sxs-lookup"><span data-stu-id="daf47-357">Replace "{your\_password\_here}" with your database password.</span></span> <span data-ttu-id="daf47-358">(Supponendo che siano stati usati gli script per distribuire l'app MVC, è stata specificata la password del database nel parametro `SqlDatabasePassword` script).</span><span class="sxs-lookup"><span data-stu-id="daf47-358">(Assuming you used the scripts to deploy the MVC app, you specified the database password in the `SqlDatabasePassword` script parameter.)</span></span>

<span data-ttu-id="daf47-359">Il risultato dovrebbe essere simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="daf47-359">The result should look like the following:</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample32.xml)]

<span data-ttu-id="daf47-360">Nello stesso file MyFixIt. WorkerRole\app.config, in `appSettings`, sostituire i due valori segnaposto per l'account di archiviazione di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-360">In the same MyFixIt.WorkerRole\app.config file, under `appSettings`, replace the two placeholder values for the Azure storage account.</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample33.xml?highlight=2-3)]

<span data-ttu-id="daf47-361">È possibile ottenere la chiave di accesso dal portale.</span><span class="sxs-lookup"><span data-stu-id="daf47-361">You can get the access key from the portal.</span></span> <span data-ttu-id="daf47-362">Vedere [come gestire gli account di archiviazione](https://docs.microsoft.com/azure/storage/common/storage-create-storage-account).</span><span class="sxs-lookup"><span data-stu-id="daf47-362">See [How To Manage Storage Accounts](https://docs.microsoft.com/azure/storage/common/storage-create-storage-account).</span></span>

<span data-ttu-id="daf47-363">In MyFixItCloudService\ServiceConfiguration.Cloud.cscfg sostituire gli stessi due segnaposto valori per l'account di archiviazione di Azure.</span><span class="sxs-lookup"><span data-stu-id="daf47-363">In MyFixItCloudService\ServiceConfiguration.Cloud.cscfg, replace the same two placeholders values for the Azure storage account.</span></span>

[!code-xml[Main](the-fix-it-sample-application/samples/sample34.xml?highlight=3)]

<span data-ttu-id="daf47-364">A questo punto si è pronti per distribuire il servizio cloud.</span><span class="sxs-lookup"><span data-stu-id="daf47-364">Now you are ready to deploy the cloud service.</span></span> <span data-ttu-id="daf47-365">In Esplora soluzioni fare clic con il pulsante destro del mouse sul progetto MyFixItCloudService e scegliere **pubblica**.</span><span class="sxs-lookup"><span data-stu-id="daf47-365">In Solution Explore, right-click the MyFixItCloudService project and select **Publish**.</span></span> <span data-ttu-id="daf47-366">Per ulteriori informazioni, vedere l'argomento relativo alla[distribuzione dell'applicazione in Azure](https://www.windowsazure.com/develop/net/tutorials/multi-tier-web-site/2-download-and-run/#deployAz), disponibile nella parte 2 di [questa esercitazione](https://code.msdn.microsoft.com/Windows-Azure-Multi-Tier-eadceb36).</span><span class="sxs-lookup"><span data-stu-id="daf47-366">For more information, see "[Deploy the Application to Azure](https://www.windowsazure.com/develop/net/tutorials/multi-tier-web-site/2-download-and-run/#deployAz)", which is in part 2 of [this tutorial](https://code.msdn.microsoft.com/Windows-Azure-Multi-Tier-eadceb36).</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="daf47-367">Precedente</span><span class="sxs-lookup"><span data-stu-id="daf47-367">Previous</span></span>](more-patterns-and-guidance.md)
